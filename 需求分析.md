# 企业招聘 Copilot 需求分析

> 文档版本：1.2
>
> 基线日期：2026-08-31
>
> 适用范围：六周单人开发的作品集级 MVP
>
> 输入文档：[技术栈选型与架构决策](./技术栈选型与架构决策.md)、[项目可行性分析](./项目可行性分析.md)

## 1. 文档目的

本文把架构设想和可行性结论转化为可以设计、开发、测试和验收的产品需求基线。它重点回答：

- MVP 为谁解决什么问题。
- 用户必须能够完成哪些业务流程。
- Agent 可以做什么、不能做什么。
- 哪些状态、数据和副作用必须持久化与审计。
- 每项核心能力如何验收。

本文中的需求关键字含义如下：

- **必须（Must）**：MVP 发布门禁，缺失时不能视为完成。
- **应该（Should）**：对作品集质量有明显帮助，但不阻塞核心闭环。
- **可以（Could）**：时间充足时实现，不进入六周关键路径。
- **不做（Won't）**：本次明确排除。

## 2. 产品定位

### 2.1 产品描述

企业招聘 Copilot 是面向 HR 和招聘主管的辅助招聘系统。系统接收岗位要求和电子简历，完成结构化解析、候选人混合检索、硬条件评估和证据化匹配分析，并在人类批准后更新岗位流程状态和创建模拟面试安排。

它不是自动招聘决策系统。模型提供候选人召回、分析建议和工具执行提案，最终业务决定由有权限的用户作出。

### 2.2 产品目标

| 编号 | 目标 | 可验证结果 |
|---|---|---|
| OBJ-001 | 降低简历整理成本 | 用户可以批量上传 PDF/DOCX，并校对结构化结果。 |
| OBJ-002 | 提高候选人查找效率 | 用户可以主动选择硬条件筛选，并结合关键词和语义搜索获取候选人排序。 |
| OBJ-003 | 提高推荐可解释性 | 每条确定性匹配结论均能定位到简历证据，并标明证据对结论的支持程度。 |
| OBJ-004 | 保留人工控制 | 淘汰、入围、更新流程状态和创建面试安排均不能绕过人工审批。 |
| OBJ-005 | 展示 Agent 工程可靠性 | 工作流可暂停、恢复、重试、审计和离线评测。 |
| OBJ-006 | 保证项目可复现 | 新环境可以按照 README 通过 Docker Compose 启动并运行演示数据。 |

### 2.3 非目标

MVP 不承诺：

- 自动录用、自动淘汰或任何无人监督的招聘决定。
- 识别扫描版 PDF 或图片简历。
- 对接真实邮箱、短信、日历、ATS 或企业 SSO。
- 处理真实候选人数据或满足特定司法辖区的生产合规要求。
- 多企业租户、跨区域容灾、生产级高可用和海量向量检索。
- 对候选人的人格、情绪、健康、民族、宗教、婚育等敏感属性作推断。

## 3. 范围、假设与约束

### 3.1 MVP 范围

- 3 至 5 个合成岗位。
- 30 至 100 份合成电子简历。
- PDF 和 DOCX 两种格式。
- 单企业逻辑空间。
- HR、招聘主管、管理员三种角色。
- 单 Agent、岗位级 MatchRun 与候选人级 ApplicationRun 两张 LangGraph 状态图。
- 单机 Docker Compose 部署。
- Qwen3.7-Plus 作为默认聊天模型，模型名通过环境变量覆盖。
- qwen3.7-text-embedding，1024 维。
- MockScheduleBackend 模拟面试排期。
- Langfuse 为可选外部能力，关闭时核心业务仍可运行。

### 3.2 关键约束

| 编号 | 约束 |
|---|---|
| CON-001 | 开发周期为六周，按 30 个有效开发日规划。 |
| CON-002 | 由一人完成设计、开发、测试、部署和演示准备。 |
| CON-003 | Celery Worker 必须运行在 Linux 容器中，不支持 Windows 原生 Worker。 |
| CON-004 | MVP 只使用合成数据，不上传真实姓名、手机号、邮箱或真实简历到模型与可观测平台。 |
| CON-005 | 外部模型不可用时允许任务失败或进入受控 Mock 演示模式，但不得伪装为真实模型结果。 |
| CON-006 | 所有副作用操作必须幂等并且可审计。 |
| CON-007 | MVP 优先使用精确向量检索；只有性能基线证明必要时才启用 HNSW。 |

## 4. 用户与权限

### 4.1 用户角色

| 角色 | 主要职责 |
|---|---|
| HR | 创建岗位、分配招聘主管、上传和校对简历、启动匹配、查看报告、处理日常审批和面试安排。 |
| 招聘主管 | 通过有效 JobAssignment 查看被分配岗位及候选人报告，对入围/淘汰和面试安排作业务审批。 |
| 管理员 | 管理演示账号和角色、查看系统健康与脱敏后的技术 Trace，不代替招聘业务审批。 |

MVP 使用预置账号或初始化脚本创建用户，不要求开发完整的用户管理后台。

### 4.2 权限矩阵

| 能力 | HR | 招聘主管 | 管理员 |
|---|---:|---:|---:|
| 登录系统 | 是 | 是 | 是 |
| 创建、编辑岗位 | 是 | 仅查看被分配岗位 | 否 |
| 分配、撤销岗位招聘主管 | 是 | 否 | 否 |
| 上传和校对简历 | 是 | 仅查看 | 否 |
| 启动候选人匹配 | 是 | 可选 | 否 |
| 查看岗位匹配报告 | 是 | 仅被分配岗位 | 仅脱敏技术视图 |
| 审批岗位流程状态变更 | 是 | 仅被分配岗位 | 否 |
| 审批面试排期 | 是 | 仅被分配岗位 | 否 |
| 查看业务审计记录 | 是 | 仅被分配岗位 | 仅脱敏技术审计 |
| 查看系统健康和技术错误 | 否 | 否 | 是 |
| 管理用户和角色 | 否 | 否 | 通过初始化脚本或管理接口 |

所有接口都必须在服务端重新校验权限，前端隐藏按钮不能代替授权控制。

## 5. 核心业务流程

### 5.1 主流程

```text
HR 登录
  → 创建并启用岗位
  → 将岗位分配给招聘主管（可选）
  → 批量上传电子简历
  → Worker 解析文档并生成结构化候选人资料
  → HR 校对并确认资料
  → 系统创建证据片段和向量
  → HR 启动岗位匹配
  → 混合检索召回候选人
  → Python 节点评估硬条件，不自动剔除候选人
  → LLM 生成带证据的匹配报告
  → MatchRun 完成
  → HR/被分配招聘主管为单个 JobApplication 启动 ApplicationRun
  → 人工审批岗位流程状态
  → Agent 生成面试问题
  → 人工审批排期提案
  → MockScheduleBackend 创建模拟安排
```

MVP 中一次 `MatchRun` 只针对一个岗位版本，最多处理配置的 Top-K 候选人，并在报告持久化后结束。它不等待人工审批，也不改变 `JobApplication` 状态。每个 `ApplicationRun` 只绑定一个 `JobApplication`，负责该候选人的审批、状态更新、问题生成和排期。拒绝某个工具提案只表示“不执行该动作”，既不代表运行失败，也不会自动把 `JobApplication` 设为 `REJECTED`。

### 5.2 UC-001：创建岗位

| 项目 | 内容 |
|---|---|
| 参与者 | HR |
| 前置条件 | 用户已登录并具有岗位编辑权限。 |
| 主流程 | 新建草稿；填写名称、描述、必须技能、加分技能、最低年限和学历要求；保存；启用岗位。 |
| 异常流程 | 必填项缺失时拒绝保存；岗位已进入匹配后修改关键要求时创建新版本。 |
| 结果 | 产生可用于检索和规则判断的岗位版本。 |

### 5.3 UC-002：上传、解析和校对简历

| 项目 | 内容 |
|---|---|
| 参与者 | HR |
| 前置条件 | 用户已登录；文件符合格式和大小限制。 |
| 主流程 | 批量上传；系统持久化文件；创建异步任务；提取文本；生成结构化资料和证据；HR 校对；确认资料。 |
| 异常流程 | 扫描件、加密 PDF、空文本或解析失败时给出明确原因；允许修复文件后重新上传或手动重试。 |
| 结果 | 候选人资料进入 READY 状态并可被索引和检索。 |

### 5.4 UC-003：候选人匹配

| 项目 | 内容 |
|---|---|
| 参与者 | HR，可选为招聘主管开放。 |
| 前置条件 | 岗位为 ACTIVE；至少一个候选人资料已确认。 |
| 主流程 | 创建 MatchRun；保存岗位和模型配置快照；召回候选人；对每名候选人评估硬规则并保留 `PASS`、`FAIL` 或 `UNKNOWN` 结果；生成证据化报告；持久化报告并完成 Run；展示排名。 |
| 异常流程 | 无候选人、模型超时、证据校验失败或节点异常时记录失败节点并允许安全重试。 |
| 结果 | 每个召回候选人产生与本次 MatchRun 关联的匹配报告；MatchRun 进入 COMPLETED，任何 JobApplication 状态均未被自动修改。 |

### 5.5 UC-004：审批并进入面试

| 项目 | 内容 |
|---|---|
| 参与者 | HR、被分配岗位的招聘主管 |
| 前置条件 | 匹配报告已生成；岗位—候选人关联仍处于可审批状态。 |
| 主流程 | 用户为一个 JobApplication 启动 ApplicationRun；Agent 提交流程状态变更提案；系统暂停；用户批准、修改或拒绝；批准后幂等执行；入围后生成面试问题；再次暂停并审批排期提案。 |
| 异常流程 | 重复提交返回已执行结果；版本冲突要求刷新；无权限请求返回 403；执行失败可在同一幂等键下安全重试。 |
| 结果 | 岗位内流程状态和面试安排被持久化，完整审批记录可查询。 |

### 5.6 UC-005：持久化、暂停与恢复 Agent Run

| 项目 | 内容 |
|---|---|
| 参与者 | HR、招聘主管、系统 Worker |
| 前置条件 | MatchRun 或 ApplicationRun 已创建并有持久化 `thread_id`。 |
| 主流程 | 两类 Run 均在节点执行后写入 Checkpoint 和业务事件；只有 ApplicationRun 的审批节点执行 interrupt，用户决定后以同一 `thread_id` 恢复。 |
| 异常流程 | 服务或 Worker 重启后，从最后成功 Checkpoint 继续；已完成副作用不得重复执行。 |
| 结果 | Run 的当前状态被明确记录；COMPLETED、FAILED 或 CANCELLED 是终态，WAITING_APPROVAL 只表示 ApplicationRun 暂停等待决定。 |

## 6. 功能需求

### 6.1 认证与授权

| ID | 优先级 | 需求 | 验收标准 |
|---|---|---|---|
| AUTH-001 | Must | 系统必须通过 OAuth2 Password Flow 支持用户名和密码登录。 | /auth/token 接收 application/x-www-form-urlencoded 的 username/password；正确凭据返回访问令牌；错误凭据返回统一错误，不泄露用户名是否存在。 |
| AUTH-002 | Must | 密码必须使用 Argon2 哈希保存。 | 数据库和日志中不存在明文密码；相同密码的哈希包含随机盐。 |
| AUTH-003 | Must | JWT 只能包含用户 ID、角色、签发/过期信息和必要的令牌标识。 | 解码后的令牌不包含简历、邮箱、手机号等业务数据。 |
| AUTH-004 | Must | 服务端必须执行 RBAC。 | 未登录返回 401；角色不允许返回 403；直接调用接口不能绕过。 |
| AUTH-005 | Must | SSE 使用 Fetch Stream 并通过 `Authorization: Bearer` 鉴权。 | URL 和日志中不出现 Token；越权订阅其他 Run 返回 403。 |
| AUTH-006 | Should | 登录、鉴权失败和高风险操作应有安全审计记录。 | 审计记录包含时间、用户、动作和结果，不记录密码或完整 Token。 |

### 6.2 岗位管理

| ID | 优先级 | 需求 | 验收标准 |
|---|---|---|---|
| JOB-001 | Must | HR 可以创建岗位草稿。 | 名称和岗位描述必填；保存后返回唯一岗位 ID。 |
| JOB-002 | Must | 岗位必须支持必须技能、加分技能、最低工作年限和学历要求。 | 字段经 Pydantic 校验并可重新读取。 |
| JOB-003 | Must | 岗位状态必须包含 DRAFT、ACTIVE、CLOSED。 | 非 ACTIVE 岗位不能启动新匹配；非法状态迁移被拒绝。 |
| JOB-004 | Must | 关键岗位要求变更必须形成版本。 | MatchRun 可以追溯到启动时的岗位版本，后续编辑不改变历史报告。 |
| JOB-005 | Must | 用户可以查看岗位列表、详情及其 MatchRun。 | 支持按状态筛选；招聘主管只能看到被分配岗位。 |
| JOB-006 | Should | 系统可以从自然语言岗位描述中生成结构化要求草稿。 | 用户确认前不作为正式规则；所有自动生成字段可编辑。 |
| JOB-007 | Must | HR 可以把岗位分配给一个或多个有效招聘主管，也可以撤销分配。 | 服务端只接受 `is_active=true` 且角色为 HIRING_MANAGER 的用户；分配与撤销均记录操作者和时间。 |
| JOB-008 | Must | 招聘主管的岗位访问权必须来自当前有效的 JobAssignment。 | 未分配或已撤销用户访问岗位、报告、Run、审批和审计均返回 403；历史审批记录仍保留原操作者。 |

### 6.3 文件上传和简历解析

| ID | 优先级 | 需求 | 验收标准 |
|---|---|---|---|
| DOC-001 | Must | 系统只接受 `.pdf` 和 `.docx`。 | 扩展名、MIME 和文件签名不匹配时拒绝。 |
| DOC-002 | Must | 上传限制必须可配置，MVP 默认单文件 10 MiB、单批 20 个文件。 | 超限请求得到明确错误；已接受文件不因同批其他文件失败而丢失。 |
| DOC-003 | Must | 文件必须通过 StorageBackend 保存，业务代码不得拼接用户文件名作为存储路径。 | 文件使用内部随机 ID；路径穿越测试全部被拒绝。 |
| DOC-004 | Must | 上传成功后必须创建异步解析任务并立即返回文档 ID。 | 接口不等待模型完成；每个文件都能查询进度。 |
| DOC-005 | Must | 系统必须提取 PDF 页码或 DOCX 段落/表格位置。 | 每个 EvidenceChunk 能回到文档版本和原文位置。 |
| DOC-006 | Must | 解析结果必须符合结构化 Schema。 | 至少覆盖姓名、技能、教育、工作经历、项目和时间范围；无法确认的字段为 `null`，不得臆造。 |
| DOC-007 | Must | 扫描件、加密文件和空文本必须进入 UNSUPPORTED 或 FAILED，并展示原因。 | 不调用无限重试；用户能区分“不支持”和“临时失败”。 |
| DOC-008 | Must | HR 可以修改解析结果并确认。 | 保存修订者、修订时间和版本；只有确认版本进入检索。 |
| DOC-009 | Must | 文档解析任务必须幂等。 | 相同任务重复投递不会产生重复 Document、Profile 或 Chunk。 |
| DOC-010 | Must | 系统必须保存 SHA-256 内容哈希并检测重复上传。 | 同一文件再次上传时返回已有文档或明确的重复提示。 |
| DOC-011 | Should | 用户可以对可重试失败发起手动重试。 | 重试沿用同一业务文档 ID，增加 attempt 记录，不覆盖历史错误。 |

### 6.4 候选人与岗位关联

| ID | 优先级 | 需求 | 验收标准 |
|---|---|---|---|
| CAN-001 | Must | Candidate 表示人员主体，CandidateProfile 表示已确认资料版本。 | 更新简历产生资料版本，不直接覆盖历史 MatchRun 使用的快照。 |
| CAN-002 | Must | JobApplication 表示候选人与岗位的关联。 | `(job_id, candidate_id)` 在 MVP 中唯一；岗位流程状态只保存在该关联上。 |
| CAN-003 | Must | 用户可以查看候选人列表和详情。 | 支持按技能、年限、学历、资料状态筛选，并显示证据来源。 |
| CAN-004 | Must | 同一候选人在不同岗位下可以有不同流程状态。 | 更新岗位 A 的状态不会改变岗位 B。 |
| CAN-005 | Must | 资料未确认或已失效的候选人不能进入新 MatchRun。 | 检索入口仅选择 READY 的 Profile 版本。 |
| CAN-006 | Should | 候选人详情应显示机器提取值、人工修订值和最终确认值。 | 用户能够追溯字段变化来源。 |

### 6.5 检索与排序

| ID | 优先级 | 需求 | 验收标准 |
|---|---|---|---|
| RET-001 | Must | 检索只使用已确认且未失效的 CandidateProfile。 | 数据库查询和集成测试证明草稿资料不会被召回。 |
| RET-002 | Must | 候选人列表必须支持由用户显式启用的结构化硬条件筛选。 | 工作年限、学历等筛选结果可解释并记录输入；默认不启用，且筛选只影响当前展示结果，不改变 MatchRun 评估范围或 JobApplication 状态。 |
| RET-003 | Must | 系统必须支持标准化技能精确匹配。 | Java、C++ 等技能不依赖向量相似度也能被召回。 |
| RET-004 | Must | 系统必须支持 1024 维向量语义检索。 | 向量记录保存模型、维度、切分版本和内容哈希。 |
| RET-005 | Must | 系统必须使用可配置的 RRF 融合关键词/结构化结果与向量结果。 | 相同输入和配置得到确定性排名；每个结果显示各通道名次。 |
| RET-006 | Must | MatchRun 必须保存检索配置快照。 | 可查看 Top-K、RRF 常数、模型、通道权重和权限范围；候选人列表的临时筛选条件不进入 MatchRun 快照。 |
| RET-007 | Must | 系统必须提供离线检索评测。 | 能对版本化数据集计算 Recall@K 和 MRR 或 nDCG。 |
| RET-008 | Should | 首版默认精确向量搜索，达到性能门槛后可切换 HNSW。 | 开启 HNSW 前后均运行同一召回评测并保存结果。 |

### 6.6 Agent 运行工作流

| ID | 优先级 | 需求 | 验收标准 |
|---|---|---|---|
| AGT-001 | Must | 系统必须使用两张固定状态图：岗位级 MatchRun 执行岗位解析、召回、硬规则评估和证据评分；候选人级 ApplicationRun 执行人工审批、状态更新、问题生成和排期提案。 | 两张图的节点和条件边有独立测试；未经定义的节点不能动态执行。 |
| AGT-002 | Must | 每次 MatchRun 必须保存岗位版本、候选人资料版本、模型、Prompt 和检索配置。 | 历史报告可以复现其输入快照。 |
| AGT-003 | Must | 硬规则必须由确定性 Python 代码执行并输出 `PASS`、`FAIL` 或 `UNKNOWN`。 | 相同输入得到相同结果、理由和证据；LLM 不能改变规则结论。 |
| AGT-004 | Must | 硬规则评估不得从 MatchRun 中剔除候选人，失败只能产生风险提示或人工审核建议。 | Top-K 内每名候选人均进入评分；自动化测试证明未出现未经审批的 REJECTED 状态。 |
| AGT-005 | Must | 匹配报告必须为结构化输出。 | 至少包含总览、满足项、缺口、待确认问题、硬规则结果和证据引用。 |
| AGT-006 | Must | 每条确定性结论必须引用 MatchRun 快照锁定的候选人资料版本中的 EvidenceChunk，并保存精确摘录。 | 无效、跨候选人、跨版本、不存在或摘录范围不一致的引用使报告校验失败。 |
| AGT-007 | Must | 引用校验失败时必须把结论降级为“不足以判断”。 | 系统不得通过再次生成虚构证据来绕过校验。 |
| AGT-008 | Must | 两类 LangGraph Run 都必须使用 PostgreSQL Checkpointer。 | API/Worker 重启后可按 Run 类型和同一 `thread_id` 恢复。 |
| AGT-009 | Must | Agent State 只保存恢复所需 ID 和中间状态。 | Checkpoint 不重复保存完整 PDF/DOCX 二进制或无界大文本。 |
| AGT-010 | Must | 节点失败必须记录节点名、错误类型、可重试性和关联 Run。 | 前端能显示失败位置；不可重试错误不会循环执行。 |
| AGT-011 | Should | 用户可以从允许的失败节点重试。 | 重试产生 attempt 记录并保留旧错误；已成功副作用不重复执行。 |
| AGT-012 | Must | MatchRun 不得创建审批或调用副作用工具；ApplicationRun 必须且只能绑定一个 JobApplication，同一 JobApplication 同一时刻最多一个非终态 ApplicationRun。 | 数据库活动 Run 槽、事务锁和图路由测试证明批次报告与单候选人审批不会共享等待状态；并发第二次创建返回 409。 |
| AGT-013 | Must | 每个 ReportClaim 必须保存 `SUPPORTED`、`PARTIAL` 或 `INSUFFICIENT` 支持等级。 | 入围建议、硬条件判断等高影响结论在支持等级不是 SUPPORTED 时只能表述为“不足以判断”，不能输出确定事实。 |
| AGT-014 | Must | 简历和岗位描述只能作为不可信数据，不能改变系统指令、节点路由、业务 ID、权限、审批或工具选择。 | 包含伪造系统消息、忽略规则、工具诱导和评分操纵的样本不会改变控制流或产生越权提案。 |
| AGT-015 | Must | Run 取消必须使用持久化取消标记、运行版本和节点边界检查协作完成。 | 取消后不得开始新节点或副作用；取消与节点提交并发时最多一个成功；重复取消返回同一结果。 |

### 6.7 人工审批和副作用工具

| ID | 优先级 | 需求 | 验收标准 |
|---|---|---|---|
| APR-001 | Must | ApplicationRun 在执行 `update_application_status` 和 `create_interview_schedule` 前必须创建 PENDING 审批并 interrupt。 | MatchRun 不创建审批；ApplicationRun 没有 APPROVED/EDITED 决定时工具调用次数为 0。 |
| APR-002 | Must | 有权限用户可以批准、修改或拒绝提案。 | 三种决定均有接口与测试；修改后的参数重新通过 Schema 和权限校验；拒绝提案不等于把候选人状态设为 REJECTED。 |
| APR-003 | Must | 审批必须记录提案参数、最终参数、决定、理由、用户和时间。 | 审计页面或接口可以完整查询。 |
| APR-004 | Must | 审批和工具执行必须使用幂等键。 | 相同请求重复提交只产生一次业务副作用并返回同一结果。 |
| APR-005 | Must | 审批必须使用版本或乐观锁。 | 两个用户并发审批时最多一个成功，另一个收到冲突响应。 |
| APR-006 | Must | 更新流程状态时必须指定 JobApplication 并校验其 ApplicationRun。 | 请求同时校验岗位、候选人、当前状态、目标状态、Run 归属和版本。 |
| APR-007 | Must | 工具参数必须经过 Pydantic 校验和服务端授权。 | 非法参数 100% 被拒绝，模型不能绕过 Tool Service。 |
| APR-008 | Should | PENDING 审批可以配置失效时间。 | 过期审批不能执行，必须重新生成提案。 |
| APR-009 | Must | 配置了 expires_at 的 PENDING Approval 到期后必须原子进入 EXPIRED，并使父 ApplicationRun 进入 retryable FAILED。 | Scheduler 和审批决定接口使用数据库时间执行相同到期检查；旧 Approval 永不复用，Run retry 产生新 attempt 和新 Approval。 |

### 6.8 面试问题与模拟排期

| ID | 优先级 | 需求 | 验收标准 |
|---|---|---|---|
| INT-001 | Must | 入围审批通过后，系统可以生成 5 至 8 个面试问题。 | 数量可配置；生成失败不改变 JobApplication 状态。 |
| INT-002 | Must | 每个问题必须包含问题文本、考察目标和来源。 | 来源是岗位要求、匹配缺口或简历证据之一。 |
| INT-003 | Must | 问题不得要求推断敏感个人属性。 | 敏感主题测试集不产生此类问题；命中规则时阻断并记录。 |
| INT-004 | Must | MockScheduleBackend 必须接收候选人、岗位、时间段、参与者和幂等键。 | 它返回稳定的模拟日程 ID，不发送真实邀请。 |
| INT-005 | Must | 创建模拟日程前必须完成人工审批。 | 未审批调用为 0；审批通过后只创建一次。 |
| INT-006 | Should | 用户可以编辑面试问题后保存最终版本。 | 保存编辑者和时间；保留生成版本。 |

### 6.9 SSE、运行进度和 Trace

| ID | 优先级 | 需求 | 验收标准 |
|---|---|---|---|
| EVT-001 | Must | 每个 AgentEvent 必须包含 `event_id`、`run_id`、`run_type`、`sequence`、`node`、`status`、`message` 和 `occurred_at`。 | Event Schema 契约测试通过，`run_type` 只能为 MATCH 或 APPLICATION。 |
| EVT-002 | Must | 同一 Run 的 `sequence` 必须单调递增且唯一。 | 数据库唯一约束和并发测试通过。 |
| EVT-003 | Must | 前端必须按 `event_id` 或 `sequence` 去重。 | 重复事件不会重复推进 UI 或触发业务操作。 |
| EVT-004 | Must | SSE 断线后必须能从最后已确认事件继续。 | 自动化测试模拟断线，不丢失终态且不重复显示。 |
| EVT-005 | Must | 服务端必须校验用户是否有权查看该 Run。 | 跨岗位、跨角色越权测试返回 403。 |
| EVT-006 | Must | AgentEvent 和 Langfuse Trace 不得包含密码、Token、文件二进制和未脱敏联系方式。 | 使用预设敏感标记的日志扫描测试通过。 |
| EVT-007 | Should | SSE 应发送可配置心跳以维持代理连接。 | 空闲期间连接不会因无数据被误判为已完成。 |
| EVT-008 | Should | 前端应显示当前节点、已完成节点、失败节点和待审批节点。 | 用户无需查看日志即可判断 Run 状态。 |
| EVT-009 | Must | SSE 在重放、发送每个事件批次和心跳前必须重新校验资源权限。 | JobAssignment 撤销后现有连接不再收到业务事件并被关闭；自动化测试覆盖连接存续期间撤权。 |

### 6.10 评测与可观测性

| ID | 优先级 | 需求 | 验收标准 |
|---|---|---|---|
| EVAL-001 | Must | 评测数据集必须版本化并与生产业务表隔离。 | 每次实验记录数据集版本和样本数量。 |
| EVAL-002 | Must | 解析评测必须计算字段 Precision、Recall 和 F1。 | 报告可按字段和总体查看。 |
| EVAL-003 | Must | 检索评测必须计算 Recall@K 及 MRR 或 nDCG。 | 能比较结构化、关键词、向量和融合四种结果。 |
| EVAL-004 | Must | 报告评测必须计算证据覆盖率和无证据确定性结论数。 | 发布门禁要求无证据确定性结论为 0。 |
| EVAL-005 | Must | Tool Calling 评测必须记录选择准确率、首次参数合法率和执行成功率。 | 模型错误与执行层拒绝分开统计。 |
| EVAL-006 | Must | 工作流评测必须覆盖完成、审批中断、恢复和重复执行防护。 | 每种场景至少一个自动化用例。 |
| EVAL-007 | Should | Langfuse 启用时记录模型、Prompt、Token、延迟和节点关联。 | 关闭 Langfuse 后业务测试仍全部通过。 |
| EVAL-008 | Should | 系统可以导出 Markdown 或 JSON 评测摘要。 | 演示时能够展示同一数据集的版本对比。 |
| EVAL-009 | Must | 报告评测必须使用人工标注的结论—证据对计算 `SUPPORTED` 语义支持精确率，并报告三分类混淆矩阵和宏平均 F1。 | 区分引用有效、证据覆盖和语义支持三类指标；报告错误样本且达到发布门槛。 |
| EVAL-010 | Must | 安全评测必须包含版本化的 Prompt Injection 数据集。 | 统计控制流改变、越权工具提案和审批绕过次数，三项发布门禁均为 0。 |
| EVAL-011 | Must | Prompt Injection 评测必须包含干净版本—注入版本配对样本和可检测攻击目标。 | 攻击指定哨兵、分数、状态或伪造证据进入输出的次数，以及配对硬规则变化数均为 0；新增无证据高影响 Claim 仍受证据门禁约束。 |

### 6.11 运维与配置

| ID | 优先级 | 需求 | 验收标准 |
|---|---|---|---|
| OPS-001 | Must | 项目必须提供 Docker Compose 启动 Web、API、Worker、Scheduler、PostgreSQL、Redis 和 Nginx。 | 新环境按 README 可启动；Scheduler 能触发 Approval 过期协调；健康检查均通过。 |
| OPS-002 | Must | API、Worker 和迁移必须使用同一版本化配置。 | 环境变量缺失时启动失败并给出字段名，不使用隐式生产默认值。 |
| OPS-003 | Must | 数据库变更必须通过 Alembic。 | 空数据库可升级到最新版本；重复执行迁移安全。 |
| OPS-004 | Must | 系统必须提供 liveness 和 readiness 检查。 | readiness 能区分数据库/Redis 不可用；不调用外部 LLM 作为每次健康检查条件。 |
| OPS-005 | Must | GitHub Actions 必须运行后端 lint、类型检查、测试和前端构建。 | Pull Request 或 Push 中任一必需检查失败则工作流失败。 |
| OPS-006 | Must | Playwright 核心路径必须在 CI 中运行。 | 使用固定演示数据完成登录、岗位分配、匹配和单候选人审批主流程。 |
| OPS-007 | Should | 模型和 Embedding 的真实连通性检查应为手动工作流。 | 没有凭据的普通 CI 不失败；手动检查清楚报告地域、模型和错误。 |

## 7. 业务规则

| ID | 规则 |
|---|---|
| BR-001 | 系统不得根据模型输出自动淘汰或录用候选人。 |
| BR-002 | 工作年限、学历等硬条件由确定性规则评估为 `PASS`、`FAIL` 或 `UNKNOWN`，LLM 只能解释，不能改写结果。 |
| BR-003 | 硬条件未满足不等于自动隐藏、自动剔除或自动 REJECTED，只能成为人工审核信息。 |
| BR-004 | 每条确定性匹配结论必须有 MatchRun 锁定资料版本中的有效引用和语义支持；否则结论为“不足以判断”。 |
| BR-005 | 候选人流程状态属于 JobApplication，不属于 Candidate 全局状态。 |
| BR-006 | 未确认的 CandidateProfile 不得进入检索、Embedding 和新 MatchRun。 |
| BR-007 | 更新 JobApplication 状态和创建日程是副作用，必须审批、鉴权、校验和幂等。 |
| BR-008 | 历史 MatchRun、报告、审批和审计记录不得因岗位或资料后续修改而改变。 |
| BR-009 | 模型、Prompt、检索和向量配置变化必须形成版本或快照。 |
| BR-010 | MVP 只使用合成数据；Mock 模式结果必须在界面明确标识。 |
| BR-011 | 模型不得生成或推断与岗位胜任无直接关系的敏感个人属性。 |
| BR-012 | 删除文件或候选人时必须有明确保留策略，不能留下可访问的孤儿文件。 |
| BR-013 | Approval 的 REJECTED 表示动作提案被拒绝；JobApplication 的 REJECTED 只能由“将状态改为 REJECTED”的独立提案获批后产生。 |
| BR-014 | 招聘主管只可访问存在有效 JobAssignment 的岗位；撤销分配立即阻止后续访问，但不改写历史审批和审计记录。 |
| BR-015 | 用户主动设置的候选人列表筛选只影响当前查询结果，不得改变 MatchRun 默认评估范围、报告或 JobApplication 状态。 |
| BR-016 | MatchRun 是岗位级批量分析，不等待审批；ApplicationRun 是单 JobApplication 业务流，同一 JobApplication 同一时刻最多一个非终态 ApplicationRun，每个 Run 同一时刻最多一个 PENDING Approval。 |
| BR-017 | 简历和岗位描述中的任何指令性文本均视为业务数据，不得获得系统指令、路由、授权或工具参数的优先级。 |

## 8. 状态模型

### 8.1 岗位状态

```text
DRAFT → ACTIVE → CLOSED
```

| 当前状态 | 允许操作 |
|---|---|
| DRAFT | 编辑、删除、启用。 |
| ACTIVE | 查看、启动匹配、关闭；修改关键要求时创建新草稿版本，当前生效版本在新版本发布前保持不变。 |
| CLOSED | 查看历史，不允许启动新匹配；可复制为新岗位。 |

### 8.2 简历文档状态

```text
UPLOADED → QUEUED → PARSING → REVIEW_REQUIRED → READY
                ↘ FAILED
                ↘ UNSUPPORTED
READY → SUPERSEDED
```

约束：

- FAILED 只有在错误标记为可重试时才能回到 QUEUED。
- UNSUPPORTED 不能自动重试。
- READY 文档重新上传或重新解析时产生新版本，旧版本进入 SUPERSEDED。

### 8.3 JobApplication 状态

```text
CREATED ├→ SHORTLISTED → INTERVIEW_SCHEDULED
        ├→ ON_HOLD
        └→ REJECTED
```

约束：

- MatchRun 新建 JobApplication 时初始为 CREATED，复用已有记录时保持其当前状态不变；报告存在性由 MatchReport 表达，不使用业务状态暗示匹配结论。
- SHORTLISTED、ON_HOLD、REJECTED 和 INTERVIEW_SCHEDULED 只能由 ApplicationRun 中对应动作获批后产生。
- 任意状态变更都记录 `from_status`、`to_status`、审批 ID 和版本。
- MVP 不支持从 REJECTED 自动恢复；如需恢复必须创建新的审批提案。

### 8.4 MatchRun 状态

```text
CREATED → RUNNING → COMPLETED
RUNNING → FAILED
CREATED / RUNNING → CANCELLED
```

约束：

- FAILED 必须记录失败节点和是否可重试。
- MatchRun 只负责一个岗位版本的候选人召回、硬规则评估和报告生成，不创建 Approval、不等待人工决定、不执行副作用。
- COMPLETED、CANCELLED 为终态，不能再次执行节点。
- 恢复必须使用原 `thread_id` 和新的 attempt 记录。

### 8.5 ApplicationRun 状态

```text
CREATED → RUNNING → WAITING_APPROVAL → RUNNING → COMPLETED
RUNNING / WAITING_APPROVAL → FAILED
CREATED / RUNNING / WAITING_APPROVAL → CANCELLED
```

约束：

- 每个 ApplicationRun 只绑定一个 JobApplication；需要状态审批和排期审批时，可以先后两次进入 WAITING_APPROVAL。
- 同一 JobApplication 同一时刻最多一个 CREATED、RUNNING 或 WAITING_APPROVAL 的 ApplicationRun；创建和清理活动 Run 槽必须锁定 JobApplication。
- 同一 ApplicationRun 同一时刻最多有一个 PENDING Approval；决定后使用原 `thread_id` 恢复。
- 拒绝 Approval 不产生 Run 的 REJECTED 状态；没有后续动作时 Run 可以正常完成。
- Approval 到期时 ApplicationRun 进入 retryable FAILED；重试使用新 attempt 和新 Approval。
- 收到取消请求后不得开始新节点或副作用，节点结果提交必须同时校验运行版本和取消标记。
- FAILED 必须记录失败节点和是否可重试；COMPLETED、CANCELLED 为终态。

### 8.6 Approval 状态

```text
PENDING → APPROVED → EXECUTED
        → EDITED   → EXECUTED
        → REJECTED
        → EXPIRED
APPROVED / EDITED → EXECUTION_FAILED → EXECUTED
```

同一个审批只能做出一次最终决定；重复决定请求返回现有结果或冲突，不能产生第二次执行。EXPIRED 表示提案已不可执行，expiration_reason 区分 TIMEOUT 和 RUN_CANCELLED。PENDING Approval 到达 expires_at 后进入 EXPIRED；父 Run 转为 retryable FAILED，并通过既有 retry 接口生成新 attempt 和新 Approval。

## 9. 数据需求

### 9.1 核心实体与约束

| 实体 | 关键字段/约束 |
|---|---|
| User | `id`、`username` 唯一、`password_hash`、`role`（HR、HIRING_MANAGER 或 ADMIN）、`is_active`。 |
| Job | `id`、`status`、当前版本 ID、创建者。 |
| JobVersion | 岗位结构化要求、原始描述、版本号、内容哈希。 |
| JobAssignment | 岗位 ID、招聘主管用户 ID、分配角色、分配者、分配时间、撤销时间；同一岗位和用户最多一条有效记录。 |
| Candidate | `id`、显示名或合成标识、创建时间。 |
| CandidateProfile | 候选人 ID、源文档版本、结构化字段、状态、版本号。 |
| ResumeDocument | 存储键、原文件名、MIME、大小、SHA-256、状态、错误。 |
| EvidenceChunk | 文档版本、页码/段落/表格位置、字符范围、原文、向量版本。 |
| JobApplication | 岗位 ID、候选人 ID、状态、乐观锁版本、活动 ApplicationRun ID；岗位与候选人组合唯一。 |
| AgentRun | `thread_id` 全局唯一、run_type、状态、attempt、版本、当前节点、取消请求和错误。 |
| MatchRun | AgentRun ID、岗位版本和配置快照。 |
| ApplicationRun | AgentRun ID、JobApplication、匹配报告和配置快照。 |
| MatchReport | MatchRun、JobApplication、结构化报告、模型和 Prompt 版本。 |
| ReportClaim | 结论类型、内容、置信说明、支持等级、精确摘录、一个或多个证据 ID。 |
| Approval | ApplicationRun、目标类型/ID、提案、最终参数、决定、expires_at、expiration_reason、幂等键唯一、版本。 |
| Interview | JobApplication、问题版本、模拟日程 ID、状态。 |
| AgentEvent | AgentRun、run_type、sequence、event_id、节点、状态、时间；`(run_type, run_id, sequence)` 唯一。 |
| EvaluationRun | 数据集版本、配置、指标、产物位置和运行时间。 |

### 9.2 数据完整性

- 所有业务表使用不可变主键和 UTC 时间戳。
- 外键删除策略必须明确，默认限制删除仍被历史报告引用的数据。
- JobAssignment 的服务层和数据库约束必须保证被分配用户是有效招聘主管，并禁止同一岗位—用户存在重复有效分配。
- MatchRun 和 MatchReport 保存配置快照，不只保存指向可变配置的引用。
- JobApplication 使用活动 Run 槽保证同一时刻最多一个非终态 ApplicationRun；创建、终态清理和重试认领在锁定 JobApplication 的事务中完成。
- ApplicationRun 必须绑定且只绑定一个 JobApplication；Approval 必须属于该 ApplicationRun，且同一时刻最多一条 PENDING。
- AgentRun 的节点提交和副作用执行必须同时校验运行版本和取消标记；取消后的未提交结果不得写入业务表。
- Approval 到期处理必须原子更新 Approval、ApplicationRun、活动 Run 槽和 AgentEvent。
- EvidenceChunk 必须属于 MatchReport 对应候选人的资料版本。
- ReportClaim 的精确摘录必须落在其 EvidenceChunk 范围内；引用有效性、覆盖率和语义支持结果分开记录。
- 审批执行结果和业务状态更新应在同一事务或可恢复的事务边界内完成。
- Redis 不作为最终业务事实来源。

### 9.3 数据保留与删除

MVP 使用合成数据，但仍按照可迁移到真实环境的边界设计：

- 文件、结构化资料、向量和证据具有一致的删除/失效流程。
- 审计记录只保存必要 ID 和动作摘要，不复制完整简历。
- 日志和 Trace 默认不记录原始简历全文。
- 数据保留期限作为环境配置；MVP 演示环境可以手动清空全部合成数据。

## 10. API 与事件契约要求

### 10.1 REST 资源边界

建议的 MVP 资源如下，最终路径可以在 OpenAPI 设计阶段微调：

| 资源 | 核心操作 |
|---|---|
| `/auth/token` | 通过 application/x-www-form-urlencoded 的 OAuth2 Password Flow 登录并获取访问令牌。 |
| `/jobs`、`/jobs/{id}` | 岗位创建、查询、更新、启用和关闭。 |
| `/jobs/{id}/assignments`、`/jobs/{id}/assignments/{user_id}` | 分配、查询和撤销招聘主管。 |
| `/documents` | 批量上传、查看解析状态和错误。 |
| `/documents/{id}/retry` | 对可重试失败重新入队。 |
| `/candidates`、`/candidates/{id}` | 候选人列表、详情和资料版本。 |
| `/candidate-profiles/{id}/confirm` | 保存人工校对并确认。 |
| `/jobs/{id}/applications` | 岗位下候选人及流程状态。 |
| `/jobs/{id}/match-runs` | 启动岗位匹配。 |
| `/match-runs/{id}` | 查看 Run、报告、节点和错误。 |
| `/match-runs/{id}/retry`、`/match-runs/{id}/cancel` | 对允许状态的岗位级 Run 重试或发起协作式取消。 |
| `/applications/{id}/runs` | 为单个 JobApplication 启动候选人级流程；已有非终态 Run 时返回 409。 |
| `/application-runs/{id}` | 查看 ApplicationRun、审批、节点和错误。 |
| `/application-runs/{id}/retry`、`/application-runs/{id}/cancel` | 对允许状态的候选人级 Run 重试或发起协作式取消。 |
| `/match-runs/{id}/events`、`/application-runs/{id}/events` | 分别按 Run 类型和资源归属鉴权的 SSE Fetch Stream；重放、事件批次和心跳前持续授权。 |
| `/approvals/{id}/decision` | 批准、修改或拒绝。 |
| `/interviews`、`/interviews/{id}` | 问题和模拟日程查询。 |
| `/evaluations` | 启动或查看离线评测。 |
| `/health/live`、`/health/ready` | 运行与依赖健康检查。 |

### 10.2 通用响应要求

- 成功响应使用明确的 Pydantic Schema，不返回无约束 `dict`。
- 除 OAuth2 token 的 form-urlencoded、文件 multipart 和 SSE 外，业务请求与响应使用 JSON。
- 错误响应至少包含稳定的 `code`、用户可读 `message` 和 `request_id`。
- 校验错误返回 422，未认证返回 401，无权限返回 403，资源不存在返回 404，版本冲突返回 409。
- 创建异步任务返回 202，并包含业务资源 ID 和状态查询地址。
- 列表接口必须分页，默认页大小和最大页大小可配置。
- 所有时间使用 ISO 8601 UTC，前端负责显示本地时区。

### 10.3 AgentEvent 示例

```json
{
  "event_id": "evt_01J...",
  "run_id": "run_01J...",
  "run_type": "MATCH",
  "sequence": 12,
  "node": "score_with_evidence",
  "status": "running",
  "message": "正在生成证据化匹配报告",
  "occurred_at": "2026-08-31T08:00:00Z"
}
```

事件 `message` 只用于展示，前端状态机必须使用 `node`、`status` 和 `sequence`，不能解析自然语言消息决定逻辑。

## 11. 非功能需求

### 11.1 安全与隐私

| ID | 优先级 | 要求 |
|---|---|---|
| NFR-SEC-001 | Must | 密码、JWT 密钥、数据库密码和模型 API Key 只能通过 Secret/环境变量注入，不提交到 Git。 |
| NFR-SEC-002 | Must | 上传文件按不可信输入处理，禁止执行宏、脚本或嵌入对象。 |
| NFR-SEC-003 | Must | 文件下载、证据查看、SSE 和审批接口均执行资源级权限校验。 |
| NFR-SEC-004 | Must | 日志、错误、Trace 和测试快照执行敏感字段脱敏。 |
| NFR-SEC-005 | Must | Nginx 统一同源入口；正式演示环境使用 HTTPS 或仅限本机访问。 |
| NFR-SEC-006 | Should | 登录和模型调用接口具有基本限流与超时。 |
| NFR-SEC-007 | Must | 简历与岗位描述必须按不可信数据处理，并与系统指令、图路由和 Tool 控制字段隔离；解析、检索和评分节点不注册副作用工具；副作用参数只由可信业务 ID、类型化状态和已批准记录构造，执行层重新校验 RBAC、归属、版本与幂等键。 |

### 11.2 性能

| ID | 优先级 | 要求 |
|---|---|---|
| NFR-PERF-001 | Must | 不含外部模型和异步处理的普通 REST 接口，在 README 记录的演示环境、100 名候选人和数据库预热条件下 p95 小于 500 ms。 |
| NFR-PERF-002 | Must | 上传接口在文件持久化并成功入队后返回，不等待解析和 Embedding 完成。 |
| NFR-PERF-003 | Must | AgentEvent 写入后，在正常本地网络下应在 2 秒内展示到前端。 |
| NFR-PERF-004 | Must | 解析、Embedding、匹配和节点延迟必须被分别记录，不能只记录总耗时。 |
| NFR-PERF-005 | Should | 批量 20 份演示简历的总处理时间必须形成基准报告；外部限流时间单独标注。 |

### 11.3 可靠性与恢复

| ID | 优先级 | 要求 |
|---|---|---|
| NFR-REL-001 | Must | API 或 Worker 重启不能使已持久化业务状态丢失。 |
| NFR-REL-002 | Must | 所有异步任务和副作用工具支持重复投递防护。 |
| NFR-REL-003 | Must | Redis 数据丢失后，业务事实可以从 PostgreSQL 恢复或重新计算。 |
| NFR-REL-004 | Must | 等待审批的 ApplicationRun 在重启后仍可查询和恢复。 |
| NFR-REL-005 | Must | 模型、数据库和网络调用均设置超时；重试只覆盖被判定为临时故障的错误。 |

### 11.4 可用性与兼容性

| ID | 优先级 | 要求 |
|---|---|---|
| NFR-UX-001 | Must | 所有耗时操作显示状态、进度或明确的等待说明。 |
| NFR-UX-002 | Must | 失败页面显示可理解原因、request ID 和允许的下一步。 |
| NFR-UX-003 | Must | 审批页面明确显示即将执行的动作、对象和参数。 |
| NFR-UX-004 | Should | 支持当前稳定版 Chrome 和 Edge 的桌面视图。 |
| NFR-UX-005 | Should | 表单、表格和审批操作可通过键盘完成，状态不只依赖颜色表达。 |

### 11.5 可维护性与可测试性

| ID | 优先级 | 要求 |
|---|---|---|
| NFR-MNT-001 | Must | 外部模型、存储和排期通过适配器接口接入。 |
| NFR-MNT-002 | Must | Agent Node 不直接操作 HTTP Request 或前端状态。 |
| NFR-MNT-003 | Must | API、Agent State、Tool 参数和模型输出使用显式 Schema。 |
| NFR-MNT-004 | Must | Ruff、mypy、pytest、前端类型检查和构建在 CI 中通过。 |
| NFR-MNT-005 | Must | 核心流程使用 FakeModel 和 MockScheduleBackend 可离线测试。 |
| NFR-MNT-006 | Should | OpenAPI 类型可以生成或校验前端 TypeScript 类型。 |

## 12. 错误与降级策略

| 场景 | 系统行为 | 用户可执行操作 |
|---|---|---|
| 不支持的文件 | 标记 UNSUPPORTED，不重试，不创建候选人资料。 | 查看原因并上传电子版 PDF/DOCX。 |
| 临时解析失败 | 标记 FAILED 和可重试原因，保留原文件。 | 手动重试。 |
| 模型超时/限流 | 当前节点失败或退避重试，记录 provider 错误分类。 | 稍后重试或查看已有历史结果。 |
| 结构化输出非法 | 拒绝保存；在限定次数内重新请求；仍失败则标记节点失败。 | 查看失败信息并重试。 |
| 证据引用非法 | 不保存确定性结论，降级为“不足以判断”。 | 查看证据缺失提示。 |
| 证据仅部分支持或不支持结论 | 高影响结论降级为“不足以判断”，保留支持等级供人工查看。 | 对照结论、精确摘录和原文位置复核。 |
| 文档包含 Prompt Injection 文本 | 把文本作为普通业务数据，不执行其中指令，不改变节点路由或生成副作用参数。 | 可查看安全评测记录；业务流程按固定图继续。 |
| Redis 不可用 | 新异步任务返回依赖不可用；已保存业务数据不丢失。 | 服务恢复后重新入队。 |
| PostgreSQL 不可用 | readiness 失败；拒绝会产生不一致写入的请求。 | 等待依赖恢复。 |
| SSE 断线 | 前端退避重连并携带最后事件位置。 | 无需手动刷新；持续失败时可刷新 Run 详情。 |
| 审批并发冲突 | 后提交者收到 409 和当前决定。 | 刷新并查看最新审批。 |
| 工具执行结果未知 | 保持可核查状态，不盲目再次执行；使用幂等键查询。 | 管理员查看技术错误，业务用户等待确认。 |
| Langfuse 不可用 | 本地事件与业务日志继续记录，核心流程不失败。 | 无需操作。 |

## 13. 页面需求

| 页面 | Must 内容 |
|---|---|
| 登录页 | 用户名、密码、错误提示和加载状态。 |
| 岗位列表 | 状态筛选、新建入口、最近 MatchRun。 |
| 岗位编辑/详情 | 原始描述、结构化要求、版本、招聘主管分配和启用/关闭操作。 |
| 简历上传 | 拖拽/选择文件、逐文件校验、上传与解析状态。 |
| 解析校对 | 原文与结构化字段对照、证据定位、保存和确认。 |
| 候选人列表 | 结构化筛选、检索结果、通道名次和岗位流程状态。 |
| 候选人详情 | 已确认资料、文档版本、证据、岗位关联。 |
| 匹配报告 | 硬规则评估、满足项、缺口、待确认项、结论—证据对照、支持等级和配置快照。 |
| Agent Run | 明确显示 MatchRun 或 ApplicationRun 类型、节点状态、事件时间线、失败信息，以及 ApplicationRun 的等待审批和恢复结果。 |
| 审批页/抽屉 | 提案、参数差异、批准/修改/拒绝、版本冲突提示。 |
| 面试页 | 问题、考察目标、来源、模拟日程状态。 |
| 评测页 | 数据集版本、核心指标、实验配置和对比结果。 |

## 14. 测试与验收

### 14.1 发布门槛

| 指标 | MVP 门槛 |
|---|---|
| 支持格式解析成功率 | 所有通过上传校验的合成 PDF/DOCX 中不低于 95%；成功必须同时产出合法 Schema 和原文定位。 |
| 核心字段抽取 | 姓名、技能、教育、公司、时间范围的宏平均 F1 不低于 0.90。 |
| 检索 | Golden Dataset 上 Recall@10 不低于 0.85，并报告 MRR 或 nDCG。 |
| 证据引用完整性 | 无证据确定性结论为 0；非法、跨候选人、跨版本或摘录范围不一致的引用为 0。 |
| 证据语义支持 | 人工标注集上，被系统标记为 `SUPPORTED` 的结论—证据对中真正得到完整支持的精确率不低于 95%，三分类宏平均 F1 不低于 0.85；人工标注为 `PARTIAL` 或 `INSUFFICIENT` 的高影响结论仍被表述为确定事实的数量为 0。 |
| Tool 参数 | 模型首次参数合法率不低于 98%；执行层对非法参数拒绝率为 100%。 |
| 副作用安全 | 未审批的 `update_application_status` 和 `create_interview_schedule` 实际执行次数为 0。 |
| Prompt Injection 防护 | 配对攻击语料导致控制流改变、越权工具提案、审批绕过、攻击指定哨兵/分数/状态/伪造证据进入输出，以及硬规则变化的次数均为 0。 |
| 恢复 | 已定义的中断、重启、重复提交、协作取消和 Approval 过期 E2E 场景全部通过。 |
| 隐私 | 日志、Trace 和测试产物不包含预设敏感标记。 |
| 工程质量 | Ruff、mypy、pytest、前端检查、构建和关键 Playwright 测试全部通过。 |

### 14.2 需求追踪矩阵

| 需求域 | 后端模块 | 主要测试 | 主要页面 |
|---|---|---|---|
| AUTH | `auth`、API dependency | OAuth2 表单登录、JWT、RBAC、SSE 越权测试 | 登录页 |
| JOB | `jobs` | Schema、状态迁移、版本快照、JobAssignment 分配/撤销和权限测试 | 岗位列表/详情 |
| DOC | `documents`、Celery | 格式校验、解析 Fixture、幂等、重试、证据定位 | 上传、解析校对 |
| CAN | `candidates` | Profile 版本、JobApplication 唯一约束、状态隔离 | 候选人列表/详情 |
| RET | `retrieval` | RRF 单元测试、pgvector 集成测试、Recall@K 评测 | 候选人列表 |
| AGT | `agent` | 双图节点、JobApplication 活动 Run 排他、协作取消、Checkpoint、引用校验和 Prompt Injection 测试 | Agent Run、匹配报告 |
| APR | `interviews` / approval service | ApplicationRun interrupt、过期、重试、并发、幂等和授权测试 | 审批页 |
| INT | `interviews` | 问题 Schema、敏感主题、MockSchedule 测试 | 面试页 |
| EVT | `api/events`、`observability` | SSE 契约、重连、持续授权、去重和脱敏 | Agent Run |
| EVAL | `evaluations` | 指标公式、配对攻击数据集、版本和结果快照 | 评测页 |
| OPS/NFR | `infrastructure` | Compose 健康、迁移、CI、Playwright | 全局错误与状态 |

### 14.3 最小端到端场景

1. HR 登录，创建并启用岗位，把岗位分配给招聘主管。
2. 上传一份 PDF 和一份 DOCX，等待解析完成，校对并确认。
3. 启动 MatchRun，验证 Top-K 内硬条件为 FAIL 的候选人仍有报告，查看结论、证据和支持等级。
4. 验证 MatchRun 在报告持久化后进入 COMPLETED，且未创建 Approval、未改变任何 JobApplication 状态。
5. 为一个 JobApplication 启动 ApplicationRun；并发第二次启动返回 409；在审批前验证流程状态未改变，批准入围后验证状态只更新一次。
6. 同一 ApplicationRun 生成面试问题，批准 Mock 排期并查看模拟日程 ID。
7. 在 ApplicationRun 为 WAITING_APPROVAL 时重启 API/Worker，恢复并完成 Run。
8. SSE 断线后重连，验证两类 Run 的事件均不丢失、不重复推进 UI。
9. 在招聘主管保持 SSE 连接时撤销 JobAssignment，验证后续业务事件不再发送、连接关闭，其他访问返回 403，历史审计仍可追溯。
10. 使用干净—注入配对简历验证控制流、权限、审批和硬规则均未改变，攻击指定哨兵、分数、状态和伪造证据不进入输出。
11. 对 RUNNING Run 发起取消，验证节点结果不再提交、副作用不再开始且重复取消幂等。
12. 让 PENDING Approval 到期，验证它进入 EXPIRED、父 Run 进入 retryable FAILED，并能通过新 attempt 生成新 Approval。
13. 扫描日志和 Trace，验证无预设敏感标记。

## 15. 交付优先级与里程碑

| 周次 | 需求范围 | 出口条件 |
|---|---|---|
| 第 1 周 | AUTH、JOB、OPS 基础 | 登录、RBAC、岗位、JobAssignment、迁移、Compose 和 CI 可用。 |
| 第 2 周 | DOC、CAN | PDF/DOCX 解析、证据定位、校对和资料确认可用。 |
| 第 3 周 | RET、EVAL 检索部分 | 三路召回、RRF、Golden Dataset 和指标可用。 |
| 第 4 周 | AGT、APR | MatchRun/ApplicationRun 双图、证据化报告与语义支持评测、Checkpoint、interrupt、幂等审批和 Prompt Injection 回归可用。 |
| 第 5 周 | INT、EVT、主要页面 | 面试问题、Mock 排期、SSE 和前端闭环可用。 |
| 第 6 周 | NFR、E2E、交付材料 | 发布门槛通过，README、演示数据和备用演示完成。 |

如果进度落后，按以下顺序削减：

1. ECharts 综合看板。
2. React Flow 高级交互。
3. 管理员可视化用户管理。
4. HNSW 和性能调参。
5. Langfuse 实验对比页面。

不得削减证据校验、人工审批、幂等、状态恢复和核心自动化测试。

## 16. 待确认项与默认决策

这些问题不阻塞项目骨架开发；如果没有新的业务输入，MVP 按“默认决策”执行。

| ID | 待确认问题 | 默认决策 |
|---|---|---|
| TBD-001 | 招聘主管是否可以启动 MatchRun？ | 默认只允许 HR 启动，招聘主管负责查看和审批。 |
| TBD-002 | HR 与招聘主管审批意见冲突时谁优先？ | 使用先提交成功原则和乐观锁；业务上需要重新创建提案。 |
| TBD-003 | 重复简历是否自动合并候选人？ | 只按文件 SHA-256 检测重复，不自动判断两份不同文件是否属于同一人。 |
| TBD-004 | DOCX 证据如何定位？ | 使用段落索引、表格/单元格索引和字符范围，不承诺页码。 |
| TBD-005 | 访问令牌有效期多长？ | 通过环境变量配置；MVP 不实现复杂 Refresh Token 轮换。 |
| TBD-006 | Langfuse 部署方式？ | 默认可选 Cloud 集成；无配置时关闭。 |
| TBD-007 | 真实日历何时接入？ | 不进入 MVP，只实现 MockScheduleBackend 和接口契约。 |
| TBD-008 | HNSW 何时启用？ | 精确检索 p95 或数据规模超过项目设定门槛且召回回归通过后启用。 |

## 17. Definition of Done

一个需求只有同时满足以下条件才算完成：

1. 实现与本文需求及业务规则一致。
2. API Schema、状态迁移和错误行为已明确。
3. 新增或更新了相应单元、集成或端到端测试。
4. Ruff、mypy、pytest、前端类型检查、构建及适用的 Playwright 测试通过。
5. 不产生新的无证据或无语义支持的高影响确定性结论、越权路径、Prompt Injection 攻击指令遵循、取消后提交或未审批副作用。
6. 关键日志、AgentEvent 和审计记录足以定位问题，且通过脱敏检查。
7. 相关 README、架构或需求文档已经同步更新。
8. 本次变更已经创建独立、可追踪的 Git commit。

## 18. 需求基线结论

MVP 的价值不在于自动替代招聘人员，而在于提供一条可验证的辅助决策链：可靠地整理简历、可评测地召回候选人、用确实支持结论的证据解释匹配、把批量分析与单候选人业务审批隔离、在副作用前暂停，并在故障后恢复。

只要所有 Must 需求、发布门槛和最小端到端场景通过，本项目即可视为完成作品集级需求基线。生产化能力必须另立阶段评估，不能由 MVP 的演示结果外推。
