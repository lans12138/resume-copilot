# 企业招聘 Copilot 技术栈选型与架构决策

> 文档日期：2026-08-31
>
> 项目方向：基于可恢复图编排的简历解析、人才检索与人机协同面试 Agent（立项选型为 LangGraph，最终运行时实现见 [ADR-0001](docs/adr/0001-agent-runtime-custom-engine-over-langgraph.md)）
>
> 项目目标：独立开发，六周完成，用于应聘 AI Agent 开发岗位

## 1. 选型背景

本项目面向企业 HR 和招聘主管，主要流程为：

~~~text
创建岗位
  ↓
批量上传 PDF/DOCX 简历
  ↓
结构化解析与人工校对
  ↓
混合检索候选人
  ↓
Agent 生成带证据的匹配报告
  ↓
岗位级 MatchRun 完成
  ↓
为单个 JobApplication 启动 ApplicationRun
  ↓
HR/招聘主管人工审批状态变更
  ↓
入围后生成面试问题并在再次审批后调用排期工具
~~~

技术选型围绕以下标准展开：

1. 与 AI Agent 开发岗位的能力要求匹配。
2. 六周内能够由一人独立完成。
3. 能够展示状态管理、工具调用、RAG、人工审批、评测和可观测性。
4. 避免为技术而技术，控制部署和运维复杂度。
5. 每个关键模块都应当可测试、可解释、可恢复。

下文对其他技术的排除只针对本项目，不代表这些技术本身不好。

## 2. 最终技术栈总览

| 层次 | 最终选择 |
|---|---|
| 总体架构 | 前后端分离、模块化单体、单 Agent 双运行图 |
| 前端 | React + TypeScript + Vite |
| UI 与交互 | Ant Design + React Router + React Flow + ECharts |
| 前端状态 | TanStack Query + Zustand |
| 通信 | REST + SSE |
| 后端 | Python 3.12 + FastAPI |
| 数据模型 | Pydantic v2 |
| ORM 与迁移 | SQLAlchemy 2 + Alembic |
| 认证授权 | OAuth2 Password Flow + JWT + RBAC |
| Agent 编排 | 自研有序节点引擎 `RunEngine` + 持久化 Checkpointer（`SqlCheckpointer`）——立项选型为 LangGraph，运行时最终实现及理由见 [ADR-0001](docs/adr/0001-agent-runtime-custom-engine-over-langgraph.md) |
| 主数据库 | PostgreSQL + pgvector |
| 检索 | PostgreSQL 结构化/关键词检索 + pgvector + RRF |
| 异步任务 | Redis + Celery |
| 文件存储 | Docker Volume + StorageBackend 抽象接口 |
| 文档解析 | PyMuPDF + python-docx |
| 大模型 | Qwen qwen3.7-plus |
| Embedding | qwen3.7-text-embedding，1024 维 |
| 模型接入 | OpenAI Python SDK + 百炼兼容接口 |
| 可观测与评测 | Langfuse + 自建评测 Harness |
| 测试 | pytest + Playwright |
| 代码质量 | Ruff + mypy |
| 部署 | Docker Compose + Nginx |
| CI | GitHub Actions |

### 2.1 关于 MinIO 的修正

最初方案曾考虑使用 MinIO 保存简历附件。当前 MinIO 官方开源仓库已标记为 archive，本项目没有必要为少量文件增加一个独立对象存储服务。因此最终改为：

- 开发和演示环境使用 Docker Volume 保存文件。
- 后端定义 StorageBackend 接口，业务代码不直接依赖本地路径。
- 将来需要云部署时，再增加 S3StorageBackend 实现。
- 不把 PDF/DOCX 二进制内容存入 PostgreSQL。

参考：[MinIO 官方组织](https://github.com/minio)

## 3. 总体架构层

### 3.1 模块化单体

**选择原因：**

- 一个 FastAPI 服务按领域拆分用户、岗位、候选人、文档、检索、Agent 和评测模块。
- 模块边界清楚，同时保留统一事务、统一调试和一键部署能力。
- 一个人在六周内能够控制代码复杂度。
- 面试时可以清楚解释模块依赖和未来拆分边界。

**排除微服务：**

- 会额外引入服务发现、跨服务认证、链路追踪和分布式事务。
- 需要维护多个构建、部署和配置单元。
- 当前数据规模和团队规模没有独立扩容需求。
- 容易做成只有多个进程、没有真实业务必要性的形式化微服务。

### 3.2 前后端分离

**选择原因：**

- React 专注复杂交互，FastAPI 专注业务与 Agent。
- API 边界明确，便于独立测试和未来增加其他客户端。
- 适合展示 Agent Trace、流式事件、人工审批和流程图。

**排除服务端模板渲染：**

- 不适合持续更新的 Agent 执行界面。
- 前端组件状态与交互会变得难以维护。

### 3.3 单 Agent 双运行图

**选择原因：**

- 保持一个 Agent 边界，但按职责拆成岗位级 `MatchRun` 和候选人级 `ApplicationRun` 两张状态图。
- `MatchRun` 负责批量检索、硬条件评估和证据化评分，完成后即结束，不进入人工审批。
- `ApplicationRun` 一次只处理一个 `JobApplication`，负责状态变更、面试问题和排期，并在每个副作用前暂停审批。
- 同一 `JobApplication` 同一时刻最多一个非终态 `ApplicationRun`；取消使用持久化标记和运行版本协作完成，不能直接假设 Worker 已停止。
- 运行对象与审批对象一一对应，避免一个岗位批次中多名候选人的审批、恢复和完成语义相互污染。
- 每个节点的输入、输出、重试和失败状态都能独立测试。
- 能重点展示 Agent 工程化，而不是 Agent 数量。

**排除多 Agent：**

- 很容易退化为多个 LLM 互相发送消息。
- 调用成本、延迟和故障点明显增加。
- 评测和归因困难，无法说明多 Agent 比确定性工作流更好。

## 4. 前端表现层

### 4.1 React

**选择原因：**

- Agent Trace、条件审批和节点图属于高交互界面。
- 能直接使用 React Flow 构建 Agent 工作流可视化。
- 组件生态适合企业后台和可视化应用。

**排除其他选项：**

- Vue 3：完全能够实现，但在没有既有技术偏好的情况下，React 与 React Flow 配合更直接。
- Angular：框架体系较重，六周内学习和配置成本高。

### 4.2 TypeScript

**选择原因：**

- 为 API 响应、Agent State、节点事件和匹配报告提供静态类型。
- 能提前发现字段拼写、空值和数据结构不一致。
- 对 vibe coding 生成的前端代码提供重要约束。

**排除 JavaScript：**

- 复杂数据结构只能在运行时发现错误。
- 重构 Agent Event 和匹配报告时风险更高。

参考：[React TypeScript 官方文档](https://react.dev/learn/typescript)

### 4.3 Vite

**选择原因：**

- 项目是登录后的 SPA，不需要搜索引擎优化和服务端渲染。
- 开发服务器启动快，HMR 反馈迅速。
- 生产构建配置简单。

**排除其他选项：**

- Next.js：SSR、Server Components 和服务端路由不是本项目核心，还会与 FastAPI 形成两套服务端逻辑。
- 手工配置 Webpack：没有额外求职价值，只会增加构建配置工作。

参考：[Vite 官方文档](https://vite.dev/guide/)

### 4.4 Ant Design

**选择原因：**

- 招聘后台需要大量表格、筛选、表单、抽屉、上传和步骤条组件。
- 可以快速形成统一、专业的企业应用界面。
- 减少将时间消耗在基础 UI 组件实现上。

**排除其他选项：**

- shadcn/ui：自由度高，但需要自行组装较多表格、表单和交互细节。
- MUI：同样可用，但对本项目没有显著优势。
- Element Plus：主要服务于 Vue 技术栈。

### 4.5 TanStack Query

**选择原因：**

- 负责岗位、候选人、匹配报告和 Trace 等服务端状态。
- 内置缓存、重试、失效、加载和错误状态管理。
- 避免在组件中重复维护请求生命周期。

**排除其他选项：**

- 使用 Redux 保存所有接口数据：会产生较多 action、slice 和同步代码。
- 手写 useEffect + fetch：缓存、竞态、异常和刷新逻辑容易失控。

参考：[TanStack Query 官方文档](https://tanstack.com/query/latest/docs/framework/react/overview)

### 4.6 Zustand

**选择原因：**

- 只保存当前岗位、筛选条件、选中节点和弹窗状态等客户端状态。
- API 简单，样板代码少。

**排除其他选项：**

- Redux Toolkit：客户端状态规模较小，引入后概念和样板代码偏多。
- React Context：状态频繁变化时容易引起不必要的组件更新。

前端状态边界如下：

~~~text
TanStack Query：岗位、简历、报告、审批、Trace
Zustand：页面选择、筛选条件、弹窗、当前节点
SSE：正在执行的 Agent 实时事件
~~~

### 4.7 React Router

**选择原因：**

- 足够处理登录、岗位、候选人、Trace 和评测页面。
- 与 Vite SPA 组合简单。

**排除 Next.js 文件路由：**

- 不值得为路由能力引入完整的服务端框架。

### 4.8 React Flow

**选择原因：**

- 展示图节点、连线、条件分支和人工中断点（当前运行时为自研 `RunEngine`，见 ADR-0001）。
- 内置缩放、拖拽、选择和自定义节点。
- 可以直观显示当前运行节点和失败节点。

**排除其他选项：**

- 自行使用 SVG 或 D3：开发成本过高。
- 普通步骤条：无法表达分支、回退和条件路由。

参考：[React Flow 自定义节点文档](https://reactflow.dev/learn/customization/custom-nodes)

### 4.9 ECharts

**选择原因：**

- 用于展示匹配分布、解析成功率、工具调用成功率、延迟和 Token 成本。
- 图表类型丰富，适合后台数据看板。

**排除其他选项：**

- D3：控制力强但过于底层。
- Recharts：更轻量，但复杂仪表盘的能力不如 ECharts 完整。

## 5. 接口与实时通信层

### 5.1 REST

**选择原因：**

- 岗位、候选人、简历、审批记录都是清晰的资源模型。
- 容易调试、测试和生成 OpenAPI 文档。
- 与 FastAPI、TanStack Query 配合直接。

**排除 GraphQL：**

- 项目没有多客户端和高度动态组合查询需求。
- 会增加 Schema、Resolver、缓存和权限管理成本。

### 5.2 SSE

**选择原因：**

- Agent 场景主要是服务器向浏览器发送节点状态、进度和工具调用结果。
- 单向事件流比 WebSocket 简单。
- Fetch Stream 可以携带 Bearer Token；前端显式实现重连和事件去重，行为更可控。

**排除其他选项：**

- WebSocket：适合高频双向通信，本项目没有持续双向交互需求。
- 轮询：产生重复请求，延迟也更高。

SSE 只传结构化事件：

~~~json
{
  "run_id": "run_123",
  "node": "score_with_evidence",
  "status": "running",
  "message": "正在生成证据化匹配报告"
}
~~~

MVP 前端使用 Fetch Stream 读取 `text/event-stream` 并携带 Bearer Token，自行实现断线重连、最后事件位置和事件去重；不把长期 JWT 放入查询参数。服务端在重放、发送每个事件批次和心跳前重新校验资源权限，JobAssignment 撤销后停止发送业务事件并关闭现有连接。

参考：[FastAPI SSE 官方文档](https://fastapi.tiangolo.com/tutorial/server-sent-events/)

## 6. 后端应用层

### 6.1 Python 3.12

**选择原因：**

- Agent、文档处理、评测和数据处理使用同一种语言。
- 依赖兼容性成熟，不追求最新语言特性。
- 与 LangGraph、FastAPI 和模型 SDK 生态直接衔接。

**排除其他选项：**

- Java/Spring Boot：业务后端能力强，但 Agent 仍可能需要 Python，最终形成双后端。
- Node/NestJS：全栈统一有优势，但 Python Agent 生态更直接。
- Python 3.14：版本更新，但当前项目没有必要为新语法承担额外兼容风险。

### 6.2 FastAPI

**选择原因：**

- 原生结合 Pydantic、OpenAPI、异步接口、安全组件和依赖注入。
- 适合 REST、SSE 和模型服务集成。
- 代码量适合六周项目。

**排除其他选项：**

- Django：认证和管理后台完整，但项目重点不是内容管理，整体偏重。
- Flask：过于基础，需要自行组合校验、文档和依赖注入。
- Spring Boot：工程开销更大，并会增加语言切换。

参考：[FastAPI 官方特性](https://fastapi.tiangolo.com/features/)

### 6.3 Pydantic v2

**选择原因：**

- 统一校验 API 请求、简历解析结果、Tool 参数、Agent State 和 LLM 结构化输出。
- 能生成 JSON Schema，方便工具调用和接口文档。
- 与 FastAPI 原生集成。

**排除其他选项：**

- 普通 dict：字段错误只能在运行时发现。
- dataclass：适合类型建模，但运行时校验能力不足。
- Marshmallow：会形成另一套 Schema 定义体系。

### 6.4 SQLAlchemy 2

**选择原因：**

- 使用 ORM 提高业务数据开发效率。
- 复杂检索时仍可使用 SQL Expression Core 和原生 SQL。
- 支持 PostgreSQL 和异步数据库访问。

**排除其他选项：**

- SQLModel：简单，但复杂关系和查询最终仍会回到 SQLAlchemy。
- 全部手写 SQL：控制力强，但 CRUD 和领域对象维护成本高。
- Django ORM：会绑定 Django 框架。

参考：[SQLAlchemy 2 官方文档](https://docs.sqlalchemy.org/en/20/intro.html)

### 6.5 Alembic

**选择原因：**

- 数据库结构变更进入 Git 历史。
- 开发、测试和部署环境可以应用相同迁移。
- 与 SQLAlchemy 配套。

**排除其他选项：**

- 启动时调用 create_all：不能安全演进已有数据库。
- 手工执行 SQL：容易造成不同环境结构不一致。

参考：[Alembic 官方文档](https://alembic.sqlalchemy.org/en/latest/)

## 7. 认证与权限层

### 7.1 OAuth2 Password Flow + JWT

**选择原因：**

- 足够支持 HR、招聘主管和管理员登录。
- 前后端分离调用方便。
- FastAPI 有标准实现参考。
- `/auth/token` 遵循 OAuth2 Password Flow，使用 `application/x-www-form-urlencoded` 的 username/password；其他业务 API 默认使用 JSON。

**排除其他选项：**

- Session Cookie：可以实现，但需要额外处理跨域和服务端 Session。
- Auth0/Keycloak：能力过剩，会增加外部依赖或部署复杂度。

### 7.2 RBAC

**选择原因：**

- HR、招聘主管和管理员的权限关系清晰。
- 便于在接口层和 Tool 层复用权限判断。

**排除 ABAC：**

- ABAC 适合复杂组织和资源属性，本项目规则没有复杂到需要策略引擎。

### 7.3 PyJWT + pwdlib/Argon2

**选择原因：**

- 使用成熟库生成和验证 JWT。
- 密码使用 Argon2 哈希后存储。
- JWT 只保存用户 ID、角色和过期时间，不保存简历等敏感数据。

**排除自行实现 Token 或加密：**

- 自行实现安全机制风险高。
- JWT 是签名而不是加密，不能用于承载隐私数据。

参考：[FastAPI OAuth2 与 JWT 文档](https://fastapi.tiangolo.com/tutorial/security/oauth2-jwt/)

## 8. Agent 编排层

### 8.1 LangGraph

> **状态说明（2026-09-15）**：本节记录的是**立项选型意图**。运行时最终实现为自研有序节点引擎 `RunEngine` + `SqlCheckpointer`，该偏差已由 [ADR-0001](docs/adr/0001-agent-runtime-custom-engine-over-langgraph.md) 正式确认。本节的选型论证保留为历史记录；当前真实运行时口径以 ADR-0001 为准。

**选择原因：**

- 可以显式建模节点、条件边、状态、重试和中断。
- 支持 Checkpoint、人工审批、恢复和历史回放。
- 状态图容易测试和可视化。

**排除其他选项：**

- LangChain 普通 Agent：原型快，但复杂流程控制不够显式。
- CrewAI/AutoGen：更偏多 Agent，本项目没有必要。
- Dify/Coze：搭建快，但关键逻辑被平台隐藏，不利于展示底层工程能力。
- 自写状态机：可控，但需要重复实现持久化、暂停和恢复。**本项目最终选择此项**——IMP-018 以约 250 行的 `RunEngine` 实现了「保存并恢复最小 interrupt」，并以 895 行不变量测试固定恢复与防重放契约；正式理由与代价见 [ADR-0001](docs/adr/0001-agent-runtime-custom-engine-over-langgraph.md)。

参考：[LangGraph Persistence 官方文档](https://docs.langchain.com/oss/python/langgraph/persistence)

### 8.2 持久化 Checkpointer

> **状态说明（2026-09-15）**：本节的选型论证同样属于立项意图。运行时实现为 `SqlCheckpointer`（同一 PostgreSQL 库内的 `agent_checkpoints` 表），未引入 LangGraph 的 PostgreSQL Checkpointer 包；结论一致，理由见 [ADR-0001](docs/adr/0001-agent-runtime-custom-engine-over-langgraph.md)。

**选择原因：**

- Agent 状态与业务数据统一保存在 PostgreSQL。
- 支持重启后恢复和执行历史查询。
- 不需要增加新的持久化组件。

**排除其他选项：**

- MemorySaver：进程重启后状态丢失。
- SQLite Checkpointer：适合本地实验，不适合并发和正式演示。
- Redis Checkpointer：可以实现，但长期审计数据仍需回写数据库。

### 8.3 Human-in-the-Loop

**选择原因：**

- 淘汰候选人、更新流程状态和创建面试安排需要人工确认。
- 能展示 Agent 暂停、审批、修改和恢复能力。
- 降低招聘推荐中的错误和偏见风险。

**排除全自动决策：**

- 招聘属于高影响场景，不应由模型直接决定淘汰或录用。
- 全自动化也无法展示人工中断和恢复机制。

Agent 只负责需要语义判断的节点：

~~~text
学历、工作年限硬条件 → Python Rule Node
技能语义匹配         → Retrieval + LLM
更新候选人在岗位下的流程状态 → Tool + 人工审批
工作流路由           → 有序节点列表 + 条件中断（自研 `RunEngine`；见 ADR-0001）
~~~

### 8.4 不可信文档与 Tool 安全边界

简历和岗位描述中的文本全部视为不可信数据。文档内容即使包含“忽略规则”“调用工具”“修改分数”等指令，也只能作为待分析资料，不能改变系统指令、图路由、权限或审批状态。

- 解析、检索、硬条件评估和评分节点不注册任何有副作用工具。
- 模型只接收带清晰数据边界的文档片段，并通过受约束 Schema 返回结果；控制字段由服务端可信状态生成。
- `update_application_status` 和 `create_interview_schedule` 只在 `ApplicationRun` 的审批后执行，参数由受信任的业务 ID、审批记录和类型化状态构造，不直接采用文档中的指令或 ID。
- 服务端在执行层重新做 RBAC、资源归属、状态版本和幂等校验；Prompt 中的警告不能替代这些控制。
- 自动化安全语料必须覆盖提示注入、工具诱导、伪造系统消息和审批绕过，并验证它们不会改变控制流或触发副作用。
- Prompt Injection 使用干净—注入配对样本，并检测攻击指定哨兵、分数、状态、伪造证据和硬规则变化；这些攻击成功数必须为 0。

## 9. 数据与检索层

### 9.1 PostgreSQL

**选择原因：**

- 岗位、候选人、审批、面试和 Agent Run 都属于关系型数据。
- 需要事务、约束、关联查询和审计记录。
- 可以同时安装 pgvector，避免业务数据与向量数据分离。

候选人的招聘流程状态必须保存在岗位与候选人的关联实体 JobApplication 上，不能作为 Candidate 的全局状态；同一候选人在不同岗位下独立流转。

**排除其他选项：**

- MongoDB：简历字段有变化，但核心业务关系和筛选更适合关系数据库。
- MySQL：业务数据可用，但需要另配向量检索方案。
- SQLite：不适合并发 Worker 和正式部署。

### 9.2 pgvector

**选择原因：**

- 向量与候选人、岗位放在同一数据库。
- 可以同时使用强制权限过滤、用户显式启用的结构化筛选和向量相似度；岗位级 MatchRun 的硬条件只参与评估和排序，不作为默认剔除条件。
- 支持精确搜索、HNSW 和 IVFFlat。

**排除其他选项：**

- Milvus、Weaviate、Qdrant：大规模向量检索能力更强，但当前数据量不需要。
- Chroma、FAISS：原型方便，但事务、权限过滤和业务关联较弱。

### 9.3 PostgreSQL 结构化/关键词检索 + pgvector + RRF

**选择原因：**

- 向量检索负责语义相似度。
- 标准化技能表和关键词检索负责 Java、C++、证书和学校等精确内容；`pg_trgm` 可作为轻量补充。
- 结构化匹配通道对已确认字段计算确定性相关度，但不删除硬条件为 `FAIL` 或 `UNKNOWN` 的候选人。
- RRF 融合结构化、关键词和向量三路排名，减少单一检索方式的偏差。

PostgreSQL 内置全文检索对中文的效果必须通过评测确认，不能未经验证直接作为中文分词方案。

**排除其他选项：**

- 纯向量检索：容易漏掉精确技能和缩写。
- 纯关键词检索：不能识别同义表达和上下文。
- Elasticsearch：能力强，但会增加服务和数据同步成本。

### 9.4 HNSW（按需启用）

**选择原因：**

- 查询性能和召回率平衡较好。
- 无需像 IVFFlat 一样先训练索引。
- 适合持续新增简历。

MVP 在几十到几百份合成简历规模下先使用精确向量检索，以建立完整召回基线；只有数据量或延迟达到预设门槛并且召回回归通过后，才启用 HNSW。

**排除 IVFFlat 作为默认索引：**

- 需要结合数据量配置 lists 和 probes。
- 当前项目优先降低调参复杂度。

参考：[pgvector 官方项目](https://github.com/pgvector/pgvector)

## 10. 异步任务层

### 10.1 Redis

**选择原因：**

- 作为 Celery Broker。
- 保存短期缓存、幂等键和任务进度。
- 部署简单，适合单机 Docker Compose。

**排除其他选项：**

- 进程内字典：多进程不共享，重启后丢失。
- 同时引入 Redis 和 RabbitMQ：对项目规模过重。

核心业务数据不能只保存在 Redis，Redis 丢失后系统应能从 PostgreSQL 恢复。

### 10.2 Celery

**选择原因：**

- 简历解析、Embedding 和批量匹配属于耗时任务。
- 支持独立 Worker、任务重试、超时和状态跟踪。
- 能展示异步任务工程能力。

**排除其他选项：**

- FastAPI BackgroundTasks：适合轻任务，但缺少可靠队列和独立 Worker。
- Kafka：面向高吞吐事件流，当前项目没有这种规模。
- RabbitMQ：作为 Celery Broker 更强，但会增加一个需要维护的服务。

参考：[Celery 官方文档](https://docs.celeryq.dev/en/stable/)、[Redis 官方文档](https://redis.io/docs/latest/get-started/)

## 11. 文件与文档解析层

### 11.1 Docker Volume + StorageBackend

**选择原因：**

- 足够保存演示规模的 PDF 和 DOCX 文件。
- StorageBackend 抽象保证业务代码不依赖实际存储方式。
- Docker Volume 支持容器重建后保留文件。

**排除其他选项：**

- MinIO：官方开源仓库已归档，增加一个独立服务收益不足。
- AWS S3：增加账号、网络和费用依赖。
- PostgreSQL BLOB：会使数据库备份和迁移变重。

### 11.2 PyMuPDF

**选择原因：**

- 适合读取文本型 PDF。
- 可以保留页码和文本位置，便于匹配报告引用原文证据。
- 依赖相对轻。

**排除其他选项：**

- pdfplumber：表格抽取更细，但简历主要需求是文本和页码。
- Unstructured：能力完整但依赖较重。
- LlamaParse：属于外部服务，增加费用和数据传输依赖。

### 11.3 python-docx

**选择原因：**

- 直接解析 DOCX 段落和表格。
- 依赖轻，适合结构化简历抽取。

**排除其他选项：**

- 使用 LibreOffice 统一转换：运行环境更重。
- 只支持 PDF：降低招聘场景完整度。

### 11.4 暂不支持 OCR

**选择原因：**

- 六周优先保证电子 PDF 和 DOCX 的解析与评测质量。
- 在界面明确提示暂不支持扫描件。

**排除 PaddleOCR/Tesseract：**

- 扫描质量差异大，会把重点从 Agent 工程转移到 OCR 调优。

## 12. 模型与 Embedding 层

### 12.1 Qwen qwen3.7-plus

**选择原因：**

- 适合中文简历和岗位文本。
- 支持 OpenAI 兼容接口和 Function Calling。
- Plus 在成本与质量之间适合作为开发和演示默认模型。

**排除其他选项：**

- Max：质量更高但开发和评测成本更高。
- Flash：成本低，但关键结构化抽取和工具选择更需要稳定性。
- 本地大模型：需要显存、量化和推理服务，偏离 Agent 应用开发主线。

### 12.2 OpenAI Python SDK

**选择原因：**

- 通过 base_url 和环境变量连接百炼兼容接口。
- 模型层容易替换供应商。
- 避免把供应商调用代码散落在业务模块中。

**排除直接依赖 DashScope SDK：**

- 会增加供应商耦合。
- 切换兼容模型时改动范围更大。

### 12.3 qwen3.7-text-embedding，1024 维

**选择原因：**

- 满足中文和跨语言检索需求。
- 1024 维兼顾检索质量、存储和成本。
- 与主模型使用同一供应商，配置简单。

**排除其他选项：**

- 更高维度：对小数据集收益有限，但增加索引体积。
- 本地 BGE：节省调用费用，但增加模型下载、运行和部署。
- 使用聊天模型生成向量：不规范且难以稳定比较。

参考：[百炼 Function Calling 文档](https://help.aliyun.com/zh/model-studio/qwen-function-calling)、[百炼 Embedding 文档](https://help.aliyun.com/zh/model-studio/embedding)

## 13. 可观测与评测层

### 13.1 Langfuse

**选择原因：**

- 记录完整 Trace、模型调用、工具调用、Token 和耗时。
- 支持数据集、实验、评分和人工标注。
- 能直观向面试官展示 Agent 的执行链路和失败原因。

**排除其他选项：**

- 只写文本日志：无法清楚关联同一次 Agent Run 的节点和模型调用。
- LangSmith：与 LangChain 集成良好，但平台绑定更强。
- 纯 OpenTelemetry：通用监控强，但缺少 LLM Prompt、Token 和评测视角。

MVP 默认使用可选的 Langfuse Cloud 集成，不在主项目 Compose 内自托管完整 Langfuse 基础设施；未配置 Langfuse 时核心业务仍须正常运行。

隐私规则：MVP 只向 Langfuse 上传合成内容，不上传真实或仅经过简单脱敏的姓名、手机号、邮箱和简历原文。

参考：[Langfuse 可观测文档](https://langfuse.com/docs/observability/overview)、[Langfuse 评测文档](https://langfuse.com/docs)

### 13.2 自建评测 Harness

**选择原因：**

- 可以确定性计算字段提取 F1、Top-K 召回率和工具调用成功率。
- 能进入 GitHub Actions 做回归测试。
- Langfuse 用于记录实验，自建 Harness 负责核心指标计算。

**排除其他选项：**

- 全部依赖 LLM-as-a-Judge：结果有波动且持续产生费用。
- 只人工测试少量案例：不能防止后续修改导致能力回退。

建议指标：

| 能力 | 指标 |
|---|---|
| 简历抽取 | 字段 Precision、Recall、F1 |
| 候选人检索 | Recall@K、MRR 或 nDCG |
| 匹配报告 | 引用有效率、证据覆盖率、`SUPPORTED` 语义支持精确率与三分类宏平均 F1、高影响无支持结论数 |
| Tool Calling | 工具选择准确率、参数合法率、执行成功率 |
| 工作流 | 完成率、恢复成功率、人工中断成功率 |
| Prompt Injection | 控制流改变、越权提案、审批绕过、攻击指令遵循成功数、配对硬规则变化数 |
| 性能 | 总延迟、节点延迟、Token 和费用 |

## 14. 测试与代码质量层

### 14.1 pytest

**选择原因：**

- 测试业务规则、解析器、检索、Tool Schema 和图节点（当前运行时为自研 `RunEngine`，见 ADR-0001）。
- Fixture 和参数化适合构建评测数据集。
- 与 Python 后端和评测 Harness 统一。

**排除 unittest：**

- 标准库可靠，但 Fixture、参数化和插件体验不如 pytest。

参考：[pytest 官方文档](https://docs.pytest.org/en/stable/getting-started.html)

### 14.2 Playwright

**选择原因：**

- 从浏览器完成上传简历、启动 Agent、人工审批和查看报告的端到端测试。
- 支持浏览器隔离、并行和 CI 运行。
- 可以验证 SSE 与页面状态同步。

**排除其他选项：**

- Selenium：驱动管理和等待逻辑更繁琐。
- 只做后端测试：无法证明完整用户流程正常。

参考：[Playwright 官方文档](https://playwright.dev/docs/intro)

### 14.3 Ruff

**选择原因：**

- 一个工具完成 lint、格式化和 import 检查。
- 配置集中在 pyproject.toml。
- 执行快，适合保存时和 CI 中运行。

**排除其他选项：**

- Black + Flake8 + isort：同样成熟，但需要维护三套工具配置。
- Pylint：规则更重，对六周项目收益有限。

参考：[Ruff 官方文档](https://docs.astral.sh/ruff/)

### 14.4 mypy

**选择原因：**

- 检查 Agent State、Tool 参数和服务层返回值。
- 降低生成代码中的类型不一致问题。
- 支持渐进式增加严格度。

**排除其他选项：**

- 完全依赖运行时测试：复杂分支难以全部覆盖。
- Pyright：同样优秀，但本项目在 Python CI 中统一使用 mypy 即可。

参考：[mypy 官方文档](https://mypy.readthedocs.io/en/stable/)

## 15. 部署与 CI 层

### 15.1 Docker Compose

**选择原因：**

- 一条命令启动 Web、API、Worker、Scheduler、PostgreSQL 和 Redis。
- Scheduler 与 Worker 使用同一应用镜像，只触发 Approval 过期等维护任务，不形成新的业务服务边界。
- 统一管理网络、环境变量、健康检查和数据卷。
- 适合本地演示、CI 和单机部署。

**排除其他选项：**

- Kubernetes：资源清单、Ingress、Secret 和集群运维远超项目规模。
- 手工安装依赖：环境难以复现。

参考：[Docker Compose 官方文档](https://docs.docker.com/compose/intro/compose-application-model/)

### 15.2 Nginx

**选择原因：**

- 托管 React 静态文件。
- 反向代理 /api 和 /events。
- 统一浏览器访问入口。

**排除其他选项：**

- 直接使用 Vite 开发服务器上线：不适合正式部署。
- Traefik：动态服务发现能力对固定服务过剩。
- Caddy：配置简单，但本项目使用 Nginx 更容易展示常见反向代理配置。

SSE 路由应关闭代理缓冲，避免事件被 Nginx 聚合后延迟发送。

参考：[Nginx 官方文档](https://nginx.org/en/docs/beginners_guide.html)

### 15.3 GitHub Actions

**选择原因：**

- Push 和 Pull Request 时自动运行前端构建、Ruff、mypy、pytest 和 Playwright。
- 可以保存测试报告和构建产物。
- 与公开作品仓库直接集成。

**排除其他选项：**

- Jenkins：需要自行维护服务器。
- GitLab CI：如果代码不在 GitLab，没有额外优势。
- 不做 CI：无法证明项目具备持续回归能力。

参考：[GitHub Actions CI 官方文档](https://docs.github.com/en/actions/get-started/continuous-integration)

## 16. 推荐的后端模块结构

~~~text
backend/
├── app/
│   ├── api/                 # REST 与 SSE 接口
│   ├── auth/                # 登录、JWT、RBAC
│   ├── jobs/                # 岗位领域
│   ├── candidates/          # 候选人领域
│   ├── documents/           # 上传与解析
│   ├── retrieval/           # 全文、向量和 RRF
│   ├── agent/
│   │   ├── state.py         # Agent State
│   │   ├── match_graph.py   # 岗位级 MatchRun
│   │   ├── application_graph.py # 候选人级 ApplicationRun
│   │   ├── nodes/           # 节点
│   │   └── tools/           # 工具
│   ├── interviews/          # 面试与审批
│   ├── evaluations/         # 评测指标
│   ├── observability/       # Trace 与脱敏
│   └── infrastructure/      # DB、Redis、模型和存储适配器
├── alembic/
├── tests/
└── pyproject.toml
~~~

## 17. 推荐的 Agent 工作流

~~~text
MatchRun（岗位级，一次生成多名候选人的报告）
parse_job
    ↓
retrieve_candidates
    ↓
hard_rule_evaluate
    ↓
score_with_evidence
    ↓
persist_reports
    ↓
COMPLETED

ApplicationRun（候选人级，一次只处理一个 JobApplication）
human_review
    ↓
update_application_status（审批后）
    ├─ 非 SHORTLISTED → COMPLETED
    └─ SHORTLISTED
           ↓
       generate_interview_questions
           ↓
       schedule_interview_tool（再次审批后）
           ↓
       COMPLETED
~~~

建议工具：

- search_candidates
- get_resume_evidence
- get_company_hiring_rules
- update_application_status
- create_interview_schedule

其中 `search_candidates`、`get_resume_evidence` 和 `get_company_hiring_rules` 是只读工具；`update_application_status` 和 `create_interview_schedule` 属于有副作用工具，只能在候选人级 `ApplicationRun` 中、经对应审批后执行。硬条件节点输出 `PASS`、`FAIL` 或 `UNKNOWN` 及理由和证据，但不自动淘汰、隐藏候选人或改变 `JobApplication` 状态。

证据化报告的每条结论同时保存证据引用、原文摘录和 `SUPPORTED`、`PARTIAL` 或 `INSUFFICIENT` 支持等级。运行时先校验引用归属、版本和范围；离线评测再用人工标注集测量“证据是否真正支持结论”。对入围建议、硬条件判断等高影响结论，证据不足时必须输出“不足以判断”。

## 18. 最终结论

本项目的核心不是堆叠最多的框架，而是建立一条完整、可验证的 Agent 工程链路：

> 用 React 展示 Agent，用 FastAPI 承载业务，用可恢复的节点引擎控制流程（立项选型为 LangGraph，最终实现见 ADR-0001），用 PostgreSQL 统一业务数据与向量，用 Celery 处理耗时任务，用 Langfuse 和自动化测试证明 Agent 不是“偶尔能跑”，而是可控制、可恢复、可评测。

应当优先保证以下能力真正可运行：

1. 简历结构化解析有校验和人工修正。
2. 匹配结果引用真实简历证据。
3. 岗位级 MatchRun 与候选人级 ApplicationRun 可分别持久化、失败重试和恢复。
4. 有副作用的工具必须人工批准。
5. 检索、证据语义支持、Prompt Injection、防越权工具调用和工作流都有量化评测。
6. README、Docker Compose 和 CI 能让其他人复现项目。
