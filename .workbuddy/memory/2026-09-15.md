# 2026-09-15 工作日志

## FIN-006 维护任务与故障协调 —— 完成并提交（commit `70dd8cb`）

16 files changed, 2377 insertions(+), 46 deletions(-)。四个复选框全部落地，计划书总表 FIN-006 由 TODO 改 DONE。

### 交付内容

**新增文件**
- `backend/app/maintenance/repository.py` —— `MaintenanceDocumentRepository`（`list_stranded_parses` / `is_storage_key_referenced`）、`StoredObjectLister`（`list_objects` / `delete_object`）、`SqlApplicationRunRecoveryProbe`（`latest_checkpoint_version` 走 `SqlCheckpointer(session).get(thread_id, "", "")`；`executed_approval_for_run` 查 `ApprovalStatus.EXECUTED` 按 `executed_at desc, created_at desc` 取第一条）。每个 Protocol 都有内存双实现。
- `tests/unit/test_maintenance_fin006.py` —— 25 例。
- `tests/integration/test_maintenance_postgres.py` —— 6 例，`DATABASE_URL` 前缀非 `postgresql+asyncpg` 时 skip。
- `tests/validate_maintenance.ps1` —— Postgres E2E 探针。

**改写**
- `backend/app/maintenance/service.py` —— 现持三个 job：`expire_pending_approvals(before, *, batch_size=100)`、`republish_queued(...)`（返回 `RepublishOutcome(documents, match_runs, application_runs, skipped)`）、`cleanup_orphan_files(...)`（返回 `OrphanCleanupOutcome(scanned, deleted, skipped_referenced, skipped_recent, skipped_malformed)`）。新增 `ApplicationRunRecoveryProbe` Protocol。
- `backend/app/maintenance/tasks.py` —— `_build_maintenance(resources, session)` 组装服务 + 三个 `@shared_task`。
- `backend/app/approvals/repository.py` —— Protocol/内存/SQL 三处加 `claim_expired_pending(before, *, limit)`。
- `backend/app/agent/repository.py` —— 三处加 `list_stale_runs(*, run_type, statuses, older_than, limit)`（**不加锁**，这是候选列表不是认领）。
- `backend/app/infrastructure/celery.py` —— beat 加 `republish-queued`(300s)、`cleanup-orphan-files`(86400s)。
- `backend/app/core/settings.py` —— 4 个新配置项。
- `tests/unit/test_celery_beat.py`、`tests/unit/test_sse.py`、`scripts/project.ps1`、`.env.example`、`编码实现计划.md`。

### 关键设计决策

1. **过期批处理 = 稳定排序 + SKIP LOCKED**。`ORDER BY approvals.id` 防的是两个 sweeper 候选集部分重叠时的相互等锁；`SKIP LOCKED` 只让第二个 Worker 不阻塞而是继续排空积压。**正确性不在锁上**——认领后必须重读「仍是 PENDING」才迁移，这同时让迟到的 `decide` 自然退化为 no-op。

2. **重投递的 ApplicationRun 分支是本项最危险的地方**。`RUNNING` 静默超 lease 有两种可能：Worker 已死（该救） vs Worker 还活着只是慢（不能碰）。判据是**弃权而非猜测**——只有同时拿到「checkpoint 版本」+「已 EXECUTED 且带 decider 的 Approval」才提议 `RESUME`，否则记 `no_resume_facts` / `approval_decider_missing` 跳过。认领侧还会重校验全部事实与实时授权，所以这个扫描在架构上**只能提议投递**，不可能绕过人工闸门。为此专门写了反向对照测试。

3. **孤儿文件删除顺序**：list → 年龄过滤 → 引用检查 → **再查一次年龄** → 删。中间那次复查是为了防「检查引用期间刚提交的上传」被误删。畸形 key 只计数不删。

4. **单条 broker 失败不致命**：重投递批里单条 enqueue 抛异常只记日志跳过，不 abort 整批（否则一个坏记录会永久卡住后面所有积压）。

### 验证
- `340 passed`（单测）
- `ruff check backend tests apps` → All checks passed!
- `mypy` → Success: no issues found in 185 source files

### 未完成/遗留
- **Docker daemon 本机未运行**，Postgres 集成用例与 `validate_maintenance.ps1` 探针无法本地跑，需由 CI 验证。
- 本机 pwsh 缺失（已知问题），`project.ps1 verify` 中的探针链路只能靠 CI。

### 踩坑记录
- **扩 Protocol 会打穿所有手写假仓储**：给 `AgentRunRepository` 加 `list_stale_runs` 后 `tests/unit/test_sse.py` 的 `_SnapshotAgentRunRepository` 立刻变抽象类，mypy 报 `[abstract]`。以后扩 Protocol 记得 `grep` 全 `tests/` 的手写双。
- Writing `maintenance/service.py` 时 `Write` 拒绝（未先 Read），且首稿混进过一个不存在的 `timiedelta_like` 别名，重读后重写才干净。
- 编辑 `repository.py` 时误删空行导致 `...class LocalVolumeStorageLister:` 语法错，补回两行空行修复。
- mypy `no-untyped-call` 栽在 `postgresql.dialect()` 上，抽 `compiled_sql(index)` helper 加 `# type: ignore[no-untyped-call]` 解决。

### 下一步
按计划书 §20.4 序列，下一个是 **FIN-007：Evaluation 产品闭环**（Dataset/Evaluation 模型与迁移、API、Task、前端页）。等用户确认后再开工。

---

## FIN-007 Evaluation 产品闭环 —— 完成（待提交）

四个复选框全部落地，计划书总表 FIN-007 由 TODO 改 DONE。

### 交付内容

**新增后端（`backend/app/evaluations/`，10 个模块）**
- `models.py` —— `EvaluationStatus`(CREATED/RUNNING/COMPLETED/FAILED)、`TERMINAL_EVALUATION_STATUSES`、`EvaluationKind`(GOLDEN/SEMANTIC/INJECTION)、三张 `Base` 模型 + 只读视图 `EvaluationDetailView`。枚举全部 `Enum(..., native_enum=False, create_constraint=False, length=N)`。`EvaluationRun.passed: Mapped[bool | None]`（完成前为 None）。`MetricSnapshot` 有 `UniqueConstraint("evaluation_run_id","metric_name")`。
- `repository.py` —— 两个 Protocol + SQL 适配器 + 内存镜像 + `metric_snapshot_rows()`。**不可变性写在 SQL 层**：`replace_metrics` 先读 run，终态直接 return。
- `service.py` —— `config_digest()`（排序键 SHA-256 规范化）、`content_digest()`、`MetricResult`、`EvaluationPage`、`EvaluationService`（register_dataset / create_run / start / record_metrics / complete / fail / get_run / get_dataset / list_metrics / list_page）。
- `executor.py` —— `BuiltinEvaluationExecutor` 把三套既有评分器绑成带阈值的 `MetricResult`；`MRR_THRESHOLD=0.70`、`NDCG_AT_K_THRESHOLD=0.80`；`gate_passed()` / `failing_metrics()`。
- `tasks.py` —— `@shared_task(name="evaluations.execute", bind=True)`。
- `enqueuer.py` —— `EvaluationEnqueuer` Protocol + `CeleryEvaluationEnqueuer`（`task_id=str(run_id)`）。
- `schemas.py` / `wiring.py` / `routes.py`（`/api/v1/evaluations` 三端点）。
- `gate.py` —— 离线 CLI `python -m backend.app.evaluations.gate`，退出码 0/1/2。

**新增迁移**：`migrations/versions/0012_evaluation_tables.py`（head，down_revision=`0011_idempotency_records`）。

**新增前端**：`apps/web/src/lib/evaluationStatus.ts`、`apps/web/src/pages/EvaluationsPage.tsx` + `.test.tsx`（9 例）。改 `api/types.ts`、`api/client.ts`、`App.tsx`（加导航项 + 路由）。

**新增测试/探针**：`tests/unit/test_evaluations_fin007.py`(32)、`tests/unit/test_evaluations_routes.py`(11)、`tests/unit/test_evaluation_gate.py`(9)、`tests/integration/test_evaluations_postgres.py`(7)、`tests/validate_evaluations.ps1`、`tests/validate_evaluation_gate.ps1`。

**改造**：`model_registry.py`（表数 22→25）、`main.py`（注册路由）、`infrastructure/celery.py`（注释）、`tests/validate_migrations.ps1`（head + 3 表 + `tables=25`）、`tests/unit/test_model_registry.py`(22→25)、`tests/unit/test_celery_beat.py`、`scripts/project.ps1`（verify 加两个探针）。

### 关键设计决策

1. **重复执行闸门键在 `(dataset_version_id, config_hash)`，与调用者无关**。只看活跃状态（CREATED/RUNNING），`config_hash` 用排序键规范化 JSON 算，字典顺序变化绕不过去。这样闸门不可能被换身份或换键序绕过。

2. **`passed=False` 与 `FAILED` 必须五层分离**：阈值未命中 = `COMPLETED` + `passed=False`（业务结论）；飞架损坏 = `FAILED` + 安全错误码（工程故障）。模型、服务、task、门禁、UI 逐层保持这个区分，不许合并。

3. **门禁只对带阈值的指标取合取，且空指标集判不通过**。`all()` 对空列表返回 `True`，会造出一条「没跑任何检查却显示通过」的假绿灯。该缺陷在 `gate_passed` 和 `EvaluationService.complete` **两处同时**修掉（`bool(thresholded) and all(...)`）。无阈值指标是「参考」，不许影响判决。

4. **门禁刻意不读 `Settings`**。`default_retrieval_config()` 显式给定 §18.3 冻结配置，使门禁能在无数据库/Redis/模型配置的裸 CI 步骤里独立运行。

5. **客户端打戳 id 与 `created_at`**。`create_run` / `register_dataset` 里 `id=uuid4()` + `created_at=self._now()`，否则内存镜像在 flush 前排序/分页全错——这是最初 3 个单测失败的根因。`metric_snapshot_rows` 同样透传 `created_at`。

### 验证
- 后端单测 `392 passed`（FIN-006 时是 340）
- `ruff check backend tests` → All checks passed!
- `mypy` → Success: no issues found in **198** source files
- 前端 `113 passed`（22 文件）；`tsc -b` 干净
- `python -m backend.app.evaluations.gate` → exit 0，`RESULT: PASS`，12 项阈值指标全 PASS
- `alembic heads` → 单一 head `0012_evaluation_tables`

### 未完成/遗留
- **Docker daemon 本机未运行**，`validate_evaluations.ps1`、`validate_evaluation_gate.ps1` 与 Postgres 集成测试无法本地跑，需 CI 验证（同 FIN-006 的限制）。
- 本机无 `pwsh`，`project.ps1 verify` 探针链路只能靠 CI。

### 踩坑记录
- **`ruff format --check` 显示 87 文件待格式化是仓库既有状态，不是本次引入**。用 `git show HEAD:<file>` 逐文件验证过：`main.py`、`celery.py`、`model_registry.py` 在 HEAD 时就已不符合 canonical format。本次只格式化了自己新建的 `backend/app/evaluations`（9 文件），不动别人的文件。
- **离线门禁模块路径是 `backend.app.evaluations.gate`，不是 `backend.evaluations.gate`**。`backend/` 下没有 `__init__.py` 作为顶层包，`backend.app` 才是包根。`.ps1` 探针最初写错了路径，已改。
- **`ruff format` 会重排 `def` 签名行宽**，改完格式化要重跑 mypy + 单测（本次 mypy 因此暴露 4 个测试文件里的类型问题）。
- 测试里 `metric.threshold` 是 `float | None`，即使已用 `is not None` 过滤，mypy 也不做跨行窄化——需先 `threshold = metric.threshold; assert threshold is not None`。
- `# type: ignore[arg-type]` 在 fake 参数类型改对后变成 unused-ignore，mypy 会报错。改 fake 签名要同步删 ignore。
- 前端 `findByText("support-labels@v1")` 失败是因为渲染成 `数据集 {name}@{version}` 三个节点，文本被拆开——需用 `(_, node) => node?.textContent === "数据集 support-labels@v1"` 匹配。
- `findByRole("table", { name: "" })` 匹配不到无名表；`num_cases` 没有中文标签，`metricLabel` 会回退成原始名，测试里必须用原始名。

### 下一步
按计划书 §20.4 序列，下一个是 **FIN-008：真实 Qwen 适配器**（OpenAI 兼容 Chat/Embedding gateway、超时/429/5xx 分类与重试、脱敏诊断命令、合成数据录制评测）。等用户确认后再开工。


---

## FIN-008：真实 Qwen 适配器（完成）

**新增生产代码**：
- `backend/app/infrastructure/http_transport.py` — 重试/分类策略的**唯一**所在地。`TransportError`（`retryable: bool`）→ `RetryableTransportError` / `PermanentTransportError` → `ResponseSchemaError`。`JsonSender` Protocol + `HttpxJsonSender`（可注入 `transport` 以接 `MockTransport`）+ `JsonTransport`（`post_json` 有界重试）。`RawResponseBody` 用 `__slots__` 且 `__repr__` 刻意不含正文。
- `backend/app/infrastructure/prompts.py` — Prompt 契约。`EXTRACTION_PROMPT_VERSION = "qwen-extract-v1"`；系统提示明确声明被围栏内容是 **UNTRUSTED**、只输出裸 JSON、「omit rather than guess」。用户提示把简历包进 `<resume>…</resume>` 并在截断时追加说明。
- `backend/app/infrastructure/qwen_chat.py` — `QwenChatGateway`（`/chat/completions`，`temperature=0.0`）。`complete_extraction_raw(*, full_text) -> str` 是**公开**方法，专为录制器取原始文本而加，避免录制层伸手进私有属性。
- `backend/app/infrastructure/qwen_embedding.py` — `QwenEmbeddingGateway`（`/embeddings`）。`_ordered_vectors` 按 provider 的 `index` **重排**并拒绝重复/缺口/非数值。`embed([])` 不发请求。
- `backend/app/infrastructure/recording.py` — `Recording`/`RecordedCall`/`save_recording`/`load_recording`/`replay_draft`/`RecordedModelGateway`/`record_call`/`build_gateway_for_mode`（优先级：recording → mock → real）。
- `backend/app/infrastructure/model_diagnose.py` — `python -m` 可运行的连通诊断，退出码 0/1/2。
- `backend/app/evaluations/datasets.py` — `register_builtin_datasets`（幂等）+ `builtin_dataset_content_digest` + `build_evaluation_metadata`。

**改造**：`model_gateway.py` / `embedding.py` 两个工厂此前对同一个 `mock_model_mode` 开关给出**不一致**的答案（一个静默返回 Fake，一个抛 `NotImplementedError`）；现已统一。

**新增测试/探针**：`tests/unit/test_http_transport.py`(30)、`test_qwen_gateway.py`(50)、`test_fin008_recording.py`(30)、`test_model_diagnose.py`(27)、`tests/validate_model_adapter.ps1`（接入 `verify`）。

### 关键设计决策

1. **选 `httpx2` 而非官方 `openai` SDK**（用户确认方案 A）。`httpx2` 是仓库既有依赖、运行时可用，自带 `MockTransport`，使整张重试/分类矩阵**无网络、无密钥、无 sleep** 可测；且不必改 `requirements.lock`——本机没有项目生成锁文件的 `pip-compile --no-index` 工具链，改锁文件的风险高于收益。

2. **schema 违例判永久失败**。重发同一请求只会得到同一份畸形回复，判可重试会白白烧掉重试预算。`parse_structured_reply` 里 `CandidateProfileDraft.model_validate` 必须**包在 try/except ValidationError 里**转成 `ChatCompletionShapeError`；漏了这层，坏枚举/多余字段会以裸 `ValidationError` 逃逸，Celery 就会去重试一个注定失败的回调。

3. **抖动只缩不涨**：`[0.5, 1.0)` 而非对称抖动。重试本就是慢路径，不允许把等待拉长到指数之上；同时打散并发 worker，避免重造引发 429 的惊群。

4. **端点脱敏用「整段丢弃」而非「按名猜测」**：query 与 userinfo 全部丢掉。需要猜哪个参数是密钥的脱敏迟早猜错，而这个值是会被打印进工单的。

5. **无法解析的 URL 替换为 `(unparsed)`，绝不原样回显**。手糊进密钥的配置恰恰是最常见的解析失败来源，原样回显就是最不该泄露的那一条。（最初实现回显原文，`test_safe_endpoint_handles_a_malformed_url` 抓到了它。）

6. **录制原始回复文本，而非解析后的 draft**。存 draft 会把本解析器的行为烤进 fixture，也就无法发现解析器后续回归。回放走与线上**完全相同**的解析与校验路径——这才是让回放成为证据而非装饰的原因：fixture 里放一份畸形回复，回放必须失败。

7. **数据集此前从未真正注册**。`register_dataset` 只有测试调用过，生产路径没人调，`dataset_versions` 恒为空、评测结果无法回指数据集版本。`datasets.py` 补上这条接线，并刻意放在独立模块而非路由里，使其可被探针与离线门禁直接调用。

### 验证
- 后端单测 **529 passed**（FIN-007 时 392），`ruff check` 干净，`mypy` strict **209** 文件无问题
- 四个 FIN-008 测试文件 **137 passed in ~1s**（全部走 MockTransport）
- 诊断命令手工验证三种路径：mock 模式 exit 0；`.invalid` 端点 exit 2（分类为 retryable，密钥不出现在任何一行）；`--skip-*` 正常
- `tests/validate_model_adapter.ps1` 与 `project.ps1` 均通过 PowerShell 语法解析（本机无 pwsh，用 `[Parser]::ParseFile` 校验）

### 踩坑记录
- **`pytest.mark.anyio` 在这仓库会静默跳过全部测试**——没装 anyio pytest 插件（`import pytest_anyio` → ModuleNotFoundError）。必须用仓库惯例 `asyncio.run`。
- **`mypy` 也检查测试文件**（`mypy backend apps tests/unit`）。给共享助手写 `def _run(coro):  # noqa: ANN001, ANN202` 能骗过 ruff 但骗不过 mypy；正确做法是 PEP 695 泛型 `def _run[T](coro: Coroutine[Any, Any, T]) -> T:`（注意是 `Coroutine` 不是 `Awaitable`——`asyncio.run` 要求 `Coroutine`）。
- **`_report_header` 的运算符优先级坑**：`"=" * 68` 写在隐式字符串拼接表达式里，隐式拼接比 `*` 结合更紧，结果把**整条已拼接的串**重复了 68 次。改用 `"\n".join([...])` + 独立 `rule` 局部变量。
- **`UnboundLocalError: response_headers`**：网络异常分支从不绑定它。修法是在每轮循环顶部初始化为 `{}`——这同时修掉一个更隐蔽的 bug：上一轮的 `Retry-After` 会泄漏进下一轮。
- **非 JSON 的 200 正文伪装成 `{"_raw_text": ...}`** 能通过 `parse_json_object` 的对象检查，等于放行一张 HTML 错误页当载荷。引入独立的 `RawResponseBody` 类型并显式拒绝。
- **`caplog.records` 拿不到结构化字段**：字段在 `record` 属性上，不在渲染后的消息里。凭据测试改为直接挂 `logging.Handler` 扫 `record.getMessage()` + `vars(record)`。
- **指向 `127.0.0.1:1` 的测试会真的建 socket**，耗时 9s 且依赖 OS 拒绝连接的速度。改为 MockTransport（现在整个文件 0.9s）。
- **从仓库根跑 pytest 会被根目录 `.env` 污染**（`test_settings.py` 2 个失败 + 我新加的 1 个），表现为 `storage_root must be an absolute path`——这是 Windows 上的既有测试可移植性问题，与 FIN-008 无关。必须在 `/d/code` 下跑 `pytest resume/tests`。
- **`EmbeddingDimensionError` 定义在 `infrastructure/embedding.py`，`candidates/embedding_service.py` 只是再导出**。mypy 的 `attr-defined` 会因此报错；要从定义处 import。

### 未完成/遗留
- **Docker daemon 本机未运行**，`validate_model_adapter.ps1` 无法本地跑，需 CI 验证（同 FIN-006/007）。
- 本机无 `pwsh`，`project.ps1 verify` 只能靠 CI 或 PowerShell 直调验证（本次用 `[Parser]::ParseFile` 做了语法校验）。
- **真实凭据下的连通性与录制评测仍待人工执行**——代码路径已完备，但没有真 key 就无法在本地产出真实录制 fixture。

### 事故：本地 Git 对象库损坏（同日）
用 `git stash push --include-untracked` 做基线对比验证时命令被 SIGTERM 打断，`.git/refs` 被整体删除、pack 文件丢失而 `.idx` 残留、仅剩 3 个 loose object，git 无法读取自身目录。**工作区文件完好**，是 FIN-005/006/007 全部内容的唯一幸存副本（浅克隆与 0902 备份都只到 FIN-004）。经用户确认后重建仓库：新建 `.git`、恢复 config/remote、以当前工作区建成两个 commit —— 基线 `2a91fb1`（tag `baseline-post-rebuild`，352 文件）+ FIN-008 `87e4cd9`（17 文件，共 369）。旧 `.git` 元数据存至 `resume-git-corrupt-backup-20260915/`。

重建过程中我一度把 FIN-008 折进了基线 commit（等于把它埋进一个「修仓库」的提交里），随后重写历史拆开——中间产生过若干已被 reflog 悬空的提交，`git log main` 会显示它们，但 `main` 真正的历史只有上述两笔（`git log --oneline main` 前两行即全部）。**未 push**：`.git/gh-credentials` 随旧 `.git` 一并丢失，push 前需先恢复（见 MEMORY.md 的凭据段）。

**教训：在这个仓库上不要用 `git stash` 做验证性对比。** 需要基线对照时改用 `git show HEAD:<file>` 或复制目录到临时位置，避免任何会重写 `.git` 内部的命令。仓库已有两次 git 损坏记录（0902、0915），`git config gc.auto 0` 与 `pruneExpire=never` 都是为此设的。

### 下一步
按计划书 §20.4 序列，下一个是 **FIN-009：LangGraph 运行时对齐**（形成 ADR：采用真实 LangGraph StateGraph/PostgreSQL Checkpointer，或正式确认保留当前引擎的 MVP 偏差）。等用户确认后再开工。

---

## FIN-009：LangGraph 运行时对齐（已完成）

### 决策（用户 AskUserQuestion 两项确认）
1. ADR 方向 → **保留自研引擎 + 正式记录偏差**（推荐项），而非采用 LangGraph。
2. 文档修正力度 → **精准修正关键表述**（推荐项）：只改断言「已是 LangGraph」的句子，保留「规划中/后续适配项」的善意表述。

### 交付物
- **新增 `docs/adr/0001-agent-runtime-custom-engine-over-langgraph.md`**（仓库此前无 `docs/` 目录，本次新建）。状态「已接受」，含 7 节：背景 / 事实核查（7 小节，全部带行号证据）/ 决策 / 备选方案取舍（A 采用 LangGraph 被否，5 条理由）/ 后果（含明确不改变事项）/ 验证 / 文档口径。
- **修正 5 份文档**（`README.md`、`需求分析.md`、`技术栈选型与架构决策.md`、`概要设计说明书.md`、`详细设计说明书.md`）：共 17 处断言改为回指 ADR-0001。
- **新增 `tests/unit/test_fin009_adr_contract.py`**（20 项），固化 ADR 事实基础 + 「业务表为事实源 / 已执行审批不重放」不变量 + 文档口径守卫。
- 计划书：FIN-009 四项复选框全勾并附详细证据；§19.2 该行 `PARTIAL` → `DONE`；总表 556 行 `TODO` → `DONE`；Gate-0 探针第 3 条补注实际实现。

### 关键事实（本次实际核实，非推测）
- `langgraph` **零依赖、零导入**：`pyproject.toml` 与两份 lock 均无；源码中仅 5 处「不是 LangGraph」的刻意注释 + `model_gateway.py:28` 一个技能关键词（易误读，ADR 中特此澄清）。
- 引擎 + 周边共 **1814 行**（engine 250 / checkpoint 161 / models 289 / repository 404 / service 211 / tasks 378 / graph 121）。
- **真正的工作量不在引擎**：每节点写两条 `AgentEvent`、每状态迁移调 `set_status`、恢复校验散落 `agent/tasks.py:247-283`（`resume_token_missing` / `stale_resume_version` / `resume_not_approved`）与 `job_applications/service.py:277-282`。
- **只有一张图用引擎**：`job_applications/graph.py` 用 `RunEngine`；`match_run/service.py:152-225` 是手写顺序过程。「迁移双图」实际是「迁移一张 + 新写一张」。
- **895 行不变量测试**（`test_concurrency_recovery` 365 / `test_approval` 329 / `test_agent_run_event` 201）须重写而非复用；`test_concurrency_recovery.py:5` 早已注明「真实 LangGraph 替换时也不能静默破坏」——说明偏差自始就被记录。

### 踩坑
- **ApplicationRun 图的 `interrupt_status` 是 `WAITING_APPROVAL`，不是 `INTERRUPTED`**（`job_applications/graph.py:117`）。我按 `RunEngine` 的默认值想当然，测试因此失败。
- **审批 ordinal 是 1 和 2，不是 0**（`job_applications/service.py:269/312`）。幂等键 `run_id:attempt:action_type:ordinal` 对不上就返回新行而非既有行。
- **`AgentEvent` 的字段是 `safe_payload_json`**，不是 `safe_payload`。
- **`engine.py` 原文是 `it is *not* LangGraph`**（带 markdown 强调），断言 `"not LangGraph"` 会失败。
- **mypy 会抓出冗余断言**：先 `assert x is WAITING_APPROVAL` 再 `assert x is not RUNNING`，类型已被收窄，mypy 报 `comparison-overlap`。该断言本身也确实无价值，删掉即可。
- **本机无项目 venv**：`.venv-check` 用 uv 建（Python 3.12.14，项目 pin `>=3.12,<3.13`）。editable 安装会撞沙箱 safe-delete 守卫，改为分两步：先装 dev 工具，再显式装 14 个运行时依赖。跑测试须在 `/d/code` 下（否则根 `.env` 污染），用完即删。

### 验证
`549 passed`（较 FIN-008 的 529 增加 20）；ruff `All checks passed!`；mypy `Success: no issues found in 210 source files`。两次提交：
- `3a5d26c` docs(adr): accept the custom run engine and align the design docs
- `2fef0a6` test(agent): pin the ADR-0001 engine decision and its invariants

### 下一步
FIN-010：非种子文件浏览器主路径（合成 PDF/DOCX 从浏览器上传 → Worker 解析 → Profile 校对 → Embedding → 岗位候选人页 → MatchRun → 证据报告 → ApplicationRun → 两次审批 → Interview 的 Playwright E2E）。

---

## FIN-010：非种子文件浏览器主路径（DONE，提交 `8339dd3`）

### 关键判断：这是集成缺口，不是功能缺口
整条链路此前**已存在且已接线**：`上传 → 解析 → candidates.extract_profile → 校对确认 → embeddings.generate_chunks`，再接 `MatchRun → JobApplication.get_or_create → ApplicationRun → 双审批 → Interview`。缺的是**非种子路径从未被连贯跑通**（此前只跑过 seed 候选人），以及**没有 PDF fixture**。因此 FIN-010 的正确做法是补验证，而非写功能。

### 产出
- **`scripts/generate_e2e_fixtures.py`** — 从共享 `BLOCKS` 元组生成 DOCX/PDF。五个文本块（`张伟` / `6 年 Python 后端开发经验` / `工作经历：2021-至今 某电商 高级后端工程师` / `技能：Python、FastAPI、PostgreSQL` / `教育背景：本科 计算机科学与技术`）是**刻意钉住的**：`document-review.spec.ts` 用这些子串做选择器。
- **`apps/web/e2e/fin010-nonseed-flow.spec.ts`** — 全程浏览器路径。用**每次运行唯一**的候选人姓名与文件名（`FIN010 候选人 + RUN_ID`），使断言能对共享的 seed 池做精确比对。
- **`tests/validate_nonseed_flow.ps1`** — 数据库不变量探针。
- **`tests/check_probe_python.ps1`** — 静态门禁，校验探针内嵌的 Python 可解析。
- **`apps/web/tsconfig.node.json`** — `include` 加入 `e2e/**/*.ts`，spec 从此受严格类型检查（此前完全没被检查）。
- **`deploy/docker/backend.Dockerfile`** — `COPY scripts ./scripts`，让 fixture 检查能在开发镜像里跑。
- **`scripts/project.ps1`** — 新任务 `nonseed-flow-test`；`verify` 中接入两个新检查。
- **`编码实现计划.md`** — FIN-010 四项打勾、汇总行 557 `TODO → DONE`。

### 踩坑（重要）
- **反斜杠引号不是 PowerShell 转义**。这是本任务最大的坑：`\"` 在 PowerShell 双引号字符串里被当成裸引号，**字符串提前闭合**，解析错误报在第 174 行，而真正的起因是**转义约定不同**。修法：整段内嵌 Python 改用**双引号 here-string**，并在 Python 侧用 `quote = chr(34)` 自己拼引号 —— 于是 PS 字符串里再没有反斜杠引号。
- **持久化计数无法经公开 API 观测**。`ApplicationRunDetail` 只暴露 `current_approval` + `interview_id`；**没有**审批列表接口；`EvidenceChunkResponse` **没有**向量标记字段。列名是 `active_application_run_id`，不是 `active_run_id`。所以删掉了 spec 里**臆造的端点**，把计数断言移到 psql 探针（与 `validate_application_entry.ps1` 一致）。上传响应是 `items[0].resource_id`；确认走 `POST /jobs/{job_id}/profiles/{profile_id}/confirm?expected_version=<n>` + `CandidateProfileEdit` 体（`extra="forbid"`，故回显草稿自身的 `profile_json`）。
- **DOCX 是 ZIP、PDF 带时间戳**，重新生成**永远不可能字节相同**。故 `--check` 比对的是**经真实解析器解析后的文本块**；字节比对会是一个永远红的门禁。已验证：内容不变时通过、内容漂移时失败。
- **PyMuPDF `insert_text` 取 `fontname`，不取 `font` 对象**。CJK 用内置 `china-s`（Droid Sans Fallback），可经 `get_text()` 往返。
- **本机无主机侧 Python 依赖惯例**：所有 `validate_*.ps1` 要么纯 PowerShell，要么走 Docker。所以 `check_probe_python.ps1` 的 CPython 取自**开发镜像**，不引入新的主机依赖。它同时做「变量泄漏检测」——展开后仍残留 `$变量` 即说明替换表漂移（已用反向用例验证该检测确实会触发）。
- **嵌套 `pwsh` 在本沙箱里不产生可捕获输出**，`& $python -c` 同样拿不到 stdout。调试这类脚本要么内联当前会话跑，要么写文件。这是环境限制，不是脚本缺陷。

### 验证
- fixture 漂移检查：`E2E fixtures parse to the expected blocks.`
- `tsc -b`：干净（4 个 spec 全部通过严格检查）
- `vitest run`：**113 passed (22 files)**
- `playwright test --list`：1 test discovered
- 三个 PowerShell 脚本 `[Parser]::ParseInput` 全部 `PARSE_OK`
- **内嵌 Python 端到端验证**：展开后 `PY_SYNTAX_OK chars=921`；并用本地 HTTP server 证明 multipart 请求体正确（`disposition_ok=True` / `closing_ok=True` / `docx_embedded=True`）—— 即原报错的那个构造已跑通
- 文档门禁不变量：`finRows=13` / `impRows=30` / `plannedDays=27` / FIN-010 `4 checked, 0 unchecked`
- **Docker 本机未运行**，探针与 E2E 只能 CI 验证

### 下一步
FIN-011（故障与安全浏览器矩阵，P0）。另有两项 FIN-008 遗留：CI 验证 `validate_model_adapter.ps1`、以及真实凭据连通性/录制；`.git/gh-credentials` 仍未恢复，**本会话未做任何 push**。

---

## FIN-011：故障与安全浏览器矩阵（DONE，提交 `8a558ff` + `5e2ab41`）

### 关键判断：后端已覆盖，缺的是浏览器一半
§19.2 原话：「SSE … 尚未通过 Nginx 的 Bearer SSE 全栈探针，**撤权和断线恢复缺浏览器级验证**」。后端语义**已有 17 项精确单测**（`test_approval.py` 8 项 + `test_sse.py` 9 项，含 `test_midstream_revocation_closes_stream` 断言序列扣留为 `[0,1]`）。所以 FIN-011 补的是 **React 客户端把每种失败转成可恢复 UI 状态**这件事。Nginx 全栈探针按计划书原文属 **FIN-012 第 2 项**，不在本项。

### 我在浏览器测试里"真跑"而不是"注入"的原则
- **真跑**：REJECT、EDIT、重复决定、**真实在线撤权**（SSE 流处于「实时同步」时经 `DELETE /jobs/{id}/assignments/{user_id}` 撤当前 HR 的授权，`finally` 恢复以免污染共享种子）。
- **注入**（用例名带 `INJECTED:` 前缀）：503、丢 Redis 通知、需第二方参与的撤权。注入的 SSE 帧**按服务端真实线格式构造**（已逐字段比对：`event:`/`id:`/`data:` 顺序、尾部空行、10 个 payload 键全一致；心跳与撤销帧逐字节相同）。

### ⚠️ 发现并修复一个真实缺陷（`sse.ts`）
**跳号后永远不重连。** 原代码在 gap 分支先 `stop("client")`（置 `closed=true` **并 `abort()`**）再调 `reconnectFrom()`，而 `open()` 第一行就是 `if (closed) return` —— 重连是**死代码**，与该文件自述的契约「gap → 重连」直接矛盾。
- 修法：新增 `abandonStream()`（只废弃当前流、**不结束会话**），并把 `AbortController` 从 `const` 改 `let` **逐次重建**（单个被 abort 过的 controller 会毒化后续所有尝试）；`scheduleReconnect` 同样重建，使「`open()` 运行时 controller 必然可用」成为**结构保证**而非巧合。
- **验证方式**：临时把修复回退成旧的 `stop("client")`，测试**确实失败**（`expected spy to be called 2 times, but got 1`），恢复后通过 —— 证明测试不是空转。

### 其它踩坑
- **撤销后的终态是 `COMPLETED` 而非 `CANCELLED`**。我最初断言「已取消」是错的：`approvals/service.py:285-291` 对 REJECT 调 `complete_run(reason="ACTION_REJECTED")`，而 `complete_run` 置 `COMPLETED`。页面渲染 `completion_reason`，故断言应为「已完成」+「ACTION_REJECTED」。
- **`page.route` 的 glob 必须匹配真实 URL**。`listJobs` 在筛选为默认 `"ALL"` 时请求 `/api/v1/jobs`**不带查询串**（`client.ts:89`），我原本写的 `**/api/v1/jobs?**` **永远匹配不上**，测试会静默穿透到真 API 变成假绿。改用正则 `/\/api\/v1\/jobs(\?.*)?$/`（已验证：匹配两种列表形式、拒绝 `/jobs/{id}` 子资源）。
- **`request.headers()` 会小写化**，读 `Last-Event-ID` 要用 `await request.allHeaders()`。
- **编辑表单的提交按钮是「应用修改」**（`EditableParamsForm.tsx:37`），标签是「编辑后参数（JSON）」；`ActionDiff` 用 `<code>` 渲染值。
- **心跳间隔 `sse_heartbeat_seconds` 默认 1 秒**，故撤权在 ~1s 内被下一次心跳发现。

### 验证
- `vitest run`：**118 passed (22 files)**（FIN-010 时 113，+5）
- `tsc -b`：干净（18 个用例全部严格通过）
- `playwright test --list`：**18 tests in 5 files**（新增 13）
- 文档门禁不变量：`finRows=13` / `impRows=30` / `plannedDays=27` / FIN-011 `4 checked, 0 unchecked`
- **Docker 本机未运行** ⇒ 浏览器矩阵只能 CI 验证；客户端不变量（14 项）本机已跑

### 下一步
**FIN-012**（完整 Compose 与 CI，P0）：nginx/web/api/worker/scheduler 全栈、Nginx 对 API/SSE 关闭缓冲并保留 Bearer 流（**FIN-011 留下的那个探针在这里**）、建 CI、以及全新 clone/Volume 一条命令启动。FIN-013 收口在后。遗留同前：`.git/gh-credentials` 未恢复，本会话未 push。
