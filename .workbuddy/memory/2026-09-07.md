# 2026-09-07 工作日志

## 阅读编码实现计划
- 通读 `编码实现计划.md`（v1.1，2026-09-07 校准版，68 节）。
- 关键发现：文档 §19/§20 自认项目**未完成**——IMP-001~030 虽全部提交，但生产运行闭环未完成。
- 诚实 PARTIAL 域（§19.2）：文档解析前端接线、HTTP 请求级幂等、Celery Worker/Scheduler 占位、Run 异步执行仍同步、SSE 全栈探针、Evaluation 持久化、真实 Qwen/LangGraph 对齐、完整 Compose/CI 均 PARTIAL。
- 剩余 13 个 FIN（FIN-001~013），理想 16.5 天 + 2 缓冲；推荐顺序 FIN-001→002→003→004→005→006→007→008→009→010→011→012→013（§20.4）。
- README / 旧项目记忆称"IMP 全部提交=MVP 完成"有误导，已在 MEMORY.md 进度校准段修正。
- 已验证基线（§19.3）：空库迁移 21 表、双审批跨两次 API 重启恢复、Playwright 种子主路径、verify 门禁全绿。

## FIN-001 实现（请求级幂等闭环，§20.3）
- 提交：新增 backend/app/idempotency 包（models/repository/service/errors/dependency/__init__）+ 迁移 0011_idempotency_records（id 主键 + key 唯一索引 + status/request_hash/response_*/resource_id/locked_at/expires_at）。
- 设计：控制会话与业务事务解耦——dependency 读 Idempotency-Key 头 + sha256(method|path|query|body) 请求哈希，经 IdempotencyService.resolve 判定 new/replay/conflict；leader 写 IN_PROGRESS（唯一键为并发底线），follower 轮询等待、完成则重放、stale 锁(超时)则由新请求接管重跑；业务本身幂等(decide exactly once / 幂等 cancel)作兜底。
- 接线：approvals.decide_approval、job_applications.retry/cancel、match_run.retry/cancel 加 IdempotencyGuardDep 并在返回前 guard.complete(status, body, resource_id)；main.py 注册 IdempotencyReplay 处理器（原样重放 status+body 并置 Idempotency-Replayed 头）。
- 前端：ApprovalPage.tsx 用 useRef 在每次点击(不是每次渲染/重试)生成一次性 Idempotency-Key，满足验收#3（同提交/网络重试复用 Key）。
- 测试：tests/unit/test_idempotency.py（6 例，内存 InMemory 后端，覆盖重放/冲突409/并发双击单次副作用/重启接管/失败清锁/错误契约）；tests/integration/test_idempotency_postgres.py（3 例，真 PG，skipif gated）+ tests/validate_idempotency.ps1（docker 运行器 build→up postgres/redis→migrate→跑集成→down）。编码实现计划.md §20.3 四条验收全勾 DONE。
- 本地验证隐患（已修，值得复盘）：① models.py 的 JSONB 必须从 sqlalchemy.dialects.postgresql 导入（顶层 sqlalchemy 不导出）；② SQL 的 claim_new/takeover_stale 必须用 INSERT/UPDATE ... RETURNING + fetchone() is not None 判定「本事务是否真插入/接管」——否则 ON CONFLICT DO NOTHING 后读回 winner 行会让并发双方都误判为 leader（破坏验收#4）；且 fetchone() 是同步（execute 返回 Result 非协程，绝不可 await）；③ 项目无 pytest-asyncio，测试须用 def test_* 同步壳包内层 async + asyncio.run，裸 async def test_* 会被 pytest 收集并失败（docker 门禁会因此不过）。
- 验证边界：本机无 Docker/Postgres，故 ruff + mypy(仅 idempotency 包) + 内存单测在隔离 venv 全绿；Postgres 集成测试经 .ps1 在容器跑（本机未执行）。full lock 安装在本机极慢（>27min 未完成），未阻塞交付。

## FIN-001 修复（follow-up）：SQL takeover_stale 漏 RETURNING（关键教训）
- **提交 `fd0d745` 里 `SqlIdempotencyRepository.takeover_stale` 的 UPDATE 实际漏了 `.returning(...)`**，却用 `result.fetchone() is not None` 判定接管成功。无 RETURNING 的 UPDATE 结果集 0 列，`fetchone()` 永远返回 None → takeover 永远 False → API 重启后的 stale 锁接管失败（follower 撞 max_wait 抛 IDEMPOTENCY_LOCKED/409），破坏验收#4。
- **为何单测没抓到**：`tests/unit` 用的是 `InMemoryIdempotencyRepository`（独立正确实现），SQL 路径只在 `tests/integration` 覆盖，而本机无 PG 跑不了。教训——**InMemory 后端会掩盖 SQL 专属 bug**；凡是 SQL 路径的分支（RETURNING/rowcount/fetchone/方言语法），单测通过不等于 SQL 正确，必须靠真实 PG 集成测试兜住。修复：给 takeover_stale 的 UPDATE 对称加 `.returning(IdempotencyRecord.id)`（与 claim_new 一致），已确认集成测试 `test_sql_stale_lock_takeover_after_restart` 能抓住该回归。
- 复用模式：SQL 判定「本事务是否真插入/接管」一律用 `INSERT/UPDATE ... RETURNING <pk>` + `result.fetchone() is not None`；不要用 `rowcount`（async Result 的 rowcount 在部分方言不可靠），更不要对无 RETURNING 的结果 fetchone()。

## FIN-001 收尾：把 idempotency 集成测试接进 CI 门禁
- 发现 `scripts/project.ps1` 的 `verify` 动作漏串 `tests/validate_idempotency.ps1`（列了一堆校验但没它），导致 FIN-001 的 PG 集成测试虽写好却没进 CI。已在 `verify` 分支 `validate_migrations` 之后插入该步（提交未命名，见下）。
- 安全性确认：compose 的 postgres/redis 无 `ports:` 映射（仅挂 backend 网络，不发布 host 端口），`validate_compose_stack`/`validate_migrations` 均 `finally down --volumes` 自清理且断言无残留；idempotency 脚本用独立 project name `resume-copilot-idempotency-probe` + `finally down --volumes`，所以接进 verify 不会与其他栈抢 5432 或留残留。PowerShell 解析检查通过（exit 0）。

## FIN-001 推送完成（git push origin main）
- 三提交（fd0d745 实现 / 80b45cd RETURNING 修复 / 2f981a9 CI 接线）已推到 `https://github.com/lans12138/resume-copilot.git`。`git ls-remote origin main` 实时 SHA = `2f981a9`（与本地 HEAD 一致），快进无冲突。
- CI 因 `ci.yml: on: push: branches:[main]` 已触发；idempotency PG 集成测试经 `verify` 在容器跑（本机无 Docker 无法验，等 GitHub Actions 结果）。本机无 `gh`，CI 结果由用户去 GitHub Actions 看。
- 工作区残留 3 个未跟踪项（`.workbuddy/memory/2026-09-04.md`、`MEMORY.md`、`skills/resume-approval-sideeffect/`）未纳入提交，与 FIN-001 无关。

## FIN-001 CI 红根因 + 修复（关键）
- CI run `34114237103`（含 0011）结论 failure，失败锁定在 `verify` 的 `validate_migrations.ps1`，具体是 `docker compose ... run --rm migrate alembic check` 步骤。`alembic check` 报告 `operations detected: [remove_index ix_idempotency_records_key, remove_table idempotency_records]`——即 DB 有该表（0011 迁移创建成功）但 `Base.metadata` 没有，check 误判需删除。
- **根因**：`backend/app/infrastructure/model_registry.py` 是显式模型注册表（import 各模型模块 + `MIGRATED_MODEL_TABLES` 白名单 + `_LOADED_MODEL_MODULES` 元组）。`IdempotencyRecord` 漏注册 → `get_model_metadata()` 返回的 metadata 不含 `idempotency_records` → alembic check 不一致。这又是「InMemory 后端掩盖、只在真 PG + alembic check 暴露」的坑（内存单测不涉及 metadata 收集）。
- **连带过期断言**：`validate_migrations.ps1` 硬编码 `revision == '0010_persist_workflow_tail'`（IMP-004 时代探针），head 到 0011 后该断言也会失败；`expectedTablesSql` 列表也缺 `idempotency_records`。
- **修复（待提交）**：model_registry 三处（import idempotency_models / `MIGRATED_MODEL_TABLES` 加 idempotency_records / `_LOADED_MODEL_MODULES` 加 idempotency_models）；validate_migrations.ps1 三处（revision 断言 0010→0011 / expectedTablesSql 加 idempotency_records（21→22 表）/ 成功输出 tables=21→22）。
- 本地验证：`get_model_metadata()` 现返回 22 表且含 idempotency_records（14 列齐全），无 drift；ruff 通过。修复后 alembic check 会看到模型与 DB 一致。
- **注意**：基线 run `33705037566`（037dcc5b，无 0011，2026-09-03）也是 failure——说明 CI 在 FIN-001 之前就红。本次确认的 idempotency 失败是 FIN-001 引入的；基线红的根因（是否也是 model_registry 漏注册别模型 / 该探针其他过期断言）尚未深究，待 FIN-001 闭合后再排。
