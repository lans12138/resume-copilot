# 企业招聘 Copilot 详细设计说明书

> 文档版本：1.0
>
> 设计日期：2026-09-01
>
> 适用范围：六周单人开发的作品集级 MVP
>
> 需求基线：[需求分析 1.2](./需求分析.md)
>
> 概要设计基线：[概要设计说明书 1.0](./概要设计说明书.md)
>
> 架构依据：[技术栈选型与架构决策](./技术栈选型与架构决策.md)
>
> 可行性依据：[项目可行性分析 1.3](./项目可行性分析.md)

## 1. 文档说明

### 1.1 编写目的

本文将概要设计细化为可以直接拆分开发任务、建立数据库迁移、定义 OpenAPI、实现 LangGraph Node、编写前端页面和自动化测试的工程设计。本文重点确定：

- 模块目录、依赖方向和应用服务职责。
- 数据表字段、枚举、约束、索引和事务锁顺序。
- API 请求、响应、错误、权限、并发和幂等契约。
- MatchRun 与 ApplicationRun 的 State、节点输入输出、路由和恢复规则。
- 文件解析、混合检索、证据校验、人工审批和模拟排期的实现边界。
- Celery、Scheduler、SSE、前端状态、可观测性和发布验证的具体方案。

本文不是代码生成规范。字段名和接口契约在实现前允许做不改变语义的微调；如果改变领域边界、状态机、核心实体、权限规则或发布门槛，必须先更新需求分析、架构决策、概要设计和本文。

### 1.2 设计范围

本次详细设计覆盖以下 MVP：

1. HR 和招聘主管登录及资源级授权。
2. 岗位创建、版本、启用、关闭与 JobAssignment。
3. PDF/DOCX 批量上传、异步解析、人工校对和确认。
4. 结构化、关键词、向量三路召回与 RRF 融合。
5. MatchRun 岗位级批量分析和证据化报告。
6. ApplicationRun 单候选人审批、面试问题和模拟排期。
7. AgentEvent 持久化、SSE 实时推送、重放和持续授权。
8. 离线评测、日志、Trace、健康检查、Docker Compose 和 CI。

不包含 OCR、真实日历、多租户、企业 SSO、Refresh Token、Kubernetes、生产高可用、真实招聘数据治理和自动候选人身份合并。

### 1.3 设计约定

| 项目 | 约定 |
|---|---|
| 后端命名 | Python 使用 snake_case；类和 Pydantic Model 使用 PascalCase。 |
| 前端命名 | TypeScript 变量使用 camelCase；组件使用 PascalCase。 |
| 数据库命名 | 表和列使用 snake_case；约束和索引使用可读前缀。 |
| 主键 | 业务表使用 PostgreSQL UUID；应用层生成 UUIDv4。 |
| 时间 | 数据库存 `timestamptz`，API 使用 ISO 8601 UTC。 |
| 金额与分数 | 分数使用 `numeric` 或整数刻度，禁止依赖浮点相等判断。 |
| 枚举 | 数据库存字符串枚举值；迁移和应用代码共同维护。 |
| 软删除 | 授权关系使用 revoked_at；核心审计事实不物理删除。 |
| JSONB | 只用于版本化快照、模型结构化结果和低频扩展字段；关系完整性不放入 JSONB。 |
| 乐观锁 | 可并发修改的聚合根使用递增 `version`。 |
| 错误 | API 返回稳定 error code，不把异常类名或栈暴露给客户端。 |

### 1.4 术语和标识

| 标识 | 用途 |
|---|---|
| request_id | 一次 HTTP 请求的关联 ID。 |
| task_id | 一次 Celery 投递 ID，只用于观测。 |
| operation_key | 业务幂等键，不依赖 Celery task_id。 |
| run_id | AgentRun 主键，同时作为 MatchRun 或 ApplicationRun 主键。 |
| thread_id | 持久化 Checkpointer 恢复键（本仓库实现为 `SqlCheckpointer`，见 ADR-0001）。 |
| attempt | 同一个 Run 的执行尝试序号，从 1 开始。 |
| sequence | 同一个 Run 内单调递增的 AgentEvent 序号。 |
| expected_version | 客户端提交的乐观锁版本。 |

## 2. 工程结构与依赖

### 2.1 仓库目录

建议实现阶段采用以下单仓结构：

~~~text
resume/
├── apps/
│   ├── api/                    # FastAPI 入口、Router、中间件
│   ├── worker/                 # Celery Worker 入口
│   ├── scheduler/              # Celery Beat 入口
│   └── web/                    # React + TypeScript + Vite
├── backend/
│   └── app/
│       ├── core/               # 配置、错误、日志、时间、ID
│       ├── auth/
│       ├── jobs/
│       ├── documents/
│       ├── candidates/
│       ├── retrieval/
│       ├── agent/
│       ├── approvals/
│       ├── interviews/
│       ├── events/
│       ├── evaluations/
│       ├── observability/
│       └── infrastructure/
├── migrations/                # Alembic
├── tests/
│   ├── unit/
│   ├── integration/
│   ├── contract/
│   ├── e2e/
│   ├── evaluations/
│   └── fixtures/
├── deploy/
│   ├── nginx/
│   └── compose/
└── scripts/
~~~

每个后端领域模块内部使用一致结构：

~~~text
module/
├── api.py              # Router 和 HTTP Schema 映射
├── schemas.py          # Pydantic 请求、响应和内部 DTO
├── service.py          # 用例编排和事务边界
├── domain.py           # 枚举、状态迁移、领域策略
├── repository.py       # Repository Port
├── models.py           # SQLAlchemy Model
└── tasks.py            # 本领域 Celery Task；没有任务时省略
~~~

### 2.2 依赖方向

~~~text
FastAPI Router / Celery Task / Graph Node
                    ↓
             Application Service
             ↙                 ↘
       Domain Policy        Port / Repository
                                  ↓
                        Infrastructure Adapter
~~~

必须遵守以下规则：

1. Router 只负责协议转换、依赖注入和响应映射，不直接查询 ORM。
2. Celery Task 只解析 task 参数、建立 request/task 上下文并调用应用服务。
3. Agent Node 只接收类型化 State，并调用显式服务或只读查询接口。
4. Domain Policy 不依赖 FastAPI、Celery、SQLAlchemy Session、Redis 或模型 SDK。
5. Repository 不提交事务；事务由应用服务的 Unit of Work 控制。
6. events 模块只发布已提交事实，不能反向决定招聘业务状态。
7. 副作用工具必须通过 approvals 和对应领域服务，不允许 Node 直接写表。

### 2.3 Unit of Work

每个 HTTP 命令、Worker 节点持久化或 Scheduler 批次使用一个显式 Unit of Work：

~~~python
class UnitOfWork(Protocol):
    users: UserRepository
    jobs: JobRepository
    documents: DocumentRepository
    candidates: CandidateRepository
    runs: RunRepository
    approvals: ApprovalRepository
    events: EventRepository

    async def __aenter__(self) -> "UnitOfWork": ...
    async def __aexit__(self, exc_type, exc, tb) -> None: ...
    async def commit(self) -> None: ...
    async def rollback(self) -> None: ...
~~~

数据库提交成功后才能发送 Redis 通知或 Celery 消息。MVP 不引入完整 Outbox；对于“数据库已提交但消息发布失败”的场景，业务对象保持可扫描状态，由手动 retry 或 maintenance 协调任务重新发布。

## 3. 配置与运行上下文

### 3.1 配置 Schema

使用 Pydantic Settings 在 API、Worker 和 Scheduler 启动时校验：

| 类别 | 字段 | 规则 |
|---|---|---|
| app | APP_ENV、LOG_LEVEL、PUBLIC_BASE_URL | APP_ENV 为 local/test/demo。 |
| auth | JWT_SECRET、JWT_ALGORITHM、ACCESS_TOKEN_TTL_MINUTES | Secret 必填；MVP 算法固定 HS256。 |
| database | DATABASE_URL、DB_POOL_SIZE、DB_POOL_TIMEOUT | PostgreSQL URL 必填。 |
| redis | REDIS_URL、CELERY_BROKER_URL | 允许相同 Redis，不把 Redis 当事实源。 |
| storage | STORAGE_ROOT、MAX_FILE_SIZE_MB、MAX_BATCH_FILES | 路径必须位于挂载目录。 |
| parser | MAX_PDF_PAGES、MAX_EXTRACTED_CHARS、PARSER_TIMEOUT_SECONDS | 超限永久失败。 |
| model | MODEL_BASE_URL、QWEN_API_KEY、CHAT_MODEL | Secret 不写日志。 |
| embedding | EMBEDDING_MODEL、EMBEDDING_DIMENSION、EMBEDDING_BATCH_SIZE | 维度固定 1024。 |
| retrieval | TOP_K、RRF_K、STRUCTURED_WEIGHT、KEYWORD_WEIGHT、VECTOR_WEIGHT | 权重非负且至少一个大于 0。 |
| run | NODE_TIMEOUT_SECONDS、MAX_TRANSIENT_RETRIES、APPROVAL_TTL_MINUTES | TTL 大于 0。 |
| sse | SSE_HEARTBEAT_SECONDS、SSE_BATCH_SIZE、SSE_RETRY_MILLISECONDS | 心跳小于代理空闲超时。 |
| feature | LANGFUSE_ENABLED、MOCK_MODEL_MODE、HNSW_ENABLED | 默认关闭 HNSW。 |

生产/演示所需字段缺失时进程快速失败；测试配置可以显式使用 FakeModel、临时 StorageBackend 和测试数据库。

### 3.2 请求和任务上下文

`RequestContext` 至少包含 request_id、actor_id、role、client_ip、user_agent。`TaskContext` 至少包含 task_id、operation_key、attempt、run_id。上下文通过 `contextvars` 注入日志，但应用服务仍显式接收 actor 和授权范围，不能只依赖隐式上下文做安全决策。

### 3.3 时间和随机性

- 领域服务通过 `Clock` Port 获取当前 UTC 时间，测试使用 FrozenClock。
- ID 通过 `IdGenerator` Port 生成，测试可以使用固定 UUID。
- Approval 到期判断只使用数据库当前时间，避免 API、Worker 和 Scheduler 容器时钟偏差。
- 排名并列使用稳定次级排序：融合分数降序、candidate_profile_id 升序。

## 4. 数据库详细设计

### 4.1 公共字段和类型

除纯关联表外，业务表默认包含：

| 字段 | PostgreSQL 类型 | 说明 |
|---|---|---|
| id | uuid | 主键，应用层 UUIDv4。 |
| created_at | timestamptz | 服务端默认数据库当前时间。 |
| updated_at | timestamptz | 每次更新设置数据库当前时间。 |
| version | integer | 需要乐观锁的表从 1 开始。 |

JSONB Schema 由 Pydantic 版本控制；读取旧版本时必须通过明确迁移器升级，不能在业务代码中散落兼容判断。

### 4.2 身份与岗位表

#### users

| 字段 | 类型 | 可空 | 约束/说明 |
|---|---|---:|---|
| id | uuid | 否 | 主键。 |
| username | varchar(128) | 否 | 规范化后唯一。 |
| password_hash | varchar(255) | 否 | Argon2 哈希。 |
| role | varchar(32) | 否 | HR、HIRING_MANAGER、ADMIN。 |
| is_active | boolean | 否 | 默认 true。 |
| last_login_at | timestamptz | 是 | 成功登录后更新。 |
| created_at、updated_at | timestamptz | 否 | 审计时间。 |

索引：`uq_users_username_normalized`；`ix_users_role_active(role, is_active)`。

#### jobs

| 字段 | 类型 | 可空 | 约束/说明 |
|---|---|---:|---|
| id | uuid | 否 | 主键。 |
| title | varchar(200) | 否 | 展示标题。 |
| status | varchar(32) | 否 | DRAFT、ACTIVE、CLOSED。 |
| current_version_id | uuid | 是 | 指向 job_versions。 |
| created_by | uuid | 否 | users 外键。 |
| version | integer | 否 | 乐观锁。 |
| created_at、updated_at | timestamptz | 否 | 审计时间。 |

约束：ACTIVE/CLOSED 必须有 current_version_id；CLOSED 不允许重新启用；通过 `(current_version_id, id)` 到 `job_versions(id, job_id)` 的复合外键保证当前版本属于当前岗位，该约束在 job_versions 建表后添加。索引：`ix_jobs_status_created_at(status, created_at desc)`。

#### job_versions

| 字段 | 类型 | 可空 | 约束/说明 |
|---|---|---:|---|
| id | uuid | 否 | 主键。 |
| job_id | uuid | 否 | jobs 外键。 |
| version_no | integer | 否 | 岗位内从 1 递增。 |
| description_text | text | 否 | 原始岗位描述。 |
| requirements_json | jsonb | 否 | 类型化岗位要求快照。 |
| content_sha256 | char(64) | 否 | 内容哈希。 |
| schema_version | varchar(32) | 否 | 快照 Schema 版本。 |
| created_by | uuid | 否 | users 外键。 |
| created_at | timestamptz | 否 | 创建时间。 |

约束和索引：`uq_job_versions_job_no(job_id, version_no)`、`uq_job_versions_job_hash(job_id, content_sha256)`、供复合外键引用的 `uq_job_versions_id_job(id, job_id)`。被 MatchRun 引用后不 UPDATE，只能新增版本。

#### job_assignments

| 字段 | 类型 | 可空 | 约束/说明 |
|---|---|---:|---|
| id | uuid | 否 | 主键。 |
| job_id | uuid | 否 | jobs 外键。 |
| user_id | uuid | 否 | users 外键，必须为激活的 HIRING_MANAGER。 |
| assigned_by | uuid | 否 | HR 用户。 |
| assigned_at | timestamptz | 否 | 数据库时间。 |
| revoked_by | uuid | 是 | HR 用户。 |
| revoked_at | timestamptz | 是 | 空表示当前有效。 |

部分唯一索引：`uq_job_assignments_active(job_id, user_id) WHERE revoked_at IS NULL`。撤销只写 revoked_by/revoked_at，不删除记录。

### 4.3 文档、候选人与证据表

#### resume_documents

| 字段 | 类型 | 可空 | 说明 |
|---|---|---:|---|
| id | uuid | 否 | 主键。 |
| original_filename | varchar(255) | 否 | 仅展示，输出时转义。 |
| storage_key | varchar(255) | 否 | 唯一随机键，不含原文件名。 |
| media_type | varchar(128) | 否 | 实际检测类型。 |
| size_bytes | bigint | 否 | 大于 0 且不超过配置上限。 |
| content_sha256 | char(64) | 否 | 重复检测。 |
| status | varchar(32) | 否 | 文档状态枚举。 |
| parser_version | varchar(64) | 是 | 解析器版本。 |
| attempt | integer | 否 | 默认 0。 |
| retryable | boolean | 否 | 默认 false。 |
| error_code | varchar(64) | 是 | 稳定错误码。 |
| error_message_safe | varchar(500) | 是 | 脱敏摘要。 |
| uploaded_by | uuid | 否 | users 外键。 |
| created_at、updated_at | timestamptz | 否 | 审计时间。 |

索引：`uq_resume_documents_sha256(content_sha256)`、`uq_resume_documents_storage_key(storage_key)`、`ix_resume_documents_status_created(status, created_at)`。

#### candidates

| 字段 | 类型 | 可空 | 说明 |
|---|---|---:|---|
| id | uuid | 否 | 主键。 |
| display_name | varchar(200) | 否 | 合成数据中的展示名。 |
| normalized_email_hash | char(64) | 是 | 仅用于提示潜在重复，不自动合并。 |
| created_at、updated_at | timestamptz | 否 | 审计时间。 |

#### candidate_profiles

| 字段 | 类型 | 可空 | 说明 |
|---|---|---:|---|
| id | uuid | 否 | 主键。 |
| candidate_id | uuid | 否 | candidates 外键。 |
| document_id | uuid | 否 | resume_documents 外键。 |
| version_no | integer | 否 | 候选人内递增。 |
| status | varchar(32) | 否 | DRAFT、REVIEW_REQUIRED、READY、SUPERSEDED。 |
| profile_json | jsonb | 否 | 结构化资料。 |
| normalized_skills | text[] | 否 | 规范化技能。 |
| years_experience | numeric(5,2) | 是 | 无法判断时为空。 |
| education_level | varchar(64) | 是 | 规范化枚举或空。 |
| schema_version | varchar(32) | 否 | Profile Schema 版本。 |
| confirmed_by、confirmed_at | uuid/timestamptz | 是 | 人工确认审计。 |
| created_at、updated_at、version | timestamptz/integer | 否 | 乐观锁。 |

约束：`uq_candidate_profiles_candidate_version(candidate_id, version_no)`、供复合外键引用的 `uq_candidate_profiles_id_document(id, document_id)`；普通索引 `ix_candidate_profiles_document(document_id)`；部分唯一索引 `uq_candidate_profiles_ready(candidate_id) WHERE status='READY'`。同一文档重新解析可以生成新的 Profile 版本；确认新版本的同一事务中先将旧 READY 置为 SUPERSEDED，再将新版本置为 READY。

#### evidence_chunks

| 字段 | 类型 | 可空 | 说明 |
|---|---|---:|---|
| id | uuid | 否 | 主键。 |
| document_id | uuid | 否 | resume_documents 外键。 |
| candidate_profile_id | uuid | 否 | candidate_profiles 外键。 |
| chunk_index | integer | 否 | 文档版本内顺序。 |
| section_type | varchar(64) | 否 | summary、experience、education、skill 等。 |
| locator_json | jsonb | 否 | PDF 页/字符范围或 DOCX 段落/表格位置。 |
| text | text | 否 | 精确证据文本。 |
| text_sha256 | char(64) | 否 | 摘录校验。 |
| embedding | vector(1024) | 是 | 未完成 Embedding 时为空。 |
| embedding_model | varchar(128) | 是 | 模型名。 |
| embedding_version | varchar(64) | 是 | 模型/预处理版本。 |
| created_at | timestamptz | 否 | 创建时间。 |

约束：`uq_evidence_chunks_document_index(document_id, chunk_index)`；通过 `(candidate_profile_id, document_id)` 到 `candidate_profiles(id, document_id)` 的复合外键保证 Profile 和 Document 归属一致。索引：`ix_evidence_chunks_profile_section(candidate_profile_id, section_type)`；MVP 默认不创建 HNSW。

### 4.4 招聘流程与运行表

#### job_applications

| 字段 | 类型 | 可空 | 说明 |
|---|---|---:|---|
| id | uuid | 否 | 主键。 |
| job_id | uuid | 否 | jobs 外键。 |
| candidate_id | uuid | 否 | candidates 外键。 |
| status | varchar(32) | 否 | CREATED、SHORTLISTED、ON_HOLD、REJECTED、INTERVIEW_SCHEDULED。 |
| active_application_run_id | uuid | 是 | 当前非终态 ApplicationRun 排他槽。 |
| version | integer | 否 | 状态乐观锁。 |
| created_at、updated_at | timestamptz | 否 | 审计时间。 |

约束和索引：

- `uq_job_applications_job_candidate(job_id, candidate_id)`。
- `ix_job_applications_job_status(job_id, status)`。
- `uq_job_applications_active_run(active_application_run_id) WHERE active_application_run_id IS NOT NULL`。
- 通过 `(active_application_run_id, id)` 到 `application_runs(run_id, application_id)` 的复合外键确保活动 Run 属于当前 JobApplication；该约束在 application_runs 建表后添加。

#### agent_runs

| 字段 | 类型 | 可空 | 说明 |
|---|---|---:|---|
| id | uuid | 否 | run_id 主键。 |
| run_type | varchar(32) | 否 | MATCH、APPLICATION。 |
| thread_id | varchar(128) | 否 | Checkpointer key，全局唯一。 |
| status | varchar(32) | 否 | CREATED、RUNNING、WAITING_APPROVAL、COMPLETED、FAILED、CANCELLED。 |
| attempt | integer | 否 | 从 1 开始。 |
| version | integer | 否 | 运行并发版本。 |
| current_node | varchar(128) | 是 | 最近开始或完成节点。 |
| next_event_sequence | bigint | 否 | 从 1 开始分配。 |
| cancel_requested_at | timestamptz | 是 | 取消标记。 |
| cancel_requested_by | uuid | 是 | users 外键。 |
| retryable | boolean | 否 | 失败是否允许 retry。 |
| error_code | varchar(64) | 是 | 稳定错误码。 |
| error_message_safe | varchar(500) | 是 | 脱敏错误摘要。 |
| failed_node | varchar(128) | 是 | 失败节点。 |
| snapshot_version | varchar(64) | 否 | 运行快照 Schema 版本。 |
| created_by | uuid | 否 | 发起用户。 |
| started_at、finished_at | timestamptz | 是 | 执行时间。 |
| created_at、updated_at | timestamptz | 否 | 审计时间。 |

约束：`attempt >= 1`、`version >= 1`、终态必须有 finished_at；CANCELLED 必须有 cancel_requested_at。索引：`uq_agent_runs_thread_id(thread_id)`、`ix_agent_runs_type_status_updated(run_type, status, updated_at)`。

#### match_runs

| 字段 | 类型 | 可空 | 说明 |
|---|---|---:|---|
| run_id | uuid | 否 | agent_runs 一对一主外键。 |
| job_id | uuid | 否 | jobs 外键。 |
| job_version_id | uuid | 否 | job_versions 外键。 |
| retrieval_config_json | jsonb | 否 | 通道、权重、RRF_K、Top-K。 |
| model_config_json | jsonb | 否 | 模型、温度、超时。 |
| prompt_version | varchar(64) | 否 | Prompt 版本。 |
| rule_version | varchar(64) | 否 | 硬规则版本。 |

`run_type` 与子表匹配由应用服务和数据库触发器/迁移测试共同保证；MVP 优先使用应用校验加一对一唯一外键。

`(job_version_id, job_id)` 通过复合外键引用 `job_versions(id, job_id)`，防止 Run 绑定其他岗位的版本。

#### match_run_candidates

| 字段 | 类型 | 可空 | 说明 |
|---|---|---:|---|
| id | uuid | 否 | 主键。 |
| run_id | uuid | 否 | match_runs 外键。 |
| candidate_profile_id | uuid | 否 | 固定 Profile 版本。 |
| application_id | uuid | 否 | job_applications 外键。 |
| snapshot_order | integer | 否 | 融合后稳定顺序。 |
| structured_rank、keyword_rank、vector_rank | integer | 是 | 未命中通道为空。 |
| structured_score、keyword_score、vector_score | numeric | 是 | 原通道分数。 |
| rrf_score | numeric(18,10) | 否 | 融合分数。 |
| hard_rule_result_json | jsonb | 是 | PASS/FAIL/UNKNOWN 明细。 |
| processing_status | varchar(32) | 否 | PENDING、COMPLETED、FAILED。 |
| error_code | varchar(64) | 是 | 单候选人失败。 |

约束：`uq_match_run_candidates_profile(run_id, candidate_profile_id)`、`uq_match_run_candidates_order(run_id, snapshot_order)`。

#### application_runs

| 字段 | 类型 | 可空 | 说明 |
|---|---|---:|---|
| run_id | uuid | 否 | agent_runs 一对一主外键。 |
| application_id | uuid | 否 | job_applications 外键。 |
| match_report_id | uuid | 是 | 触发流程所依据的报告。 |
| completion_reason | varchar(64) | 是 | SUCCESS、ACTION_REJECTED 等。 |
| question_set_json | jsonb | 是 | 版本化面试问题集。 |
| question_schema_version | varchar(32) | 是 | 问题 Schema 版本。 |

约束：`uq_application_runs_run_application(run_id, application_id)`，供活动 Run 复合外键引用。历史允许同一 application_id 有多个终态 Run。

### 4.5 报告、审批、面试和事件表

#### match_reports

| 字段 | 类型 | 可空 | 说明 |
|---|---|---:|---|
| id | uuid | 否 | 主键。 |
| run_id | uuid | 否 | match_runs 外键。 |
| application_id | uuid | 否 | job_applications 外键。 |
| candidate_profile_id | uuid | 否 | 快照 Profile。 |
| overall_score | numeric(5,2) | 否 | 0 至 100。 |
| recommendation | varchar(32) | 否 | STRONG_MATCH、MATCH、REVIEW、WEAK_MATCH。 |
| summary | text | 否 | 结构化摘要。 |
| model_snapshot_json | jsonb | 否 | 模型和 Prompt 参数。 |
| created_at | timestamptz | 否 | 创建时间。 |

约束：`uq_match_reports_run_application(run_id, application_id)`。

#### report_claims 与 claim_evidences

`report_claims` 保存 claim_type、claim_text、impact_level、support_level、confidence_note 和 display_order。`claim_evidences` 保存 claim_id、evidence_chunk_id、quote_text、quote_start、quote_end。

约束：

- `uq_report_claims_order(report_id, display_order)`。
- `uq_claim_evidences_claim_chunk_range(claim_id, evidence_chunk_id, quote_start, quote_end)`。
- quote_start >= 0、quote_end > quote_start。
- 保存前应用服务必须校验 Chunk 属于报告的 CandidateProfile，且 `chunk.text[quote_start:quote_end] == quote_text`。
- 高影响 Claim 的 support_level 非 SUPPORTED 时，claim_text 必须使用“不足以判断”模板，不能保存确定性入围、淘汰或硬条件结论。

#### approvals

| 字段 | 类型 | 可空 | 说明 |
|---|---|---:|---|
| id | uuid | 否 | 主键。 |
| application_run_id | uuid | 否 | application_runs 外键。 |
| action_type | varchar(64) | 否 | UPDATE_APPLICATION_STATUS、CREATE_INTERVIEW_SCHEDULE。 |
| status | varchar(32) | 否 | Approval 状态。 |
| original_params_json | jsonb | 否 | Agent 提案。 |
| final_params_json | jsonb | 是 | 批准或编辑后的参数。 |
| expected_application_version | integer | 是 | 状态变更所需版本。 |
| idempotency_key | varchar(128) | 否 | 全局唯一。 |
| version | integer | 否 | 审批乐观锁。 |
| expires_at | timestamptz | 是 | 数据库时间判断。 |
| expiration_reason | varchar(32) | 是 | TIMEOUT、RUN_CANCELLED。 |
| decided_by、decided_at | uuid/timestamptz | 是 | 决定审计。 |
| executed_at | timestamptz | 是 | 副作用确认时间。 |
| execution_result_json | jsonb | 是 | 类型化执行结果。 |
| execution_error_code | varchar(64) | 是 | 执行失败。 |
| created_at、updated_at | timestamptz | 否 | 审计时间。 |

部分唯一索引：`uq_approvals_pending_run(application_run_id) WHERE status='PENDING'`；唯一索引：`uq_approvals_idempotency_key(idempotency_key)`；扫描索引：`ix_approvals_pending_expiry(status, expires_at)`。

#### application_status_histories

字段为 id、application_id、from_status、to_status、approval_id、actor_id、application_version、occurred_at。表只追加。唯一约束 `(application_id, application_version)` 防止同一版本重复记录迁移。

#### interviews

字段为 id、application_id、application_run_id、question_set_json、schedule_params_json、schedule_backend、external_schedule_id、status、created_by_approval_id、created_at、updated_at。`external_schedule_id` 唯一；MVP backend 固定 MOCK，状态为 SCHEDULED 或 FAILED。

#### agent_events

| 字段 | 类型 | 可空 | 说明 |
|---|---|---:|---|
| id | uuid | 否 | event_id 主键。 |
| run_id | uuid | 否 | agent_runs 外键。 |
| run_type | varchar(32) | 否 | 冗余用于读取和契约校验。 |
| sequence | bigint | 否 | Run 内单调递增。 |
| event_type | varchar(64) | 否 | NODE_STARTED、NODE_COMPLETED、STATUS_CHANGED 等。 |
| node | varchar(128) | 是 | 相关节点。 |
| status | varchar(32) | 否 | 面向 UI 的结构化状态。 |
| message_key | varchar(128) | 否 | 前端可映射文案。 |
| safe_payload_json | jsonb | 否 | 不含完整简历和 Secret。 |
| occurred_at | timestamptz | 否 | 数据库时间。 |

约束：`uq_agent_events_run_sequence(run_id, sequence)`。写事件时锁定 AgentRun，读取并递增 next_event_sequence，再插入 AgentEvent；禁止使用 `max(sequence)+1`。

### 4.6 评测与审计表

- `dataset_versions`：name、version、content_hash、schema_version、manifest_json、created_at。
- `evaluation_runs`：dataset_version_id、status、config_hash、model_snapshot_json、prompt_versions_json、started_at、finished_at、error_code。
- `metric_snapshots`：evaluation_run_id、metric_name、metric_value、threshold、passed、dimensions_json。
- `audit_logs`：actor_id、action、resource_type、resource_id、job_id、request_id、safe_details_json、occurred_at。

EvaluationRun 和 MetricSnapshot 完成后不可原地修改；重跑创建新 EvaluationRun。审计日志不得保存 Token、密码、完整简历、模型 Secret 或文件二进制。

### 4.7 迁移顺序

Alembic 初始迁移按以下顺序：

1. 扩展 `vector`、枚举/检查约束和 users。
2. jobs、job_versions、job_assignments。
3. resume_documents、candidates、candidate_profiles、evidence_chunks。
4. job_applications、agent_runs、match_runs、application_runs。
5. 回补 job_applications.active_application_run_id 复合外键。
6. MatchRun 快照、报告、证据关系。
7. approvals、状态历史、interviews、agent_events。
8. evaluation 和 audit 表。
9. 部分唯一索引与数据一致性检查。

每个迁移必须支持从空库升级；不要求为未发布 MVP 编写 downgrade，但删除表或列的迁移必须在 PR 中明确数据恢复方案。

## 5. 状态迁移与并发控制

### 5.1 状态迁移实现

状态迁移使用纯函数表，不接受任意目标状态字符串：

~~~python
JOB_TRANSITIONS = {
    JobStatus.DRAFT: {JobStatus.ACTIVE},
    JobStatus.ACTIVE: {JobStatus.CLOSED},
    JobStatus.CLOSED: set(),
}
~~~

每个 `transition_*` 服务必须接收当前 version、目标状态、actor 和原因，校验后使用 `WHERE id=:id AND version=:expected_version` 条件更新。影响行数为 0 时重新读取并返回 409。

### 5.2 Job 和 Document

- Job：DRAFT → ACTIVE → CLOSED。
- ResumeDocument：UPLOADED → QUEUED → PARSING → REVIEW_REQUIRED → READY；QUEUED/PARSING 可进入 FAILED；不支持格式进入 UNSUPPORTED；重新解析成功后旧 READY 可进入 SUPERSEDED。
- CandidateProfile：DRAFT → REVIEW_REQUIRED → READY → SUPERSEDED。
- 文档 FAILED 只有 retryable=true 才能重试到 QUEUED；UNSUPPORTED 不可重试。

### 5.3 JobApplication

允许迁移：

| 当前状态 | 可达状态 |
|---|---|
| CREATED | SHORTLISTED、ON_HOLD、REJECTED |
| SHORTLISTED | INTERVIEW_SCHEDULED |
| ON_HOLD | 无；如需恢复必须先扩展需求和状态机。 |
| INTERVIEW_SCHEDULED | 无 |
| REJECTED | 无 |

MatchRun 只能创建或复用 JobApplication，不能调用状态迁移。所有状态变化必须引用 EXECUTED 的 UPDATE_APPLICATION_STATUS Approval，并追加 ApplicationStatusHistory。

### 5.4 Run

MatchRun：CREATED → RUNNING → COMPLETED；CREATED/RUNNING → CANCELLED；RUNNING → FAILED。

ApplicationRun：CREATED → RUNNING → WAITING_APPROVAL → RUNNING → COMPLETED；RUNNING/WAITING_APPROVAL → FAILED；CREATED/RUNNING/WAITING_APPROVAL → CANCELLED。

公共规则：

- COMPLETED 和 CANCELLED 不可重试。
- FAILED 仅在 retryable=true 时允许 retry；同一 run_id/thread_id，attempt 加 1。
- retry 先锁定 Run；ApplicationRun 还必须锁定 JobApplication 并重新认领 active_application_run_id。
- 进入任一终态时，以 `active_application_run_id = run_id` 为条件清理活动槽，旧尝试不得清除新槽。
- Worker 节点提交使用 run status、version、cancel_requested_at 条件更新；条件失效则丢弃结果。

### 5.5 Approval

| 当前状态 | 可达状态 | 触发者 |
|---|---|---|
| PENDING | APPROVED、EDITED、REJECTED | 当前授权用户。 |
| PENDING | EXPIRED | Scheduler、决定接口或 Run 取消。 |
| APPROVED/EDITED | EXECUTED | 工具执行服务。 |
| APPROVED/EDITED | EXECUTION_FAILED | 工具执行服务。 |
| EXECUTION_FAILED | EXECUTED | 同一幂等键受控重试。 |

REJECTED 表示动作未执行，父 ApplicationRun 以 completion_reason=ACTION_REJECTED 正常 COMPLETED，并清理活动槽；JobApplication 状态保持不变。

TIMEOUT 过期在同一事务中将 Approval 置 EXPIRED、ApplicationRun 置 retryable FAILED/error_code=APPROVAL_EXPIRED、写 AgentEvent 并清理活动槽。RUN_CANCELLED 过期随父 Run 进入 CANCELLED，不再可重试。

### 5.6 锁顺序

为避免死锁，涉及多个聚合时统一按以下顺序加 `SELECT ... FOR UPDATE`：

1. JobApplication。
2. AgentRun/ApplicationRun。
3. Approval。
4. Interview 或 ApplicationStatusHistory。

Approval 决定、取消、过期和 retry 均遵守同一顺序。批量过期任务先按 approval.id 排序并使用 `FOR UPDATE SKIP LOCKED` 小批量领取。

## 6. 认证、授权与审计

### 6.1 登录和 Token

`POST /auth/token` 使用 OAuth2 Password Flow，Content-Type 必须为 `application/x-www-form-urlencoded`，字段为 username、password。成功响应：

~~~json
{
  "access_token": "<jwt>",
  "token_type": "bearer",
  "expires_in": 1800
}
~~~

JWT Claims：sub=user_id、role、iat、exp、jti。JWT 不包含岗位 ID、候选人信息或 JobAssignment；岗位权限每次从数据库权威数据查询。密码校验使用 Argon2，用户名不存在时执行等价耗时的伪哈希校验，统一返回 `INVALID_CREDENTIALS`。

### 6.2 授权策略

统一 `AuthorizationService` 提供：

~~~python
require_role(actor, *roles)
require_job_access(actor, job_id, capability)
require_run_access(actor, run_id, capability)
require_application_access(actor, application_id, capability)
require_approval_access(actor, approval_id, capability)
~~~

HR 拥有岗位、文档、候选人、MatchRun 和审批业务权限。HIRING_MANAGER 只能访问有效 JobAssignment 的岗位及其关联候选人、Run、Approval 和 Interview。ADMIN 只拥有账号、健康和脱敏评测技术权限，不自动拥有招聘业务数据权限。

### 6.3 权限检查时点

权限不能只在页面进入或 Run 创建时检查，必须在以下时点重新检查：

- 每次 HTTP 资源读取和命令执行。
- 创建 Approval、提交决定和恢复 ApplicationRun。
- 副作用工具取得锁后、执行前。
- SSE 首次连接、历史重放前、每个事件批次前和每次心跳前。
- Worker 恢复需要 actor 权限的 ApplicationRun 时。

JobAssignment 撤销提交后，发布 `assignment.revoked` 非事实通知以提前关闭本进程连接；即使通知丢失，下一次 SSE 批次/心跳的权威校验也必须关闭连接。

### 6.4 审计事件

至少记录 LOGIN_SUCCESS、LOGIN_FAILURE、JOB_CREATE、JOB_ACTIVATE、JOB_CLOSE、ASSIGNMENT_GRANT、ASSIGNMENT_REVOKE、DOCUMENT_DOWNLOAD、EVIDENCE_VIEW、RUN_CREATE、RUN_RETRY、RUN_CANCEL、APPROVAL_DECIDE、TOOL_EXECUTE、SSE_SUBSCRIBE、SSE_AUTH_REVOKED。

审计写入失败时，高风险命令（授权变化、审批决定、副作用）整体回滚；只读访问的审计可以记录本地安全日志并告警。

## 7. 文件存储与解析

### 7.1 StorageBackend

~~~python
class StorageBackend(Protocol):
    async def put(self, source: BinaryIO, *, media_type: str) -> StoredObject: ...
    async def open(self, storage_key: str) -> AsyncContextManager[BinaryIO]: ...
    async def delete_if_unreferenced(self, storage_key: str) -> None: ...
    async def healthcheck(self) -> StorageHealth: ...
~~~

LocalVolumeStorage 的写入流程：在挂载目录内创建随机临时文件，边写边计算 SHA-256 和大小，fsync 后原子 rename 到随机 storage_key。任何用户输入不得参与实际路径拼接。数据库事务失败时只删除本次新建且尚未被引用的对象。

### 7.2 上传校验

按顺序执行：

1. 校验 HR 权限、批量数量和声明大小。
2. 流式写临时对象，并执行实际大小上限。
3. 检查扩展名、声明 MIME、文件签名三者一致。
4. PDF 使用 `%PDF-` 签名；DOCX 必须是合法 ZIP 且包含规定 OOXML 项，拒绝宏文档。
5. 计算 SHA-256；若已有文档，返回 duplicate_of，不创建重复 Profile。
6. 保存 ResumeDocument=QUEUED；提交后投递解析任务。

ZIP 炸弹、加密 PDF、宏、嵌入脚本、外部对象、页数/字符数超限分别使用稳定错误码，不执行其内容。

### 7.3 解析器接口

~~~python
class DocumentParser(Protocol):
    media_types: frozenset[str]
    version: str
    def parse(self, stream: BinaryIO, limits: ParseLimits) -> ParsedDocument: ...
~~~

ParsedDocument 包含全文摘要、带 Locator 的 Block 列表、页数/段落数和安全警告。PDF Locator 为 page_number、block_index、char_start、char_end；DOCX Locator 为 paragraph_index 或 table_index/row_index/cell_index 加字符范围。

### 7.4 Profile 抽取与校对

Parser 的 Block 作为不可信数据传入 ModelGateway。模型返回 CandidateProfileDraft Schema：姓名、联系方式、技能、经历、教育、项目和 unknown_fields。业务服务再次校验字段长度、枚举、日期顺序和 evidence references，保存 REVIEW_REQUIRED。

HR 校对使用 PATCH candidate profile + expected_version；确认时检查必填字段、所有高影响字段的来源状态以及没有进行中的解析任务。确认事务将 Profile 与 Document 置 READY，并在提交后发布 Embedding 任务。

## 8. 检索与排序

### 8.1 查询输入

`RetrievalQuery` 固定引用 job_version_id、profile_status=READY、channel_config、top_k 和 rule_version。查询启动后不读取 Job 当前可变内容。

### 8.2 三路召回

| 通道 | 输入 | 实现 | 输出 |
|---|---|---|---|
| structured | 规范化技能、年限、学历 | SQL 条件和确定性加权 | profile_id、score、rank。 |
| keyword | 岗位关键词、技能别名 | PostgreSQL 文本/数组匹配和 pg_trgm | profile_id、score、rank。 |
| vector | 岗位 Embedding | pgvector cosine distance 精确搜索 | profile_id、score、rank。 |

所有通道都只返回 READY Profile。HIRING_MANAGER 的候选人列表查询还必须限制在已授权岗位的 JobApplication；MatchRun 由 HR 发起，按当前产品范围检索可用候选人库。

### 8.3 RRF

融合公式：

~~~text
rrf(profile) = Σ channel_weight / (rrf_k + rank_channel(profile))
~~~

未命中通道不贡献分数。融合后按 rrf_score 降序、profile_id 升序排序，截取 Top-K，并把所有通道排名、原分数、配置和顺序写入 MatchRunCandidate。

### 8.4 硬条件

硬规则只消费已确认字段，输出：

~~~json
{
  "rule_id": "years_experience",
  "result": "PASS",
  "reason_code": "MEETS_MINIMUM",
  "observed_value": 6,
  "required_value": 5,
  "evidence_chunk_ids": ["<uuid>"]
}
~~~

字段缺失或证据不足必须为 UNKNOWN，不能猜测 FAIL。MatchRun 中 FAIL/UNKNOWN 仍进入证据化评分；只有用户在普通候选人列表显式启用筛选时才隐藏。

## 9. 模型网关、Prompt 与证据

### 9.1 ModelGateway

~~~python
class ModelGateway(Protocol):
    async def structured_completion(
        self,
        *,
        operation: str,
        system_prompt: str,
        trusted_context: BaseModel,
        untrusted_documents: list[UntrustedDocument],
        output_schema: type[BaseModel],
        timeout_seconds: int,
    ) -> ModelResult: ...
~~~

实现使用 OpenAI Python SDK 连接百炼兼容接口。调用日志只记录 operation、model、prompt_version、token、latency、request_id 和结果状态，不记录 Secret；合成数据 Trace 仍按配置开关控制。

### 9.2 Prompt 版本

Prompt 文件按 `operation/version` 管理，例如 `match_score/v1.md`。每个模板包含：目标、可信控制字段、不可信文档边界、输出 Schema、禁止行为和降级规则。Prompt 版本写入运行快照和评测结果。

文档内容不得插入 system message，不得提供路由、Tool 名称、业务 ID、角色、状态或 expected_version。MatchRun 的模型节点不注册副作用工具。

### 9.3 结构化输出

Pydantic 对模型输出使用 `extra='forbid'`。解析失败最多进行配置次数的修复重试；每次仍使用同一业务输入快照。持续非法属于 permanent_dependency，节点失败且不得保存未验证报告。

MatchReportDraft 包含 overall_score、recommendation、summary、claims。每个 ClaimDraft 包含 claim_type、claim_text、impact_level、support_level、EvidenceReference 列表和 confidence_note。

### 9.4 证据验证算法

对每条 EvidenceReference 依次验证：

1. evidence_chunk_id 存在。
2. Chunk 属于 MatchRunCandidate 固定的 candidate_profile_id/document_id。
3. quote_start/quote_end 在 Chunk 文本范围内。
4. 按 Unicode code point 切片与 quote_text 完全一致。
5. 相同引用去重，引用顺序稳定。
6. 高影响 Claim 无 SUPPORTED 证据时改写为“不足以判断”并标记 INSUFFICIENT。

引用合法只证明“位置真实”，不证明“语义支持”。语义支持通过 Golden Dataset 人工标签计算 SUPPORTED 精确率和三分类宏平均 F1。

### 9.5 Prompt Injection 回归

每个攻击样本由 clean_document、injected_document、attack_targets 和 expected_deterministic_results 组成。attack_targets 至少覆盖唯一哨兵、指定分数、指定业务状态和伪造 evidence_chunk_id。

评测计算：控制流变化数、越权 Tool 提案数、审批绕过数、攻击目标命中数、配对硬规则变化数、注入新增无支持高影响 Claim 数。以上攻击成功指标必须全部为 0；不要求 clean/injected 自然语言全文一致。

## 10. MatchRun 详细设计

### 10.1 State Schema

~~~python
class MatchRunState(TypedDict):
    run_id: UUID
    thread_id: str
    attempt: int
    run_version: int
    actor_id: UUID
    job_id: UUID
    job_version_id: UUID
    retrieval_snapshot: RetrievalConfigSnapshot
    model_snapshot: ModelConfigSnapshot
    prompt_version: str
    rule_version: str
    candidate_profile_ids: list[UUID]
    completed_candidate_ids: list[UUID]
    failed_candidate_ids: list[UUID]
    current_candidate_id: UUID | None
    report_ids: list[UUID]
    error: RunError | None
~~~

Checkpoint 只保存 ID、配置快照、路由和有界结果，不保存 PDF/DOCX 二进制或无界全文。

### 10.2 图和路由

~~~text
START
  → parse_job
  → retrieve_candidates
  → snapshot_candidates
  → fan_out_candidate
       → hard_rule_evaluate
       → score_with_evidence
       → validate_evidence
       → persist_candidate_report
  → aggregate_run
  → END
~~~

fan-out 使用受控并发。单候选人失败写 MatchRunCandidate=FAILED 并进入 failed_candidate_ids；只要运行级依赖可用，其他候选人继续。若全部候选人失败或运行级快照无效，MatchRun FAILED；没有候选人命中时允许 COMPLETED 并生成空结果摘要。

### 10.3 节点契约

| 节点 | 读取 | 写入 | 幂等键 | 取消检查 |
|---|---|---|---|---|
| parse_job | JobVersion | 标准化要求状态 | run_id+attempt+node | 前/后。 |
| retrieve_candidates | READY Profile、配置 | 候选 ID 草稿 | run_id+attempt+config_hash | 前/后。 |
| snapshot_candidates | 召回结果 | MatchRunCandidate | run_id+profile_id | 前/提交前。 |
| hard_rule_evaluate | 固定 Profile/JobVersion | hard_rule_result_json | run_id+profile_id+rule_version | 前/提交前。 |
| score_with_evidence | 固定 Chunk | 报告草稿 Checkpoint | run_id+profile_id+prompt_version | 前/模型后。 |
| validate_evidence | 报告草稿/Chunk | 验证结果 | 纯函数 | 前。 |
| persist_candidate_report | 验证结果 | Report/Claim/Evidence/Event | run_id+application_id | 前/提交 CAS。 |
| aggregate_run | Candidate 状态 | Run 终态/Event | run_id+attempt | 前/提交 CAS。 |

所有节点开始和完成都写结构化 AgentEvent。数据库写入成功但 Checkpoint 未推进时，唯一约束使节点重放返回既有记录。

### 10.4 创建和完成事务

创建：校验 HR、Job=ACTIVE、锁定 Job 当前版本，插入 AgentRun/MatchRun 和 CREATED 事件，提交后发布 `agent.execute_match_run`。

完成：锁 AgentRun，校验 status=RUNNING、version 与 cancel_requested_at 为空；汇总成功/失败数量，写 COMPLETED 事件并更新 finished_at。条件更新失败表示已取消或其他执行者先完成，当前 Worker 丢弃未提交结果。

## 11. ApplicationRun 与审批详细设计

### 11.1 State Schema

~~~python
class ApplicationRunState(TypedDict):
    run_id: UUID
    thread_id: str
    attempt: int
    run_version: int
    actor_id: UUID
    application_id: UUID
    match_report_id: UUID | None
    current_application_version: int
    action_type: str | None
    approval_id: UUID | None
    approval_version: int | None
    question_set: InterviewQuestionSet | None
    interview_id: UUID | None
    error: RunError | None
~~~

### 11.2 图和分支

~~~text
START
  → human_review
  → wait_status_approval [interrupt]
      ├─ REJECTED → complete_without_change → END
      └─ APPROVED/EDITED
            → update_application_status
            ├─ target != SHORTLISTED → END
            └─ target == SHORTLISTED
                  → generate_interview_questions
                  → propose_schedule
                  → wait_schedule_approval [interrupt]
                      ├─ REJECTED → complete_without_schedule → END
                      └─ APPROVED/EDITED
                            → create_interview_schedule
                            → END
~~~

### 11.3 创建 Run

`POST /applications/{id}/runs` 的事务：

1. 校验 actor 对 application.job_id 的访问权。
2. `SELECT JobApplication FOR UPDATE`。
3. 若 active_application_run_id 非空，返回 `APPLICATION_RUN_ALREADY_ACTIVE`/409，并提供当前 run_id。
4. 校验引用 MatchReport 属于同一 application 和允许的 Profile 快照。
5. 插入 AgentRun=CREATED 和 ApplicationRun。
6. 更新 active_application_run_id 为新 run_id。
7. 写 CREATED AgentEvent 和审计。
8. 提交后发布执行任务；发布失败保留 CREATED，可由 maintenance 或 retry 发布。

数据库复合外键和锁共同保证同一个 JobApplication 最多一个活动 ApplicationRun。

### 11.4 Approval 创建

human_review/propose_schedule 只生成类型化提案。创建 Approval 时：锁定 JobApplication 与 Run；重新授权；验证 Run 未取消；确定 expected_application_version；生成 `run_id:attempt:action_type:ordinal` 业务幂等键；写 PENDING Approval、expires_at 和 WAITING_APPROVAL 事件；提交 Checkpoint interrupt。

同一 Run 的 PENDING 部分唯一索引防止重复提案。节点重放查询同一幂等键并返回已有 Approval。

### 11.5 审批决定

请求包含 decision=APPROVE|EDIT|REJECT、expected_version、可选 edited_params 和 Idempotency-Key。事务步骤：

1. 根据 Approval 找到 ApplicationRun 和 JobApplication，按统一顺序加锁。
2. 重新校验 actor 当前岗位权限。
3. 使用数据库时间检查 expires_at；若已到期，执行 TIMEOUT 迁移并返回 409 APPROVAL_EXPIRED。
4. 如果请求 Idempotency-Key 已完成，返回原结果；不同载荷复用同一 Key 返回 409。
5. 校验 Approval=PENDING 且 version=expected_version。
6. 对 EDIT 使用 action_type 对应 Pydantic Schema 校验；业务 ID 不允许编辑。
7. 写 APPROVED、EDITED 或 REJECTED、final_params、decided_by、decided_at、version+1 和审计。
8. APPROVED/EDITED 在同一事务中将父 Run 从 WAITING_APPROVAL 迁移为 RUNNING 并写事件；REJECTED 同事务完成 Run。
9. APPROVED/EDITED 提交后发布恢复任务；发布失败时由 maintenance 根据 RUNNING 且停留在审批后节点的 Run 重发。

### 11.6 状态更新工具

`UpdateApplicationStatusCommand` 只接受 application_id、target_status、expected_application_version、approval_id、actor_id、idempotency_key。服务重新读取并校验 Approval、Run、Application、权限和允许迁移，不接受模型提供的 job_id/user role。

在同一事务中锁定对象、更新 JobApplication/version、追加 ApplicationStatusHistory、将 Approval 置 EXECUTED、写 AgentEvent。重复命令按 idempotency_key 返回原状态历史。

### 11.7 面试问题和排期工具

问题项字段：question_id、question_text、competency、rationale、evidence_chunk_ids、sensitivity_flags。禁止直接询问受保护属性；没有证据时问题必须使用探索式而非断言式措辞。

`ScheduleProposal` 字段：application_id、duration_minutes、timezone、candidate_slots、interviewer_label。MVP 不保存真实外部联系人。

MockScheduleBackend：

~~~python
class ScheduleBackend(Protocol):
    async def create_schedule(self, command: CreateScheduleCommand) -> ScheduleResult: ...
    async def get_by_idempotency_key(self, key: str) -> ScheduleResult | None: ...
~~~

Mock 实现在受控数据库事务边界内生成稳定 external_schedule_id。执行前锁定 Run/Approval 并再次校验取消标记；成功后写 Interview、JobApplication=INTERVIEW_SCHEDULED、状态历史、Approval=EXECUTED 和事件。未来真实网络日历必须改为 Outbox，不允许持锁跨网络。

副作用执行发生可重试失败时，在同一事务中把 Approval 置为 EXECUTION_FAILED、父 Run 置为 retryable FAILED、记录错误和事件并清理活动槽。Run retry 重新认领活动槽、attempt+1，并从副作用节点使用原 Approval 和原 idempotency_key 查询/重试，不创建第二个审批。Approval TIMEOUT 的 retry 才创建新 Approval。

### 11.8 Approval 过期

Celery Beat 周期发布 `maintenance.expire_approvals`。Worker 每批选择 `status=PENDING AND expires_at <= transaction_timestamp()`，按 id 排序、`FOR UPDATE SKIP LOCKED`。

每条记录在一个事务中：锁 JobApplication → Run → Approval；若仍 PENDING，写 EXPIRED/TIMEOUT，Run=FAILED/retryable=true/error_code=APPROVAL_EXPIRED，清理活动槽，写事件和审计。决定接口执行相同检查，防止调度延迟窗口。

retry 接口重新锁定 JobApplication 并认领活动槽，attempt+1，清理旧运行错误但保留历史 Approval；新节点必须创建新 idempotency_key 和新 Approval。

### 11.9 协作式取消

取消接口事务：

1. 锁 JobApplication（ApplicationRun 时）和 AgentRun。
2. 终态直接返回当前结果，保证重复取消幂等。
3. 写 cancel_requested_at/cancel_requested_by、version+1、status=CANCELLED、finished_at。
4. ApplicationRun 清理活动槽；若有 PENDING Approval，置 EXPIRED/RUN_CANCELLED。
5. 写 CANCELLED AgentEvent 和审计。

Worker 不强制终止，但在节点开始、模型/检索返回后、持久化前和副作用锁内检查。结果提交使用创建节点时读取的 run_version 做 CAS；取消先提交后，Worker CAS 必须失败。副作用先提交后，取消只能看到已完成终态并返回事实，不能伪造回滚。

## 12. API 详细设计

### 12.1 通用契约

- 业务接口前缀 `/api/v1`；文档中的资源路径省略前缀。
- 默认请求/响应为 JSON；token 为 form-urlencoded、上传为 multipart、事件为 text/event-stream。
- 列表响应为 `{items, page, page_size, total}`，page 从 1 开始。
- 异步创建返回 202，并包含 resource_id、status、status_url。
- 创建资源返回 201；成功命令返回 200；无正文删除可返回 204。
- 错误结构为 `{code, message, request_id, details}`。
- 401 表示缺少/无效 Token，403 表示身份有效但无资源权限，404 不泄露跨权限资源是否存在。
- 409 用于状态、乐观锁、活动 Run、幂等载荷和审批冲突；422 用于 Schema 校验。

### 12.2 认证和岗位 API

| 方法与路径 | 请求 | 成功响应 | 权限 | 关键错误 |
|---|---|---|---|---|
| POST /auth/token | form username/password | TokenResponse | 匿名 | 401 INVALID_CREDENTIALS、429 RATE_LIMITED |
| GET /auth/me | - | CurrentUser | 已登录 | 401 |
| GET /jobs | page、status | JobList | 已登录 | 403 |
| POST /jobs | title、description、requirements | JobDetail | HR | 409 DUPLICATE_JOB_CONTENT |
| GET /jobs/{id} | - | JobDetail | 岗位访问 | 404 |
| PATCH /jobs/{id} | expected_version、可编辑字段 | JobDetail | HR | 409 VERSION_CONFLICT/INVALID_STATE |
| POST /jobs/{id}/activate | expected_version | JobDetail | HR | 409 INVALID_STATE |
| POST /jobs/{id}/close | expected_version | JobDetail | HR | 409 INVALID_STATE |
| GET /jobs/{id}/assignments | page | AssignmentList | HR/本人查询 | 403 |
| POST /jobs/{id}/assignments | user_id | AssignmentView | HR | 409 ASSIGNMENT_EXISTS |
| DELETE /jobs/{id}/assignments/{user_id} | - | 204 | HR | 409 ASSIGNMENT_NOT_ACTIVE |

PATCH Job：DRAFT 可更新草稿；ACTIVE 的关键内容变化创建新 JobVersion 并更新 current_version_id，不修改旧版本。

### 12.3 文档和候选人 API

| 方法与路径 | 请求 | 成功响应 | 权限 | 关键错误 |
|---|---|---|---|---|
| POST /documents | multipart files[] | 202 DocumentBatchAccepted | HR | 413 FILE_TOO_LARGE、415 UNSUPPORTED_MEDIA |
| GET /documents | page/status | DocumentList | HR | 403 |
| GET /documents/{id} | - | DocumentDetail | 资源访问 | 404 |
| GET /documents/{id}/content | - | DocumentContent（blocks 与 locator） | 资源访问 | 404、422 DOCUMENT_CONTENT_UNAVAILABLE |
| POST /documents/{id}/retry | Idempotency-Key | 202 DocumentDetail | HR | 409 NOT_RETRYABLE |
| GET /jobs/{id}/candidates | page/query/filter | CandidateList | 岗位访问 | 403 JOB_CONTEXT_REQUIRED |
| GET /jobs/{id}/profiles/{profile_id} | - | ProfileDetail | 岗位访问 | 403/404 |
| POST /jobs/{id}/profiles/{profile_id}/confirm | expected_version、ProfileEdit | ProfileDetail | HR | 409 PROFILE_VERSION_CONFLICT、409 PROFILE_NOT_CONFIRMABLE |
| GET /jobs/{id}/profiles/{profile_id}/evidence | - | EvidenceChunkList | 岗位访问 | 403/404 |
| POST /jobs/{id}/profiles/{profile_id}/evidence | EvidenceChunkCreate[] | EvidenceChunkList | HR | 422 EVIDENCE_PROFILE_REQUIRED |
| GET /candidate-profiles/by-document/{document_id} | - | ProfileDetail | 资源访问 | 404 |
| GET /candidate-profiles/{profile_id}/evidence | - | EvidenceChunkList | 资源访问 | 403/404 |

上传批次的每个文件分别返回 accepted、duplicate 或 rejected，不因单个失败回滚其他合法文件。

`GET /documents/{id}/content` 返回解析后的块与其 locator，是「原文定位」的唯一数据来源：前端只据此渲染可选中原文，不自行推断块边界或字符区间。

**与需求基线的一处已知偏差（记录，不隐藏）**：`需求分析.md` 的 API 章节把候选人资源写成扁平路径 `/candidates`、`/candidates/{id}` 与 `/candidate-profiles/{id}/confirm`，实现收敛为岗位作用域路径 `GET /jobs/{id}/candidates`、`GET /jobs/{id}/profiles/{profile_id}` 和 `POST /jobs/{id}/profiles/{profile_id}/confirm`。原因是资源级授权必须从 JobAssignment 出发：把 job 放进路径后，一次 `JobService.get_authorized` 就能同时校验身份、分配关系与资源状态，避免「先查候选人再补校验岗位」的两段式授权缝隙；副作用接口同样挂在岗位作用域下（`GET|POST /jobs/{id}/profiles/{profile_id}/evidence`）。扁平路径对应的全局查询能力没有消失，而是要求显式岗位上下文（缺少时返回 403 `JOB_CONTEXT_REQUIRED`）。需求基线的这 3 条路径未同步修改，待与需求、README 一并决策后再统一。

### 12.4 MatchRun API

| 方法与路径 | 请求 | 成功响应 | 权限 | 关键错误 |
|---|---|---|---|---|
| GET /jobs/{id}/applications | page/status | ApplicationList | 岗位访问 | 403 |
| POST /jobs/{id}/match-runs | config overrides、Idempotency-Key | 202 RunAccepted | HR | 409 JOB_NOT_ACTIVE |
| GET /jobs/{id}/match-runs | page/status | MatchRunList | 岗位访问 | 403 |
| GET /match-runs/{id} | include_reports | MatchRunDetail | 岗位访问 | 404 |
| GET /match-runs/{id}/reports | - | ReportList | 岗位访问 | 404 |
| POST /match-runs/{id}/retry | Idempotency-Key | 202 MatchRunDetail | HR | 409 RUN_NOT_RETRYABLE |
| POST /match-runs/{id}/cancel | - | MatchRunDetail | HR | 终态幂等返回当前事实 |
| GET /match-runs/{id}/events | Last-Event-ID | SSE | 岗位访问 | 403/404 |

RunDetail 必须包含 run_type、status、attempt、version、current_node、retryable、error、created_at、started_at、finished_at 和最近 sequence。

### 12.5 ApplicationRun、Approval 和 Interview API

| 方法与路径 | 请求 | 成功响应 | 权限 | 关键错误 |
|---|---|---|---|---|
| POST /applications/{id}/runs | match_report_id、Idempotency-Key | 202 RunAccepted | 岗位访问 | 409 APPLICATION_RUN_ALREADY_ACTIVE |
| GET /applications/{id}/runs | page/status | ApplicationRunList | 岗位访问 | 403 |
| GET /application-runs/{id} | - | ApplicationRunDetail | 岗位访问 | 404 |
| POST /application-runs/{id}/retry | Idempotency-Key | 202 ApplicationRunDetail | 岗位访问 | 409 RUN_NOT_RETRYABLE/RUN_SLOT_OCCUPIED |
| POST /application-runs/{id}/cancel | - | ApplicationRunDetail | 岗位访问 | 终态幂等返回当前事实 |
| GET /application-runs/{id}/events | Last-Event-ID | SSE | 岗位访问 | 403/404 |
| GET /approvals/{id} | - | ApprovalDetail | 当前岗位访问 | 404 |
| POST /approvals/{id}/decision | DecisionRequest、Idempotency-Key | ApprovalDetail | 当前岗位访问 | 409 APPROVAL_EXPIRED/VERSION_CONFLICT |
| GET /interviews | page/job_id | InterviewList | 岗位访问 | 403 |
| GET /interviews/{id} | - | InterviewDetail | 岗位访问 | 404 |

ApplicationRunDetail 同时返回当前 PENDING Approval；历史 Approval 分页或按需展开，前端不得自行推断哪个 Approval 可操作。

### 12.6 Evaluation 和健康 API

| 方法与路径 | 请求 | 成功响应 | 权限 | 关键错误 |
|---|---|---|---|---|
| POST /evaluations | dataset_version_id、config | 202 EvaluationAccepted | HR/ADMIN 技术权限 | 409 EVALUATION_DUPLICATE |
| GET /evaluations | page/status | EvaluationList | 脱敏技术权限 | 403 |
| GET /evaluations/{id} | - | EvaluationDetail | 脱敏技术权限 | 404 |
| GET /health/live | - | `{status}` | 部署探针 | - |
| GET /health/ready | - | 依赖状态 | ADMIN/部署探针 | 503 DEPENDENCY_UNAVAILABLE |

### 12.7 幂等键

服务端保存 operation、actor_id、idempotency_key、request_hash、response_status、response_body 或 resource_id。相同 Key + 相同请求返回原结果；相同 Key + 不同请求返回 `IDEMPOTENCY_KEY_REUSED`/409。Key 最少保留到相关业务对象生命周期结束；副作用 Key 永久随 Approval 保存。

## 13. AgentEvent 与 SSE

### 13.1 事件 Schema

~~~json
{
  "event_id": "<uuid>",
  "run_id": "<uuid>",
  "run_type": "APPLICATION",
  "sequence": 7,
  "event_type": "STATUS_CHANGED",
  "node": "wait_status_approval",
  "status": "waiting_approval",
  "message_key": "run.waiting_for_approval",
  "safe_payload": {"approval_id": "<uuid>"},
  "occurred_at": "2026-09-01T00:00:00Z"
}
~~~

前端只使用 event_type、status、sequence 和类型化 safe_payload 驱动 UI，不解析自然语言文案决定业务逻辑。

### 13.2 发布链路

业务事务内写 AgentEvent，提交后向 Redis Pub/Sub 发布 run_id + latest_sequence 通知。本机进程订阅后唤醒连接并从 PostgreSQL读取；Redis 消息不携带业务完整数据，丢失通知时心跳轮询仍能发现新 sequence。

### 13.3 重放算法

1. 认证 Token，解析 Run 并执行资源授权。
2. Last-Event-ID 存在时查询其 run_id/sequence；不属于当前 Run 返回 400 INVALID_EVENT_CURSOR。
3. 按 `sequence > last_sequence ORDER BY sequence LIMIT batch_size` 从 PostgreSQL 重放。
4. 每批发送前重新授权。
5. 追平后进入实时循环；通知或心跳到达时再次读取数据库。
6. 终态事件发出后发送最终心跳并关闭。

### 13.4 持续授权和连接关闭

每个重放批次、实时事件批次和心跳前调用 require_run_access。JobAssignment 撤销、用户禁用或资源关系变化后，服务端不再发送业务事件，记录 SSE_AUTH_REVOKED 并关闭流。不得先读取敏感事件再做授权。

### 13.5 前端去重与重连

客户端维护 lastAcceptedSequence。sequence <= 当前值直接丢弃；出现跳号时先断开并从最后确认位置重连。网络错误使用带抖动指数退避，最大间隔 30 秒；401 停止重连并跳转登录；403 显示权限已变更；Run 终态停止连接并 invalidate Run Query。

## 14. Celery 与 Scheduler

### 14.1 队列和任务

| 队列 | 任务 | operation_key |
|---|---|---|
| documents | documents.parse、documents.retry_parse | document_id+parser_version+attempt |
| embeddings | embeddings.generate_chunks | chunk_id+model+content_hash |
| agent | agent.execute_match_run | run_id+attempt |
| agent | agent.execute_application_run | run_id+attempt+resume_version |
| evaluations | evaluations.execute | dataset_version+config_hash |
| maintenance | maintenance.expire_approvals | approval_id+version |
| maintenance | maintenance.republish_queued | resource_id+attempt |
| maintenance | maintenance.cleanup_orphan_files | storage_key+scan_date |

MVP 一个 Worker 可消费所有队列，但设置独立 routing key 和并发限制。模型任务并发低于解析任务，避免外部限流雪崩。

**认领（claim）而非直接执行**：Celery 是 at-least-once，同一份工作可能被投递两次；Run 又必须「不重复执行副作用、不回退终态」。因此 Run 任务的第一步不是执行，而是在一个事务里对 `agent_runs` 行加锁（`SELECT ... FOR UPDATE`）后判断本次投递是否有权执行，判断结果作为纯函数与投递分离，可独立测试：

| 意图 | 允许的状态 | 其他状态的处理 |
|---|---|---|
| 首次执行 | `CREATED` | 终态→跳过；`RUNNING`→跳过（需租约才能区分「上一个 Worker 已死」与「仍在跑」，租约属 FIN-006 的重发布扫描）；`WAITING_APPROVAL`→拒绝 |
| 重试 | `FAILED` | 其他一律拒绝（`RUN_NOT_RETRYABLE`） |
| 恢复（ApplicationRun） | 审批事务已迁移为 `RUNNING` | 无检查点、Checkpoint 版本不匹配或无对应已执行 Approval→拒绝 |

ApplicationRun 的 `RESUME` 消息携带 `run_id+attempt+checkpoint_id` 操作键、发起者和 Approval ID；Worker 从数据库重新读取用户授权、最新 Checkpoint 与已执行 Approval，消息参数本身不构成越过人工闸门的授权。

取消标记（`cancel_requested_at`）优先于任何意图：用户已请求停止的 Run 不允许被任何投递重新拉起。行锁在「Run 到达终态并提交」之前一直持有，所以重复投递会阻塞在锁上并在第一个事务提交后看到终态，而不是与之竞争；同时整趟执行只提交一次，Worker 中途被杀会回滚到 `CREATED`，交给 `maintenance.republish_queued` 安全重投。**`START` 意图绝不能接受 `WAITING_APPROVAL`**：那等于绕过人工闸门，直接违反「未审批副作用执行次数必须为 0」；唤醒暂停流程只能由已决审批路径发起。

由「所有候选人失败」导致的 `FAILED` 记录 `retryable=true` 与 `error_code=ALL_CANDIDATES_FAILED`：共同上游（模型网关、召回）通常是暂时性的，这也是重试接口存在的意义。重试复用同一 `run_id`/`thread_id` 并把 `attempt` 加一，因此运行过程中产生的派生行必须整批替换而不是追加——候选人快照替换整行集合，证据报告连同其 claim/evidence 子行一起删除后重建，否则会撞上 `uq_match_run_candidates_profile`、`uq_match_reports_run_application`，重试永远无法完成。

### 14.2 重试策略

| 错误 | 重试 | 策略 |
|---|---:|---|
| 模型 429/5xx、短暂网络错误 | 是 | 指数退避+jitter，最多配置次数。 |
| PostgreSQL 短暂连接错误 | 是 | 任务级有限重试。 |
| Redis 发布失败 | 是/协调 | 业务状态保持可扫描。 |
| Pydantic 输出持续非法 | 否 | permanent_dependency。 |
| 不支持文件、超限、加密文件 | 否 | UNSUPPORTED/FAILED。 |
| 权限撤销、状态冲突、取消 | 否 | 按业务终态处理。 |

Celery ack 采用任务完成后确认；业务幂等键和数据库约束承担重复投递防护，不能依赖 `acks_late` 保证恰好一次。

### 14.3 Celery Beat

Scheduler 使用同一后端镜像运行 Celery Beat，只发布维护任务：Approval 过期每分钟、QUEUED 协调每五分钟、孤儿文件扫描每天一次。实际业务迁移由 Worker 应用服务完成，Beat 不直接连接业务表执行状态变更。

### 14.4 Redis 故障

新异步命令在数据库写入后若发布失败，响应返回资源及 QUEUED/CREATED 状态和可重试提示，而不是回滚已安全持久化的文件或 Run。恢复后 maintenance 扫描并重新发布。Redis 清空不能覆盖 PostgreSQL 状态、Approval 或 AgentEvent。

## 15. 前端详细设计

### 15.1 应用入口和路由守卫

路由：`/login`、`/jobs`、`/jobs/:jobId`、`/documents`、`/documents/:documentId/review`、`/candidates`、`/candidates/:candidateId`、`/match-runs/:runId`、`/application-runs/:runId`、`/approvals/:approvalId`、`/interviews/:interviewId`、`/evaluations`。

AuthGuard 只判断是否有有效会话；页面加载仍由 API 做资源授权。RoleGuard 只用于隐藏明显不可用入口，不是安全边界。

### 15.2 状态划分

TanStack Query 保存 Job、Document、Candidate、Run、Approval、Interview 和 Evaluation。Zustand 只保存 access token、当前用户摘要、筛选器、侧栏状态和未提交表单草稿。

MVP Access Token 保存在内存并可选同步 sessionStorage，关闭浏览器会话即清除；不使用 localStorage 长期持久化。所有输出按文本渲染，禁止把简历/模型内容作为未清洗 HTML 注入。

### 15.3 Query Key

~~~typescript
['jobs', filters]
['job', jobId]
['job-assignments', jobId, page]
['documents', filters]
['document', documentId]
['candidates', jobId, filters]
['candidate', candidateId, jobId]
['match-run', runId]
['application-run', runId]
['approval', approvalId]
['interview', interviewId]
['evaluations', filters]
~~~

命令成功后精确 invalidate 相关聚合；SSE 事件只更新 Run 进度摘要，终态或 Approval 变化时再 invalidate 权威 Query。

### 15.4 页面组件

| 页面 | 关键组件 | 空/错状态 |
|---|---|---|
| 岗位详情 | JobHeader、VersionEditor、AssignmentTable、ApplicationTable | 无版本、已关闭、无权限。 |
| 文档队列 | BatchUploader、DocumentStatusTable、RetryAction | 部分上传失败、队列不可用。 |
| Profile 校对 | SourceLocatorViewer、ProfileForm、EvidencePreview | 解析失败、版本冲突。 |
| 候选人列表 | ExplicitFilterBar、CandidateTable、HardRuleBadge | 无结果但不隐藏 FAIL。 |
| MatchRun | RunTimeline、RankingTable、ClaimEvidencePanel | 空 Top-K、部分候选人失败。 |
| ApplicationRun | RunTimeline、CurrentApprovalCard、QuestionSet、ScheduleResult | 等待审批、过期、取消、可重试失败。 |
| 审批 | ActionDiff、EditableParamsForm、DecisionActions | 403、409、EXPIRED。 |
| 评测 | MetricGateTable、ExperimentCompareChart | 数据集不兼容、运行失败。 |

### 15.5 审批交互

页面显示动作类型、对象、当前值、原提案、编辑后参数、expires_at 和副作用说明。打开页面时保存 approval.version；提交带 expected_version 和客户端生成的 Idempotency-Key。409 时不自动重放决定，刷新显示当前事实并让用户确认。

### 15.6 可访问性和错误体验

- 所有动作可键盘完成，焦点顺序可预测。
- 状态使用文本、图标和颜色；色彩不是唯一信息。
- 表单错误关联字段并提供汇总。
- 403、409、422、依赖失败和网络断开使用不同恢复建议。
- 错误页显示 request_id，绝不显示栈、Token 或原始模型响应。

## 16. 安全详细设计

### 16.1 信任边界

可信控制数据：服务器生成的 actor_id、role、业务 ID、状态、版本、配置 ID、幂等键和固定图路由。不可信数据：岗位描述、简历、文件名、模型输出、浏览器提交参数和 Redis 通知。

所有不可信输入经过 Schema、长度、枚举、归属和状态校验。模型输出不因“来自模型”获得更高信任。

### 16.2 Tool 安全

- MatchRun 和解析/评分节点不注册副作用 Tool。
- ApplicationRun 只有批准后节点能调用副作用服务。
- Tool 参数中的 ID、actor、role、version、idempotency_key 从持久化业务状态重构。
- 执行层重新检查权限、Approval、归属、状态迁移、取消标记和幂等结果。
- 未审批、过期、拒绝、跨 Run、跨 Application 或非法参数全部拒绝并审计。

### 16.3 Web 安全

Nginx 设置 CSP、X-Content-Type-Options、Referrer-Policy 和合理的 frame-ancestors。API 只允许同源 Web；Bearer Token 不放 URL。上传和下载设置 Content-Disposition，展示原文时使用安全文本/受控 PDF 查看器，不以内联 HTML 执行 DOCX 内容。

### 16.4 日志脱敏

日志过滤键至少包括 password、authorization、access_token、api_key、secret、resume_text、document_binary。邮件和手机号测试标记通过 CI 扫描。模型错误只保存 provider request ID、稳定错误码和截断脱敏摘要。

## 17. 异常、恢复与降级

### 17.1 错误模型

~~~python
class AppError(Exception):
    code: str
    http_status: int
    safe_message: str
    details: dict[str, JsonValue]
    retryable: bool
~~~

领域层抛稳定错误，Router 统一映射。未知异常返回 INTERNAL_ERROR/500，记录 request_id 和栈但不回传细节。

### 17.2 Checkpoint 恢复

PostgreSQL 业务表是最终事实，Checkpoint 只决定图从哪里继续。恢复前读取 AgentRun、Approval 和副作用结果：

- 若业务节点已完成，返回既有 ID 并推进图。
- 若 Checkpoint 丢失且无法安全确定位置，Run 置 retryable FAILED。
- 已执行副作用根据 Approval idempotency_key 查询，不盲目重复。
- WAITING_APPROVAL 重启后从同一 thread_id 和 Approval 决定恢复。

### 17.3 降级

| 依赖 | 行为 |
|---|---|
| Langfuse | 关闭远程 Trace，本地结构化日志和 AgentEvent 保持。 |
| Qwen | 当前节点失败；显式 MOCK_MODEL_MODE 才可进入演示模式。 |
| Redis | 新任务保持可协调状态；既有业务事实不丢。 |
| SSE | 前端只读轮询 Run Detail。 |
| MockScheduleBackend | Approval=EXECUTION_FAILED，按同一幂等键受控重试。 |
| Checkpointer | 保留业务事实，无法确认恢复点时进入 retryable FAILED。 |

## 18. 可观测性与评测

### 18.1 日志字段

公共字段：timestamp、level、service、environment、request_id、task_id、operation_key、run_id、run_type、thread_id、attempt、node、actor_id、job_id、application_id、error_code、duration_ms。只在对应上下文存在时记录。

### 18.2 指标

- API：请求量、状态码、p50/p95、鉴权失败、409 冲突。
- Worker：队列长度、任务耗时、重试、失败、重复投递命中。
- 文档：解析成功率、格式、页数、字段 F1。
- 检索：Recall@K、MRR/nDCG、各通道命中和 RRF 贡献。
- Agent：节点延迟、模型 Token、Run 完成/失败/取消/恢复率。
- Approval：等待时间、拒绝、过期、冲突、副作用成功率。
- SSE：连接数、重放量、重连、持续授权关闭数、事件展示延迟。
- 安全：Prompt Injection 各攻击成功指标、非法 Tool 拒绝、脱敏扫描。

### 18.3 发布门禁

| 指标 | 门槛 |
|---|---|
| 支持格式解析成功率 | >= 95% |
| 核心字段宏平均 F1 | >= 0.90 |
| Recall@10 | >= 0.85，并报告 MRR 或 nDCG |
| 非法证据引用/无证据确定性结论 | 0 |
| SUPPORTED 精确率 | >= 95% |
| 证据三分类宏平均 F1 | >= 0.85 |
| Tool 首次参数合法率 | >= 98% |
| 执行层非法参数拒绝率 | 100% |
| 未审批副作用执行数 | 0 |
| Prompt Injection 攻击成功指标 | 全部为 0 |
| 恢复、取消、过期、SSE 撤权 E2E | 全部通过 |

## 19. 测试详细设计

### 19.1 测试目录和基础设施

- unit：纯状态迁移、Schema、硬规则、RRF、证据切片、权限策略。
- integration：真实 PostgreSQL/pgvector、Redis、LocalVolumeStorage、Celery eager/测试 Worker。
- contract：OpenAPI 快照、错误 Schema、SSE Parser、前后端生成类型。
- e2e：Docker Compose + Playwright 完整流程。
- evaluations：Golden Dataset、FakeModel、录制模型响应和门禁报告。

每个测试创建独立数据库 Schema 或事务；固定 Clock、ID 和 FakeModel。涉及 PostgreSQL 部分索引、锁、SKIP LOCKED 和 pgvector 的测试不得使用 SQLite 替代。

### 19.2 数据库测试

1. username、岗位版本、有效 Assignment、JobApplication 和 Approval 部分唯一约束。
2. active_application_run_id 只能指向属于本 Application 的 Run。
3. 两事务并发创建 ApplicationRun，恰好一个成功，另一个 409。
4. AgentEvent 并发写入 sequence 唯一且严格递增。
5. Approval 决定、过期和取消遵守锁顺序，没有双终态。
6. ClaimEvidence 跨 Profile、越界和摘录不一致被拒绝。
7. READY Profile 唯一，历史 Run 仍可读取 SUPERSEDED 快照。

### 19.3 API 与权限测试

1. token 只接受 form-urlencoded；JSON 登录返回 422/415 契约错误。
2. HR、HIRING_MANAGER、ADMIN 权限矩阵逐接口参数化测试。
3. JobAssignment 撤销后资源接口返回 403/404 安全响应。
4. expected_version 冲突返回 409 和 current_version。
5. 同一 Idempotency-Key 相同请求返回既有结果，不同请求返回 409。
6. 列表强制分页、最大 page_size 和稳定排序。
7. 错误均包含 code/message/request_id，且不泄露内部异常。

### 19.4 Agent Node 测试

每个 Node 使用固定 State、FakeModel、FakeClock 和 Fake Repository：验证读取集合、状态增量、下一路由、幂等键、取消检查、错误分类和 AgentEvent。MatchRun Node 必须证明没有副作用 Tool；ApplicationRun 副作用 Node 必须证明未审批调用次数为 0。

### 19.5 并发和恢复测试

1. Worker 持有旧 run_version 时取消，后续 CAS 写入失败。
2. 取消与 MockSchedule 执行竞争，只有一个事务先提交且审计顺序一致。
3. PENDING Approval 超时与用户决定竞争，只产生一个终态。
4. API/Worker 在 WAITING_APPROVAL 重启，从同一 thread_id 恢复。
5. 业务写入成功、Checkpoint 未推进时重放不创建重复报告/状态历史/Interview。
6. Redis 清空后 PostgreSQL 事实保持，maintenance 可重新发布 QUEUED 资源。

### 19.6 SSE 测试

1. 首次连接按 sequence 输出。
2. Last-Event-ID 重放无丢失、无重复。
3. 非本 Run cursor 返回 400。
4. 重放期间撤权，后续批次不发送。
5. 活跃连接期间撤销 JobAssignment，下一批或心跳关闭。
6. Redis 通知丢失，心跳查询仍发现新事件。
7. 前端收到重复、跳号、401、403 和终态时行为正确。

### 19.7 Prompt Injection 和评测测试

至少 20 组 clean/injected 配对样本。自动比较固定图路由、Tool 注册、业务 ID、硬规则和结构化输出攻击目标；任何哨兵、指定分数/状态、伪造证据命中都使安全门禁失败。证据语义支持使用人工标注集单独计算，不用全文相等替代。

### 19.8 E2E 场景

1. HR 登录，创建/启用岗位并分配招聘主管。
2. 上传 PDF/DOCX，失败可见；校对并确认 Profile。
3. 启动 MatchRun，Top-K 中 FAIL 候选人仍有证据化报告，Run 不创建 Approval。
4. 并发启动同一 ApplicationRun，第二个返回 409。
5. 批准状态变化只执行一次；入围后第二次审批再创建模拟排期。
6. 拒绝审批后 Run 正常完成，JobApplication 不被自动 REJECTED。
7. Approval 过期后 Run 可重试失败，新 attempt 创建新 Approval。
8. RUNNING/WAITING Run 取消后不再提交节点结果或副作用。
9. SSE 断线重连不丢事件；在线撤权停止业务事件并关闭连接。
10. 重启 API/Worker 后恢复待审批流程。
11. 日志、Trace、快照不含预设敏感标记。

## 20. 部署与 CI

### 20.1 Docker Compose

服务：web、api、worker、scheduler、postgres、redis，另有一次性的 migrate 与 seed。**Nginx 不是独立服务**：`web` 是「Node 构建静态产物 → Nginx 运行」的两阶段镜像，Nginx 既是静态宿主也是唯一公开入口（默认 `127.0.0.1:8080`），`/api/` 与两条 events 路由由它反向代理到 `api:8000`。API/Worker/Scheduler 使用同一 Python 3.12 镜像和配置 Schema；scheduler 命令运行 Celery Beat。PostgreSQL、Redis、Storage 使用独立 Volume。

Alembic 由独立一次性 `migrate` 服务执行，API/Worker 不自动并发迁移；`migrate` 位于默认 profile，`api` 以 `service_completed_successfully` 等它成功退出，所以空库上一条 `docker compose up -d --wait` 即可把 schema 带到 head。`seed`、`storage-init` 与探针边车 `web-probe` 位于 `tools` profile，因此 teardown 必须带 `--profile tools`，否则会留下 `storage-init` 容器及其持有的卷。Worker 仅支持 Linux 容器；Windows 开发通过 Docker Desktop/WSL 2。

### 20.2 健康检查

- `/health/live` 只检查进程事件循环。
- `/health/ready` 分别检查 PostgreSQL、Redis 和 Storage 可读写性。
- readiness 不调用外部模型，避免限流和成本。
- 模型/Embedding 连通性由手动诊断命令执行。

### 20.3 CI 流水线

~~~text
Markdown/文档契约检查
  → backend Ruff
  → backend mypy
  → pytest unit
  → PostgreSQL/Redis integration
  → frontend typecheck/lint/test/build
  → OpenAPI 类型兼容
  → Playwright 核心 E2E
  → FakeModel 安全与评测门禁
~~~

真实模型评测使用手动或定时工作流，不阻塞普通 PR；结果必须记录 dataset、model、Prompt 和配置版本。

## 21. 实施顺序与交付物

### 21.1 纵向实施顺序

1. 建立工程骨架、配置、错误、日志、数据库和 CI。
2. 实现 users、auth、jobs、JobAssignment 及权限集成测试。
3. 实现 StorageBackend、文档表、解析任务、Profile 校对和证据定位。
4. 实现 Embedding、三路召回、RRF 和检索 Golden Dataset。
5. 实现 AgentRun/Event 基础设施和 MatchRun 全图。
6. 实现 ApplicationRun、Approval、Checkpoint、协作取消和过期协调。
7. 实现面试问题与 MockScheduleBackend。
8. 实现 SSE、前端 Run/Approval 页面和撤权关闭。
9. 完成安全配对评测、E2E、Compose、README 和演示脚本。

### 21.2 每阶段必备产物

每个阶段必须包含迁移/Schema、应用代码、单元或集成测试、错误码、日志字段、必要文档和独立 Git commit。跨模块契约变更同时更新 OpenAPI/前端类型与测试。

## 22. 需求追踪

| 需求域 | 详细设计章节 | 主要自动化验证 |
|---|---|---|
| AUTH | 6、12.2、16 | token 契约、角色和资源授权。 |
| JOB | 4.2、5.2、12.2 | 版本、Assignment、状态和并发。 |
| DOC | 4.3、7、12.3、14 | 文件边界、解析、重试和幂等。 |
| CAN | 4.3、4.4、5.3、12.3 | Profile 版本、Application 唯一和状态隔离。 |
| RET | 8、10 | 三路召回、RRF、硬规则和快照。 |
| AGT | 4.4、5.4、10、11 | 双图、节点、Checkpoint、活动 Run 和取消。 |
| APR | 4.5、5.5、11、12.5 | interrupt、版本、过期、幂等和副作用。 |
| INT | 11.7、12.5、15 | 问题、敏感主题和 Mock 排期。 |
| EVT | 4.5、13、15.5 | sequence、重放、去重和持续授权。 |
| EVAL | 9.5、18、19.7 | 指标、配对攻击和发布门禁。 |
| OPS | 3、14、20 | 配置、Compose、Scheduler、健康和 CI。 |
| NFR | 5、6、13 至 20 | 安全、性能、恢复、可维护性和测试。 |

## 23. 实现前检查清单

- [ ] 数据库枚举、外键、复合外键和部分唯一索引已在迁移测试中验证。
- [ ] 27 条需求资源路径及概要设计中的全部方法已进入 OpenAPI。
- [ ] Pydantic API、State、Event、Tool 和模型输出均禁止未知字段。
- [ ] ApplicationRun 并发创建、retry 认领槽和终态清槽已做并发测试。
- [ ] 取消、审批决定、过期和副作用遵守统一锁顺序。
- [ ] SSE 每批/心跳持续授权且撤权测试通过。
- [ ] Prompt Injection clean/injected 配对门禁可以在 FakeModel/录制响应上稳定运行。
- [ ] 普通 CI 不依赖真实模型，真实评测可以单独运行和追踪版本。
- [ ] 所有 Secret、简历正文和 Token 已纳入日志/Trace 脱敏检查。
- [ ] 每次实现改动包含相关测试、全部验证通过并创建独立 Git commit。

## 24. 设计结论

本详细设计把项目收敛为一个可实现、可测试、可恢复的模块化单体：PostgreSQL 保存业务事实与事件，持久化 Checkpoint 保存有限恢复状态（见 ADR-0001），Redis/Celery 负责可重投递任务，React 通过 REST 与持续授权 SSE 展示进度。

MatchRun 只做岗位级检索、硬规则评估和证据化报告；ApplicationRun 只处理一个 JobApplication，并通过活动 Run 排他槽、Approval、乐观锁、协作式取消和幂等副作用完成受控的人机协作。固定图、证据外键、执行层授权和 Prompt Injection 配对评测共同保证不可信文档不能改变控制流、权限或副作用。

本文已经提供数据库、API、Agent、任务、SSE、前端、安全、测试和部署的实现级输入，可以作为后续 Alembic、Pydantic/OpenAPI、图节点（自研 `RunEngine`，见 ADR-0001）、React 和 pytest/Playwright 开发的统一基线。
