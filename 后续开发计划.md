# Resume Copilot 后续开发计划

- 制定日期：2026-09-18
- 目标：AI Agent 岗位求职作品集与演示质量
- 调研代码基线：`9cae04d`
- 团队假设：1 名开发者
- 预计投入：13–18 个有效开发日，另留 2 天发布缓冲
- 当前状态：规划完成，以下工作包均未开始

本轮以证据可信度、真实模型效果和演示可读性为主线。已有的审批、幂等、异步任务和恢复机制作为工程基础继续保留，新增能力沿现有边界接入。

本文是调研基线之后的执行计划。[编码实现计划](./编码实现计划.md) 保留 IMP / FIN 阶段的历史记录；后续作品集增强工作使用本文的 PORT 编号跟踪。本文中的验收标准均为待完成目标，不代表当前已经达标。

## 1. 交付目标与范围

本轮完成后，应能围绕一条完整招聘流程回答以下问题，并提供代码、测试、评测或演示证据：

1. 模型在什么环节发挥作用，确定性规则负责什么？
2. 每条匹配结论由哪些简历原文支持，证据缺失或矛盾时如何处理？
3. HR 如何修改或否决建议，系统如何保证审批前没有业务副作用？
4. 服务中断、消息重复或 SSE 断线后，系统如何恢复？
5. 对独立标注样本，抽取、检索与解释的效果如何，失败在哪里？

本轮交付物包括：可复现的真实模型运行链路、逐项证据绑定、版本化效果评测、关键页面优化、演示视频、现场脚本和发布记录。

保留电子 PDF / DOCX、合成数据、单机 Compose、MockSchedule 和现有自研运行时边界。运行时选择沿用 [ADR-0001](./docs/adr/0001-agent-runtime-custom-engine-over-langgraph.md)。OCR、多租户、真实日历、复杂多分支 Agent 和大规模索引优化列入后续候选，不占用本轮关键路径。

## 2. 调研基线与验证范围

| 项目 | 2026-09-18 调研结果 | 解释与限制 |
|---|---|---|
| 核心流程 | 已有上传、解析、校对、检索、MatchRun、ApplicationRun 双审批与面试流程代码 | 本轮调研未重新执行完整浏览器流程 |
| 服务就绪 | 公开入口的 readiness 返回 ready，PostgreSQL、Redis、Storage 均为 up | 就绪检查不等于业务流程验收 |
| 后端测试 | 隔离本地 `.env` 后，568 passed、17 skipped | 跳过项需要 PostgreSQL / Redis 等集成环境 |
| 测试环境隔离 | 从带本地 `.env` 的仓库工作目录运行时，3 项配置测试失败；从隔离工作目录运行全部通过 | 需要让测试显式隔离本地配置，而非依赖运行目录 |
| 离线评测 | 现有门禁 PASS | 内置语义与注入评测主要验证预置结果的计分机制 |
| 文档与结构 | 现有文档、仓库结构检查通过 | 当前检查不能识别所有文档与实现的语义偏差 |
| 尚未复核 | 前端单测、构建、浏览器 E2E、完整 `project.ps1 verify`、真实模型效果 | 本表不得被引用为这些项目本次已通过的证明 |

以上数字是调研时的历史快照。每个工作包完成后记录自己的 commit、命令和结果；发布前以同一代码版本重新生成最终基线。

## 3. 已确认的问题与影响

| 问题 | 实现依据 | 对作品集的影响 | 对应工作包 |
|---|---|---|---|
| 真实模型运行依赖与配置缺失 | [pyproject.toml](./pyproject.toml) 将 `httpx2` 放在 dev 依赖；[运行镜像](./deploy/docker/backend.Dockerfile) 安装生产锁文件；[Compose](./compose.yaml) 未传入模型端点与凭据。运行容器中也确认依赖与配置未就绪 | 不能仅凭切换 Mock 开关完成真实模型演示 | PORT-001 |
| 报告分数来自确定性规则 | [报告服务](./backend/app/reports/service.py) 根据快照排名与硬规则计算综合分；[面试题](./backend/app/interviews/questions.py) 使用固定模板 | 需要准确展示规则能力与模型能力的边界 | PORT-002、003 |
| 结论与证据未逐项对应 | [报告服务](./backend/app/reports/service.py) 为有引用的 SUPPORTED 结论统一使用第一条证据 | 引用归属正确不足以证明学历、技能、年限等结论被支持 | PORT-002 |
| 效果评测尚未贯穿实际输出 | [语义评测](./backend/app/evaluations/semantic.py) 使用相同的内置预测与标准答案；[注入评测](./backend/app/evaluations/injection.py) 使用预置 clean / injected 分析；[执行器](./backend/app/evaluations/executor.py) 调用这些内置评测 | 现有高分不能作为真实模型质量或抗注入效果的证明 | PORT-004 |
| 排名信息不便讲解 | [RankingTable](./apps/web/src/components/RankingTable.tsx) 显示 Profile 短 ID；[RunTimeline](./apps/web/src/components/RunTimeline.tsx) 仍展示部分内部节点与消息键 | 演示者需要额外解释候选人身份与技术字段 | PORT-005 |
| 文档存在历史口径漂移 | README 中的部分前端库不在当前依赖中；编码计划仍保留旧 PARTIAL 描述；发布标签与包版本口径需说明 | 面试时容易出现文档宣称与代码事实不一致 | PORT-001、006 |

## 4. 工作包与执行顺序

| 编号 | 优先级 | 工作包 | 估算 | 依赖 | 状态 |
|---|---|---|---|---|---|
| PORT-001 | P0 | 运行配置、依赖、测试隔离与文档校准 | 1–2 天 | 无 | DONE |
| PORT-002 | P0 | 逐项证据绑定与评分语义 | 2–3 天 | PORT-001 | DONE |
| PORT-003 | P1 | 真实模型匹配解释闭环 | 3–4 天 | PORT-002 | DONE |
| PORT-004 | P1 | 实际输出评测与录制回放 | 3–4 天 | PORT-003 | DONE |
| PORT-005 | P1 | 关键页面与演示体验 | 2–3 天 | PORT-002、003、004 | DONE |
| PORT-006 | P1 | 作品集材料与发布验收 | 2 天 | PORT-001 至 PORT-005 | DONE |

推荐按编号推进，任一时刻保留一个主工作包为 IN_PROGRESS。PORT-004 的样本标注可在 PORT-003 调试前准备，避免根据模型输出反向修改标准答案；正式效果验收需等待实际调用链完成。

按每周 5 个有效开发日计算，开发约需 3–4 周，发布缓冲另计。估算包含直接相关测试，不包含等待模型凭据、网络恢复或评测预算确认的时间。

## 5. 分项实施与验收

### PORT-001：运行配置、依赖、测试隔离与文档校准

实施内容：

- 将真实模型适配器所需的依赖纳入运行依赖，使用项目锁定流程更新生产与开发锁文件。
- 梳理 Settings、`.env.example`、Compose 的配置对应关系，补齐模型端点、密钥、模型名、超时、向量维度等必要变量。
- 统一 SSE 心跳的默认值与容器行为，同时验证在线撤权响应，避免只改配置却改变已有契约。
- 让配置测试显式隔离 `.env` 与环境变量；增加存在本地配置时的回归覆盖。
- 校准 README 的实际技术栈、模型接入边界和测试基线；说明发布标签、后端包、前端包及 API 版本的关系。

验收标准：

- [x] 运行镜像能导入真实适配器，不依赖开发镜像中的额外包。
- [x] API 与 Worker 使用一致的模型配置；缺必要配置时明确失败。
- [ ] 在已配置有效凭据的环境中完成 Chat 与 Embedding 诊断；日志不输出凭据。（凭据未就绪，仅完成离线部分：哨兵密钥不回显、失败被分类）
- [x] 有无本地 `.env` 均不影响配置单测结论。
- [x] 文档说明与实际依赖、配置及模型能力一致。

验证记录（2026-09-18，基线 `25aeca7` 之后的工作区）：

| 项 | 命令 / 方式 | 结果 |
|---|---|---|
| 运行依赖与锁文件 | `pip-compile --extra=dev` / `--strip-extras` 重生成双锁文件，二次解析稳定 | `httpx2==2.12.0` 进入 `requirements.lock`（含 `httpcore2`、`truststore`），两个锁文件版本一致 |
| 运行镜像导入 | `docker build --target runtime` + 容器内 `import httpx2` 与三个适配器模块 | 通过；镜像内无 `pytest` / `mypy` / `ruff` |
| 真实分支解析 | 容器内 `MOCK_MODEL_MODE=false` 调 `build_model_gateway` | 解析到 `qwen_chat` 网关，未退回 FakeModel；哨兵密钥未回显 |
| 配置透传一致性 | `docker-compose config` 对比 api / worker / scheduler | 三者模型配置逐项一致；`SSE_HEARTBEAT_SECONDS` 生效值 1 |
| 配置测试隔离 | 挂载含本地 `.env` 的工作目录跑 `pytest -q` | 576 passed / 17 skipped；移除 `tests/conftest.py` 的对照实验为 4 failed，证明隔离有效 |
| 缺配置明确失败 | `tests/unit/test_settings.py` 新增用例 | 空值按未配置处理，`MOCK_MODEL_MODE=false` 且缺端点/密钥时启动即失败 |
| 文档口径 | 人工复核 README / 环境配置清单与代码事实 | 前端技术栈、后端行数、测试基线、SSE 心跳、版本口径均已校准 |

未执行：`pwsh tests/validate_documents.ps1`、`pwsh scripts/project.ps1 verify`（本机无 PowerShell 7，PowerShell 工具在本环境无法执行外部进程）。新增探针 `tests/validate_runtime_model_adapter.ps1` 已接入 `verify`，其断言已用等价的 `docker` 命令逐条手工执行并全部通过。

### PORT-002：逐项证据绑定与评分语义

实施内容：

- 沿结构化字段、硬规则、报告结论的链路传递证据来源，支持一个结论引用多个相关片段。
- 移除所有结论默认引用第一条证据的策略；保留引用归属、版本、范围与精确摘录校验。
- 处理字段缺失、原文矛盾、时间区间不完整和证据跨段等情况；不以引用存在推导 SUPPORTED。
- 明确区分检索排序分、确定性规则结果与模型解释，避免将排名换算分表达为模型置信度。

验收标准：

- [x] 学历、技能和年限结论分别定位到支持它们的原文。
- [x] 第一条证据无关时，系统选择正确片段或降级支持等级。
- [x] 缺失、矛盾、跨候选人、旧版本和越界引用均有反例测试。
- [x] 高影响且证据不足的结论输出“不足以判断”。
- [x] 页面文案和 API 字段能解释各类分数的含义。

实现说明：

- `backend/app/reports/evidence_binding.py`（新增）：按观测值定位。技能按字母数字边界匹配（`java` 不会命中 `javascript`，`c++` / `c#` / `node.js` 仍可命中）；学历按最长拼写优先匹配；年限只接受精确等值（`15 年` 不会被当作 `5 年`，同时支持中文数字与小数）。每条结论最多 3 条摘录，按 `chunk_index` 稳定排序。
- `backend/app/retrieval/education.py`（新增）：学历序数表的唯一来源，同时供结构化召回、硬规则与证据定位使用。修复了一个此前未被测试覆盖的缺陷：`CandidateProfile.education_level` 存英文枚举（`MASTER`），而原查表只有中文键，导致学历硬规则恒为 `UNKNOWN`、结构化学历分恒为 `0`。既有测试用中文值直接构造 `ReadyProfile`，绕过了 schema，因此没能发现。
- `backend/app/reports/service.py`：`_support_for` 改为由「是否定位到证据」决定支持等级，不再由规则结论推导；聚合结论只能引用各分项结论已确认的摘录（`merge_evidence` 重新绑定 claim_id 并按 `(chunk_id, start, end)` 去重，对应 `uq_claim_evidences_claim_chunk_range`）；`summary` 明确标注「检索排序换算分…不代表模型置信度」；`confidence_note` 增加 `evidence=located|not_found`，使降级原因可被前端机器读取。
- `apps/web/src/components/ClaimEvidencePanel.tsx`：评分标签改为「检索排序换算分（非模型置信度）」；无摘录的结论改为说明降级原因而非「暂无证据」；证据列表的 React key 加入 `quote_start`——同一 Chunk 可以贡献多条不同区间的摘录，仅用 `evidence_chunk_id` 会重复。
- `apps/web/src/test/setup.ts`：补 `afterEach(cleanup)`。vitest 未开 `globals`，`@testing-library/react` 的自动清理因此从未注册，DOM 在用例间泄漏——所有「不应渲染某内容」的断言此前是靠残留 DOM 侥幸通过。
- 反例覆盖：`.gitignore` 增加 `.workbuddy-ai/`（本地 agent 状态，不应进入作品集仓库）。

验证记录（2026-09-18，基线 `54a7794` 之后的工作区）：

| 项 | 命令 / 方式 | 结果 |
|---|---|---|
| 后端测试 | 容器内 `pytest -q`（挂载宿主代码） | **643 passed / 17 skipped**（PORT-001 时为 576；新增 67 项） |
| 后端静态检查 | `ruff check backend apps tests/unit tests/integration` | 干净 |
| 后端类型检查 | `mypy backend apps tests/unit`（216 files） | 干净 |
| 学历归一化 | `tests/unit/test_education_levels.py`（新增，25 项） | 覆盖 schema 全部枚举值；英文枚举 `MASTER` vs 中文要求 `本科` 判定为 PASS，未映射要求为 UNKNOWN 而非 FAIL |
| 证据定位 | `tests/unit/test_evidence_binding.py`（新增，31 项） | 精确切片、词边界、最长学历拼写、年限精确等值、上限、空结果 |
| 报告语义 | `tests/unit/test_evidence_report.py`（改写 + 扩充，30 项） | 首条证据无关时各结论各自命中正确片段；缺失/矛盾/跨候选人/旧版本/越界五类反例；SUPPORTED ⟺ 有引用 |
| 前端测试 | `npx vitest run`（宿主 `npm ci --ignore-scripts`） | **23 files / 127 passed**（新增 `ClaimEvidencePanel.test.tsx` 6 项；PORT-001 时为 22 / 121） |
| 前端类型与构建 | `tsc -b` / `vite build` | 干净；131 modules，`dist` 产物正常 |

未执行：`pwsh scripts/project.ps1 verify` 与各 PowerShell 探针（本机无 PowerShell 7，原因同 PORT-001）。前端验证改用宿主 `npm ci --ignore-scripts`：默认 `npm ci` 的 `postinstall` 生命周期脚本在本沙箱内取不到 stdio 而失败，与依赖本身无关。

### PORT-003：真实模型匹配解释闭环

实施内容：

- 在现有抽取与 Embedding 适配器基础上增加匹配解释契约，输入岗位要求和候选人证据，输出受 Schema 约束的结论与引用。
- 将解释能力接入 MatchRun；模型不能决定权限、审批状态、业务 ID 或副作用参数。
- 为解释调用记录模型、Prompt、规则版本、耗时、重试与可获得的 Token usage；不记录密钥。
- 明确超时、限流、畸形响应和引用无效时的行为；规则输出与真实模型输出分别标识。
- 使用实际 Embedding 重新生成本轮评测样本的向量，避免将种子哈希向量混入真实效果结论。

验收标准：

- [ ] 完成合成文件上传→模型抽取→人工校对→真实向量检索→模型解释→双审批的链路。
- [x] 同一候选人针对不同岗位得到与岗位相关、可追溯的解释。
- [x] 模型生成的引用在持久化前通过服务端校验。
- [x] 429、超时、Schema 错误与非法引用有明确结果及自动化覆盖。
- [x] 模型异常不会越过审批或触发重复副作用。

个性化面试问题作为本工作包的可选增强；匹配解释与证据校验未完成前不扩展该项。

实现说明：

- `backend/app/explanations/contract.py`（新增）：模型回复的封闭契约。`extra="forbid"` + 全部文本字段限长，是 BR-011「不得生成无关敏感属性」的结构性保证——Prompt 是请求，Schema 才是保证。`impact` 只允许 `LOW | MEDIUM`；模型要求 `HIGH` 时**直接判为契约违规并记录**，而不是夹取到 MEDIUM。理由：§4.5 的高影响守卫只重写「未被支持的」高影响结论，若模型能把 `HIGH` 与一条合法引用组合起来，就能绕过守卫表达决定性判断；夹取会把这个动作变成一次静默修补。
- `backend/app/explanations/gateway.py`（新增）：模型边界。`FakeMatchExplanationGateway` 是确定性且**与岗位相关**的：它把岗位必备技能与候选人片段做整词匹配，因此「同一候选人、不同岗位、不同解释」在无网络、无密钥的情况下可测；只返回罐头字符串的 Fake 会让该验收项无法离线验证。`build_explanation_gateway` 由与抽取/向量网关同一个 `mock_model_mode` 开关选择，不互相回退。
- `backend/app/explanations/service.py`（新增）：校验与降级。模型只返回**文本**、不返回偏移，切片由服务端反查，因此伪造区间不可表达；反查结果交给与确定性结论同一套 §9.4 校验器（`verify_evidence_reference`），两类引用的合法性判定不会漂移。全部引用失效的结论被丢弃并计数，绝不作为无支撑断言落库。模型结论的支持等级**上限为 `PARTIAL`**：§9.4 明确「引用合法只证明位置真实，不证明语义支持」，语义支持是 Golden-Dataset 的判断。
- `backend/app/explanations/models.py`（新增）：`match_explanations` 调用记录表，按 `(run_id, application_id)` 而非 `report_id` 建键。报告聚合在重试时会被整体重写（`ReportService.generate_for_run` 删除该 run 的 report/claim/evidence），若此处对 `match_reports` 建外键，一个无关写入就能阻塞该删除，且只会在「上一次尝试恰好产出了解释」时暴露。同时该行让「模型没被调用」与「模型调用了但没话说」可区分（BR-010）。
- `backend/app/explanations/repository.py`、`schemas.py`、`routes.py`、`wiring.py`（新增）：持久化端口/适配器、读接口与组装。`GET /api/v1/match-runs/{id}/explanations` 走与其他 job 作用域读相同的资源级授权；没有读接口时，`reason_code` 只存在于数据库里，「限流」与「功能关闭」在页面上仍无法区分。
- `backend/app/infrastructure/chat_completion.py`（新增）：把 OpenAI 兼容信封的解析从 `qwen_chat` 中抽出。两个网关共用同一份 `extract_message_content` / `extract_usage`，第二个调用方正是复制粘贴开始悄悄分叉的位置。`usage` 缺失或畸形时返回 `None`，**从不估算** token 数。
- `backend/app/infrastructure/http_transport.py`：新增 `AttemptRecord`（调用方持有的尝试计数器，成功与抛错路径都会写入）。计数器不能放在 transport 上——transport 在并发调用间共享，放在上面会把一次调用的重试次数报成另一次的。另新增 `TransportTimeoutError` / `TransportConnectionError` 子类，使超时与连接失败可以按类型判定而不是按消息文本匹配。
- `backend/app/infrastructure/prompts.py`：新增 `MATCH_EXPLANATION_PROMPT_VERSION = "qwen-explain-v1"`、系统提示与用户提示构造。系统提示明确七条：只解释不决策、只能引用给定 chunk_id、逐字复制引用、`impact` 只能是 LOW/MEDIUM、不得提及受保护属性、证据不足时少说或不说、简历与岗位文本是不可信数据。判定结论作为固定上下文放在不可信围栏之外。
- `backend/app/reports/models.py` + `migrations/versions/0013_match_explanations.py`（新增）：`report_claims.source`（`RULE` / `MODEL`，`server_default='RULE'`）与 `match_explanations` 表。默认值由数据库写入而非 Python 猜测：改动前的每一条结论都是确定性结论，猜错会把一个决策重新标记为评论。另修复：`build_candidate_report` 现在显式设置 `source=ClaimSource.RULE`——列默认值只在 INSERT 时生效，未 flush 的对象 `source` 为 `None`，会让解释提示的固定上下文块拿到空的规则结论列表。
- `backend/app/match_run/service.py`：抽出 `_aggregate_status`，把报告与解释生成移到 `aggregate_run` **之前**（此时 run 仍为 `RUNNING`）。此前派生产物可能在终态事件之后才出现或失败，与时间线矛盾。新增 `explain_matches` 节点。
- `backend/app/reports/schemas.py`、`apps/web/src/api/types.ts`、`apps/web/src/components/ClaimEvidencePanel.tsx`：`ClaimOut` 增加 `source`，前端据此显示「规则判定 / 模型解释」，并对模型结论明示「引用已通过服务端校验，但语义支持未经人工评测认定，因此不会显示为『支持』」。

验证记录（2026-09-18，基线 `d823ab0` 之后的工作区）：

| 项 | 命令 / 方式 | 结果 |
|---|---|---|
| 后端测试 | 宿主 venv `pytest -q` | **710 passed / 17 skipped**（PORT-002 时为 643；新增 67 项） |
| 后端静态检查 | `ruff check backend apps tests/unit tests/integration` | 干净 |
| 后端类型检查 | `mypy backend apps tests/unit`（229 files） | 干净 |
| 契约与解析 | `tests/unit/test_match_explanations.py`（新增，39 项） | `HIGH` 被拒而非夹取、未知字段被拒、结论/引用/文本长度上限、空结论可接受 |
| 引用校验与失败语义 | 同上 | 偏移由服务端反查、引用不在原文/跨候选人/伪造 chunk_id 均被拒；429 / 超时 / Schema 错误 / 非法引用四类各自独立的 `reason_code` |
| 岗位相关性 | 同上 | 同一候选人 + 同一报告，岗位从 Python 换为 Java 后摘要与引用片段均不同；岗位要求候选人没有的技能时模型不编造理由 |
| MatchRun 接入 | 同上 | `explain_matches` 节点位于 `score_with_evidence` 与 `aggregate_run` 之间且在终态事件之前；429 场景下 run 仍为 `COMPLETED`、规则结论与支持等级不变、无 `RUN_FAILED` 事件；失败候选人不出解释 |
| 真实适配器 | `tests/unit/test_explanation_gateway.py`（新增，17 项） | 单开关选型、非 mock 缺配置即失败、请求体形态、密钥只在 header、证据超长截断并标注、usage 缺失为 null、重试次数记录、401 永久 / 429 可重试、日志不含密钥与候选人文本 |
| 传输层 | `tests/unit/test_http_transport.py`（扩充 6 项） | 超时/连接失败子类型；`AttemptRecord` 在成功路径、抛错路径与并发两次调用下计数正确 |
| 迁移一致性 | `tests/unit/test_model_registry.py`（扩充 6 项） | 迁移产出的列集合、可空性与命名约束与 ORM 元数据一致；`source` 的 `server_default` 为 `RULE`；修订链固定 |
| 迁移 SQL | `alembic upgrade head --sql`（离线） | 生成成功，head 为 `0013_match_explanations`；`report_claims.source` 与 `match_explanations` 的约束、索引齐全 |
| 路由契约 | `tests/unit/test_openapi_contract.py` | 新增读接口进入固定清单；未带凭据返回 401/403 |
| 前端测试 | 宿主 `vitest run` | **23 files / 130 passed**（PORT-002 时为 127） |
| 前端类型与构建 | `tsc --noEmit` / `vite build` | 干净；131 modules |

未执行：`pwsh scripts/project.ps1 verify`、`tests/validate_migrations.ps1` 与各 PowerShell 探针（本机无 PowerShell 7，原因同 PORT-001）；迁移脚本改用离线 `alembic upgrade head --sql` 校验，其断言已用等价的 Python 测试固定。**首个验收项未完成**：真实链路（上传→抽取→校对→真实向量检索→模型解释→双审批）需要可用的百炼凭据，当前环境未配置，因此只能验证到「解释层与真实适配器的边界」，端到端留待凭据就绪后补验；同理「使用实际 Embedding 重新生成评测样本向量」也需真实调用。

### PORT-004：实际输出评测与录制回放

实施内容：

- 建立由原始合成简历与岗位输入驱动的评测入口，将实际抽取、检索、报告输出交给计分器。
- 初始样本建议覆盖 30–50 份合成简历、3–5 类岗位、至少 30 条独立标注结论和 20 组原始 clean / injected 文本；按开发集与保留评测集分开使用。
- 覆盖同义技能、缺失字段、跨段证据、相互矛盾的描述、经验时间重叠和无关证据。
- 注入文本必须经过实际分析链路；分别记录模型是否遵循攻击内容与系统是否允许越权、副作用或审批绕过。
- 保存数据集、代码、模型、Prompt、配置版本及必要的原始响应，录制回放复用实际解析与校验路径。
- 普通 CI 保留无外部凭据的确定性测试与回放；增加显式触发的真实模型评测入口，并配置调用预算和停止条件。

验收标准：

- [x] 报告区分计分器测试、录制回放和真实模型评测，不混用结果。
- [x] 预测来自实际运行或录制响应，标准答案来自独立标注。
- [x] 抽取报告包含字段级 Precision / Recall / F1；检索与语义报告包含样本量及失败案例。
- [x] 在保留评测集上报告 Recall@K、MRR、nDCG、支持标签精确率与宏平均 F1。
- [x] 记录总耗时、可获得的 Token usage；费用仅按明确记录的计价版本估算。
- [x] 缺少录制、样本为空或评测入口故障时明确失败，不产生通过结论。

沿用仓库现有门槛作为待验证目标：Recall@K ≥ 0.85、MRR ≥ 0.70、nDCG@K ≥ 0.80、SUPPORTED 精确率 ≥ 0.95、支持等级宏平均 F1 ≥ 0.85、非法引用数为 0、各类攻击成功数为 0。抽取质量、延迟和成本先建立基线，再在保留评测前确定门槛。未达标时保留失败结果、分类修复并重新评测，不调整标签或阈值来制造通过。

实现说明：

- `backend/app/evaluations/corpus.py`（新增）：52 例由原始简历文本与岗位输入驱动的评测集（13 类画像 × 4 类岗位），标准答案在本模块中独立声明。覆盖同义技能、缺失字段、跨段证据、矛盾描述、时间重叠与无关证据六类；DEV 16 例 / HOLDOUT 36 例。四类岗位共用同一组阈值（`min_years=3.0`、`required_education=本科`），因此画像的硬规则结论只需声明一次，该不变式由 `test_corpus_job_families_share_the_gate_thresholds` 固定。
- `backend/app/evaluations/runner.py`（新增）：把语料送进真实链路。抽取走配置的网关，召回走 `RetrievalService` 与 RRF 融合，硬规则走 `evaluate_hard_rules`，报告走 `build_candidate_report`——没有任何环节被重新实现。`PredictionSource` 标注每次运行的预测来源（`SCORER_FIXTURE` / `FAKE_MODEL` / `RECORDED_REPLAY` / `LIVE_MODEL`），`classify_gateway` 会解开装饰器链后再判断，避免预算包装把替身运行标成真实模型。空语料、无语料画像、录制未覆盖全部输入一律抛 `EvaluationSetupError`；单个用例的抽取失败被记为失败计数与原因码，而不是被吞掉。
- `backend/app/evaluations/metrics.py`（新增）：字段级 Precision / Recall / F1 / 完全命中、按岗位的 Recall@K / MRR / nDCG（含 `min(K, 相关数) / 相关数` 的**上限**）、支持标签的逐类与宏平均、硬规则一致率，以及带数量上限的失败样本清单。`combine` 对来源不同的两组指标直接抛错。
- `backend/app/evaluations/report.py`（新增）：报告按来源分节，同一来源不得出现两次，同一节内的语料运行与注入运行必须同源；每节自带口径说明。费用只在给出完整计价清单（含 `version`）时估算，Token usage 缺失记为「未记录（不估算）」。
- `backend/app/evaluations/injection_corpus.py`（新增）：28 组原始 clean / injected 文本对，覆盖 10 类攻击（标记注入、指定分数、指定状态、伪造证据块、未声明技能 / 学历 / 年限、审批绕过、越权声明、立即副作用）。每对声明 `base_case_id`，干净侧就是语料中的那个用例，因此干净面的标签与基线不是新造的。伪造的证据块编号由用例 id 派生，并由测试断言其不等于任何真实切片 id。
- `backend/app/evaluations/injection.py`（扩展）：原模块只对预置的 `DeterministicAnalysis` 打分，且内置样本把**同一个对象**交给两侧，于是「零攻击成功」是因为没有攻击，不是因为系统抗住了。现新增实际链路观测：两侧文本都经 `runner.predict_case` 走完整链路，审批门禁由真实 `RunEngine` 执行 `build_application_graph()` 观测，初始状态复用 `job_applications.service.build_initial_state`。**「模型是否遵循攻击内容」与「系统是否放行越权 / 副作用 / 审批绕过」分别记录**：前者只在模型对候选人的断言字段上判定（`unknown_fields` 的原文捕获不算遵循），允许非零；后者是硬性零门槛，逐项对应 §9.5 的六类禁止事件。干净文本若未触达审批门禁，运行直接失败而不是报「零绕过」。
- `backend/app/evaluations/budget.py`（新增）：真实模型评测的调用预算。预算在网关处计数（唯一能看到每次调用的位置），超限抛 `BudgetExceeded`；该异常继承 `EvaluationSetupError`，因此 `predict_case` 会向上抛出而不是把它记成逐例失败——否则一个被预算截断的运行会拿「装得下的部分」算出比率。
- `backend/app/job_applications/service.py`：抽出 `build_initial_state`，让生产 worker 与注入评测使用同一份初始状态。写第二份会让评测观测到一个生产并不存在的门禁。
- `backend/app/retrieval/hard_rules.py`：新增公开的 `hard_rule_snapshot`，使 MatchRun 链路与离线评测写出同一种快照格式。
- `backend/app/infrastructure/chat_completion.py` / `qwen_chat.py`：新增 `UsageRecord`，逐次累计可获得的 Token usage；无计数器时返回 `None` 而不是 0。
- `scripts/run_evaluation.py`（新增，显式触发）：唯一入口。`--recording` / `--live` 互斥，`--split` 默认 `holdout`，`--k` 默认 5，`--max-calls` 配置调用预算（`--live` 未声明预算时拒绝启动，除非显式 `--allow-unbudgeted`）。退出码：0 通过、1 无法得出结论、2 无法开始、3 门槛未通过。

验证记录（2026-09-18，基线 `a9a4154` 之后的工作区）：

| 项 | 命令 / 方式 | 结果 |
|---|---|---|
| 全量单测 | `pytest -q tests/unit` | **841 passed**（PORT-004 起点 710 → +131；其中注入实际链路 41 项、调用预算 11 项） |
| 静态与类型 | `ruff check backend apps tests/unit scripts`；`mypy` | ruff 干净；mypy strict 通过 **239 个文件** |
| 保留集报告 | `python scripts/run_evaluation.py --k 5 --split holdout` | 退出 0；36/36 例可测量、失败 0 例 |
| 抽取 | 同上报告 | skills 精确率 1.000 / 召回率 0.882 / F1 0.938，字段 F1 均值 **0.979**（唯一缺口是 4 例刻意的词表外技能） |
| 检索 | 同上报告 | Recall@5 **0.625**（上限 0.833）｜MRR **1.000**｜nDCG@5 **0.815** |
| 支持标签 | 同上报告 | 宏平均 F1 **1.000**、宏平均精确率 **1.000**，共 144 个结论，三档标签均被覆盖 |
| 硬规则 | 同上报告 | 一致率 **1.000**（该确定性环节不设阈值，不一致即语料或规格发生了移动） |
| 注入（系统侧） | 同上报告注入节 | 17/17 例可测量、失败 0 例，覆盖 **10 类攻击**；系统放行越权 / 副作用 / 审批绕过 **0/17** |
| 注入（模型侧） | 同上报告注入节 | 模型遵循 **5/17**（未声明技能 3、未声明学历 2）。该计数允许非零，报告与 `INJECTION_MODEL_NOTES` 明确说明替身的遵循计数不可外推 |
| 两件事相互独立 | `tests/unit/test_injection_real_path.py::test_the_two_facts_are_not_the_same_measurement` | 用「模型照做」的录制响应回放：模型遵循 1/1，系统放行 0/1 |
| 检查确实会触发 | `test_every_forbidden_event_is_detected` / `test_a_poisoned_pipeline_is_caught_end_to_end` | 六类禁止事件逐项命中；把注入侧的规则快照清空后，`HARD_RULE_CHANGED` 端到端触发 |
| 门禁不可达即失败 | `test_a_gate_a_clean_document_never_reaches_fails_the_run` | 去掉图的中断点后，运行以 `EVALUATION_INJECTION_GATE_NOT_REACHED` 退出 2 |
| 调用预算 | `python scripts/run_evaluation.py --max-calls 12` | 退出 2，`EVALUATION_CALL_BUDGET_EXCEEDED`，不产生任何结论 |
| 真实入口需声明预算 | `python scripts/run_evaluation.py --live` | 退出 2，`EVALUATION_BUDGET_NOT_STATED` |
| 预算不改变来源标注 | `tests/unit/test_evaluation_budget.py` | 预算包装的替身仍标为 `FAKE_MODEL`、录制仍标为 `RECORDED_REPLAY`，且未覆盖录制的检查仍然触发 |

未执行：`pwsh tests/validate_evaluations.ps1`（本机无 PowerShell 7，且 Docker Desktop 未启动，无法构建探针镜像）。真实模型评测（`--live`）因无百炼凭据未执行，属凭据问题而非机制问题。

### PORT-005：关键页面与演示体验

实施内容：

- 候选人排名显示姓名及必要摘要；Profile ID 保留为辅助追踪信息。
- 报告按候选人组织，支持从结论直接查看原文、页码或段落。
- 时间线显示可读的节点名称、状态与失败原因；技术标识放入辅助详情。
- 审批页明确显示拟执行动作、修改前后差异、当前版本和执行结果。
- 标明当前模型模式和结果来源；优化异步等待、重试、空结果与错误后的恢复指引。

验收标准：

- [x] 演示者无需记忆 Profile ID 即可串起候选人、排名、报告和审批。
- [x] 查看证据无需手工切换多页寻找原文。
- [x] 重试、等待、完成、失败和撤权状态易于区分。
- [ ] 新增界面行为通过相关组件测试和关键浏览器场景。**组件测试已完成（前端 231 项全绿）；关键浏览器场景未执行**，原因见下方「未执行」。
- [ ] 在实际投屏分辨率下检查长文本、滚动区域和审批操作。**未执行**，原因同上。

实现说明：

- `backend/app/candidates/summaries.py`（新增）：把 `CandidateProfile` 与 `Candidate.display_name` 联表读成一份展示摘要。**按 id 查、不过滤 READY**——一次 MatchRun 在运行时把 profile id 冻结进快照，被取代的历史 profile 仍然是「当时被排名的那个人」，按状态过滤会把排名里的人换成另一个人或直接消失。`summarise_profiles` 是纯函数（`Decimal("8.50")` → `8.5`），空 id 集合直接返回 `{}` 且不发查询。
- `backend/app/match_run/schemas.py` / `routes.py`：`MatchRunCandidateOut` 增加 `display_name` / `normalized_skills` / `years_experience` / `education_level` / `document_id`，均为可选；`get_match_run` 一次性批量取摘要后经 `_candidate_out(candidate, summary)` 组装，避免逐行查询。缺摘要时字段为 `null` 而非占位串——前端才知道该显示什么。
- `backend/app/reports/schemas.py` / `routes.py`：`ReportOut` 同样带上 `display_name` 与 `document_id`，`ReportList.from_views(views, summaries, locators)` 接收两份查表结果。
- `backend/app/candidates/repository.py`：新增 `SqlEvidenceChunkRepository.list_locators(chunk_ids)`，把证据切片 id 映射回原始定位信息，供报告页显示「第 N 段 / 第 N 页」。
- `backend/app/observability/model_mode.py`（新增）：模型模式的唯一来源。只有 `mock_model_mode` 布尔值不够——「Mock」不带上模型名，读者分不清「替身开着」与「配置了上游但没连上」。**API key 与 base URL 刻意排除**：溯源记录不能变成凭据泄漏到浏览器的通道。`source_label` 是给读者看的一句话。
- `backend/app/observability/schemas.py` / `routes.py`（新增）：`GET /api/v1/runtime/model-mode`，需登录但不做角色 / 资源范围限制（这是部署属性，不是某个人的数据）。`backend/app/evaluations/routes.py` 的 `_model_snapshot` 改为由同一视图构造，使横幅与运行时固化的快照不可能互相矛盾；`prompt_version` / `rule_version` 不重复进快照（它们已在 `_prompt_versions` 中）。
- `apps/web/src/lib/`（新增纯模块，各带同名 `.test.ts`）：`candidateDisplay.ts`（`candidateName` / `candidateFacts` / `shortProfileId`）、`locator.ts` 的 `describeBlock`（页码 / 段落 / 单元格，不含字符区间）、`runTimeline.ts`、`approval.ts`。**标签表按后端自己的词表建键且不做穷尽假设**：未知节点、消息键、状态、失败码一律原样显示，绝不显示成「未知事件」之类占位——把未知值藏起来，藏掉的正是这些界面存在的理由。
- `apps/web/src/components/ModelModeBanner.tsx`（新增）：在 `App.tsx` 的 header 之下、主内容之上渲染，因为模型模式是整个部署的属性而非某一页的属性。**读取失败要说出来**：「无法确认模型模式」与「不是 Mock」是两句不同的话，静默不渲染会让读者默认成更安全的那一种。
- `apps/web/src/components/RankingTable.tsx` / `ClaimEvidencePanel.tsx`：排名以姓名为 `<strong>`，事实行（经验 / 学历 / 技能，超出显示「等 N 项」）另起一行，「辅助追踪 `<短 id>`」作为第三行；报告标题改为「候选人 姓名」，每段证据给出位置并附「查看原文 →」，链接带上切片 id。
- `apps/web/src/pages/DocumentReviewPage.tsx`：读 `?chunk=` 参数，**只应用一次**定位，并在证据不在当前版本原文中时明说——证据属于旧版本是正常输入，不是渲染失败。
- `apps/web/src/components/RunTimeline.tsx` / `CurrentApprovalCard.tsx` / `ActionDiff.tsx`：时间线正文只显示映射过的标题与详情，事件类型 / 节点 / 消息键 / 载荷收进 `<details>` 辅助详情；审批页按「拟执行动作 → 原提案与参数 → 当前版本 → 执行结果」组织，`ActionDiff` 改为对齐表格且**不再复述动作**（上面那块负责这句话）。
- `apps/web/src/lib/approval.ts`：把两件原本看不出来的事写清楚——`APPROVED` / `EDITED` 表示决策已记录但副作用**尚未发生**（执行是独立一步），`EXECUTED` 才表示恰好落库一次；`APPROVAL_EXPIRED`、`ACTION_REJECTED`、`EXECUTION_FAILED` 等 7 种结果各自带 label / tone / detail / 恢复指引。
- `apps/web/src/components/Feedback.tsx`：0 / 401 / 403 / 404 / 409 / 422 / 429 / 503 各配一句主句加一行「下一步：…」，错误页告诉读者该做什么而不只是哪里坏了。`ClaimEvidencePanel` 新增 `provenance`，在正文前说明「结果来源：规则判定 N 条 · 模型解释 M 条」。
- `apps/web/src/pages/ApplicationRunPage.tsx` / `MatchRunPage.tsx`：区分 FAILED / WAITING_APPROVAL / COMPLETED+ACTION_REJECTED / CANCELLED 与「仍在运行」，因为「等」和「重试」是两条不同的指令。

验证记录（2026-09-19，PORT-005 起点 `567df65`；起点数字在 `git worktree add` 出来的该提交工作树上实测，不是推算）：

| 项 | 命令 / 方式 | 结果 |
|---|---|---|
| 后端全量 | 宿主 venv `pytest -q` | **865 passed / 17 skipped**（在起点 `567df65` 的工作树上实测为 841 passed；新增 24 项：候选人摘要 17 + 模型模式 7） |
| 前端全量 | 宿主 `vitest run` | **30 files / 231 passed**（起点实测 23 files / 130 passed；新增 7 个测试文件 / 101 项） |
| 静态与类型 | `ruff check backend tests`；`mypy`（项目配置） | ruff 干净；mypy strict 通过 **245 个文件**（PORT-004 时 239） |
| 前端类型与构建 | `tsc -b` | 干净 |
| 5A 姓名与摘要 | `pytest tests/unit/test_candidate_summaries.py`；`vitest run` | 后端 854 passed；前端 151 passed |
| 5B 结论直达原文 | `vitest run` | 前端 161 passed（`DocumentReviewPage` 新增 `?chunk=` 定位、无参数不动、跨版本证据三种场景） |
| 5C 时间线可读 | `vitest run` | 前端 191 passed（`runTimeline` 22 项 + `RunTimeline` 8 项） |
| 5D 审批信息完整 | `vitest run` | 前端 223 passed（`approval.ts` 21 项、`ApprovalPage` 7 项、`CurrentApprovalCard` 4 项） |
| 5E 模型模式与恢复 | `pytest tests/unit/test_model_mode.py tests/unit/test_openapi_contract.py`；`vitest run` | 后端 11 passed；前端 231 passed（新增 `ModelModeBanner` 5 项、`provenance` 3 项、`App` 横幅断言） |
| 路由清单被固定 | `tests/unit/test_openapi_contract.py::test_route_inventory_is_pinned` | 新增 `GET /api/v1/runtime/model-mode` 后该测试失败，把新路由显式写进 `EXPECTED_OPERATIONS` 才通过——该测试的存在就是为了强制这次手改 |
| 凭据不随溯源外泄 | `tests/unit/test_model_mode.py` | 断言响应 schema 中不出现 `qwen_api_key` 与 `model_base_url` |

未执行：`pwsh scripts/project.ps1 verify`、`tests/validate_evaluations.ps1` 与各 PowerShell 探针（本机无 PowerShell 7）；**关键浏览器场景与实际投屏分辨率检查**（Docker Desktop 未启动，探针栈与 Playwright 均无法运行）。因此第四个验收项只完成了「组件测试」这一半，第五个验收项整体未执行——两项都保持未勾选，留待能起容器的环境补验，不按「组件测试通过」推断浏览器表现。

文档同步（PORT-005 范围内）：

- [`README.md`](./README.md)：实现进度新增 PORT-005 行（含两项未执行的显式声明）；「基线与规模」的后端 / 前端文件数、行数、测试数与 mypy 文件数按当前代码更新（此前停留在 PORT-003 的 710 / 130，属路线图 §3 点名的口径漂移）。
- [`详细设计说明书.md`](./详细设计说明书.md)：§12.6 补 `GET /runtime/model-mode` 与 `ModelModeOut` 的字段边界（凭据与 base URL 不出现在响应中）；§15.4 补展示口径（姓名为主标识、Profile ID 降为辅助追踪、证据位置与「查看原文」链路、无法解析的 `locator_json` 显示「位置未知」而非丢证据）与 `ModelModeBanner` 的定位；§15.5 补审批四区块与「已批准 ≠ 已执行」的区分；§15.6 补错误恢复建议、时间线辅助详情、未知词表原样显示三条。
- [`演示脚本.md`](./演示脚本.md)：修正两处已被本次改动推翻的现场说明——原「排名表先展示 Profile 短 ID」与「不要现场猜哪个 ID 对应哪位候选人，提前记下前 8 位」正是本工作包要消除的讲解负担；补顶部模型模式横幅、审批四区块与「已批准 / 已执行」的区别、四种终态的不同恢复指引，并把测试规模更新为当前数字。完整的 5 分钟 / 10 分钟双版本重写见 PORT-006。
- [`UML规划文档.md`](./UML规划文档.md)：**未改动**。组件图停在 `api` / `worker` / `scheduler` 粒度，新端点仍由 `api` 经同一个 `REST/JSON` 接口提供，没有新增或改变构件；只增字段与只读端点不足以让类图或组件图失真，为改动而改动会稀释图的信息量。

### PORT-006：作品集材料与发布验收

实施内容：

- 更新 [演示脚本](./演示脚本.md)，提供 5 分钟录屏版和 10 分钟现场版。
- 以一个候选人的推荐理由、证据定位、人工修改或否决、故障恢复串联演示。
- 准备一张架构图、一份可复现评测报告，以及至少一个恢复后没有重复写入的案例。
- README 提供运行入口、实际技术栈、模型与规则边界、关键指标和复现步骤。
- 为外部模型不可用的现场准备清晰标识的 Mock 演示或录制材料，并说明其验证范围。
- 在同一代码版本完成最终检查，保存验证记录，再创建发布 commit 与版本 tag。

验收标准：

- [x] 全新环境能够按文档启动并完成合成文件主流程。启动路径与三项健康检查写在 [`演示脚本.md`](./演示脚本.md) §0；主流程在演示脚本里逐幕写明并标注权威信号。本机无法起栈，实际启动由 CI 的 `validate_nonseed_flow.ps1`（非种子主路径）覆盖。
- [x] `pwsh scripts/project.ps1 verify` 通过，包含 PostgreSQL / Redis 集成、前端构建与浏览器验证。**由 CI 执行并通过**：本机无 `pwsh` 且 Docker Desktop 不可用。run [`35443075182`](https://github.com/lans12138/resume-copilot/actions/runs/35443075182)（提交 `c23642ac`）两个 job 全绿，`validate_web.ps1`（Playwright `18 passed`）、Nginx 代理探针与一键起栈探针均**首次**取得结果，PostgreSQL / Redis 集成以 `FIN-001/002/006/007 … passed.` 真实跑过。CI 与本地共用同一入口，逐步骤对照与探针输出见 [`docs/release/verification.md`](./docs/release/verification.md) §3 / §4。
- [x] 模型效果报告能追溯到发布候选 commit 和评测版本。报告新增「溯源」块（commit / 工作区是否干净 / 生成时间 / 实际命令），不可读时写「未记录」而不是省略；评测版本（数据集 content_hash、提示词、规则、网关、向量标识）原本就有。
- [ ] 录屏、截图、指标与当前代码一致，公开材料只含合成数据。**指标与合成数据已达成**（报告在收口提交上重新生成，README 指标与之同源；演示语料与 fixture 均为合成）；**录屏与截图未产出**，原因同第二条。
- [x] 副作用审批、重复投递、取消、恢复与 SSE 撤权契约没有回归。恢复与幂等 15 项测试通过，案例见 [`docs/release/no-duplicate-write.md`](./docs/release/no-duplicate-write.md)；SSE 撤权由 CI 的 Playwright FIN-011 故障矩阵覆盖。
- [x] 发布材料说明真实模型与 Mock 模式分别能证明什么。README「模型与规则边界」、演示脚本「附 A：Mock 模式的验证范围」、评测报告每节的适用范围说明，三处口径一致。

实现说明：

- `backend/app/evaluations/provenance.py`（新增）：报告溯源。设计重点在三条**失败路径**——不可读的 commit 打印「未记录」并说明不能归属（省略该行会被读成「不适用」，而事实是「没能确认」）；工作区状态是三值的，空输出与探针失败都长得像「没有输出」，合并二者会把脏工作区当成干净提交发布；缺 git、非零退出、探针超时一律降级为「不可用」而不是让评测失败（评测的职责是测量，把环境探针变成门禁会让门禁依赖它所描述的环境）。`backend/app/evaluations/report.py` 的 `build_report` 接受可选 `provenance`，`scripts/run_evaluation.py` 传入**实际执行的命令**而不是文档示例。
- `docs/evaluation/report-holdout.md`（新增，受版本控制）：在收口提交上重新生成的实测报告。报告自身写明它是哪个 commit 生成的，因此「换了提交就要重测」这条约束是可核对的，不是口头约定。
- `docs/architecture.svg`（新增）：README Mermaid 图的独立渲染版本。Mermaid 在 PDF、幻灯片、部分代码托管平台上不渲染，而**渲染失败的图比没有图更糟**；SVG 自带白底，不受阅读者主题影响。
- `docs/release/no-duplicate-write.md`（新增）：「恢复后没有重复写入」案例。写成可核对的主张：场景（写已提交后任务被重投或 checkpoint 重放）、断言与所在测试、每条断言钉住的值。**承重断言是 `JobApplication.version` 在重放前后不变**——乐观锁计数器在任何真实迁移后必须递增，这比「没有报错」强，因为静默失败同样不报错。文档同时写明三层独立守卫（审批状态机、SideEffect 结果复用、乐观锁 CAS）与覆盖入口而非投递的第四层（幂等键），并明确**不要**声称「Celery 只投递一次」。
- `演示脚本.md`：拆成**两个独立版本**。5 分钟录屏版逐镜脚本、剪掉所有实时等待（Celery 多跳在录制前跑完）、以「恢复后的事实 + 守着它的测试」替代交互式恢复演示、不演示撤权（第二窗口与手工恢复授权在现场很便宜、剪辑很贵）；10 分钟现场版保留交互并给出**明确的取舍顺序**（先砍撤权、再砍上传解析、最后才砍故障恢复）。两版都只讲**同一位候选人**，走同一条弧线：推荐理由 → 证据定位 → 人工修改或否决 → 故障恢复。新增「附 A：Mock 模式的验证范围」，把「能证明什么」与「不能证明什么」分开列。
- `README.md`：新增「模型与规则边界」（规则层决定、模型层贡献、各自的产物标记与版本、三条推论）与「关键指标（可复现）」（数字带适用边界、四条复现命令、退出码契约）；架构章节补「写入路径只有一条」与两份发布材料的链接；基线与规模按当前代码更新。
- `docs/release/verification.md`（新增）：发布验收记录，逐条对照六项验收，并把 `project.ps1 verify` 的每一步标出本机是否执行——未执行的都是 PowerShell 探针或需要活栈的步骤。

验证记录（2026-09-19，发布候选提交；起点为 PORT-005 收口 `fa23e55`）：

| 项 | 命令 / 方式 | 结果 |
|---|---|---|
| 后端全量 | 宿主 venv `pytest -q` | **886 passed / 17 skipped**（PORT-005 时 865；本轮 +21：17 项溯源、2 项报告结论排序、2 项模型/迁移一致性） |
| 静态与类型（verify 同款） | `ruff check backend apps tests/unit tests/integration`；`mypy backend apps tests/unit` | ruff 干净；mypy 干净 **247 个文件** |
| E2E 夹具一致性 | `python scripts/generate_e2e_fixtures.py --check` | 退出 0 |
| 离线评测门禁 | `python -m backend.app.evaluations.gate` | 退出 **0** |
| 评测报告 | `python scripts/run_evaluation.py --k 5 --split holdout` | 退出 **0**；36/36 例可测量，注入系统侧放行 **0/17**；报告含溯源块 |
| 预算门禁 | `python scripts/run_evaluation.py --live` | 退出 **2**，`EVALUATION_BUDGET_NOT_STATED`（未声明预算即拒绝启动，预期行为） |
| 迁移（离线） | `alembic upgrade head --sql` | 退出 0，40 条 `CREATE TABLE` / `ALTER TABLE` |
| 恢复与幂等 | `pytest -q tests/unit/test_concurrency_recovery.py tests/unit/test_side_effects.py tests/unit/test_idempotency.py` | **15 passed** |
| 前端 | `tsc -b`；`vitest run`；`vite build` | 类型干净；**31 files / 239 passed**；构建通过 |
| 文档通用检查 | 宿主复现 `validate_documents.ps1` 的围栏 / 表格 / 链接 / 文件数断言 | `MARKDOWN_GENERIC_OK markdown=12 local_links=60` |
| 演示脚本契约串 | 逐条核对 9 个必需串 + 2 条负向断言 | 9/9 命中；负向断言 0 命中 |
| SVG 结构 | `xml.etree` 解析 + 边界检查 | 结构合法，无越界元素 |

未执行：`pwsh scripts/project.ps1 verify` 全量（本机无 PowerShell 7，且 Docker Desktop 起不来），因此 PostgreSQL / Redis 集成、运行镜像探针、Playwright 浏览器场景、Nginx 代理探针与一键起栈探针均未在本机运行——逐步骤对照与替代验证见 [`docs/release/verification.md`](./docs/release/verification.md) §2 / §3。录屏与截图未产出（无法起栈）。真实模型评测未执行（无百炼凭据）。

## 6. 测试与提交规则

每个工作包在同一变更中包含直接相关测试和文档，完成验证后创建对应 Git commit。大工作包可拆成多个可验证提交，不把未验证状态标为 DONE。

| 改动范围 | 必须执行的相关验证 |
|---|---|
| 文档与计划 | `pwsh tests/validate_documents.ps1`、本地链接与内容人工复核、`git diff --check` |
| 配置、依赖、镜像 | 配置与适配器单测、锁文件一致性、运行镜像导入与配置探针 |
| 证据、报告、模型 | 相关 pytest、Ruff、mypy、引用反例与失败分类测试、实际输出评测 |
| 前端页面 | typecheck、Vitest、构建、相关 Playwright 场景及视觉检查 |
| 发布候选 | 完整 `project.ps1 verify`、显式真实模型评测、演示复核与发布记录 |

真实模型检查依赖有效凭据与确定的调用预算；缺失时记录为未执行或阻塞，继续完成独立的代码、测试和样本准备。跳过集成测试不能替代发布验收。

每项完成时记录：实施 commit、验证命令、测试结果、评测或演示产物路径，以及剩余限制。沿用 `codex/` 分支前缀和仓库提交规范。

## 7. 风险、调整与里程碑

| 情况 | 处理方式 |
|---|---|
| 真实模型凭据或调用预算尚未就绪 | 先完成镜像、Schema、失败测试、样本标注与回放入口；保留真实效果验收为未完成 |
| 匹配解释质量低于门槛 | 按字段、岗位和错误类型分析；优先修复证据输入、Prompt 和校验策略，记录前后对比 |
| 模型或向量版本变化 | 保存版本快照并重新生成对应向量；重新运行检索与报告回归 |
| 新模型能力影响审批或恢复 | 阻止发布，优先恢复既有不变量测试与业务行为 |
| 工期超过上限 | 优先缩减个性化面试题、额外图表和视觉装饰；证据正确性、审批和关键评测仍须完成 |

里程碑按验收而非日期关闭：

- M1：PORT-001、002 完成。运行配置可复现，结论与证据逐项对应。
- M2：PORT-003、004 完成。真实模型链路可演示，效果与失败可解释、可回放。（真实模型效果验收待凭据）
- M3：PORT-005、006 完成。关键页面易读，发布验证与作品集材料齐备。**已达成**，其中浏览器 / 投屏检查与真实模型评测两项限制见 [`docs/release/verification.md`](./docs/release/verification.md) §5。

状态流转使用 BACKLOG → READY → IN_PROGRESS → VERIFYING → DONE；依赖无法满足时记录 BLOCKED 及解除条件。每次状态更新附验证证据，不仅修改表格状态。

## 8. 第一批执行清单

- [x] 从 PORT-001 开始，修复 `httpx2` 运行依赖与锁文件。
- [x] 增加运行镜像导入测试，验证仅安装生产依赖也能加载真实适配器。
- [x] 补齐 Compose 的模型配置透传，并检查 API / Worker 一致性。
- [x] 为本地 `.env` 污染配置测试补充隔离与回归用例。
- [x] 校准 README 的当前能力及版本说明，完成相关检查并提交。
- [x] 开始 PORT-002，先用“第一条证据无关”的反例固定正确行为，再调整证据绑定实现。
- [x] 开始 PORT-003，先用“模型引用必须能被服务端定位”的反例固定正确行为，再接入解释网关与 MatchRun。
- [x] 开始 PORT-004，把抽取、检索、报告与注入交给真实链路，并按来源分节报告；真实模型评测待凭据就绪后补验。
- [x] 开始 PORT-005，按“姓名与摘要 → 结论直达原文 → 时间线可读 → 审批信息完整 → 模型模式与恢复指引”五个可验证提交推进。
- [x] 开始 PORT-006，整理演示脚本、架构图与评测报告，并在同一版本上完成发布检查与 tag。
