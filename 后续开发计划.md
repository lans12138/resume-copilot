# Resume Copilot 后续开发计划

- 制定日期：2026-09-18
- 目标：AI Agent 岗位求职作品集与演示质量
- 调研代码基线：`9cae04d`
- 团队假设：1 名开发者
- 预计投入：13–18 个有效开发日，另留 2 天发布缓冲
- 当前状态：规划完成，以下工作包均未开始

本轮以证据可信度、真实模型效果和演示可读性为主线。已有的审批、幂等、异步任务和恢复机制作为工程基础继续保留，新增能力沿现有边界接入。

本文是调研基线之后的执行计划。[编码实现计划](./编码实现计划.md) 保留 IMP / FIN 阶段的历史记录；后续作品集增强工作使用本文的 PORT 编号跟踪。本文中的验收标准均为待完成目标，不代表当前已经达标。

## 1. 交付目标与范围

本轮完成后，应能围绕一条完整招聘流程回答以下问题，并提供代码、测试、评测或演示证据：

1. 模型在什么环节发挥作用，确定性规则负责什么？
2. 每条匹配结论由哪些简历原文支持，证据缺失或矛盾时如何处理？
3. HR 如何修改或否决建议，系统如何保证审批前没有业务副作用？
4. 服务中断、消息重复或 SSE 断线后，系统如何恢复？
5. 对独立标注样本，抽取、检索与解释的效果如何，失败在哪里？

本轮交付物包括：可复现的真实模型运行链路、逐项证据绑定、版本化效果评测、关键页面优化、演示视频、现场脚本和发布记录。

保留电子 PDF / DOCX、合成数据、单机 Compose、MockSchedule 和现有自研运行时边界。运行时选择沿用 [ADR-0001](./docs/adr/0001-agent-runtime-custom-engine-over-langgraph.md)。OCR、多租户、真实日历、复杂多分支 Agent 和大规模索引优化列入后续候选，不占用本轮关键路径。

## 2. 调研基线与验证范围

| 项目 | 2026-09-18 调研结果 | 解释与限制 |
|---|---|---|
| 核心流程 | 已有上传、解析、校对、检索、MatchRun、ApplicationRun 双审批与面试流程代码 | 本轮调研未重新执行完整浏览器流程 |
| 服务就绪 | 公开入口的 readiness 返回 ready，PostgreSQL、Redis、Storage 均为 up | 就绪检查不等于业务流程验收 |
| 后端测试 | 隔离本地 `.env` 后，568 passed、17 skipped | 跳过项需要 PostgreSQL / Redis 等集成环境 |
| 测试环境隔离 | 从带本地 `.env` 的仓库工作目录运行时，3 项配置测试失败；从隔离工作目录运行全部通过 | 需要让测试显式隔离本地配置，而非依赖运行目录 |
| 离线评测 | 现有门禁 PASS | 内置语义与注入评测主要验证预置结果的计分机制 |
| 文档与结构 | 现有文档、仓库结构检查通过 | 当前检查不能识别所有文档与实现的语义偏差 |
| 尚未复核 | 前端单测、构建、浏览器 E2E、完整 `project.ps1 verify`、真实模型效果 | 本表不得被引用为这些项目本次已通过的证明 |

以上数字是调研时的历史快照。每个工作包完成后记录自己的 commit、命令和结果；发布前以同一代码版本重新生成最终基线。

## 3. 已确认的问题与影响

| 问题 | 实现依据 | 对作品集的影响 | 对应工作包 |
|---|---|---|---|
| 真实模型运行依赖与配置缺失 | [pyproject.toml](./pyproject.toml) 将 `httpx2` 放在 dev 依赖；[运行镜像](./deploy/docker/backend.Dockerfile) 安装生产锁文件；[Compose](./compose.yaml) 未传入模型端点与凭据。运行容器中也确认依赖与配置未就绪 | 不能仅凭切换 Mock 开关完成真实模型演示 | PORT-001 |
| 报告分数来自确定性规则 | [报告服务](./backend/app/reports/service.py) 根据快照排名与硬规则计算综合分；[面试题](./backend/app/interviews/questions.py) 使用固定模板 | 需要准确展示规则能力与模型能力的边界 | PORT-002、003 |
| 结论与证据未逐项对应 | [报告服务](./backend/app/reports/service.py) 为有引用的 SUPPORTED 结论统一使用第一条证据 | 引用归属正确不足以证明学历、技能、年限等结论被支持 | PORT-002 |
| 效果评测尚未贯穿实际输出 | [语义评测](./backend/app/evaluations/semantic.py) 使用相同的内置预测与标准答案；[注入评测](./backend/app/evaluations/injection.py) 使用预置 clean / injected 分析；[执行器](./backend/app/evaluations/executor.py) 调用这些内置评测 | 现有高分不能作为真实模型质量或抗注入效果的证明 | PORT-004 |
| 排名信息不便讲解 | [RankingTable](./apps/web/src/components/RankingTable.tsx) 显示 Profile 短 ID；[RunTimeline](./apps/web/src/components/RunTimeline.tsx) 仍展示部分内部节点与消息键 | 演示者需要额外解释候选人身份与技术字段 | PORT-005 |
| 文档存在历史口径漂移 | README 中的部分前端库不在当前依赖中；编码计划仍保留旧 PARTIAL 描述；发布标签与包版本口径需说明 | 面试时容易出现文档宣称与代码事实不一致 | PORT-001、006 |

## 4. 工作包与执行顺序

| 编号 | 优先级 | 工作包 | 估算 | 依赖 | 状态 |
|---|---|---|---|---|---|
| PORT-001 | P0 | 运行配置、依赖、测试隔离与文档校准 | 1–2 天 | 无 | DONE |
| PORT-002 | P0 | 逐项证据绑定与评分语义 | 2–3 天 | PORT-001 | READY |
| PORT-003 | P1 | 真实模型匹配解释闭环 | 3–4 天 | PORT-002 | BACKLOG |
| PORT-004 | P1 | 实际输出评测与录制回放 | 3–4 天 | PORT-003 | BACKLOG |
| PORT-005 | P1 | 关键页面与演示体验 | 2–3 天 | PORT-002、003、004 | BACKLOG |
| PORT-006 | P1 | 作品集材料与发布验收 | 2 天 | PORT-001 至 PORT-005 | BACKLOG |

推荐按编号推进，任一时刻保留一个主工作包为 IN_PROGRESS。PORT-004 的样本标注可在 PORT-003 调试前准备，避免根据模型输出反向修改标准答案；正式效果验收需等待实际调用链完成。

按每周 5 个有效开发日计算，开发约需 3–4 周，发布缓冲另计。估算包含直接相关测试，不包含等待模型凭据、网络恢复或评测预算确认的时间。

## 5. 分项实施与验收

### PORT-001：运行配置、依赖、测试隔离与文档校准

实施内容：

- 将真实模型适配器所需的依赖纳入运行依赖，使用项目锁定流程更新生产与开发锁文件。
- 梳理 Settings、`.env.example`、Compose 的配置对应关系，补齐模型端点、密钥、模型名、超时、向量维度等必要变量。
- 统一 SSE 心跳的默认值与容器行为，同时验证在线撤权响应，避免只改配置却改变已有契约。
- 让配置测试显式隔离 `.env` 与环境变量；增加存在本地配置时的回归覆盖。
- 校准 README 的实际技术栈、模型接入边界和测试基线；说明发布标签、后端包、前端包及 API 版本的关系。

验收标准：

- [x] 运行镜像能导入真实适配器，不依赖开发镜像中的额外包。
- [x] API 与 Worker 使用一致的模型配置；缺必要配置时明确失败。
- [ ] 在已配置有效凭据的环境中完成 Chat 与 Embedding 诊断；日志不输出凭据。（凭据未就绪，仅完成离线部分：哨兵密钥不回显、失败被分类）
- [x] 有无本地 `.env` 均不影响配置单测结论。
- [x] 文档说明与实际依赖、配置及模型能力一致。

验证记录（2026-09-18，基线 `25aeca7` 之后的工作区）：

| 项 | 命令 / 方式 | 结果 |
|---|---|---|
| 运行依赖与锁文件 | `pip-compile --extra=dev` / `--strip-extras` 重生成双锁文件，二次解析稳定 | `httpx2==2.12.0` 进入 `requirements.lock`（含 `httpcore2`、`truststore`），两个锁文件版本一致 |
| 运行镜像导入 | `docker build --target runtime` + 容器内 `import httpx2` 与三个适配器模块 | 通过；镜像内无 `pytest` / `mypy` / `ruff` |
| 真实分支解析 | 容器内 `MOCK_MODEL_MODE=false` 调 `build_model_gateway` | 解析到 `qwen_chat` 网关，未退回 FakeModel；哨兵密钥未回显 |
| 配置透传一致性 | `docker-compose config` 对比 api / worker / scheduler | 三者模型配置逐项一致；`SSE_HEARTBEAT_SECONDS` 生效值 1 |
| 配置测试隔离 | 挂载含本地 `.env` 的工作目录跑 `pytest -q` | 576 passed / 17 skipped；移除 `tests/conftest.py` 的对照实验为 4 failed，证明隔离有效 |
| 缺配置明确失败 | `tests/unit/test_settings.py` 新增用例 | 空值按未配置处理，`MOCK_MODEL_MODE=false` 且缺端点/密钥时启动即失败 |
| 文档口径 | 人工复核 README / 环境配置清单与代码事实 | 前端技术栈、后端行数、测试基线、SSE 心跳、版本口径均已校准 |

未执行：`pwsh tests/validate_documents.ps1`、`pwsh scripts/project.ps1 verify`（本机无 PowerShell 7，PowerShell 工具在本环境无法执行外部进程）。新增探针 `tests/validate_runtime_model_adapter.ps1` 已接入 `verify`，其断言已用等价的 `docker` 命令逐条手工执行并全部通过。

### PORT-002：逐项证据绑定与评分语义

实施内容：

- 沿结构化字段、硬规则、报告结论的链路传递证据来源，支持一个结论引用多个相关片段。
- 移除所有结论默认引用第一条证据的策略；保留引用归属、版本、范围与精确摘录校验。
- 处理字段缺失、原文矛盾、时间区间不完整和证据跨段等情况；不以引用存在推导 SUPPORTED。
- 明确区分检索排序分、确定性规则结果与模型解释，避免将排名换算分表达为模型置信度。

验收标准：

- [ ] 学历、技能和年限结论分别定位到支持它们的原文。
- [ ] 第一条证据无关时，系统选择正确片段或降级支持等级。
- [ ] 缺失、矛盾、跨候选人、旧版本和越界引用均有反例测试。
- [ ] 高影响且证据不足的结论输出“不足以判断”。
- [ ] 页面文案和 API 字段能解释各类分数的含义。

### PORT-003：真实模型匹配解释闭环

实施内容：

- 在现有抽取与 Embedding 适配器基础上增加匹配解释契约，输入岗位要求和候选人证据，输出受 Schema 约束的结论与引用。
- 将解释能力接入 MatchRun；模型不能决定权限、审批状态、业务 ID 或副作用参数。
- 为解释调用记录模型、Prompt、规则版本、耗时、重试与可获得的 Token usage；不记录密钥。
- 明确超时、限流、畸形响应和引用无效时的行为；规则输出与真实模型输出分别标识。
- 使用实际 Embedding 重新生成本轮评测样本的向量，避免将种子哈希向量混入真实效果结论。

验收标准：

- [ ] 完成合成文件上传→模型抽取→人工校对→真实向量检索→模型解释→双审批的链路。
- [ ] 同一候选人针对不同岗位得到与岗位相关、可追溯的解释。
- [ ] 模型生成的引用在持久化前通过服务端校验。
- [ ] 429、超时、Schema 错误与非法引用有明确结果及自动化覆盖。
- [ ] 模型异常不会越过审批或触发重复副作用。

个性化面试问题作为本工作包的可选增强；匹配解释与证据校验未完成前不扩展该项。

### PORT-004：实际输出评测与录制回放

实施内容：

- 建立由原始合成简历与岗位输入驱动的评测入口，将实际抽取、检索、报告输出交给计分器。
- 初始样本建议覆盖 30–50 份合成简历、3–5 类岗位、至少 30 条独立标注结论和 20 组原始 clean / injected 文本；按开发集与保留评测集分开使用。
- 覆盖同义技能、缺失字段、跨段证据、相互矛盾的描述、经验时间重叠和无关证据。
- 注入文本必须经过实际分析链路；分别记录模型是否遵循攻击内容与系统是否允许越权、副作用或审批绕过。
- 保存数据集、代码、模型、Prompt、配置版本及必要的原始响应，录制回放复用实际解析与校验路径。
- 普通 CI 保留无外部凭据的确定性测试与回放；增加显式触发的真实模型评测入口，并配置调用预算和停止条件。

验收标准：

- [ ] 报告区分计分器测试、录制回放和真实模型评测，不混用结果。
- [ ] 预测来自实际运行或录制响应，标准答案来自独立标注。
- [ ] 抽取报告包含字段级 Precision / Recall / F1；检索与语义报告包含样本量及失败案例。
- [ ] 在保留评测集上报告 Recall@K、MRR、nDCG、支持标签精确率与宏平均 F1。
- [ ] 记录总耗时、可获得的 Token usage；费用仅按明确记录的计价版本估算。
- [ ] 缺少录制、样本为空或评测入口故障时明确失败，不产生通过结论。

沿用仓库现有门槛作为待验证目标：Recall@K ≥ 0.85、MRR ≥ 0.70、nDCG@K ≥ 0.80、SUPPORTED 精确率 ≥ 0.95、支持等级宏平均 F1 ≥ 0.85、非法引用数为 0、各类攻击成功数为 0。抽取质量、延迟和成本先建立基线，再在保留评测前确定门槛。未达标时保留失败结果、分类修复并重新评测，不调整标签或阈值来制造通过。

### PORT-005：关键页面与演示体验

实施内容：

- 候选人排名显示姓名及必要摘要；Profile ID 保留为辅助追踪信息。
- 报告按候选人组织，支持从结论直接查看原文、页码或段落。
- 时间线显示可读的节点名称、状态与失败原因；技术标识放入辅助详情。
- 审批页明确显示拟执行动作、修改前后差异、当前版本和执行结果。
- 标明当前模型模式和结果来源；优化异步等待、重试、空结果与错误后的恢复指引。

验收标准：

- [ ] 演示者无需记忆 Profile ID 即可串起候选人、排名、报告和审批。
- [ ] 查看证据无需手工切换多页寻找原文。
- [ ] 重试、等待、完成、失败和撤权状态易于区分。
- [ ] 新增界面行为通过相关组件测试和关键浏览器场景。
- [ ] 在实际投屏分辨率下检查长文本、滚动区域和审批操作。

### PORT-006：作品集材料与发布验收

实施内容：

- 更新 [演示脚本](./演示脚本.md)，提供 5 分钟录屏版和 10 分钟现场版。
- 以一个候选人的推荐理由、证据定位、人工修改或否决、故障恢复串联演示。
- 准备一张架构图、一份可复现评测报告，以及至少一个恢复后没有重复写入的案例。
- README 提供运行入口、实际技术栈、模型与规则边界、关键指标和复现步骤。
- 为外部模型不可用的现场准备清晰标识的 Mock 演示或录制材料，并说明其验证范围。
- 在同一代码版本完成最终检查，保存验证记录，再创建发布 commit 与版本 tag。

验收标准：

- [ ] 全新环境能够按文档启动并完成合成文件主流程。
- [ ] `pwsh scripts/project.ps1 verify` 通过，包含 PostgreSQL / Redis 集成、前端构建与浏览器验证。
- [ ] 模型效果报告能追溯到发布候选 commit 和评测版本。
- [ ] 录屏、截图、指标与当前代码一致，公开材料只含合成数据。
- [ ] 副作用审批、重复投递、取消、恢复与 SSE 撤权契约没有回归。
- [ ] 发布材料说明真实模型与 Mock 模式分别能证明什么。

## 6. 测试与提交规则

每个工作包在同一变更中包含直接相关测试和文档，完成验证后创建对应 Git commit。大工作包可拆成多个可验证提交，不把未验证状态标为 DONE。

| 改动范围 | 必须执行的相关验证 |
|---|---|
| 文档与计划 | `pwsh tests/validate_documents.ps1`、本地链接与内容人工复核、`git diff --check` |
| 配置、依赖、镜像 | 配置与适配器单测、锁文件一致性、运行镜像导入与配置探针 |
| 证据、报告、模型 | 相关 pytest、Ruff、mypy、引用反例与失败分类测试、实际输出评测 |
| 前端页面 | typecheck、Vitest、构建、相关 Playwright 场景及视觉检查 |
| 发布候选 | 完整 `project.ps1 verify`、显式真实模型评测、演示复核与发布记录 |

真实模型检查依赖有效凭据与确定的调用预算；缺失时记录为未执行或阻塞，继续完成独立的代码、测试和样本准备。跳过集成测试不能替代发布验收。

每项完成时记录：实施 commit、验证命令、测试结果、评测或演示产物路径，以及剩余限制。沿用 `codex/` 分支前缀和仓库提交规范。

## 7. 风险、调整与里程碑

| 情况 | 处理方式 |
|---|---|
| 真实模型凭据或调用预算尚未就绪 | 先完成镜像、Schema、失败测试、样本标注与回放入口；保留真实效果验收为未完成 |
| 匹配解释质量低于门槛 | 按字段、岗位和错误类型分析；优先修复证据输入、Prompt 和校验策略，记录前后对比 |
| 模型或向量版本变化 | 保存版本快照并重新生成对应向量；重新运行检索与报告回归 |
| 新模型能力影响审批或恢复 | 阻止发布，优先恢复既有不变量测试与业务行为 |
| 工期超过上限 | 优先缩减个性化面试题、额外图表和视觉装饰；证据正确性、审批和关键评测仍须完成 |

里程碑按验收而非日期关闭：

- M1：PORT-001、002 完成。运行配置可复现，结论与证据逐项对应。
- M2：PORT-003、004 完成。真实模型链路可演示，效果与失败可解释、可回放。
- M3：PORT-005、006 完成。关键页面易读，发布验证与作品集材料齐备。

状态流转使用 BACKLOG → READY → IN_PROGRESS → VERIFYING → DONE；依赖无法满足时记录 BLOCKED 及解除条件。每次状态更新附验证证据，不仅修改表格状态。

## 8. 第一批执行清单

- [x] 从 PORT-001 开始，修复 `httpx2` 运行依赖与锁文件。
- [x] 增加运行镜像导入测试，验证仅安装生产依赖也能加载真实适配器。
- [x] 补齐 Compose 的模型配置透传，并检查 API / Worker 一致性。
- [x] 为本地 `.env` 污染配置测试补充隔离与回归用例。
- [x] 校准 README 的当前能力及版本说明，完成相关检查并提交。
- [ ] 开始 PORT-002，先用“第一条证据无关”的反例固定正确行为，再调整证据绑定实现。
