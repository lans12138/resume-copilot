# 企业招聘 Copilot 编码实现计划

> 计划版本：1.1（实际进度校准版）
>
> 制定日期：2026-09-01
>
> 最近校准日期：2026-09-07
>
> 校准代码基线：`07f18b8 test(e2e): cover full recruitment journey`
>
> 计划周期：6 周，共 30 个有效开发日
>
> 团队假设：1 名全职开发者
>
> 需求基线：[需求分析 1.2](./需求分析.md)
>
> 架构基线：[技术栈选型与架构决策](./技术栈选型与架构决策.md)
>
> 概要设计：[概要设计说明书 1.0](./概要设计说明书.md)
>
> 详细设计：[详细设计说明书 1.0](./详细设计说明书.md)
>
> 可行性依据：[项目可行性分析 1.3](./项目可行性分析.md)

## 1. 计划目标

本计划用于把已经完成的软件设计转化为可执行的编码、测试和交付安排。六周结束时必须形成一个可以在干净环境中启动、演示、测试和追踪问题的作品集级 MVP，完成以下纵向闭环：

~~~text
登录与岗位授权
  → 岗位创建和版本管理
  → PDF/DOCX 上传、解析、校对和确认
  → 三路检索、RRF 和硬规则评估
  → MatchRun 证据化报告
  → ApplicationRun 人工审批
  → 面试问题和 Mock 排期
  → SSE 进度、恢复、评测和演示
~~~

编码阶段的成功标准不是“页面和接口数量完成”，而是关键流程可验证：状态、权限、证据、副作用、取消、恢复和评测均达到需求发布门槛。

## 2. 计划假设与边界

### 2.1 时间与容量

- 使用 30 个有效开发日，不绑定具体自然周起止日期。
- 30 个开发日中，工作包理想工期合计 27 天；其余 3 天中，0.5 天用于第 1 周技术探针，2.5 天用于跨周波动和发布加固。最后 2 天保留为显式缓冲，不再开始新功能。
- 每天按 6 小时专注开发、1 小时测试/修复、1 小时记录与计划调整估算。
- 外部模型真实评测不阻塞普通编码；日常 CI 使用 FakeModel 或录制响应。
- 某阶段提前完成时，优先补测试、可观测性和文档，不提前引入范围外功能。

### 2.2 MVP 不变边界

本计划不加入 OCR、真实日历、企业 SSO、Refresh Token、多租户、HNSW 默认索引、Kubernetes、生产高可用、真实招聘数据或候选人自动合并。

以下能力不能因进度压力削减：

1. 证据归属、摘录范围和语义支持验证。
2. JobAssignment 资源级授权和 SSE 持续授权。
3. ApplicationRun 活动槽、Approval、幂等和副作用隔离。
4. Checkpoint 恢复、协作式取消和 Approval 过期。
5. Prompt Injection 配对回归和核心自动化测试。

### 2.3 优先级

| 级别 | 定义 | 处理原则 |
|---|---|---|
| P0 | 核心纵向闭环或安全/一致性门禁。 | 未完成不能发布。 |
| P1 | 明显改善演示、可维护性或诊断体验。 | P0 稳定后实现。 |
| P2 | 展示增强或未来演进能力。 | 仅使用明确剩余时间。 |

ECharts 综合看板、React Flow 高级交互、管理员可视化用户管理、HNSW 调优和 Langfuse 实验对比页面均为 P2。

## 3. 实施原则

### 3.1 纵向切片

每个工作包应尽可能包含数据库/Schema、应用服务、API 或任务、最小 UI、自动化测试和必要文档。禁止先连续数周只建后端，最后才首次连接前端和 E2E。

每周末必须有可运行演示：

- 第 1 周：登录并创建、查看岗位。
- 第 2 周：上传简历并完成 Profile 校对确认。
- 第 3 周：对岗位执行可量化检索。
- 第 4 周：运行 MatchRun，并暂停/恢复 ApplicationRun 审批。
- 第 5 周：通过前端完成审批、模拟排期和 SSE 展示。
- 第 6 周：从干净环境完成发布演示和故障恢复演示。

### 3.2 测试伴随实现

测试不是独立的末期阶段。每个工作包在同一提交中包含相应测试：

- 状态、Schema、RRF、硬规则和证据算法先写单元测试。
- 数据库约束、锁、事务、pgvector 和 Celery 使用集成测试。
- API 权限、错误和 OpenAPI 使用契约测试。
- 每周纵向演示路径至少有一个可重复的 smoke/E2E 测试。
- 真实模型差异使用离线评测，不放入不稳定的普通 CI。

### 3.3 小步提交

- 一个 Git commit 只处理一个可说明的目标。
- 功能代码与直接相关测试放在同一 commit，不能先提交无测试实现。
- 文档或契约变化同时更新文档测试/OpenAPI 测试。
- 每次提交前执行受影响测试；每日结束前执行当前完整快速测试集。
- main 必须保持可构建；未完成实验放在短期分支，不向 main 提交已知失败状态。

建议提交格式：

~~~text
chore(scope): 建立工具或工程骨架
feat(scope): 完成一个可验收能力
fix(scope): 修复已复现问题并附回归测试
test(scope): 增强独立测试能力
docs(scope): 更新设计、契约或使用说明
refactor(scope): 保持行为不变的结构调整
~~~

## 4. 关键路径与依赖

### 4.1 关键路径

~~~text
工程骨架与数据库
  → Auth / JobAssignment
  → 文件解析与证据定位
  → READY Profile
  → Embedding / 三路检索 / RRF
  → AgentRun / AgentEvent / Checkpoint
  → MatchRun 证据化报告
  → ApplicationRun / Approval / 副作用
  → SSE / 前端闭环
  → E2E / 安全评测 / 发布材料
~~~

关键路径上的任何失败必须优先处理。展示增强不得占用关键路径问题的修复时间。

### 4.2 前置依赖

| 能力 | 必须先完成 |
|---|---|
| JobAssignment 授权 | User、JWT、Job。 |
| Profile 确认 | StorageBackend、Document、Parser、Candidate。 |
| 向量检索 | READY Profile、EvidenceChunk、Embedding。 |
| MatchRun | JobVersion、检索、AgentRun、AgentEvent。 |
| ApplicationRun | JobApplication、MatchReport、Checkpoint。 |
| Approval 执行 | ApplicationRun、状态迁移、授权、幂等。 |
| SSE | AgentEvent、Run 资源授权、Redis 通知。 |
| E2E | Compose、后端纵向能力、前端路由和固定测试数据。 |

### 4.3 技术探针

第 1 周必须完成以下短探针，失败时立即调整而不是拖到后期：

1. Python 3.12 容器中 Celery Worker 能连接 Redis。
2. PostgreSQL 安装 pgvector，SQLAlchemy 能迁移 `vector(1024)`。
3. 持久化 Checkpointer 能保存并恢复最小 interrupt（立项原文为「LangGraph PostgreSQL Checkpointer」；实际以自研 `SqlCheckpointer` 满足，见 [ADR-0001](docs/adr/0001-agent-runtime-custom-engine-over-langgraph.md)）。
4. FastAPI Fetch SSE 能携带 Bearer Token，Nginx 关闭响应缓冲。
5. PyMuPDF 和 python-docx 能从固定样本提取位置。
6. Qwen Chat/Embedding 适配器完成启动探测；无密钥时 FakeModel 可运行。

探针只验证集成风险，不在第 1 周建设完整功能。

## 5. 工作包总表

| ID | 工作包 | 优先级 | 估算 | 主要依赖 | 完成标志 |
|---|---|---:|---:|---|---|
| IMP-001 | 单仓工程骨架与开发命令 | P0 | 0.5 天 | 无 | API/Web/测试命令可运行。 |
| IMP-002 | 配置、错误、日志和请求关联 | P0 | 0.5 天 | IMP-001 | 启动校验、统一错误和 request_id 可测。 |
| IMP-003 | Compose、PostgreSQL、Redis、Volume | P0 | 0.5 天 | IMP-001 | 核心服务健康。 |
| IMP-004 | SQLAlchemy、Alembic、CI 基线 | P0 | 0.5 天 | IMP-003 | 空库迁移和最小 CI 通过。 |
| IMP-005 | User、OAuth2 Password Flow、JWT | P0 | 0.75 天 | IMP-002、004 | form 登录和角色校验通过。 |
| IMP-006 | Job、JobVersion、JobAssignment | P0 | 1.25 天 | IMP-005 | 岗位和撤权 API/集成测试通过。 |
| IMP-007 | Web 骨架、登录和岗位页面 | P0 | 0.5 天 | IMP-005、006 | 浏览器完成首个纵向流程。 |
| IMP-008 | StorageBackend 与上传边界 | P0 | 0.75 天 | IMP-003、004 | PDF/DOCX 安全落盘和重复检测。 |
| IMP-009 | PDF/DOCX Parser 与 Locator | P0 | 1 天 | IMP-008 | 固定样本位置可回溯。 |
| IMP-010 | Candidate/Profile 抽取和校对 | P0 | 1 天 | IMP-009 | FakeModel 草稿可编辑确认。 |
| IMP-011 | EvidenceChunk 与文档/Profile 页面 | P0 | 1 天 | IMP-009、010 | UI 可定位证据。 |
| IMP-012 | Celery 文档任务、幂等和重试 | P0 | 0.75 天 | IMP-008 至 010 | 重复投递不重复写数据。 |
| IMP-013 | EmbeddingGateway 和向量持久化 | P0 | 0.75 天 | IMP-010、011 | 1024 维向量可查询。 |
| IMP-014 | 结构化、关键词和向量召回 | P0 | 1.25 天 | IMP-013 | 三通道结果可复现。 |
| IMP-015 | RRF、硬规则和快照 | P0 | 1 天 | IMP-014 | 排名稳定，FAIL 不被隐式过滤。 |
| IMP-016 | 检索 Golden Dataset/Harness | P0 | 1 天 | IMP-014、015 | Recall@10/MRR 报告可生成。 |
| IMP-017 | 候选人和检索前端 | P1 | 0.5 天 | IMP-014、015 | 筛选和排名可演示。 |
| IMP-018 | AgentRun、AgentEvent、Checkpoint | P0 | 0.75 天 | IMP-004 | 最小 Run 可恢复，事件有序。 |
| IMP-019 | MatchRun 图和候选快照 | P0 | 1 天 | IMP-015、018 | Top-K 每人执行固定节点。 |
| IMP-020 | 证据化报告和语义评测 | P0 | 1 天 | IMP-019 | 非法引用为 0，报告可查询。 |
| IMP-021 | ApplicationRun 活动槽和图 | P0 | 0.75 天 | IMP-018、020 | 并发第二个 Run 返回 409。 |
| IMP-022 | Approval、interrupt 和恢复 | P0 | 1 天 | IMP-021 | 决定一次、重启可恢复。 |
| IMP-023 | 协作取消和 Approval 过期 | P0 | 0.5 天 | IMP-021、022 | 竞态和 retry 测试通过。 |
| IMP-024 | 面试问题和 MockScheduleBackend | P0 | 1.25 天 | IMP-022、023 | 二次审批后只执行一次。 |
| IMP-025 | SSE 重放、持续授权和客户端 | P0 | 1.25 天 | IMP-018、006 | 断线恢复和在线撤权通过。 |
| IMP-026 | Run、Approval、Interview 前端闭环 | P0 | 1.25 天 | IMP-021 至 025 | 浏览器完成完整流程。 |
| IMP-027 | Prompt Injection 配对评测 | P0 | 0.75 天 | IMP-020、024 | 攻击成功指标全为 0。 |
| IMP-028 | E2E、故障恢复和并发回归 | P0 | 1.5 天 | IMP-001 至 IMP-027 | 核心 E2E 全部通过。 |
| IMP-029 | 观测、性能、健康和脱敏 | P0 | 1.5 天 | IMP-001 至 IMP-028 | 门槛报告和日志扫描通过。 |
| IMP-030 | README、演示数据、脚本和发布 | P0 | 1 天 | IMP-028、029 | 干净环境一键演示。 |

估算已包含直接相关测试，但第 1 周的六个技术探针另设 0.5 天 timebox。估算用于排序和暴露风险，不是承诺精确到小时。工作包超过 1.5 天仍未完成时必须拆分或触发范围调整。

## 6. 第 1 周：工程骨架、认证与岗位

### 6.1 本周目标

建立可持续开发的纵向骨架：Compose、数据库迁移、统一配置/错误/日志、CI、OAuth2 登录、岗位版本、JobAssignment 和最小 Web 页面。

### 6.2 每日安排

| 开发日 | 工作包 | 实现内容 | 同步测试/验证 |
|---|---|---|---|
| D01 | IMP-001、002 | 仓库目录、Python/Node 工程、Settings、AppError、request_id、结构化日志；启动容器/Celery 探针。 | 配置单测、错误映射、日志脱敏最小测试。 |
| D02 | IMP-003、004 | Compose、PostgreSQL/pgvector、Redis、Volume、Alembic、CI；在 0.5 天 timebox 内完成其余技术探针。 | `docker compose config`、空库升级、最小 API/DB 测试和探针记录。 |
| D03 | IMP-005 | User、Argon2、form-urlencoded token、JWT、当前用户依赖。 | 成功/失败/禁用用户/JSON 登录契约测试。 |
| D04 | IMP-006 | Job、JobVersion、状态迁移、乐观锁 API。 | 状态表、版本不可变、409 冲突集成测试。 |
| D05 | IMP-006、007 | JobAssignment、撤销授权、React 登录/岗位页、TanStack Query。 | 权限矩阵、撤权、Playwright 登录—创建岗位 smoke。 |

### 6.3 周出口门禁 G1

- 一条命令启动 web、api、worker、scheduler、postgres、redis。
- Alembic 可以从空库升级到 head。
- `/auth/token` 仅按 OAuth2 form 契约工作。
- HR 可创建/启用岗位并分配招聘主管；撤销后访问失败。
- Ruff、mypy、pytest、前端 typecheck/build 和最小 Playwright 通过。
- 已建立后续模块可复用的错误、日志、配置和测试 Fixture。

## 7. 第 2 周：简历解析、校对与证据定位

### 7.1 本周目标

完成文件到 READY CandidateProfile 的数据闭环，优先保证不可信文件边界、解析失败可见、人工校对和证据定位。

### 7.2 每日安排

| 开发日 | 工作包 | 实现内容 | 同步测试/验证 |
|---|---|---|---|
| D06 | IMP-008 | LocalVolumeStorage、流式 SHA-256、大小/MIME/签名、重复检测。 | 路径穿越、超限、伪扩展名、重复文件测试。 |
| D07 | IMP-009 | PyMuPDF、python-docx Parser、PDF/DOCX Locator、限制和错误分类。 | 固定 PDF/DOCX、加密/空文本/超限样本。 |
| D08 | IMP-010 | Candidate/Profile Schema、FakeModel 抽取、REVIEW_REQUIRED。 | null/枚举/日期/未知字段和模型非法输出测试。 |
| D09 | IMP-011 | Profile 编辑确认、READY/SUPERSEDED、EvidenceChunk、原文定位页面。 | 乐观锁、READY 唯一、跨文档证据拒绝。 |
| D10 | IMP-012 | Celery 解析/重试、operation_key、队列失败协调、文档队列页面。 | 重复投递、临时失败、手动重试和纵向 E2E。 |

### 7.3 周出口门禁 G2

- PDF/DOCX 上传立即返回，解析在 Worker 完成。
- 不支持、临时失败和重复文件可区分、可查询。
- HR 能对照原文校对并确认 Profile。
- 每个 EvidenceChunk 能回到 PDF 页或 DOCX 段落/表格字符范围。
- 未确认 Profile 不进入后续检索入口。
- 文档解析重复投递不创建重复 Profile 或 Chunk。

## 8. 第 3 周：Embedding、混合检索与评测

### 8.1 本周目标

完成可量化的候选人召回，建立 Golden Dataset，在 Agent 评分之前先证明检索输入可靠。

### 8.2 每日安排

| 开发日 | 工作包 | 实现内容 | 同步测试/验证 |
|---|---|---|---|
| D11 | IMP-013 | EmbeddingGateway、批处理、1024 维校验、内容哈希幂等。 | Fake Embedding、维度错误、重复生成测试。 |
| D12 | IMP-014 | 结构化和关键词召回、技能规范化、稳定排序。 | READY 过滤、中文技能/别名、边界数据测试。 |
| D13 | IMP-014、015 | pgvector 精确向量召回、RRF、排名快照。 | 各通道缺失、并列排序、配置快照测试。 |
| D14 | IMP-015、016 | PASS/FAIL/UNKNOWN 硬规则、Golden Dataset Schema、指标。 | UNKNOWN 不猜测、FAIL 不删除、指标公式单测。 |
| D15 | IMP-016、017 | Recall@K/MRR 报告、候选人列表和显式筛选；调试召回。 | 固定数据回归、筛选不改变 MatchRun 范围。 |

### 8.3 周出口门禁 G3

- 三路召回和 RRF 结果在固定输入下可复现。
- Top-K 保存 Profile 版本、通道排名、原分数和配置快照。
- 硬条件只输出 PASS/FAIL/UNKNOWN，不自动改变业务状态。
- Golden Dataset 可版本化，Recall@10 不低于 0.85，并报告 MRR 或 nDCG。
- 前端能展示候选人、显式筛选和各通道结果。

## 9. 第 4 周：双运行图、证据报告与审批核心

### 9.1 本周目标

完成 Agent 后端核心：AgentRun/AgentEvent/Checkpoint、MatchRun 证据化报告、ApplicationRun 活动槽、Approval interrupt/恢复、协作取消和超时。

### 9.2 每日安排

| 开发日 | 工作包 | 实现内容 | 同步测试/验证 |
|---|---|---|---|
| D16 | IMP-018 | AgentRun/Event 表、sequence、Checkpointer、Run service、Fake graph。 | 并发 sequence、业务事实与 Checkpoint 恢复测试。 |
| D17 | IMP-019 | MatchRun parse/retrieve/snapshot/hard-rule 节点和受控 fan-out。 | Node State/路由、FAIL 仍评分、单候选人失败隔离。 |
| D18 | IMP-020 | score/validate/persist、Report/Claim/Evidence、语义评测框架。 | 引用归属/范围/摘录、高影响降级、节点重放。 |
| D19 | IMP-021、022 | ApplicationRun 活动槽、human_review、Approval、interrupt 和决定。 | 并发创建 409、PENDING 唯一、版本/权限/幂等。 |
| D20 | IMP-022、023 | Checkpoint 恢复、协作取消、Approval 过期、retry 认领槽。 | 决定/取消/过期竞态、取消后 CAS 失败、重启恢复。 |

### 9.3 周出口门禁 G4

- MatchRun 对固定 Top-K 全量评估并保存证据化报告。
- MatchRun 不创建 Approval、不进入 WAITING_APPROVAL、不改变 JobApplication 状态。
- 同一 JobApplication 并发创建 ApplicationRun 时只有一个成功。
- Approval 只能决定一次；未审批副作用执行次数为 0。
- WAITING_APPROVAL 重启后从同一 thread_id 恢复。
- 取消后不提交节点结果，Approval 过期后 Run 可用新 attempt 重试。

如果 D20 结束时 G4 未通过，禁止开始 P2 页面和图表；第 5 周前两天优先修复 G4。

## 10. 第 5 周：面试、SSE 与前端闭环

### 10.1 本周目标

把后端核心串成用户可操作流程，完成面试问题、Mock 排期、SSE 持续授权和主要前端页面。

### 10.2 每日安排

| 开发日 | 工作包 | 实现内容 | 同步测试/验证 |
|---|---|---|---|
| D21 | IMP-024 | 面试问题 Schema、敏感主题校验、ScheduleProposal、MockScheduleBackend。 | 二次审批、非法参数、重复执行和执行失败恢复。 |
| D22 | IMP-025 | SSE 数据库重放、Redis 通知、心跳、Nginx 配置、持续授权。 | Last-Event-ID、丢通知、重放中/在线撤权测试。 |
| D23 | IMP-025、026 | Fetch SSE 客户端、Run Timeline、MatchRun 报告/证据页面。 | 去重、跳号、401/403、终态关闭前端测试。 |
| D24 | IMP-026 | ApplicationRun、Approval Diff、编辑/拒绝/冲突、Interview 页面。 | 409 刷新、过期、取消、键盘操作组件测试。 |
| D25 | IMP-026、028 | 完整前后端纵向串联，修复接口和状态缝隙。 | 登录至模拟排期 Playwright 主路径。 |

### 10.3 周出口门禁 G5

- 用户可在浏览器查看 MatchRun 排名、Claim、支持等级和原文证据。
- ApplicationRun 页面只展示当前可操作 Approval。
- 状态更新和 Mock 排期分别审批，重复请求只执行一次。
- SSE 断线重连不丢失或重复推进 UI。
- JobAssignment 在线撤销后不再发送业务事件并关闭连接。
- 403、409、422、依赖失败和网络断开有不同恢复提示和 request_id。

## 11. 第 6 周：加固、评测与发布

### 11.1 本周目标

停止扩展功能，集中完成安全、恢复、性能、可观测、E2E 和交付材料。D29、D30 不安排新的 P0 功能。

### 11.2 每日安排

| 开发日 | 工作包 | 实现内容 | 同步测试/验证 |
|---|---|---|---|
| D26 | IMP-028 | 完整 E2E、并发、重启、Redis 故障、Checkpoint 重放。 | 所有核心恢复和幂等场景。 |
| D27 | IMP-027、029 | 至少 20 组 Prompt Injection 配对评测、Tool 安全、日志/Trace 脱敏。 | 攻击指标为 0、敏感标记扫描。 |
| D28 | IMP-029 | API/任务/节点指标、健康检查、100 候选人基准和瓶颈修复。 | p95、事件延迟、召回回归和依赖降级。 |
| D29 | IMP-030 | README、配置示例、合成演示数据、启动/重置/演示脚本、架构图。 | 全新 Volume/空库按 README 完成演示。 |
| D30 | 缓冲与发布 | 只修阻塞问题、完整回归、版本标记、录屏备用。 | G6 全部门禁、工作区干净、提交可追踪。 |

### 11.3 周出口门禁 G6

- Ruff、mypy、pytest、前端 typecheck/test/build 和关键 Playwright 全部通过。
- 解析、检索、证据、Tool 和 Prompt Injection 指标达到发布门槛。
- 重启、断线、重复提交、取消、过期和撤权 E2E 全部通过。
- Compose 在干净环境启动，Alembic 从空库升级，健康检查通过。
- README 和演示脚本由新环境实际走通。
- 无 Secret、真实隐私数据或敏感测试标记进入 Git、日志和 Trace。
- 每项实现有对应测试和 Git commit，最终工作区干净。

## 12. 测试实施计划

### 12.1 测试层级随周次增长

| 周次 | 新增重点 | 每日快速集 | 周门禁集 |
|---|---|---|---|
| 1 | 配置、状态、Auth、Job、权限、迁移 | unit + 受影响 integration | lint/type + backend + web smoke |
| 2 | Storage、Parser、Profile、Celery 幂等 | unit + document integration | 文档纵向 E2E |
| 3 | Embedding、召回、RRF、硬规则、指标 | retrieval unit/eval | Golden Dataset 回归 |
| 4 | Node、Checkpoint、证据、审批、并发 | agent unit + DB integration | G4 Agent 安全恢复集 |
| 5 | Schedule、SSE、前端页面 | contract + component | Playwright 主流程 |
| 6 | 故障、安全、性能、发布 | 全量快速集 | 完整发布门禁 |

### 12.2 计划中的标准命令

第 1 周应把以下命令固化到 Makefile、Taskfile 或脚本；实际名称可调整，但 CI 与本地必须一致：

~~~text
ruff check backend tests
mypy backend
pytest -q
npm --prefix apps/web run typecheck
npm --prefix apps/web run test
npm --prefix apps/web run build
npm --prefix apps/web run e2e
docker compose config --quiet
alembic upgrade head
~~~

数据库集成测试必须使用 PostgreSQL，不用 SQLite 代替部分唯一索引、行锁、SKIP LOCKED、JSONB 和 pgvector 行为。

### 12.3 失败处理

- 受影响测试失败：不得提交，先修复或回退本次变更。
- 非受影响测试失败：先复现并记录，仍不得让 main 保持红色。
- 外部模型波动：不降低普通 CI 稳定性，转入版本化真实模型评测。
- 不稳定 E2E：先定位时序/数据隔离问题，不通过无限重试掩盖。
- 门槛指标下降：保存基线与本次报告，回滚配置或阻止合并。

## 13. Definition of Ready

一个工作包进入编码前必须满足：

1. 对应需求 ID、详细设计章节和优先级明确。
2. 输入/输出 Schema、状态迁移、权限和错误行为可说明。
3. 数据迁移及回滚/兼容策略已识别。
4. 正常、边界、失败、并发或安全测试场景已列出。
5. 外部依赖可用，或已经准备 Fake/Mock。
6. 估算不超过 1.5 天；更大工作包已拆分。
7. 不依赖尚未完成且未排期的隐含工作。

不满足 Ready 的事项先澄清或做短探针，不能直接以“边写边定状态机”的方式进入实现。

## 14. Definition of Done

一个工作包只有同时满足以下条件才完成：

1. 实现符合需求、架构和详细设计；偏差已先更新文档。
2. 数据库迁移、Pydantic/OpenAPI、前端类型和状态契约同步。
3. 新增或更新单元、集成、契约或 E2E 测试。
4. 受影响测试和当前完整快速测试集通过。
5. 权限、错误、日志、审计、幂等和恢复行为可观察。
6. 不产生非法证据、未审批副作用、取消后写入或 Prompt Injection 攻击成功。
7. 没有未解释的 TODO、临时 Secret、真实个人数据或跳过测试。
8. 文档和演示数据按需更新。
9. 创建一个范围清晰、可回滚的 Git commit。

## 15. Git 与变更管理

### 15.1 分支策略

单人开发保持 main 始终可运行。超过半天或需要实验的工作使用短期分支 `feature/imp-xxx-name`；完成 DoD 后合并。小型、低风险文档或修复可直接在 main 完成，但仍必须独立提交并验证。

### 15.2 提交粒度

推荐一个工作包形成 1 至 3 个可独立验证的 commit，例如：

~~~text
feat(documents): add validated local storage backend
feat(documents): parse pdf and docx locators
feat(documents): add profile review workflow
~~~

不接受“week 2 work”“misc fixes”一类无法追踪的提交。格式化产生的大范围机械改动与业务逻辑分开提交。

### 15.3 设计偏差

实现发现设计不可行时：

1. 记录事实、复现和受影响需求。
2. 比较最小修正方案，不直接扩大范围。
3. 先更新需求/设计/计划及对应文档测试。
4. 再提交实现和自动化回归。

## 16. 进度跟踪与例行检查

### 16.1 每日开始

- 确认 main 和 CI 为绿色。
- 选择一个 Ready 且优先级最高的工作包。
- 写出当天唯一的可演示结果和必须通过的测试。
- 检查是否影响数据库、API、State、事件或前端类型契约。

### 16.2 每日结束

- 执行受影响测试和快速测试集。
- 检查 `git status`，不遗留不明文件或 Secret。
- 创建对应 commit，并记录完成/未完成原因。
- 更新工作包状态、实际耗时、风险和下一步。
- 未完成内容留在短期分支，不把 main 置于已知失败状态。

### 16.3 每周复盘

每周末记录：计划完成率、门禁结果、失败测试、缺陷数、指标变化、下周依赖和剩余缓冲。是否“完成一周”以出口门禁为准，不以写了多少代码为准。

建议状态：BACKLOG、READY、IN_PROGRESS、BLOCKED、VERIFYING、DONE。任一时刻只保持一个主工作包为 IN_PROGRESS，降低单人并行切换损耗。

## 17. 风险与调整规则

| 风险/触发条件 | 影响 | 立即动作 | 可削减项 |
|---|---|---|---|
| 第 1 周技术探针失败超过 0.5 天 | 后续关键路径阻塞 | 冻结页面工作，先替换/收敛适配器。 | P2 全部。 |
| 文档解析准确率不足 | 证据和检索输入无效 | 增加固定失败样本、缩小支持版式、强化人工校对。 | 页面美化。 |
| Recall@10 第 3 周未达 0.85 | MatchRun 无可靠输入 | 分析各通道贡献、技能规范化和标注，不先加 HNSW。 | ECharts、React Flow。 |
| 第 4 周 G4 未通过 | 核心 Agent 不安全 | 第 5 周前两天只修活动槽、审批、取消和恢复。 | 高级前端交互。 |
| SSE 不稳定 | 进度体验受影响 | 保留 Run Detail 轮询降级，继续修重放/授权。 | 动画和图形化 Trace。 |
| 模型输出波动 | 测试和演示不稳定 | 固定 Prompt/模型版本，使用 Fake/录制响应，分离真实评测。 | 非关键模型文案。 |
| 外部服务不可用 | 集成阻塞 | 使用 FakeModel/MockSchedule/Langfuse off。 | 云端对比页面。 |
| D28 后仍有 P0 缺陷 | 发布风险 | 停止新功能，只做修复、回归和文档。 | 所有 P1/P2。 |

不得通过降低安全、证据、幂等、恢复或测试门槛来“追回进度”。

## 18. 里程碑验收矩阵

| 门禁 | 需求域 | 演示结果 | 自动化证据 |
|---|---|---|---|
| G1 | AUTH、JOB、OPS | 登录、岗位、分配和撤权。 | Auth/权限/迁移/Playwright smoke。 |
| G2 | DOC、CAN | 上传、解析、校对、确认和证据定位。 | Parser/Storage/Celery/Profile 集成。 |
| G3 | RET、EVAL | 三路召回、RRF 和硬规则。 | Golden Dataset 指标报告。 |
| G4 | AGT、APR | 双图、报告、interrupt、恢复、取消、过期。 | Node/DB 并发/Checkpoint/安全测试。 |
| G5 | INT、EVT、UX | 二次审批、Mock 排期、SSE 和完整页面。 | SSE 契约、组件和主路径 E2E。 |
| G6 | NFR、交付 | 干净环境演示和故障恢复。 | 全量 CI、评测、性能和脱敏报告。 |

## 19. 当前实际进度校准

### 19.1 校准口径

本节以代码、迁移、可执行命令和自动化测试为事实来源，不以文件存在、接口占位或 README 中的完成标记代替验收。状态含义如下：

- `DONE`：生产路径已接通，相关自动化验证通过，并有可追踪提交。
- `PARTIAL`：领域代码或测试已存在，但生产接线、页面、后台进程或关键故障验证仍不完整。
- `TODO`：设计中明确要求，但仓库尚无可运行实现。

每完成一个收尾任务，必须同时更新本节、20.2 总表和对应复选框；只完成部分子项时不得把整项标为 `DONE`。

### 19.2 代码基线事实

| 能力域 | 当前状态 | 已有事实 | 尚缺内容 |
|---|---|---|---|
| 工程骨架、配置、数据库、认证、岗位授权 | DONE | 空库迁移、PostgreSQL/Redis/Storage 探针、JWT、JobVersion、JobAssignment 和撤权测试已通过。 | 继续纳入最终全栈 Compose 与 CI。 |
| 检索、RRF、硬规则和证据报告 | DONE | 三路召回、Golden Dataset、MatchRun 快照、SQL 报告与证据查询已接入生产路由。 | 真实模型适配器与录制回放已由 FIN-008 落地；真实凭据下的连通性与录制评测仍待人工执行。 |
| ApplicationRun、双审批和后半段持久化 | DONE | `0010_persist_workflow_tail`、SQL Checkpoint、题集、状态历史、Interview、完成原因和活动槽均已持久化；两次 API 重启恢复验证通过。 | 请求级幂等仍需 FIN-001。 |
| 种子数据浏览器主路径 | DONE | Playwright 已覆盖登录、MatchRun、报告、ApplicationRun、两次审批和 Interview 页面。 | 当前依赖种子 Profile，不等同于真实上传闭环。 |
| 文档上传、解析、Profile 校对与证据定位 | PARTIAL | 安全上传、PDF/DOCX Parser、Profile/Chunk 领域服务和 Celery task 已有单元/集成覆盖。 | 无可运行 Worker；解析后 Profile 生产接线、Profile API、上传/校对页面和真实文件 E2E 未完成。 |
| HTTP 请求级幂等 | PARTIAL | Approval 业务键和副作用重复执行防护已存在。 | `Idempotency-Key` 参数未持久化，不能按相同请求返回原响应，也不能识别同 Key 不同请求。 |
| Celery Worker、Scheduler 与维护协调 | PARTIAL | Celery factory、队列路由、文档/Embedding/过期 task 已存在。 | `apps/worker`、`apps/scheduler` 仍是占位；Compose 无对应服务；Agent task、重发布和孤儿清理 task 未实现。 |
| MatchRun/ApplicationRun 异步执行 | DONE | 两类 Run 均由 `agent` 队列 Worker 认领执行；ApplicationRun 以 Checkpoint 版本和已执行 Approval 恢复，API 只提交业务事实并在提交后投递。 | QUEUED 重发布与租约扫描归 FIN-006。 |
| SSE | PARTIAL | PostgreSQL 重放、Redis 通知、前端客户端、去重/跳号/授权单测已存在。 | 尚未通过 Nginx 的 Bearer SSE 全栈探针，撤权和断线恢复缺浏览器级验证。 |
| Evaluation | PARTIAL | Golden、语义支持和 Prompt Injection 离线评测模块已存在。 | 无 Evaluation 数据表、迁移、REST API、Celery task 和结果页面。 |
| Qwen 与 LangGraph 技术对齐 | DONE | 真实 Qwen Chat/Embedding 适配器、重试分类传输层、录制回放与诊断命令均已落地（FIN-008）；Agent 运行时经 [ADR-0001](docs/adr/0001-agent-runtime-custom-engine-over-langgraph.md) 正式确认采用自研 `RunEngine` + `SqlCheckpointer`，偏差已闭环，5 份设计文档同步修正。 | 无。 |
| 部署、CI 与发布 | PARTIAL | `scripts/project.ps1 verify`、合成 seed 和 API 镜像可用。 | Compose 缺 web/nginx/worker/scheduler，仓库无完整 CI 流水线；README 完成状态早于实际实现。 |

### 19.3 当前已验证基线

- [x] Alembic 可从空库升级到 `0010_persist_workflow_tail`，21 张业务表与关键外键通过验证。
- [x] ApplicationRun 可跨两次 API 重启完成双审批、题集/面试持久化和活动槽释放。
- [x] Playwright 可用合成数据完成登录至 Mock 排期的浏览器主路径。
- [x] 当前 `verify` 门禁通过：后端、前端、迁移、Compose 探针、浏览器 E2E 和文档契约均为绿色。
- [x] Prompt Injection、检索指标、日志脱敏、并发与恢复已有离线/服务层回归。

上述基线只表示已经接通的能力稳定，不表示项目已经完工。项目状态在 FIN-001 至 FIN-013 全部关闭前保持 `IN_PROGRESS`。

## 20. 项目完工待办事项清单

### 20.1 使用规则

- FIN 编号是从当前代码基线到项目完工的唯一剩余工作清单；按依赖顺序执行，任一时刻只允许一项为 `IN_PROGRESS`。
- 每项不超过 1.5 个有效开发日；超过时必须拆分，并在本计划中增加新的 FIN 编号。
- 只有代码、测试、文档、完整验证和对应 Git commit 同时完成，才能把总表状态与全部子项改为 `DONE`。
- P0 全部完成前不开始 P2；P1 若决定不做，必须先形成设计偏差记录并同步需求、详细设计和 README，不能直接删除待办。
- 每次更新在“完成证据”中记录 commit、验证命令或报告路径，避免再次出现文档完成状态领先于代码的情况。

### 20.2 剩余工作总表

| ID | 优先级 | 状态 | 估算 | 工作项 | 主要依赖 | 完成证据 |
|---|---:|---|---:|---|---|---|
| FIN-001 | P0 | DONE | 1 天 | HTTP 请求级幂等闭环 | 当前审批事务 | commit 见正文；单测 `tests/unit/test_idempotency.py`，Postgres 集成 `tests/integration/test_idempotency_postgres.py`+`tests/validate_idempotency.ps1`。 |
| FIN-002 | P0 | DONE | 1 天 | Worker、Scheduler 与 Compose 运行基线 | Celery factory、Redis | Worker/Scheduler 入口、Compose（删不可靠 healthcheck）、Celery Beat（60s 过期扫描）、Worker/Beat 集成探针（`tests/validate_worker.ps1`）全部就绪，`verify` CI 转绿；关键 commits：86c7d7c(队列)、1dd24e6(模块形式启动)、ce75cc6(单事件循环任务)、dfca48c/3bd9bc6/f6ff305/b247539(探针正则↔shared_task 注册↔断言修复)。 |
| FIN-003 | P0 | DONE | 1.5 天 | 文档解析到 Profile/Embedding 后端闭环 | FIN-002 | 后端闭环（解析→Profile→证据→Embedding）已合入并单测转绿；真实 PostgreSQL/Redis E2E 由 `tests/validate_document_pipeline.ps1` 在 `verify` 内执行并通过（CI run 34831481488 日志含 `FIN-003 document pipeline end-to-end tests passed`）。关键 commits：3c01855/4f73d84/556569a/081b790。 |
| FIN-004 | P0 | DONE | 1.5 天 | 文档上传、Profile 校对和证据定位前端 | FIN-003 | 4 项验收全部达成。关键 commits：80a1d92/ef789ee/9392fd8（定位收窄、原文定位与字段编辑组件、文档与候选人路由）、245243d（浏览器上传—校对—READY 流程与探针日志转储）、c8a76fb/06c9a02/a9170c0（确认 500 的 flush、证据体契约、状态枚举单一来源）。验证：`tests/validate_web.ps1` → `flow=upload-review-readiness`；CI run 34831481488。 |
| FIN-005 | P0 | DONE | 1.5 天 | MatchRun/ApplicationRun 后台异步执行 | FIN-001、002 | MatchRun commit a4fe697；ApplicationRun 后台 START/RESUME、Checkpoint/Approval 双重恢复校验、提交后投递与 Worker 强杀恢复由本次 FIN-005 提交及 `tests/validate_application_entry.ps1` 验证。 |
| FIN-006 | P0 | DONE | 1 天 | Approval 过期、QUEUED 重发布和孤儿清理 | FIN-002、005 | 三项维护任务全部落地：过期批处理改为稳定排序 + `FOR UPDATE SKIP LOCKED` 批量领取并释放活动槽；`maintenance.republish_queued` 覆盖文档 `QUEUED` / MatchRun `CREATED` / 有 Checkpoint+已执行 Approval 的 ApplicationRun `RUNNING` 三条积压（第三条只能提议、无法越过人工闸门）；`maintenance.cleanup_orphan_files` 按「列表→年龄→引用→删除」顺序只删无引用且过安全窗口的对象。Worker 竞争、重复扫描、单条投递失败、Redis 恢复后的补齐均由单测 + 真 PostgreSQL 集成测试固定；新增 `tests/validate_maintenance.ps1` 接入 `verify`。 |
| FIN-007 | P0 | DONE | 1.5 天 | Evaluation 持久化、API、任务和最小页面 | FIN-002 | Dataset/Evaluation 三表 + 迁移 `0012`；`(dataset, config_hash)` 重复执行闸门与 202/404/409 语义；`evaluations.execute` 幂等 task；`/evaluations` 页面与离线门禁退出码 0/1/2。门禁空指标集判不通过，`passed=False` 与 `FAILED` 五层分离。真 PostgreSQL 集成测试与两个探针接入 `verify`。 |
| FIN-008 | P1 | DONE | 1 天 | Qwen Chat/Embedding 适配器与诊断命令 | FIN-003、007 | OpenAI 兼容的 Chat/Embedding gateway；`mock_model_mode` 是唯一开关（此前两个工厂对该开关给出不一致答案：一个静默返回 Fake、一个抛 `NotImplementedError`）。重试/分类策略集中在 `http_transport.py`，schema 违例判永久以免浪费重试预算。诊断命令 `python -m backend.app.infrastructure.model_diagnose` 退出码 0/1/2 且绝不打印凭据。`recording.py` 录制原始回复 + 回放走真实解析路径；三个内置合成数据集首次真正注册进生产路径。新增 `tests/validate_model_adapter.ps1` 接入 `verify`。 |
| FIN-009 | P1 | DONE | 1.5 天 | LangGraph 运行时对齐或正式设计偏差收敛 | FIN-005 | [ADR-0001](docs/adr/0001-agent-runtime-custom-engine-over-langgraph.md) 正式确认保留自研 `RunEngine` + `SqlCheckpointer`：零依赖事实核查、备选方案成本对比（接线面积远大于引擎本身）、5 项重新评估触发条件。5 份文档精准修正并回指 ADR。新增 `tests/unit/test_fin009_adr_contract.py` 20 项固化 ADR 事实与「业务表为事实源 / 已执行审批不重放」不变量。全量 `549 passed`。 |
| FIN-010 | P0 | TODO | 1.5 天 | 非种子文件的端到端浏览器主路径 | FIN-004、005 | 待补 commit + 上传至排期 Playwright。 |
| FIN-011 | P0 | TODO | 1.5 天 | 故障、取消、过期、撤权与 SSE 浏览器矩阵 | FIN-001、005、006、010 | 待补 commit + 故障 E2E 报告。 |
| FIN-012 | P0 | TODO | 1.5 天 | web/nginx/api/worker/scheduler 完整 Compose 与 CI | FIN-002 至 011 | 待补 commit + 全新环境 CI。 |
| FIN-013 | P0 | TODO | 1 天 | 发布文档校准、最终演示、版本标记 | FIN-001 至 012 | 待补 release commit + tag。 |

剩余理想工期为 10.5 个有效开发日，另保留 2 天发布缓冲。估算不包含等待真实模型凭据或外部服务审批的时间。

### 20.3 分项验收清单

#### FIN-001：HTTP 请求级幂等闭环

- [x] 新增幂等记录模型和迁移，保存 operation、actor_id、key、request_hash、响应或资源引用及生命周期。
- [x] 相同 Key + 相同请求返回首次结果；相同 Key + 不同请求返回 `IDEMPOTENCY_KEY_REUSED`/409。
- [x] Approval 页面在同一次提交/网络重试中复用 Key，不因重新渲染或重试生成新 Key。
- [x] 覆盖并发双击、响应丢失、API 重启和重复副作用为 0/1 次的 PostgreSQL 集成测试。

#### FIN-002：Worker、Scheduler 与 Compose 运行基线

- [x] 实现 `apps/worker` 和 `apps/scheduler` 可执行入口（`python -m backend.worker`/`backend.scheduler`），Celery autodiscover 注册全部 task 模块。
- [x] Compose 增加 worker/scheduler、依赖、队列和共享 Storage 配置（healthcheck 改为功能探针断言，见下）。
- [x] 配置 Celery Beat：Approval 每分钟过期（`maintenance.expire_approvals`）；QUEUED/孤儿清理为 FIN-006 范围。
- [x] 增加 Linux 容器 Worker 连接 Redis、消费测试任务和 Beat 发布任务的集成探针（`tests/validate_worker.ps1`，`verify` CI 已转绿）。

#### FIN-003：文档解析到 Profile/Embedding 后端闭环

- [x] 将上传后的 `documents.parse`、Profile 抽取、REVIEW_REQUIRED 草稿和证据切分接入同一生产状态链（`ProfileExtractionService` / `ProfileReviewService` / `embeddings.generate_chunks`）。
- [x] 增加 Profile 查询、编辑确认、EvidenceChunk 查询/定位 API，并落实 HR 权限（`JobService.get_authorized`）和乐观锁（`confirm_profile(expected_version=)`）。
- [x] Profile 确认提交后发布 Embedding task（`CeleryEmbeddingEnqueuer(celery_app, "embeddings.generate_chunks")`）；发布失败保留可扫描状态，不丢业务事实。
- [x] 使用真实 PostgreSQL、Redis/Celery eager 或测试 Worker 验证上传、解析、确认、Embedding 和重复投递（`tests/validate_document_pipeline.ps1` 在 `verify` 门禁内起真 PostgreSQL 17 + Redis，再跑 `tests/integration/test_document_pipeline_e2e.py`；用例覆盖上传→解析→抽取→确认→Embedding 入队与写入，并以第二次调用同一 task 断言重复投递被拒（`assert duplicate["status"] == "skipped"`）。**更正**：此前记录的「hermetic `verify` CI 默认 skip」不成立——`skipif` 只要求 `DATABASE_URL` 前缀为 `postgresql+asyncpg`（`.env.example` 满足），普通 `pytest` 里才 skip，探针里是实跑；CI run 34831481488 日志含 `FIN-003 document pipeline end-to-end tests passed`。仅「Live Worker 进程」不在本项范围，归 FIN-006/FIN-012）。

#### FIN-004：文档与 Profile 前端闭环

- [x] 增加 `/documents`、文档详情/重试、`/documents/:id/review` 和候选人详情路由（`DocumentsPage`/`DocumentDetailPage`/`DocumentReviewPage`/`CandidateDetailPage`，`App.tsx` 路由表；校对页按 `documentId` 分组挂载，切换候选人时内层状态重建）。
- [x] 展示上传队列、解析失败类别、重试动作和 request_id，不用统一错误文案掩盖状态（`DocumentQueue` 按 `error_code` 渲染失败类别并保留原始 `error_message`，`canRetry` 决定重试按钮可用性，`Feedback` 展示 `request_id`）。
- [x] 实现原文定位、字段编辑、证据预览与确认操作，键盘和焦点行为可用（`SourceLocatorViewer` 定位时 `focus()` 到 `tabIndex={-1}` 的块，钉住计数用 `role="status"` 播报，钉住操作区为带 `aria-label` 的 `role="group"`；`ProfileForm` 携带 `expected_version` 提交，冲突返回 409 `PROFILE_VERSION_CONFLICT`）。
- [x] 增加 PDF 与 DOCX 的组件测试及浏览器上传—校对—READY 流程（`SourceLocatorViewer.test.tsx` 用 PDF locator 断言聚焦与分块，`DocumentReviewPage.test.tsx` 用 `docx_paragraph` 断言草稿、钉证据与版本冲突；`apps/web/e2e/document-review.spec.ts` 上传 `e2e-candidate-resume.docx` 后轮询队列、在原文中钉选「Python 后端」并确认至 READY，探针输出 `flow=upload-review-readiness`）。

#### FIN-005：Run 后台异步执行

- [x] 实现 `agent.execute_match_run` 与 `agent.execute_application_run` task：认领策略为纯函数 `decide_claim`（状态 × 取消标记 × 意图），用 `SELECT ... FOR UPDATE` 锁 Run；ApplicationRun 支持 START/RESUME，Celery 侧指数退避 + jitter 重试。
- [x] MatchRun 与 ApplicationRun 创建接口都只提交业务事实并返回 202 + `CREATED`；依赖退出时先提交再投递，发布失败不回滚已提交事实、也不让请求变 5xx。
- [x] Worker 使用 SQL Checkpoint 恢复审批后的流程；RESUME 必须同时匹配当前 Checkpoint ID、已执行 Approval、决定人和数据库实时授权，旧投递不能穿过第二道审批。
- [x] 覆盖任务重复投递与终态不回退：`tests/unit/test_agent_tasks.py` 19 例覆盖认领全分支、`WAITING_APPROVAL` 两个意图都拒绝、取消标记优先于意图、`with_for_update` 断言，以及终态/已取消/不存在三种投递一律 `skipped`（且回滚而不提交）。用例有效性用变异验证过：把终态判断移到重试分支之前，`FAILED + retry` 立刻退化为 `ALREADY_TERMINAL` 并被抓出。
- [x] 覆盖重试幂等（`tests/validate_application_entry.ps1` 新增段）：把 run 置为 `FAILED/retryable` 后走真实 `POST /match-runs/{id}/retry`，Worker 以 attempt 2 重跑到 `COMPLETED`，且候选人/报告/claim/evidence 四类行数与重试前逐项相等（替换而非追加）。单元侧由 `test_generate_replaces_a_runs_previous_reports` 与重试用例共同固定。
- [x] 覆盖 Worker 强制退出、API/Worker 分别重启后的恢复：`tests/validate_application_entry.ps1` 在两次审批前重启 API、审批投递时强杀 Worker，再由新 Worker 从 PostgreSQL Checkpoint 完成恢复；QUEUED 主动扫描仍归 FIN-006。

#### FIN-006：维护任务与故障协调

- [x] Approval 过期批处理使用稳定排序与 `FOR UPDATE SKIP LOCKED`，并正确释放活动槽。`ApprovalRepository.claim_expired_pending(before, limit=)` 用 `ORDER BY approvals.id LIMIT n FOR UPDATE SKIP LOCKED` 批量领取（稳定排序避免两个扫描者在部分重叠的候选集上死锁；`SKIP LOCKED` 让第二个 Worker 跳过已锁行继续排空积压而不是阻塞）；`MaintenanceService.expire_pending_approvals(before, batch_size=100)` 逐条独立处理，一条坏记录不会中断整批；每条仍是「重读 Approval 且仍为 PENDING」才转移，所以 `SKIP LOCKED` 只避免重叠、正确性由该守卫承担。测试：`tests/unit/test_maintenance_fin006.py::test_expired_pending_query_uses_stable_order_and_skip_locked`（编译 PG 方言 SQL 断言 `FOR UPDATE SKIP LOCKED` + `ORDER BY approvals.id` + `LIMIT 25`）、`test_expire_skips_a_record_a_concurrent_worker_already_took`、`test_fake_claim_expired_pending_honours_the_limit`；真实数据库侧 `tests/integration/test_maintenance_postgres.py::test_claim_expired_pending_is_ordered_and_skips_locked_rows`（两个会话并发领取 6 条，断言两批各 3 条、互不相交、整体按 id 有序，第二个会话对第一个已锁的行不阻塞）。
- [x] 实现 `maintenance.republish_queued`，扫描已提交但未成功发布的文档、`CREATED` Run，以及已有已执行 Approval/Checkpoint 的 `RUNNING` ApplicationRun 恢复片段。三条积压各有独立的「是否真的搁浅」判据：①文档 `QUEUED` 且 `updated_at <= now - REPUBLISH_QUEUED_AFTER_SECONDS`（解析服务按 `attempt`/`parser_version` 幂等，重复投递是 no-op）；②MatchRun `CREATED` 且静默超过 `RUN_LEASE_SECONDS`（`CREATED` 意味着没有任何 Worker 开始过——claim 才会把它移出该状态且整趟只提交一次，Worker 被杀即回滚到 `CREATED`），以 `START` 意图重投（唯一接受 `CREATED` 的意图）；③ApplicationRun `RUNNING` 且静默超过租约窗口。第三条是最窄也最危险的一条：`RUNNING` 同样意味着「活着的 Worker 正持有行锁」，所以必须先拿到**持久事实**——`SqlApplicationRunRecoveryProbe` 同时读「可续跑的 Checkpoint」与「已 `EXECUTED` 的 Approval + 决定人」，两者齐备才投 `RESUME`；claim 会再次校验 checkpoint id/approval id/决定人/实时授权，因此该扫描只能「提议」投递，**无法自行把任何 Run 推过人工闸门**（无 checkpoint 或无已执行审批时记录 `skipped=no_resume_facts|approval_decider_missing` 并留给人处理）。单条投递失败只记日志不中断整批（§14.2）。测试：`tests/unit/test_maintenance_fin006.py` 12 例覆盖三通道的候选判定、`WAITING_APPROVAL`/终态不入选、租约未到不投、无 probe 不投、`test_republish_application_run_never_proposes_an_unapproved_run`（负向对照：此处「什么都不做」才是正确答案）、重复扫描不放大、`test_republish_survives_a_broker_failure_per_record`、`batch_size` 上限、最旧优先；真实库侧 `test_stranded_document_scan_filters_by_status_and_age`、`test_agent_run_scan_filters_status_type_and_age`、`test_recovery_probe_reads_checkpoint_and_executed_approval`。
- [x] 实现 `maintenance.cleanup_orphan_files`，仅删除数据库无引用且超过安全窗口的对象。顺序不可调换：**列表 → 按年龄过滤 → 查引用 → 删除**；引用检查紧贴删除之前执行，所以「刚提交的引用」也能被看到，安全窗口只需覆盖 `put()` 到上传事务提交之间的间隙，而不是文档的整个生命周期。`LocalVolumeStorageLister` 只对 `objects/<2 hex>/<32 hex>` 托管布局给出 storage_key，其余（上传期的 `.tmp` 暂存文件、游离文件）以 `storage_key=None` 上报——计数但不删，因为本代码无法寻址的对象不是它该删的；删除时再次校验路径未逃逸 `STORAGE_ROOT`。测试：`test_cleanup_deletes_only_unreferenced_aged_objects`（被引用/太新/不可寻址三类各跳过一类）、`test_cleanup_never_deletes_inside_the_safety_window`、`test_cleanup_is_idempotent`、`test_cleanup_keeps_an_object_referenced_by_a_recent_upload`、`test_cleanup_respects_batch_size`、`test_local_volume_lister_reports_managed_keys_and_ignores_strays`（真实文件系统）、`test_local_volume_lister_refuses_keys_outside_the_root`、`test_local_volume_lister_is_deterministic_and_capped`。
- [x] 验证 Redis 不可用、恢复、重复扫描、多 Worker 竞争和 maintenance 重投递幂等。Redis 不可用/恢复：发布失败不回滚已提交事实，只体现在「行静默留在 `QUEUED`/`CREATED`/`RUNNING`」，由下一轮扫描补齐（`test_republish_survives_a_broker_failure_per_record` 用单条失败注入证明坏记录不拖垮整批）；重复扫描：`test_republish_is_idempotent_across_repeated_sweeps`（两轮提议同一批投递，不逐轮放大）；多 Worker 竞争：`test_claim_expired_pending_is_ordered_and_skips_locked_rows`（真实 Postgres 两会话并发领取互不重叠、不阻塞）+ `test_expire_skips_a_record_a_concurrent_worker_already_took`；maintenance 重投递幂等：`test_cleanup_is_idempotent`、`test_fake_claim_expired_pending_honours_the_limit`，以及 Celery 侧注册与调度断言 `tests/unit/test_celery_beat.py::test_beat_schedule_registers_fin006_sweeps`（把「已实现但从未被发出」这种看起来活着的死代码钉住）。新增 `tests/validate_maintenance.ps1`（起真 PostgreSQL 17 + 迁移后跑集成套件）并接入 `project.ps1 verify`。

#### FIN-007：Evaluation 产品闭环

- [x] 按详细设计新增 Dataset/Evaluation 相关模型和迁移，保存数据集、配置、模型/Prompt 版本和指标。新增 `dataset_versions` / `evaluation_runs` / `metric_snapshots` 三张表（`0012_evaluation_tables`）：数据集以 `(name, version)` 唯一定位并留 `content_hash` + `schema_version` + `manifest_json`；运行时冻结 `config_hash`、`model_snapshot_json`、`prompt_versions_json`（快照只含模型名与 `mock_model_mode`，**不含任何凭据**，由 `test_model_snapshot_never_leaks_credentials` 钉住）。完成态的 Run/Metric 一律不可就地改写，重跑生成新行；该不可变性写在 SQL 层（`replace_metrics` 以终态状态谓词拦住），而不是依赖调用方自觉。指标值一律带 `threshold` 与 `passed`，`dimensions_json` 保存分桶明细。`model_registry` 与迁移门禁同步：表数 22 → 25，`tests/validate_migrations.ps1` 固定 head=`0012_evaluation_tables`。
- [x] 实现 `POST /evaluations`、列表和详情 API，补齐技术权限与重复执行错误。三个端点挂在 `/api/v1/evaluations`：`POST` 返回 202 `EvaluationAccepted`，`GET` 返回分页 `EvaluationList`（`MAX_PAGE_SIZE=100`），`GET /{id}` 返回 `EvaluationDetail`。重复执行以 `(dataset_version_id, config_hash)` 为键，**与调用者无关**、只看活跃状态（`CREATED`/`RUNNING`），命中即 409 `EVALUATION_DUPLICATE`；`config_hash` 用排序键 SHA-256 规范化，字典顺序变化绕不过去。数据集版本不存在 → 404 `DATASET_VERSION_NOT_FOUND`，同 `(name, version)` 但 `content_hash` 不同 → 409 `DATASET_VERSION_IMMUTABLE`。权限用**技术**角色（`require_roles(HR, ADMIN)`），与业务角色 `HIRING_MANAGER` 分开——它拿不到技术入口，这是 `test_reader_role_is_enforced` 覆盖的负例。畸形 UUID 直接 404 而不是 500。
- [x] 实现 `evaluations.execute` task，将 Golden、语义支持和 Prompt Injection 结果持久化。`enqueuer` 以 `task_id=str(evaluation_run_id)` 投递，天然幂等；task 内**每次调用重建 `RuntimeResources` 并在同一个 `asyncio.run()` 里释放**（遵守单事件循环规则），`start` 返回 `None`（已被别的 Worker 认领或已终态）时返回 `{"status": "skipped"}`，评分器抛异常则落 `FAILED` + 安全错误码而不是把异常穿出去。三套既有评分器（`retrieval/golden.py`、`evaluations/semantic.py`、`evaluations/injection.py`）本次**未改动、未重复验证**，FIN-007 只做其上的持久化编排。
- [x] 增加最小结果页面/报告入口，并让离线 CI 对发布阈值失败时返回非零状态。前端新增 `/evaluations` 页面 + 导航项：结论一律**读存储字段、绝不重算**，`passed === null`（未完成）与未达标在 UI 上是两种不同呈现，无阈值指标标为「参考」且不参与门禁。离线门禁 `backend.app.evaluations.gate` 退出码契约为 0=通过 / 1=阈值未达标 / 2=飞架故障；它**刻意不读 `Settings`**，因此能在无数据库/Redis/模型的裸 CI 步骤里独立运行。新增 `tests/validate_evaluations.ps1`（起真 PostgreSQL 跑集成套件）与 `tests/validate_evaluation_gate.ps1`（在开发镜像内跑门禁并断言 `RESULT: PASS`），两者均接入 `project.ps1 verify`。
- 判决语义：**阈值未命中 = `COMPLETED` + `passed=False`**（业务结论），**飞架损坏 = `FAILED` + 安全错误码**（工程故障），两者在模型、服务、task、门禁和 UI 五层都不许混淆。门禁只对**带阈值的指标**取合取，且**空指标集判为不通过**——`all()` 对空列表返回 `True` 会造出一条「没跑任何检查却显示通过」的假绿灯，该缺陷在 `BuiltinEvaluationExecutor.gate_passed` 与 `EvaluationService.complete` 两处同时修掉。
- 测试：`tests/unit/test_evaluations_fin007.py`（32）、`tests/unit/test_evaluations_routes.py`（11）、`tests/unit/test_evaluation_gate.py`（9）、`tests/integration/test_evaluations_postgres.py`（7，仅在 `DATABASE_URL` 为 `postgresql+asyncpg` 时执行）、前端 `EvaluationsPage.test.tsx`（9）。后端单测 392 全绿，ruff 干净，mypy strict 198 文件无问题，前端 113 测试全绿 + `tsc -b` 干净。

#### FIN-008：真实 Qwen 适配器

- [x] 实现 OpenAI 兼容的 Qwen Chat 与 Embedding gateway，Fake/Real 仅由显式配置切换。新增 `backend/app/infrastructure/qwen_chat.py`（`/chat/completions`）与 `qwen_embedding.py`（`/embeddings`），共用 `prompts.py` 的 Prompt 契约。切换唯一入口是 `mock_model_mode`：`build_model_gateway`/`build_embedding_gateway` 此前会**静默返回 Fake**（前者）或**直接抛 `NotImplementedError`**（后者），两个工厂对同一个开关给出不一致的答案——这是本次修掉的核心缺陷。真实工厂在缺 endpoint/key 时抛 `ValueError` 而非退化到 Fake：用启发式假模型伪造出「看起来合理」的抽取结果，远比重启时直接失败更危险。
- [x] 加入超时、429/5xx 分类、有限重试、结构化输出校验和敏感字段脱敏。策略集中在 `http_transport.py` 一处，调用方不再各自推导分类：timeout/connect/429/5xx 为**可重试**，其余 4xx、畸形 JSON、200 但 schema 不符为**永久失败**——`Celery` 靠这一位决定是否值得再烧一次尝试次数。结构化输出一律**当作不可信输入**：先 `json.loads` 再 `model_validate`，schema 违例判永久（重发同一请求只会得到同一份畸形回复）。重试遵守 `Retry-After`（上限 60s），否则指数退避 + `[0.5,1.0)` 抖动（抖动只缩不涨，且打散并发 worker 避免再造 429 风暴）。脱敏：`RawResponseBody` 的 `__repr__` 刻意不含正文，日志只记录路径与计数，不记录正文与凭据。
- [x] 提供不打印凭据的手动模型/Embedding 连通诊断命令。`python -m backend.app.infrastructure.model_diagnose` 退出码 0=全部可达 / 1=永久失败 / 2=临时失败，与 `evaluations.gate` 契约一致但**刻意读 `Settings`**（gate 不读，因为要在裸 CI 跑；诊断存在的意义正是回答「我配置指向的端点是否真的能用」）。端点被归约为 scheme+host+port+path，**query 与 userinfo 整段丢弃**而非脱敏——需要猜哪个参数是密钥的脱敏迟早会猜错，而这个值是会被打印的；无法解析的 URL 一律替换为 `(unparsed)` 而非原样回显（手糊进密钥的配置恰恰是最常见的解析失败来源）。凭据绝不出现在报告头、失败行或端点里。
- [x] 用合成数据保存 dataset、model、Prompt、配置版本和录制评测结果；普通 CI 继续使用 FakeModel。新增 `recording.py`：录制**原始回复文本**而非解析后的 draft（存 draft 会把本解析器的行为烤进 fixture，也就无法发现解析器后续回归），回放时走与线上完全相同的解析与校验路径，缺 case 直接抛错而非返回空 draft 静默通过。`datasets.py` 注册三个内置合成数据集（golden/semantic/injection）并**首次真正接入生产路径**——此前 `register_dataset` 只有测试调用，`dataset_versions` 因此恒为空，评测结果无法回指数据集版本。录制文件经断言不含凭据、base URL 与用例载荷（`manifest_json` 是 FIN-007 定义的**有界描述**，不得承载 case 载荷）。Prompt 版本纪律：改 Prompt 文本必须同一次改动里升版本，否则已录制的评测无法归因；`is_stale_for` 检出陈旧 fixture 只告警不失败，避免无关 Prompt 编辑打断 CI。
- 测试：`tests/unit/test_http_transport.py`（30）、`tests/unit/test_qwen_gateway.py`（50）、`tests/unit/test_fin008_recording.py`（30）、`tests/unit/test_model_diagnose.py`（27）。全部 HTTP 走 `httpx2.MockTransport`，真实客户端路径在**无网络、无密钥、无 sleep** 下被完整覆盖。新增 `tests/validate_model_adapter.ps1` 并接入 `project.ps1 verify`：注入哨兵密钥断言诊断输出不含它，用 RFC 2606 保留域 `.invalid` 保证请求不逃逸 CI。后端单测 529 全绿，ruff 干净，mypy strict 209 文件无问题。
- 技术选型：复用仓库既有的 `httpx2`（dev 依赖，运行时可用）而**不引入官方 `openai` SDK**——`httpx2` 自带 `MockTransport`，使整张重试/分类矩阵可离线测试，且无需改动 `requirements.lock`（本机缺少项目生成锁文件的 `pip-compile --no-index` 工具链，改锁文件风险高于收益）。

#### FIN-009：LangGraph 运行时对齐

- [x] 形成 ADR：采用真实 LangGraph StateGraph/PostgreSQL Checkpointer，或正式确认保留当前引擎的 MVP 偏差。**已形成 [ADR-0001](docs/adr/0001-agent-runtime-custom-engine-over-langgraph.md)，决策为「正式确认保留自研 `RunEngine` + `SqlCheckpointer`」。** 事实核查：`pyproject.toml` 与两份 lock 均无 langgraph/langchain 依赖，源码零导入。ADR 记录了关键发现——真正的工作量不在 250 行引擎，而在围绕它的接线（每节点双事件写入、每状态迁移 `set_status`、恢复校验散落 `agent/tasks.py` 与 `job_applications/service.py`），且 895 行不变量测试须被重写而非复用；同时 MatchRun 图事实上从未使用引擎，「迁移双图」等于「迁移一张 + 新写一张」。
- [x] 若采用 LangGraph，迁移固定双图、interrupt/resume、thread_id 和 checkpoint 命名空间；若保留当前引擎，同步全部设计与宣传材料。**适用后置分支**：`技术栈选型与架构决策.md`（§8.1/§8.2 标注为立项意图 + 排除项补记实际选择、表格行、总结句）、`需求分析.md`（AGT-008 与双图表述）、`概要设计说明书.md`、`详细设计说明书.md`、`README.md` 共 5 份文档精准修正断言「已是 LangGraph」的表述并回指 ADR；「规划中/后续适配」类善意表述按约定保留。
- [x] 保证升级前后业务表仍是事实源，已执行 Approval 不会因 checkpoint 重放再次执行副作用。`tests/unit/test_fin009_adr_contract.py::test_business_tables_remain_the_source_of_truth` 断言中断后**业务行**已为 `WAITING_APPROVAL`（检查点仅决定继续位置）；`::test_executed_approval_does_not_refire_side_effect_on_replay` 断言以 ordinal=1 重放同一审批返回同一行且 Interview 恒为一条。
- [x] 通过 API/Worker 重启、旧 checkpoint 兼容或明确迁移失败策略的测试。`test_fin009_adr_contract.py` 共 20 项：AC 冻结 ADR 事实基础（零依赖、契约方法名、`metadata["next_node"]` 恢复、主中断不重复触发）、覆盖恢复后可重放且事件序列连续无空洞、检查点不携带大文本、以及文档口径守卫（含「需求不再强制特定第三方库」）。全量 `549 passed`。ADR §5.3 同时明确列出未来重新评估的 5 个触发条件与迁移路径（保留 `Checkpointer` 协议、仅替换调度实现、以既有不变量测试作验收门禁），作为「迁移失败策略」的正式答复。

#### FIN-010：非种子文件浏览器主路径

- [ ] 使用仓库内合成 PDF/DOCX，从浏览器上传并等待 Worker 解析。
- [ ] 完成 Profile 校对确认和 Embedding，随后在岗位候选人页看到该 Profile。
- [ ] 继续完成 MatchRun、证据报告、ApplicationRun、两次审批和 Interview。
- [ ] 断言数据库只有一条目标申请、两次已执行审批、一条 Interview，且无孤儿文件/活动槽残留。

#### FIN-011：故障与安全浏览器矩阵

- [ ] 覆盖审批 EDIT/REJECT、版本冲突、过期、新 attempt、取消后禁止写入和重复请求。
- [ ] 覆盖 SSE Last-Event-ID 重连、跳号恢复、Redis 通知丢失和终态关闭。
- [ ] 通过浏览器会话验证 JobAssignment 在线撤权与用户禁用后不再发送业务事件。
- [ ] 覆盖 401/403/409/422/503 的差异化 UI、request_id 和可恢复动作。

#### FIN-012：完整 Compose 与 CI

- [ ] Compose 提供 nginx、web、api、worker、scheduler、postgres、redis 和一次性 migrate/seed，并固定健康依赖。
- [ ] Nginx 对 API/SSE 关闭缓冲并保留 Bearer 流；前端只通过统一公开入口访问。
- [ ] 建立 CI：文档、Ruff、mypy、pytest、PostgreSQL/Redis 集成、前端测试/构建、OpenAPI 兼容、Playwright 和安全评测。
- [ ] 在全新 clone/Volume 上用一条入口命令启动，完成迁移、seed、健康检查和核心 E2E 后彻底清理资源。

#### FIN-013：发布收口

- [ ] 按实际状态同步 README、环境清单、架构图和设计偏差，移除所有“占位但已完成”的描述。
- [ ] 固化演示/重置/诊断命令，记录非敏感版本、耗时、指标和已知 MVP 边界。
- [ ] 执行最终 `scripts/project.ps1 verify`、Secret/隐私扫描、依赖审计和工作区检查。
- [ ] 创建发布 commit 与版本 tag，保存最终门禁报告；此后才把项目状态改为 `DONE`。

### 20.4 推荐执行顺序

~~~text
一致性基础：FIN-001 → FIN-002
  → 数据入口：FIN-003 → FIN-004
  → 异步恢复：FIN-005 → FIN-006
  → 评测与适配：FIN-007 → FIN-008 → FIN-009
  → 完整验收：FIN-010 → FIN-011 → FIN-012 → FIN-013
~~~

### 20.5 项目完工判定

- [ ] FIN-001 至 FIN-013 的总表状态和全部子项均为 `DONE`，每项有 commit 与自动化证据。
- [ ] 从真实合成 PDF/DOCX 上传开始的浏览器闭环可重复完成，不只依赖数据库 seed。
- [ ] API、Worker、Scheduler、Redis 任一可恢复故障不会造成重复副作用、丢业务事实或取消后写入。
- [ ] 完整 Compose 在全新环境可一键启动并通过健康、迁移、E2E、重启和清理验证。
- [ ] FakeModel 普通 CI 稳定；真实模型诊断/评测可显式运行且结果可追踪。
- [ ] README、实现计划、详细设计和实际代码边界一致，不再把 PARTIAL/占位能力标成完成。
- [ ] 最终全量门禁绿色、无 Secret/真实个人数据、无未解释 TODO，Git 工作区干净并已创建版本 tag。

## 21. 计划结论

截至 2026-09-07，项目已经具备可演示的种子数据主路径，并完成 MatchRun 报告、ApplicationRun 双审批、SQL Checkpoint、后半段持久化和浏览器验收；但它仍是“核心演示闭环完成、生产运行闭环未完成”，不能再用 IMP 文件是否存在判断整体完工。

后续从 FIN-001 开始严格按 20.4 顺序推进。若实际进度与计划偏离，应优先减少 P1/P2 展示能力，保持 P0 的证据链、权限、审批、幂等、取消、恢复和自动化验证不变；只有 20.5 的完工条件全部满足，项目才可正式标记为完成。
