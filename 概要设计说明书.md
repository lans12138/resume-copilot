# 企业招聘 Copilot 概要设计说明书

> 文档版本：1.0
>
> 设计日期：2026-08-31
>
> 适用范围：六周单人开发的作品集级 MVP
>
> 需求基线：[需求分析 1.2](./需求分析.md)
>
> 架构依据：[技术栈选型与架构决策](./技术栈选型与架构决策.md)
>
> 可行性依据：[项目可行性分析 1.3](./项目可行性分析.md)

## 1. 文档说明

### 1.1 编写目的

本文把需求基线转换为系统级设计方案，作为后续数据库详细设计、OpenAPI 契约、前后端实现、Agent 节点实现和测试设计的共同依据。

本文回答以下问题：

- 系统由哪些组件和领域模块组成。
- 模块之间如何依赖和交换数据。
- 岗位匹配、单候选人审批、文档解析和事件恢复如何执行。
- 关键业务状态、数据约束和事务边界如何设计。
- 权限、Prompt Injection、证据质量和副作用如何被控制。
- 系统如何部署、观测、测试和验收。

本文属于概要设计，不展开以下详细内容：

- 完整数据库 DDL、Alembic 脚本和物理字段类型。
- 每个 Pydantic Schema 的全部字段定义。
- OpenAPI 的完整请求和响应示例。
- React 组件树、CSS 和像素级页面稿。
- 每个 LangGraph Node 的具体 Prompt 文本。

### 1.2 设计基线

| 项目 | 基线 |
|---|---|
| 产品范围 | 单企业逻辑空间、3 至 5 个合成岗位、30 至 100 份合成简历。 |
| 文档格式 | 电子版 PDF、DOCX；不支持 OCR。 |
| 用户角色 | HR、HIRING_MANAGER、ADMIN。 |
| Agent 模型 | 单 Agent 边界，岗位级 MatchRun 与候选人级 ApplicationRun 两张固定状态图。 |
| 数据边界 | MVP 只使用合成数据；真实招聘数据和生产合规不在范围内。 |
| 部署边界 | 单机 Docker Compose；不做高可用、多租户和 Kubernetes。 |
| 外部集成 | Qwen 模型和 Embedding；Langfuse 可选；排期使用 MockScheduleBackend。 |

### 1.3 术语

| 术语 | 含义 |
|---|---|
| Job | 岗位主体。 |
| JobVersion | 某次生效的岗位描述和结构化要求快照。 |
| JobAssignment | 岗位与招聘主管之间可撤销的授权关系。 |
| CandidateProfile | 经人工确认、可进入检索的候选人资料版本。 |
| EvidenceChunk | 可定位到原文版本和位置的证据片段。 |
| JobApplication | 一个候选人与一个岗位的业务关联及流程状态。 |
| MatchRun | 针对一个 JobVersion 的岗位级批量分析运行。 |
| ApplicationRun | 针对一个 JobApplication 的候选人级审批和面试运行。 |
| ReportClaim | 匹配报告中的一条结构化结论。 |
| Approval | ApplicationRun 中一个待人工决定的副作用提案。 |
| Checkpoint | LangGraph 保存的可恢复执行状态。 |
| AgentEvent | 持久化的运行事件，也是 SSE 重放的数据源。 |

## 2. 设计目标与原则

### 2.1 设计目标

1. 完成“岗位—简历—检索—证据化报告—人工审批—模拟排期”的纵向闭环。
2. 保证批量分析与单候选人业务审批互不污染。
3. 保证每个高影响结论可定位、可复核，并可离线评测其证据支持质量。
4. 保证 JobApplication 状态变更和排期等高影响业务副作用经过人工批准、授权、版本校验和幂等保护。
5. 保证 API、Worker 或浏览器中断后可以恢复或安全重试。
6. 保持模块化单体和单机部署复杂度，满足六周单人交付约束。

### 2.2 设计原则

| 原则 | 设计约束 |
|---|---|
| 人类最终决策 | 模型不得自动淘汰、录用或创建面试安排。 |
| 分析与执行分离 | MatchRun 只分析；ApplicationRun 才能提出并执行副作用。 |
| 业务事实与 Checkpoint 分离 | PostgreSQL 业务表保存最终事实；Checkpoint 只服务执行恢复。 |
| 文档不可信 | 简历和岗位描述不能改变系统指令、路由、权限、业务 ID 或工具参数。 |
| 证据优先 | 确定性结论必须有合法引用、精确摘录和支持等级。 |
| 默认可恢复 | 异步任务、审批和工具执行均考虑重复投递、超时和进程重启。 |
| 契约优先 | API、事件、Agent State、模型输出和工具参数均使用显式 Schema。 |
| 可替换外部依赖 | 模型、Embedding、文件存储、排期和可观测平台通过适配器接入。 |

## 3. 系统上下文

### 3.1 外部参与者与系统

~~~text
HR ───────────────┐
HIRING_MANAGER ───┼── HTTPS ──> Nginx ──> Web / FastAPI
ADMIN ────────────┘                         │
                                           ├── PostgreSQL + pgvector
                                           ├── Redis / Celery Worker
                                           ├── Docker Volume
                                           ├── Qwen Chat / Embedding API
                                           ├── MockScheduleBackend
                                           └── Langfuse Cloud（可选）
~~~

### 3.2 系统边界

系统负责：

- 身份认证和资源级授权。
- 岗位、岗位版本和招聘主管分配。
- 简历上传、解析、人工校对、版本化和索引。
- 候选人检索、硬条件评估和证据化报告。
- 单候选人状态审批、面试问题和模拟排期。
- 运行事件、审计、评测和故障恢复。

系统不负责：

- 真实邮件、短信、日历或 ATS 集成。
- 自动招聘决策。
- 扫描件识别。
- 真实企业组织同步和多租户隔离。
- 真实候选人数据的生产合规承诺。

## 4. 总体架构

### 4.1 逻辑分层

~~~text
┌─────────────────────────────────────────────────────┐
│ 表现层：React / TypeScript / Ant Design             │
│ 页面、表单、Run 时间线、审批、报告与评测展示        │
└───────────────────────┬─────────────────────────────┘
                        │ REST + Fetch SSE
┌───────────────────────▼─────────────────────────────┐
│ 接口层：FastAPI Router / Auth Dependency / Schema   │
│ 鉴权、校验、错误映射、分页、流式连接                │
└───────────────────────┬─────────────────────────────┘
                        │
┌───────────────────────▼─────────────────────────────┐
│ 应用与领域层：模块化单体                            │
│ jobs / documents / candidates / retrieval / agent  │
│ approvals / interviews / events / evaluations      │
└──────────────┬───────────────────────┬──────────────┘
               │                       │
┌──────────────▼────────────┐  ┌───────▼──────────────┐
│ 异步与编排                │  │ 基础设施适配器       │
│ Celery / LangGraph        │  │ Model / Storage /    │
│ Checkpoint / Scheduler    │  │ Schedule / Langfuse  │
└──────────────┬────────────┘  └───────┬──────────────┘
               │                       │
┌──────────────▼───────────────────────▼──────────────┐
│ PostgreSQL + pgvector / Redis / Docker Volume       │
└─────────────────────────────────────────────────────┘
~~~

### 4.2 架构风格

后端采用模块化单体。所有领域模块运行在一个 FastAPI 代码库中，共享数据库和事务管理，但必须通过应用服务或显式接口交互，不允许跨模块直接修改对方的数据表。

采用前后端分离：

- React 负责交互、查询缓存和运行可视化。
- FastAPI 是唯一业务入口和授权执行点。
- Celery Worker 执行解析、Embedding、匹配和评测等耗时任务。
- PostgreSQL 保存业务事实、AgentEvent、Checkpoint 和向量。
- Redis 只用于队列和短期协调，不作为最终事实来源。

### 4.3 运行时组件

| 组件 | 职责 | 状态性 |
|---|---|---|
| Nginx | 同源入口、静态资源、API 反向代理、SSE 透传。 | 无状态。 |
| Web | React 构建产物。 | 无状态。 |
| API | REST、SSE、鉴权、业务服务和 Run 控制。 | 业务状态外置。 |
| Worker | Celery 任务、LangGraph 执行、解析、Embedding 和评测。 | 业务状态外置。 |
| Scheduler | 触发 Approval 过期和持久化任务协调；不保存业务事实。 | 无状态。 |
| PostgreSQL | 业务数据、向量、事件、审计和 Checkpoint。 | 核心持久化。 |
| Redis | Celery Broker、任务协调和短期缓存。 | 可重建。 |
| File Volume | PDF/DOCX 原文件。 | 持久化。 |
| Qwen API | 结构化抽取、证据化分析、问题生成和 Embedding。 | 外部依赖。 |
| Langfuse | 合成数据的可选 Trace 和实验记录。 | 外部可选。 |

## 5. 模块概要设计

### 5.1 前端模块

| 模块 | 主要职责 | 主要依赖 |
|---|---|---|
| auth | 登录、令牌生命周期、当前用户和路由守卫。 | API client、Zustand。 |
| jobs | 岗位列表、版本编辑、启用/关闭和 JobAssignment。 | TanStack Query、表单组件。 |
| documents | 批量上传、解析进度、错误和手动重试。 | Upload、SSE/轮询。 |
| candidates | 列表、显式筛选、详情和 Profile 校对。 | Query、证据查看器。 |
| match-runs | MatchRun 节点、排名、报告和配置快照。 | SSE client、报告组件。 |
| application-runs | 单候选人审批流、恢复和面试进度。 | SSE client、审批组件。 |
| approvals | 提案差异、批准、修改、拒绝和冲突处理。 | 表单、版本控制。 |
| interviews | 问题、来源和模拟日程结果。 | Query。 |
| evaluations | 指标、数据集版本和实验对比。 | ECharts。 |
| shared | API client、错误页、权限组件、时间和脱敏展示。 | React Router、Ant Design。 |

前端状态划分：

- TanStack Query 保存服务器状态和缓存。
- Zustand 只保存会话、页面级展示偏好和未提交的 UI 状态。
- Run、Approval、JobApplication 等业务事实不得只存在浏览器状态中。

### 5.2 后端模块

| 模块 | 主要职责 | 不允许承担的职责 |
|---|---|---|
| auth | 密码校验、JWT、当前身份、角色和资源授权。 | 不读取完整简历内容。 |
| jobs | Job、JobVersion、JobAssignment 和岗位状态。 | 不直接执行 Agent。 |
| documents | 文件元数据、StorageBackend、解析任务和证据定位。 | 不决定候选人业务状态。 |
| candidates | Candidate、CandidateProfile、JobApplication 和状态历史。 | 不直接调用外部模型。 |
| retrieval | 结构化、关键词、向量召回、RRF 和检索评测。 | 不改变 JobApplication 状态。 |
| agent | MatchRun、ApplicationRun、图、节点、Checkpoint 和工具提案。 | 不绕过领域服务直接写业务状态。 |
| approvals | Approval 生命周期、乐观锁、幂等和审批恢复。 | 不替代授权服务。 |
| interviews | 面试问题、ScheduleBackend 和 Interview。 | 不直接决定入围。 |
| events | AgentEvent 持久化、SSE 重放和订阅鉴权。 | 不把 Redis 当事件事实源。 |
| evaluations | 数据集、Harness、指标和实验快照。 | 不污染业务数据。 |
| observability | 日志关联、Trace、指标和脱敏。 | 不保存完整真实简历。 |
| infrastructure | DB、Redis、模型、文件和第三方适配器。 | 不包含招聘业务规则。 |

### 5.3 模块依赖规则

~~~text
API Router
  → Application Service
      → Domain Model / Domain Policy
      → Repository Port
      → External Adapter Port

Agent Node
  → 只调用显式应用服务或只读查询接口
  → 副作用必须经过 Approval Service
~~~

约束：

1. Router 不直接操作 ORM Session。
2. Agent Node 不直接接收 HTTP Request 或修改前端状态。
3. 基础设施适配器实现领域定义的 Port，不反向依赖 Router。
4. documents 可以创建或更新 CandidateProfile，但必须通过 candidates 应用服务。
5. agent 可以读取 jobs、candidates 和 retrieval，但状态更新必须经过 approvals 和 candidates。
6. events 只消费已提交的业务事件，不驱动招聘业务决策。

## 6. 核心业务流程设计

### 6.1 岗位创建与分配

~~~text
HR 提交岗位草稿
  → API 校验字段和 HR 权限
  → 创建 Job + JobVersion
  → HR 启用岗位
  → 校验 DRAFT → ACTIVE
  → HR 分配招聘主管
  → 校验目标用户 is_active 且 role=HIRING_MANAGER
  → 创建 JobAssignment 和审计记录
~~~

设计要点：

- JobVersion 一旦被 MatchRun 引用便不可原地修改。
- ACTIVE 岗位关键要求变化时创建新版本，不改变历史 Run。
- JobAssignment 使用撤销时间保留历史，不物理删除历史授权记录。
- 招聘主管的每次业务访问都校验当前有效分配；撤销后立即失去访问权。
- ADMIN 可以管理账号和查看脱敏技术信息，但不自动获得业务数据权限。

### 6.2 简历上传、解析与确认

~~~text
HR 上传 PDF/DOCX
  → 文件签名、MIME、扩展名、大小和批量限制校验
  → StorageBackend 以随机存储键落盘
  → 保存 ResumeDocument 与 SHA-256
  → 事务提交后发布 Celery 任务
  → Worker 提取文本和位置
  → 模型输出结构化 CandidateProfile 草稿
  → 创建 EvidenceChunk
  → 状态进入 REVIEW_REQUIRED
  → HR 对照原文校对并确认
  → Profile 进入 READY
  → 异步生成 Embedding
~~~

失败处理：

- 文件不支持：ResumeDocument 进入 UNSUPPORTED，不创建候选人资料。
- 临时失败：进入 FAILED 并记录 error_code、retryable 和 attempt。
- 队列发布失败：文档保持 QUEUED，可由重试接口重新发布。
- 重复文件：按 SHA-256 返回已有文档或明确重复结果，不重复创建 Profile 和 Chunk。
- 重新解析：产生新资料版本；旧 READY 版本进入失效或被替代状态，历史 MatchRun 仍引用旧快照。

### 6.3 候选人检索

检索候选集仅包含 READY 且未失效的 CandidateProfile。岗位级检索由三路组成：

1. 结构化匹配：技能、年限、学历等字段产生确定性相关度。
2. 关键词检索：标准化技能、精确关键词和 pg_trgm 补充。
3. 向量检索：使用 qwen3.7-text-embedding 的 1024 维向量计算语义相似度。

三路结果通过可配置 RRF 融合。每个结果保存各通道名次和融合分数。

必须区分两类条件：

- 资源权限过滤属于强制过滤。
- 候选人列表中的硬条件筛选由用户显式启用，只影响当前查询。
- MatchRun 的硬条件是评估项，不默认删除 FAIL 或 UNKNOWN 候选人。

### 6.4 MatchRun 岗位级流程

~~~text
POST /jobs/{job_id}/match-runs
  → 校验 HR 权限和 Job=ACTIVE
  → 创建 MatchRun、thread_id 和配置快照
  → CREATED → RUNNING
  → parse_job
  → retrieve_candidates
  → 锁定 Top-K CandidateProfile 版本
  → 对 Top-K 每名候选人执行 hard_rule_evaluate
  → 对 Top-K 每名候选人执行 score_with_evidence
  → 校验并持久化 MatchReport / ReportClaim
  → COMPLETED
~~~

MatchRun 约束：

- 一次只绑定一个 JobVersion。
- 对候选人新建 JobApplication 时状态为 CREATED；复用时保持原状态。
- Top-K 中每名候选人都必须进入硬条件评估和评分。
- 硬条件只输出 PASS、FAIL 或 UNKNOWN、理由和证据，不更新 JobApplication。
- MatchRun 不创建 Approval、不进入 WAITING_APPROVAL、不暴露副作用工具。
- 报告保存完成后 Run 即可 COMPLETED，即使没有候选人被后续入围。

### 6.5 ApplicationRun 候选人级流程

~~~text
POST /applications/{application_id}/runs
  → 校验当前用户对岗位的访问权
  → 锁定 JobApplication 并确认 active_application_run_id 为空
  → 创建绑定单个 JobApplication 的 ApplicationRun
  → 写入 active_application_run_id
  → human_review 生成状态变更提案
  → 创建 PENDING Approval
  → WAITING_APPROVAL / interrupt
  → 用户批准、修改或拒绝
  → 重新校验权限、归属、版本和幂等键
  → 执行 update_application_status
  → 非 SHORTLISTED：COMPLETED
  → SHORTLISTED：生成面试问题
  → 创建排期 PENDING Approval
  → WAITING_APPROVAL / interrupt
  → 审批后执行 create_interview_schedule
  → 保存 Interview 和模拟日程 ID
  → COMPLETED
~~~

ApplicationRun 约束：

- 一个 Run 只能绑定一个 JobApplication。
- 一个 JobApplication 同一时刻最多有一个 CREATED、RUNNING 或 WAITING_APPROVAL 的活动 ApplicationRun；并发创建返回 409。
- JobApplication.active_application_run_id 是活动 Run 排他槽，创建、终态清理和重试认领均在锁定 JobApplication 的事务中完成。
- 同一个 Run 同一时刻最多一个 PENDING Approval。
- Run 可以先后两次进入 WAITING_APPROVAL，但每次审批对象和幂等键不同。
- Approval 被拒绝只表示动作不执行，不把 Run 或 JobApplication 自动设为 REJECTED。
- Approval 到期时原子进入 EXPIRED，ApplicationRun 进入可重试 FAILED 并记录 APPROVAL_EXPIRED；用户通过既有 retry 接口产生新 attempt 和新 Approval。
- 招聘主管在决定审批时必须仍有有效 JobAssignment。
- 已执行副作用在恢复、重复请求或 Worker 重启后不得重复执行。

### 6.6 SSE 事件与恢复

~~~text
业务节点完成
  → 在 PostgreSQL 写入 AgentEvent
  → 提交事务
  → 通知在线订阅者
  → 前端按 sequence 更新

连接中断
  → 前端携带最后 event_id 重新请求
  → 服务端先从 PostgreSQL 重放缺失事件
  → 再切换到实时流
~~~

SSE 使用 Fetch Stream 携带 Bearer Token。Nginx 对事件路由关闭响应缓冲。前端只使用 run_type、node、status 和 sequence 推进显示，不解析 message 决定业务逻辑。服务端在重放、发送每个事件批次以及心跳前重新校验 Run 的岗位访问权；JobAssignment 撤销后不再发送任何业务事件并关闭连接。

## 7. 状态设计

### 7.1 状态归属

| 状态对象 | 状态用途 | 事实来源 |
|---|---|---|
| Job | 岗位是否可编辑、匹配或关闭。 | jobs 表。 |
| ResumeDocument | 文件处理生命周期。 | resume_documents 表。 |
| CandidateProfile | 是否已确认和可检索。 | candidate_profiles 表。 |
| JobApplication | 候选人在特定岗位中的业务决定。 | job_applications 表。 |
| MatchRun | 岗位级分析执行状态。 | agent_runs 公共表与 match_runs 子表。 |
| ApplicationRun | 单候选人审批和面试执行状态。 | agent_runs 公共表与 application_runs 子表。 |
| Approval | 一个提案的决定与执行状态。 | approvals 表。 |
| Checkpoint | 图执行恢复位置。 | LangGraph Checkpointer。 |

### 7.2 关键状态机

~~~text
Job:
DRAFT → ACTIVE → CLOSED

ResumeDocument:
UPLOADED → QUEUED → PARSING → REVIEW_REQUIRED → READY
                ├→ FAILED
                └→ UNSUPPORTED
READY → SUPERSEDED

JobApplication:
CREATED ├→ SHORTLISTED → INTERVIEW_SCHEDULED
        ├→ ON_HOLD
        └→ REJECTED

MatchRun:
CREATED → RUNNING → COMPLETED
RUNNING → FAILED
CREATED / RUNNING → CANCELLED

ApplicationRun:
CREATED → RUNNING → WAITING_APPROVAL → RUNNING → COMPLETED
RUNNING / WAITING_APPROVAL → FAILED
CREATED / RUNNING / WAITING_APPROVAL → CANCELLED

Approval:
PENDING → APPROVED → EXECUTED
        ├→ EDITED → EXECUTED
        ├→ REJECTED
        └→ EXPIRED
APPROVED / EDITED → EXECUTION_FAILED → EXECUTED
~~~

所有状态迁移都在服务端通过显式状态迁移表校验。非法迁移返回 409，不允许通过任意 PATCH 写入状态字符串。

- ResumeDocument 的 FAILED 只有 retryable=true 时才能重新进入 QUEUED；UNSUPPORTED 不自动重试。
- MatchRun 不允许进入 WAITING_APPROVAL。
- ApplicationRun 可以顺序等待两次审批，但同一时刻只允许一个 PENDING Approval。
- Approval 只能做出一次最终决定；重复决定返回既有结果或 409。
- Approval.EXPIRED 表示提案已不可执行，expiration_reason 区分 TIMEOUT 和 RUN_CANCELLED。
- PENDING Approval 到达 expires_at 后进入 EXPIRED，并使父 ApplicationRun 进入 retryable FAILED；新 attempt 不复用旧 Approval。
- COMPLETED 和 CANCELLED 是 Run 终态；FAILED 只能通过新的 attempt 重试。
- Run 收到取消请求后不得开始新节点或新副作用；终态写入必须通过运行版本和取消标记的条件更新。

## 8. 数据概要设计

### 8.1 实体关系

~~~text
User ──< JobAssignment >── Job ──< JobVersion
                                  │
                                  └──< MatchRun ──< MatchRunCandidate >── CandidateProfile
                                             │                  │
                                             └──< MatchReport   └── JobApplication
                                                      │
                                                      └──< ReportClaim
                                                              │
                                                              └──< ClaimEvidence >── EvidenceChunk

Candidate ──< CandidateProfile ── ResumeDocument ──< EvidenceChunk
    │
    └──< JobApplication >── Job
              │
              ├──< ApplicationRun ──< Approval
              ├──< ApplicationStatusHistory
              └──< Interview

AgentRun ──1 MatchRun 或 ApplicationRun
AgentRun ──< AgentEvent
EvaluationRun ──> DatasetVersion / MetricSnapshot
~~~

AgentRun 是存储层公共基表，用于统一 run_id、thread_id、run_type、状态、attempt 和 AgentEvent 外键；MatchRun 与 ApplicationRun 仍是两个独立领域对象和状态图，各自以 run_id 与公共表一对一关联。这样可以避免 AgentEvent 使用无法建立数据库外键的多态 ID。

### 8.2 核心实体

| 实体 | 关键内容 | 主要约束 |
|---|---|---|
| User | 用户名、密码哈希、角色、激活状态。 | username 唯一；角色枚举。 |
| Job | 当前状态、当前版本、创建者。 | 状态迁移受控。 |
| JobVersion | 原始描述、结构化要求、版本、内容哈希。 | job_id + version 唯一；被引用后不可变。 |
| JobAssignment | job_id、user_id、分配角色、分配者、分配/撤销时间。 | 同一岗位和用户最多一个有效分配。 |
| ResumeDocument | 存储键、文件元数据、SHA-256、状态和错误。 | SHA-256 重复检测；存储键不使用原文件名。 |
| Candidate | 人员主体和合成标识。 | 不保存岗位级状态。 |
| CandidateProfile | 候选人、源文档、结构化字段、状态和版本。 | 只有 READY 版本可检索。 |
| EvidenceChunk | 文档版本、位置、字符范围、原文和向量版本。 | 必须可回到固定文档版本。 |
| JobApplication | job_id、candidate_id、状态、乐观锁版本和 active_application_run_id。 | job_id + candidate_id 唯一；活动 Run 槽只指向属于本 Application 的 Run。 |
| AgentRun | run_type、thread_id、状态、attempt、版本、当前节点、取消请求和错误。 | run_id 主键；thread_id 全局唯一；run_type 决定唯一子表。 |
| MatchRun | run_id、JobVersion 和配置快照。 | 与 AgentRun 一对一；run_type=MATCH；无 Approval。 |
| MatchRunCandidate | Run、Profile 版本、JobApplication、各通道排名和快照顺序。 | run_id + candidate_profile_id 唯一；锁定 Top-K 输入。 |
| MatchReport | MatchRun、JobApplication、报告和模型快照。 | 每个 Run 与 Application 组合唯一。 |
| ReportClaim | 类型、文本、影响等级、支持等级、精确摘录。 | 支持等级为 SUPPORTED、PARTIAL 或 INSUFFICIENT。 |
| ClaimEvidence | claim_id、evidence_chunk_id、摘录范围。 | 防止用 JSON 数组绕过外键和归属校验。 |
| ApplicationRun | run_id、JobApplication 和匹配报告引用。 | 与 AgentRun 一对一；run_type=APPLICATION；一个 Run 只绑定一个 Application。 |
| Approval | Run、动作类型、原始/最终参数、状态、版本、expires_at、expiration_reason 和幂等键。 | 同一 Run 最多一个 PENDING；幂等键唯一。 |
| ApplicationStatusHistory | from、to、Approval、操作者和时间。 | 只追加，不覆盖。 |
| Interview | Application、问题版本、模拟日程 ID 和状态。 | 模拟日程 ID 唯一。 |
| AgentEvent | run_type、run_id、sequence、event_id、节点和状态。 | run_type + run_id + sequence 唯一；event_id 唯一。 |
| EvaluationRun | 数据集、配置、模型、Prompt、指标和产物。 | 结果不可原地改写。 |

### 8.3 关键索引

| 数据 | 建议索引 |
|---|---|
| 用户 | username 唯一索引。 |
| 岗位 | status、created_at；JobVersion 的 job_id + version 唯一索引。 |
| 岗位分配 | job_id + user_id 的有效记录部分唯一索引；user_id + revoked_at 查询索引。 |
| 文档 | content_sha256、status、created_at。 |
| Profile | candidate_id + version 唯一索引；status。 |
| EvidenceChunk | document_id、section/position；vector 列先使用精确搜索。 |
| JobApplication | job_id + candidate_id 唯一索引；job_id + status；active_application_run_id 唯一外键。 |
| Run | AgentRun.thread_id 全局唯一索引；run_type + status + updated_at；两个子表的 run_id 唯一外键。 |
| MatchRunCandidate | run_id + candidate_profile_id 唯一索引；run_id + snapshot_order。 |
| Approval | idempotency_key 唯一索引；application_run_id 上 PENDING 部分唯一索引；status + expires_at。 |
| AgentEvent | run_type + run_id + sequence 唯一索引；event_id 唯一索引。 |

小规模数据默认不创建 HNSW。只有精确搜索 p95 或数据规模超过项目设定门槛，且同一 Golden Dataset 的召回回归通过后才添加 HNSW。

### 8.4 快照与版本

MatchRun 必须保存：

- JobVersion ID 和内容哈希。
- Top-K CandidateProfile 版本。
- 聊天模型、Embedding 模型和维度。
- Prompt 版本。
- 检索通道、权重、RRF 常数和 Top-K。
- 硬规则版本。

Top-K 的 CandidateProfile 版本、JobApplication、通道名次和快照顺序保存在 MatchRunCandidate，不只保存在 Checkpoint 或 JSON 中。

MatchReport 和 Approval 保存运行时参数快照，不通过可变配置反推历史结果。

### 8.5 事务边界

| 场景 | 事务边界 |
|---|---|
| 岗位版本发布 | 校验状态、创建版本、更新当前版本和审计在一个事务中。 |
| JobAssignment | 校验用户、创建或撤销分配和审计在一个事务中。 |
| 文档接收 | 文件成功落盘后保存 Document；数据库失败时删除本次未引用文件。 |
| 报告保存 | MatchReport、ReportClaim、ClaimEvidence 和完成事件在一个事务中。 |
| 状态审批 | 锁定 Approval 与 JobApplication，校验版本，更新状态、历史和执行结果。 |
| 模拟排期 | 幂等查询、Interview 写入、Application 状态和 Approval 执行结果在一个事务中。 |
| ApplicationRun 创建 | 锁定 JobApplication，确认活动槽为空，创建 Run 并写入 active_application_run_id。 |
| Run 终态 | 以 run_id 比较并清空 active_application_run_id，避免旧 Run 清除新 Run 的槽。 |
| Approval 过期 | 锁定 PENDING Approval、ApplicationRun 和 JobApplication，写入 EXPIRED、FAILED、事件并清理活动槽。 |

未来接入真实外部日历时，不能把数据库事务跨越网络调用，应改用 Outbox 和外部幂等查询；该能力不进入 MVP。

## 9. Agent 概要设计

### 9.1 公共运行模型

两类 Run 共享以下概念：

- run_id：业务 Run ID。
- thread_id：LangGraph Checkpointer 恢复键。
- attempt：失败重试次数。
- version：并发控制和取消条件写入版本。
- status：业务可查询状态。
- current_node：最近节点。
- cancel_requested_at、cancel_requested_by：协作式取消标记。
- snapshot_version：输入和配置快照版本。
- error_code、failed_node、retryable：失败信息。

Checkpoint 只保存恢复所需 ID、路由和有限中间结果，不保存文件二进制或无界简历全文。

### 9.2 MatchRun State

| 字段组 | 内容 |
|---|---|
| 标识 | run_id、thread_id、attempt。 |
| 输入 | job_id、job_version_id、actor_id。 |
| 配置 | retrieval_config_id、model_config_id、prompt_version、rule_version。 |
| 候选集 | CandidateProfile 版本 ID 列表和通道分数引用。 |
| 进度 | current_node、completed_candidate_ids、failed_candidate_ids。 |
| 输出 | MatchReport ID 列表。 |
| 错误 | error_code、failed_node、retryable。 |

MatchRun 节点：

| 节点 | 输入 | 输出 | 失败策略 |
|---|---|---|---|
| parse_job | JobVersion ID | 标准化岗位要求引用 | Schema 错误终止。 |
| retrieve_candidates | 岗位要求、检索配置 | Top-K Profile 版本和通道分数 | 临时依赖错误可重试。 |
| hard_rule_evaluate | 岗位要求、确认资料 | PASS/FAIL/UNKNOWN、理由和证据 | 单候选人错误记录，不静默过滤。 |
| score_with_evidence | 岗位、候选人、EvidenceChunk | 结构化报告草稿 | 模型错误有限重试。 |
| validate_evidence | 报告草稿和快照 | 合法 Claim 或降级结果 | 非法引用不得保存为确定性结论。 |
| persist_reports | 已验证报告 | 报告 ID 和完成事件 | 数据库事务失败可重试。 |

### 9.3 ApplicationRun State

| 字段组 | 内容 |
|---|---|
| 标识 | run_id、thread_id、attempt。 |
| 输入 | application_id、match_report_id、actor_id。 |
| 审批 | action_type、approval_id、expected_application_version。 |
| 面试 | question_set_id、schedule_proposal_id、interview_id。 |
| 进度 | current_node、status。 |
| 错误 | error_code、failed_node、retryable。 |

ApplicationRun 节点：

| 节点 | 副作用 | 控制 |
|---|---:|---|
| human_review | 否 | 生成状态变更提案并持久化 Approval。 |
| wait_status_approval | 否 | interrupt；等待唯一决定。 |
| update_application_status | 是 | 审批、RBAC、归属、版本和幂等全部通过后执行。 |
| generate_interview_questions | 否 | 仅 SHORTLISTED 路径执行。 |
| wait_schedule_approval | 否 | interrupt；展示和允许修改排期参数。 |
| create_interview_schedule | 是 | 再次校验后调用 MockScheduleBackend。 |

### 9.4 Tool 边界

只读工具：

- search_candidates
- get_resume_evidence
- get_company_hiring_rules

副作用工具：

- update_application_status
- create_interview_schedule

副作用工具只在 ApplicationRun 的批准后节点注册。工具执行服务不信任模型输出，必须从已持久化的 ApplicationRun、Approval 和 JobApplication 重新构造或校验以下信息：

- 业务对象 ID 和岗位归属。
- 当前状态、目标状态和 expected_version。
- 当前用户权限及有效 JobAssignment。
- 审批状态和最终参数。
- 幂等键和既有执行结果。

### 9.5 证据链

每条 ReportClaim 包含：

- claim_type 和 claim_text。
- impact_level。
- support_level：SUPPORTED、PARTIAL 或 INSUFFICIENT。
- 一个或多个 EvidenceChunk 外键。
- 与证据位置一致的精确摘录。
- 置信说明，不把模型概率表述为事实概率。

运行时校验分两层：

1. 确定性引用校验：候选人归属、资料版本、Chunk 存在性、字符范围和摘录一致性。
2. 输出约束：高影响 Claim 若不是 SUPPORTED，只能输出“不足以判断”。

运行时不能仅凭引用存在证明语义正确。语义支持质量通过人工标注的结论—证据对离线评测，计算 SUPPORTED 精确率、三分类混淆矩阵和宏平均 F1。

## 10. API 概要设计

### 10.1 通用约定

| 项目 | 约定 |
|---|---|
| 协议 | HTTPS；MVP 本机环境可使用 HTTP。 |
| 格式 | 默认 JSON；OAuth2 token 端点使用 application/x-www-form-urlencoded；文件上传使用 multipart/form-data；事件使用 text/event-stream。 |
| 鉴权 | Authorization: Bearer JWT。 |
| 时间 | ISO 8601 UTC。 |
| 分页 | page、page_size；默认值和最大值由配置控制。 |
| 异步任务 | 返回 202、业务资源 ID、状态和查询地址。 |
| 并发更新 | expected_version 或 If-Match；冲突返回 409。 |
| 幂等 | 高风险异步创建、JobApplication 状态变更和排期副作用接受 Idempotency-Key。 |
| 关联 | 每个响应和错误包含 request_id；Run 响应包含 run_type。 |

统一错误结构：

~~~json
{
  "code": "APPLICATION_VERSION_CONFLICT",
  "message": "候选人流程状态已发生变化，请刷新后重试",
  "request_id": "req_01J...",
  "details": {
    "current_version": 4
  }
}
~~~

### 10.2 资源接口

| 方法与路径 | 用途 | 关键控制 |
|---|---|---|
| POST /auth/token | OAuth2 Password Flow 登录。 | application/x-www-form-urlencoded 的 username/password；统一认证错误和基本限流。 |
| GET /auth/me | 当前登录身份与角色。 | 已登录；前端据此决定可见动作，不作为权限判定依据。 |
| GET /jobs | 岗位列表。 | 招聘主管只返回有效分配岗位。 |
| POST /jobs | 创建岗位草稿。 | HR。 |
| GET /jobs/{id} | 岗位详情和当前版本。 | 资源级授权。 |
| PATCH /jobs/{id} | 编辑草稿或创建新版本。 | HR、乐观锁。 |
| POST /jobs/{id}/activate | 启用岗位。 | DRAFT → ACTIVE。 |
| POST /jobs/{id}/close | 关闭岗位。 | ACTIVE → CLOSED。 |
| GET /jobs/{id}/assignments | 查询分配。 | HR；招聘主管只能获知自己的当前授权结果。 |
| POST /jobs/{id}/assignments | 分配招聘主管。 | HR；校验用户角色和激活状态。 |
| DELETE /jobs/{id}/assignments/{user_id} | 撤销分配。 | HR；保留历史。 |
| POST /documents | 批量上传。 | HR；文件校验。 |
| GET /documents | 查询上传和解析任务。 | HR；分页和状态筛选。 |
| GET /documents/{id} | 查询解析状态和错误。 | 资源级授权。 |
| GET /documents/{id}/content | 查询原文块与 locator。 | 资源级授权；原文定位的唯一数据来源。 |
| POST /documents/{id}/retry | 手动重试。 | 仅 retryable。 |
| GET /jobs/{id}/candidates | 列表、关键词和显式筛选。 | 岗位授权；招聘主管只返回该岗位已关联候选人，缺少岗位上下文时 403 `JOB_CONTEXT_REQUIRED`（扁平路径的偏差记录见详细设计 12.3）。 |
| GET /jobs/{id}/profiles/{profile_id} | 候选人资料详情与当前版本。 | 岗位授权；同时校验候选人已与该岗位关联。 |
| POST /jobs/{id}/profiles/{profile_id}/confirm | 保存人工校对并确认资料。 | HR、乐观锁（expected_version）；确认后进入 READY。 |
| GET /jobs/{id}/profiles/{profile_id}/evidence | 查询证据块。 | 岗位授权。 |
| POST /jobs/{id}/profiles/{profile_id}/evidence | 钉住证据块。 | HR；证据必须绑定到候选人资料。 |
| GET /candidate-profiles/by-document/{document_id} | 按文档取草稿。 | 资源级授权；校对页据此加载待校对资料。 |
| GET /candidate-profiles/{profile_id}/evidence | 按资料查询证据块。 | 资源级授权。 |
| GET /jobs/{id}/applications | 岗位候选人和业务状态。 | 岗位授权。 |
| POST /jobs/{id}/match-runs | 启动 MatchRun。 | 默认 HR；Job=ACTIVE。 |
| GET /jobs/{id}/match-runs | 查询岗位历史 MatchRun。 | 岗位授权；分页。 |
| GET /match-runs/{id} | Run、报告和错误。 | 岗位授权。 |
| GET /match-runs/{id}/reports | 该 Run 的证据化报告列表。 | 岗位授权；报告结论必带证据引用。 |
| POST /match-runs/{id}/retry | 重试允许节点。 | retryable、原 thread_id。 |
| POST /match-runs/{id}/cancel | 请求取消未终态 Run。 | 幂等写入 cancel_requested_at；Worker 协作停止。 |
| POST /applications/{id}/runs | 启动 ApplicationRun。 | 锁定 JobApplication；已有活动 Run 时返回 409。 |
| GET /applications/{id}/runs | 查询候选人在该岗位的运行历史。 | 岗位授权；分页。 |
| GET /application-runs/{id} | Run、Approval 和面试进度。 | 岗位授权。 |
| POST /application-runs/{id}/retry | 重试允许节点。 | 不重复副作用。 |
| POST /application-runs/{id}/cancel | 请求取消未终态 Run。 | 幂等写入取消请求；WAITING 时同时使 PENDING Approval 失效。 |
| GET /approvals/{id} | 查询提案、最终参数和决定。 | 当前岗位授权。 |
| POST /approvals/{id}/decision | 批准、修改或拒绝。 | expected_version、幂等、重新授权。 |
| GET /interviews | 查询有权访问的面试记录。 | 岗位授权；分页。 |
| GET /interviews/{id} | 问题和模拟日程。 | 岗位授权。 |
| POST /evaluations | 启动离线评测。 | HR 或 ADMIN 的技术权限。 |
| GET /evaluations | 查询评测历史。 | 脱敏；分页。 |
| GET /evaluations/{id} | 查看指标和产物。 | 脱敏。 |
| GET /health/live | 进程存活。 | 不检查外部模型。 |
| GET /health/ready | 核心依赖就绪。 | 分别报告 PostgreSQL 和 Redis。 |

### 10.3 SSE 接口

| 路径 | Run 类型 |
|---|---|
| GET /match-runs/{id}/events | MATCH。 |
| GET /application-runs/{id}/events | APPLICATION。 |

事件数据：

~~~json
{
  "event_id": "evt_01J...",
  "run_id": "run_01J...",
  "run_type": "MATCH",
  "sequence": 12,
  "node": "score_with_evidence",
  "status": "running",
  "message": "正在生成证据化匹配报告",
  "occurred_at": "2026-08-31T08:00:00Z"
}
~~~

服务端处理顺序：

1. 鉴权并校验 Run 的岗位归属。
2. 根据 Last-Event-ID 或最后 sequence 查询缺失事件。
3. 按 sequence 重放。
4. 切换到实时订阅。
5. 每次事件批次和心跳前从权威数据重新校验用户、岗位和有效 JobAssignment。
6. 授权失效时停止发送业务数据并关闭连接；本地连接注册表收到 assignment.revoke 通知时可提前关闭，但通知不能替代权威校验。
7. 定期发送心跳。
8. 连接关闭时只释放订阅资源，不改变 Run。

## 11. 前端概要设计

### 11.1 路由

| 路由 | 页面 |
|---|---|
| /login | 登录页。 |
| /jobs | 岗位列表。 |
| /jobs/:jobId | 岗位详情、版本和分配。 |
| /documents | 上传和解析队列。 |
| /documents/:documentId/review | 原文与 Profile 校对。 |
| /candidates | 候选人检索和显式筛选。 |
| /candidates/:candidateId | 候选人、资料版本和岗位关联。 |
| /match-runs/:runId | MatchRun 节点、排名和报告。 |
| /application-runs/:runId | ApplicationRun、审批和恢复。 |
| /approvals/:approvalId | 独立审批页或可分享的受权入口。 |
| /interviews/:interviewId | 问题和模拟日程。 |
| /evaluations | 评测列表和对比。 |

### 11.2 页面交互原则

- 所有耗时操作显示状态、最近事件和可执行下一步。
- MatchRun 页面不出现“等待审批”状态。
- ApplicationRun 页面一次只展示当前 PENDING Approval。
- 匹配报告并排展示 Claim、支持等级、精确摘录和原文定位。
- FAIL 硬条件不得通过隐藏行或默认过滤伪装成“候选人不存在”。
- Mock 结果和真实模型结果在页面显著标识。
- 403、409、422 和依赖失败显示不同的恢复建议和 request_id。

### 11.3 SSE 客户端

前端使用 Fetch + ReadableStream：

1. 注入 Bearer Token。
2. 解析 SSE 帧并按 event_id 或 sequence 去重。
3. 保存最后确认的 event_id。
4. 断线后指数退避重连。
5. 收到终态后关闭连接并刷新 Run 查询。
6. Token 过期时停止自动重连并引导重新登录。

### 11.4 可用性与兼容性

- 支持当前稳定版 Chrome 和 Edge 的桌面视图。
- 表单、表格、上传、审批和错误恢复路径可以通过键盘完成。
- 状态同时使用文本、图标和颜色表达，不能只依赖颜色。
- 审批页必须先展示动作对象、当前值、提案值和修改后值，再允许提交。
- 页面刷新后从服务端恢复 Run 和 Approval，不依赖浏览器内存保持流程。

## 12. 异步任务设计

### 12.1 队列划分

| 队列 | 任务 |
|---|---|
| documents | PDF/DOCX 文本提取和结构化解析。 |
| embeddings | EvidenceChunk 和 Profile Embedding。 |
| agent | MatchRun、ApplicationRun 恢复和允许的节点重试。 |
| evaluations | 离线数据集评测和报告生成。 |
| maintenance | Approval 过期、孤儿文件和 QUEUED 任务协调。 |

MVP 可以由一个 Worker 消费多个队列，但任务路由需预先分开，便于后续限流和独立扩容。

### 12.2 任务幂等

每个任务持有稳定业务 ID 和 operation_key：

- 文档解析：document_id + parser_version + attempt。
- Embedding：chunk_id + model + dimension + content_hash。
- MatchRun：run_id + attempt。
- ApplicationRun 恢复：run_id + approval_id + decision_version。
- 评测：dataset_version + config_hash。

Worker 开始前查询业务状态，已完成任务直接返回既有结果。Celery task_id 不能代替业务幂等键。

### 12.3 Redis 故障

- Redis 不可用时，新异步请求返回依赖不可用或保持可重发的 QUEUED 状态。
- 已写入 PostgreSQL 的文档、Run、审批和事件不丢失。
- 恢复后由手动重试或受控协调任务重新发布。
- 不根据 Redis 中的短期状态覆盖 PostgreSQL 事实。

### 12.4 Approval 过期协调

可配置 expires_at 的 Approval 由 Scheduler 周期触发 maintenance 任务处理。任务使用数据库当前时间，并按小批量锁定 status=PENDING 且 expires_at 已到达的记录：

1. 再次校验 Approval 仍为 PENDING。
2. 写入 EXPIRED 和 expiration_reason=TIMEOUT。
3. 将父 ApplicationRun 标记为 retryable FAILED，错误码为 APPROVAL_EXPIRED。
4. 以 run_id 比较并清理 JobApplication.active_application_run_id。
5. 写入 AgentEvent，使等待页面立即收到终态。

审批决定接口也执行相同的到期检查，防止 Scheduler 延迟期间通过过期审批。处理任务按 approval_id + version 幂等；重新尝试流程必须通过 ApplicationRun retry 接口创建新 attempt 和新 Approval。

## 13. 安全与权限设计

### 13.1 身份认证

- 密码使用 Argon2 哈希。
- JWT 只包含 user_id、role、签发/过期时间和必要令牌标识。
- JWT 不包含简历、岗位或联系方式。
- Secret 通过环境变量或部署 Secret 注入，不提交 Git。
- 登录失败响应不泄露用户名是否存在。

### 13.2 资源级授权

| 资源 | HR | HIRING_MANAGER | ADMIN |
|---|---|---|---|
| 岗位和报告 | 可访问。 | 仅有效 JobAssignment。 | 仅脱敏技术视图。 |
| 岗位分配 | 可分配和撤销。 | 不可。 | 不可。 |
| 简历校对 | 可。 | 只读且限已分配岗位上下文。 | 不可。 |
| 启动 MatchRun | 可。 | MVP 默认不可。 | 不可。 |
| 审批 | 可。 | 仅有效分配岗位。 | 不可。 |
| 系统健康 | 不开放技术详情。 | 不开放技术详情。 | 可查看。 |

授权必须在以下时点重新执行：

- 请求资源时。
- 创建 Approval 时。
- 提交审批决定时。
- 恢复 ApplicationRun 时。
- 副作用工具执行前。
- 建立或恢复 SSE 连接时。
- SSE 重放、每个事件批次和每次心跳前；授权失效时立即停止发送业务数据并关闭连接。

### 13.3 不可信文件

- 同时校验扩展名、MIME 和文件签名。
- 不执行宏、脚本、嵌入对象或外部链接。
- 存储路径只使用内部随机键。
- 原文件名只作为经过转义的展示元数据。
- 解析器设置文件大小、页数、文本量、超时和资源限制。

### 13.4 Prompt Injection 防护

简历和岗位描述即使包含伪造系统消息、忽略规则、调用工具、替换 ID 或指定评分等文本，也只作为待分析数据。

分层控制：

1. Prompt 层：系统指令与文档内容使用不同消息和清晰数据边界。
2. Schema 层：模型只能返回定义字段，未知字段拒绝。
3. 图层：节点和条件边固定，文档不能提供节点名或路由。
4. Tool 层：MatchRun 和分析节点不注册副作用工具。
5. 参数层：业务 ID、角色、版本和幂等键来自可信状态。
6. 审批层：副作用必须引用已持久化 Approval。
7. 执行层：重新执行 RBAC、资源归属、状态和幂等校验。
8. 评测层：版本化攻击语料验证控制流改变、越权提案和审批绕过均为 0。

Prompt 分隔和 Schema 不能单独证明模型没有遵循文档指令。攻击数据集必须包含“干净版本—注入版本”配对样本，并为攻击指令设置可检测目标，例如唯一哨兵文本、指定分数、指定业务状态或伪造 evidence_chunk_id。评测分别报告：

- 控制流改变、越权工具提案和审批绕过次数。
- 攻击指令遵循成功数，即攻击指定的哨兵、分数、状态或伪造证据进入结构化输出。
- 硬条件确定性结果在配对样本间的变化数。
- 因注入指令新增且没有证据支持的高影响 Claim 数。

上述攻击成功数必须为 0；普通模型表述差异仍由证据语义支持指标约束，不用不稳定的全文完全一致作为门槛。

### 13.5 隐私与日志

- MVP 只使用合成数据。
- 日志、错误、AgentEvent 和测试快照不记录完整简历。
- Langfuse 只接收合成内容；关闭时核心流程正常运行。
- 下载文件、查看证据、订阅 Run 和审批均产生资源级审计。
- 密码、Token、API Key 和文件二进制禁止进入日志。

## 14. 可靠性与异常设计

### 14.1 错误分类

| 类型 | 示例 | 策略 |
|---|---|---|
| validation | 文件、Schema 或状态参数非法。 | 不重试，返回 422。 |
| authorization | 未认证或无资源权限。 | 返回 401/403，记录安全审计。 |
| conflict | 版本、审批或幂等冲突。 | 返回 409 和当前状态。 |
| transient_dependency | 模型限流、网络暂时失败、Redis 暂不可用。 | 有限退避重试。 |
| permanent_dependency | 不支持文件、模型输出持续非法。 | 标记失败，不循环重试。 |
| unknown_side_effect | 外部执行结果未知。 | 查询幂等结果，禁止盲目再次执行。 |

### 14.2 重试规则

- 只有明确标记 retryable 的错误可以重试。
- 重试增加 attempt 并保留原错误。
- 重试使用原 thread_id，从合法 Checkpoint 恢复。
- 已完成副作用先按幂等键查询，不能重复调用。
- COMPLETED 和 CANCELLED 不重新进入运行节点。

### 14.3 协作式取消

取消不是直接杀死 Worker，而是通过 AgentRun 上的 cancel_requested_at、cancel_requested_by 和运行 version 协作完成：

1. cancel 接口锁定 AgentRun；终态请求直接返回现有结果，否则在同一事务中写入取消请求、递增 version 并把 Run 置为 CANCELLED。
2. ApplicationRun 取消时以 run_id 比较并清理活动 Run 槽；WAITING_APPROVAL 的 PENDING Approval 同时进入 EXPIRED，expiration_reason=RUN_CANCELLED。
3. 尚在执行的 Worker 不被强制杀死，但在每个节点开始、模型或检索返回、持久化前检查取消标记和终态。
4. 节点结果使用 status、version 和 cancel_requested_at 的条件更新；条件不再满足时丢弃未提交结果。
5. 副作用执行前锁定 AgentRun 和 Approval 并再次检查取消标记。MVP 的 MockScheduleBackend 在该受控事务边界内执行，因此取消与副作用只有一个能先取得锁并提交。
6. 如果副作用已经确认执行成功，后到达的取消看到终态后只能返回现有结果，不能回滚事实；完整顺序进入审计。

未来真实外部日历调用必须改用 Outbox 和外部幂等状态查询，不能依赖持有数据库锁跨越网络请求。

### 14.4 Checkpoint 与业务事实

Checkpoint 丢失时：

- 已提交的 Job、Profile、Report、Approval、Interview 和状态历史仍是事实。
- 系统不得根据旧内存状态覆盖业务表。
- 如果不能安全确定恢复点，将 Run 标记 FAILED 并要求创建受控重试 attempt。

业务写入成功但 Checkpoint 未推进时：

- 节点重放通过唯一约束和幂等键返回已有结果。
- 节点必须设计为“可重复进入”，不能假设只执行一次。

### 14.5 降级

| 依赖不可用 | 降级行为 |
|---|---|
| Langfuse | 关闭外部 Trace，本地日志和 AgentEvent 继续。 |
| Qwen | 当前模型节点失败或进入明确标识的 Mock 演示模式。 |
| Redis | 拒绝或延后新异步任务，保留 PostgreSQL 事实。 |
| SSE | 前端轮询 Run 详情作为临时只读降级。 |
| MockScheduleBackend | Approval 保留 EXECUTION_FAILED，可按同一幂等键重试。 |

### 14.6 性能设计

- 在 README 记录的演示环境、100 名候选人和数据库预热条件下，普通 REST 接口 p95 目标小于 500 ms；外部模型和异步处理不计入该指标。
- 上传接口只完成文件持久化、业务记录和任务入队，不同步等待解析或 Embedding。
- AgentEvent 提交后在正常本地网络中应在 2 秒内展示到前端。
- 列表接口强制分页；详情聚合使用批量查询，避免候选人、证据和报告的 N+1 查询。
- MatchRun 对候选人评分设置受控并发和模型限流，单候选人失败不造成无限并发重试。
- 默认使用精确向量搜索；任何索引优化必须同时保存延迟和召回回归结果。
- 解析、Embedding、检索、每个 Agent Node 和总运行耗时分别记录。

## 15. 可观测与评测设计

### 15.1 关联标识

所有关键日志和 Trace 使用：

- request_id：一次 HTTP 请求。
- task_id：一次 Celery 投递。
- run_id、run_type、thread_id：Agent 运行。
- attempt：运行尝试。
- user_id：脱敏后的操作者 ID。
- job_id、application_id：只记录业务 ID，不复制原文。

### 15.2 指标

| 领域 | 指标 |
|---|---|
| API | 请求量、状态码、p50/p95 延迟。 |
| Worker | 队列长度、任务耗时、重试和失败。 |
| 文档 | 格式解析成功率、字段 Precision/Recall/F1。 |
| 检索 | Recall@K、MRR 或 nDCG、各通道贡献。 |
| 证据 | 引用有效率、覆盖率、SUPPORTED 精确率、三分类宏平均 F1。 |
| Tool | 选择准确率、首次参数合法率、拒绝率、执行成功率。 |
| 工作流 | 完成率、中断成功率、恢复率、重复执行防护。 |
| 安全 | Prompt Injection 控制流改变、越权提案、审批绕过、攻击指令遵循成功数和配对硬规则变化数。 |
| 成本 | 模型 Token、节点延迟和估算费用。 |

### 15.3 评测数据

- 30 至 50 份合成简历。
- 3 至 5 个岗位及人工相关性等级。
- 每份简历 3 至 8 条结论—证据对，覆盖三种支持等级。
- 正确工具调用和不应调用工具的正反案例。
- 至少 20 组干净版本—注入版本配对案例，覆盖伪造系统消息、工具诱导、唯一哨兵、指定评分、指定状态和伪造证据 ID。
- 审批并发、重复提交、超时和拒绝案例。
- 模型超时、Worker 重启、SSE 断线和 Redis 故障案例。

外部真实模型评测使用手动或定时工作流；普通 CI 使用 FakeModel 或录制响应。

## 16. 部署设计

### 16.1 Docker Compose 服务

~~~text
browser
  ↓
nginx
  ├── web
  └── api
        ├── postgres + pgvector
        ├── redis
        ├── file-volume
        └── external model APIs

worker
  ├── postgres + pgvector
  ├── redis
  ├── file-volume
  └── external model APIs

scheduler
  ├── postgres
  └── redis
~~~

PostgreSQL、Redis 和文件目录使用独立 Volume。API、Worker 与 Scheduler 使用同一镜像版本和配置 Schema。Scheduler 只发布受控维护任务，不直接修改业务事实。Alembic 作为独立一次性迁移步骤，不在多个服务中并发执行。

API 和 Worker 镜像固定 Python 3.12；Web 构建镜像固定满足当前 Vite 要求的受支持 Node LTS。Celery Worker 只在 Linux 容器中运行，不支持 Windows 原生 Worker。

### 16.2 配置分类

| 类别 | 示例 |
|---|---|
| Secret | JWT_SECRET、DATABASE_PASSWORD、QWEN_API_KEY、LANGFUSE_SECRET。 |
| 连接 | DATABASE_URL、REDIS_URL、MODEL_BASE_URL。 |
| 模型 | CHAT_MODEL、EMBEDDING_MODEL、EMBEDDING_DIMENSION。 |
| 文件 | MAX_FILE_SIZE、MAX_BATCH_FILES、STORAGE_ROOT。 |
| 检索 | TOP_K、RRF_K、通道权重、HNSW 开关。 |
| 运行 | 节点超时、最大重试、SSE 心跳。 |
| 功能开关 | LANGFUSE_ENABLED、MOCK_MODEL_MODE。 |

配置在 API 和 Worker 启动时用 Pydantic 校验。缺失必需配置时快速失败，不使用隐式生产默认值。

### 16.3 健康检查

- liveness：进程事件循环可响应。
- readiness：分别检查 PostgreSQL、Redis 和必要文件目录。
- readiness 不在每次检查中调用外部模型。
- 模型和 Embedding 的真实连通性由手动诊断命令或工作流执行。

## 17. 测试概要设计

### 17.1 测试分层

| 层级 | 设计重点 |
|---|---|
| 单元测试 | Schema、状态迁移、硬规则、RRF、权限策略、证据范围和幂等。 |
| Node 测试 | 固定 State + FakeModel，验证增量、路由、错误和无副作用。 |
| Repository 集成 | PostgreSQL 约束、部分唯一索引、事务和 pgvector 查询。 |
| Worker 集成 | Celery 重复投递、失败重试和队列恢复。 |
| API 契约 | OpenAPI Schema、错误结构、分页、状态码和授权。 |
| SSE 契约 | 事件字段、重放、去重、顺序和越权。 |
| 安全测试 | 文件边界、RBAC、Prompt Injection、非法工具参数和日志脱敏。 |
| E2E | 岗位分配、解析校对、MatchRun、ApplicationRun、审批和恢复。 |
| 离线评测 | 解析、检索、证据语义支持、Tool Calling 和攻击语料。 |

### 17.2 关键自动化场景

1. 招聘主管只可访问有效 JobAssignment 的岗位；撤销后新请求返回 403，已建立 SSE 在发送下一批数据前关闭。
2. 不支持文件不创建 Profile；重复任务不创建重复数据。
3. 未确认 Profile 不进入检索。
4. MatchRun 的 Top-K 中，硬条件 FAIL 候选人仍生成报告。
5. MatchRun 不创建 Approval、不改变 JobApplication 状态。
6. 一个 ApplicationRun 只绑定一个 JobApplication，同一 JobApplication 最多一个活动 Run；并发第二次创建返回 409。
7. 未审批副作用调用数为 0，重复审批只执行一次。
8. WAITING_APPROVAL 时重启 API/Worker，可以从同一 thread_id 恢复。
9. 非法、跨候选人、跨版本和摘录范围不一致的证据被拒绝。
10. Prompt Injection 配对样本不改变控制流、工具选择、业务 ID、硬规则或审批，且不输出攻击指定的哨兵、分数、状态和伪造证据。
11. SSE 断线重连不丢事件、不重复推进 UI。
12. 日志、Trace 和测试产物不包含预设敏感标记。
13. RUNNING Run 被取消后不再持久化节点结果或开始副作用；重复取消返回同一终态。
14. PENDING Approval 到期后进入 EXPIRED，父 Run 进入 retryable FAILED，并能通过新 attempt 生成新 Approval。

### 17.3 发布门槛

| 指标 | 门槛 |
|---|---|
| 支持格式解析成功率 | 不低于 95%。 |
| 核心字段抽取 | 宏平均 F1 不低于 0.90。 |
| 检索 | Recall@10 不低于 0.85，并报告 MRR 或 nDCG。 |
| 证据引用 | 无证据确定性结论和非法引用均为 0。 |
| 证据语义支持 | SUPPORTED 精确率不低于 95%，三分类宏平均 F1 不低于 0.85。 |
| 高影响结论 | 人工标注为 PARTIAL/INSUFFICIENT 却作为确定性事实的数量为 0。 |
| Tool 参数 | 首次合法率不低于 98%，执行层非法参数拒绝率为 100%。 |
| 副作用 | 未审批实际执行次数为 0。 |
| Prompt Injection | 控制流改变、越权提案、审批绕过、攻击指令遵循成功数和配对硬规则变化数均为 0。 |
| 恢复 | 定义的重启、断线和重复提交 E2E 全部通过。 |
| 工程质量 | Ruff、mypy、pytest、前端检查、构建和关键 Playwright 全部通过。 |

## 18. 需求追踪

| 需求域 | 主要设计章节 | 主要验证 |
|---|---|---|
| AUTH | 10、13 | 登录、JWT、RBAC、SSE 鉴权。 |
| JOB | 5、6.1、8、10 | 版本、状态、JobAssignment 和权限。 |
| DOC | 6.2、8、12、13.3 | 文件校验、解析、幂等和证据定位。 |
| CAN | 5、7、8 | Profile 版本、Application 唯一和状态隔离。 |
| RET | 6.3、8.3、15 | RRF、pgvector、Recall@K。 |
| AGT | 6.4、6.5、9 | 双图、节点、Checkpoint 和证据。 |
| APR | 6.5、8.5、9.4、10 | interrupt、版本、幂等和副作用。 |
| INT | 6.5、9.3、10 | 问题 Schema、敏感主题和模拟排期。 |
| EVT | 6.6、8、10.3、11.3 | SSE、重放、顺序、去重和授权。 |
| EVAL | 9.5、15、17 | 指标、数据集和发布门槛。 |
| OPS | 12、16、17 | Compose、迁移、健康检查和 CI。 |
| NFR | 11 至 17 | 性能、安全、恢复、可维护性和测试。 |
| OBJ/CON/BR/TBD | 1、2、6、7、13、19、21 | 设计原则、业务规则、约束和默认决策复核。 |

## 19. 默认决策与演进边界

| 事项 | MVP 默认 | 未来演进 |
|---|---|---|
| 招聘主管启动 MatchRun | 不允许，仅查看和审批。 | 增加岗位级 capability。 |
| 候选人自动合并 | 不做，只检测相同文件哈希。 | 引入人工确认的身份合并。 |
| DOCX 证据定位 | 段落、表格/单元格和字符范围。 | 增加渲染页码映射。 |
| Token | 短期 Access Token，不做复杂轮换。 | Refresh Token 或企业 SSO。 |
| Langfuse | 可选 Cloud，关闭可运行。 | 独立部署和组织级治理。 |
| 排期 | MockScheduleBackend。 | Outbox + 真实日历适配器。 |
| 向量索引 | 精确搜索。 | 基于性能和召回门槛启用 HNSW。 |
| 数据 | 只使用合成数据。 | 另立隐私、合规和模型治理项目。 |

## 20. 详细设计输入清单

进入编码前，需要在详细设计或实现 PR 中继续产出：

1. SQLAlchemy 模型、枚举、外键、部分唯一索引和 Alembic 迁移。
2. Pydantic API、Agent State、Event、Tool 和模型输出 Schema。
3. OpenAPI 方法、请求、响应、错误码和权限表。
4. MatchRun 与 ApplicationRun 的 LangGraph 图定义和节点契约。
5. Prompt 模板版本、数据边界和 FakeModel Fixture。
6. StorageBackend、ModelGateway、EmbeddingGateway 和 ScheduleBackend 接口。
7. SSE 重放查询、心跳和前端解析器契约。
8. Golden Dataset 格式、指标公式和发布门禁脚本。
9. Docker Compose、配置 Schema、健康检查和 CI 工作流。
10. 页面线框、路由守卫、查询键和错误状态。

## 21. 设计结论

本设计采用模块化单体和单 Agent 双运行图，在不扩大六周 MVP 边界的前提下形成清晰的工程闭环：

- MatchRun 负责可评测的岗位级批量分析。
- ApplicationRun 负责可恢复的单候选人审批与副作用。
- JobAssignment 提供招聘主管资源级授权依据。
- EvidenceChunk、ReportClaim 和 ClaimEvidence 提供可验证证据链。
- 固定图、分层授权、审批与幂等共同隔离不可信文档和副作用。
- PostgreSQL 业务事实、LangGraph Checkpoint 和 AgentEvent 分工明确。

该概要设计可以作为数据库、API、Agent、前端和测试详细设计的统一输入。实现过程中如果需要改变本文的运行边界、核心实体或发布门槛，应先同步更新需求分析、架构决策和本说明书，再进入代码变更。
