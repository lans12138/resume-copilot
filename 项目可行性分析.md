# 企业招聘 Copilot 项目可行性分析

> 分析日期：2026-08-31
>
> 复核版本：1.3（2026-08-31）
>
> 分析依据：[技术栈选型与架构决策](./技术栈选型与架构决策.md)
>
> 项目约束：一人独立开发、六周完成、用于应聘 AI Agent 开发岗位

## 1. 分析结论

### 1.1 总体结论

本项目作为**求职作品集级 MVP 有条件可行**，作为可处理真实企业招聘数据的生产系统则**无法在六周内由一人完整交付**。

现有方案的业务闭环、模块化单体、单 Agent 双运行图、PostgreSQL + pgvector、Human-in-the-Loop 和自动化评测方向合理，不需要推倒重来。岗位级 `MatchRun` 与候选人级 `ApplicationRun` 的拆分能够消除批量匹配与单人审批之间的状态歧义，增加的模型和接口成本可控。真正影响六周目标的不是单个框架，而是同时实现完整前端、认证、文档解析、混合检索、Agent、异步任务、可观测、评测、部署和端到端测试所带来的范围叠加。

只要执行以下收缩，六周内可以完成一个有说服力、可运行、可演示、可测试的版本：

1. 只支持电子版 PDF 和 DOCX，不做 OCR。
2. MVP 只使用合成简历，不接入真实招聘数据；脱敏样本也不默认视为无隐私风险。
3. 只实现单企业、少量角色和单机 Docker Compose，不实现生产级多租户与高可用。
4. 排期工具先使用可替换的本地模拟实现，不接入真实日历平台。
5. Langfuse 使用托管服务或作为可选能力，不在主项目 Compose 中自托管完整基础设施。
6. 先做精确向量检索，数据规模证明有必要后再启用 HNSW。
7. 将所有非核心页面、复杂仪表盘和高级权限放到核心闭环完成之后。

### 1.2 分维度判断

| 维度 | 判断 | 说明 |
|---|---|---|
| 业务价值 | 高 | 招聘流程天然需要解析、检索、证据生成、工具调用和人工审批，适合展示 Agent 工程能力。 |
| 技术可行性 | 高 | 所选组件能够覆盖需求，LangGraph 持久化、pgvector 混合检索和 Qwen Function Calling 均有官方支持。 |
| 六周进度可行性 | 中 | 完整原始范围偏大；按本文 MVP 边界实施后可行。 |
| 单人维护可行性 | 中高 | 模块化单体适合单人开发，但必须避免自托管过多基础设施。 |
| 演示可行性 | 高 | 可构造稳定的合成数据和端到端演示路径。 |
| 生产上线可行性 | 低 | 隐私合规、真实身份集成、多租户、备份、高可用、安全审计和模型治理尚未覆盖。 |

## 2. 建议的 MVP 边界

### 2.1 六周内必须交付

- 创建和查看岗位。
- 上传文本型 PDF、DOCX 简历，并展示解析状态。
- 提取候选人基本信息、技能、教育和工作经历。
- 支持人工修改并确认解析结果。
- 保存简历原文片段、页码或段落位置，形成可追溯证据。
- 使用结构化条件、关键词和向量完成候选人混合检索。
- 使用确定性规则评估学历、年限等硬条件，输出 `PASS`、`FAIL` 或 `UNKNOWN`，但不自动淘汰或隐藏候选人。
- 使用 LangGraph 生成带证据的匹配报告。
- 使用岗位级 `MatchRun` 生成一批报告，使用候选人级 `ApplicationRun` 更新单个 `JobApplication`、生成面试问题并在副作用前等待人工审批。
- 支持两类 Agent Run 的持久化和失败展示，以及 `ApplicationRun` 暂停后的恢复。
- 通过 SSE 展示关键节点状态，不要求逐 Token 输出。
- 建立解析、检索、工具调用、审批和恢复的自动化测试。
- 提供 Docker Compose、演示数据、README 和 CI。

### 2.2 有余力再做

- React Flow 完整动态图，而不是只读节点状态图。
- ECharts 综合运营仪表盘。
- 更复杂的 RBAC 管理界面。
- HNSW 参数调优和大数据量性能测试。
- 多岗位批量匹配。
- Prompt 在线版本管理和多模型对比实验。
- 面试问题的人工编辑历史。

### 2.3 明确不进入首版

- 扫描件 OCR。
- 多 Agent 协作。
- 自动淘汰、自动录用或无人工确认的排期。
- 企业 SSO、组织架构同步和复杂多租户隔离。
- 真实邮箱、短信或第三方日历集成。
- Kubernetes、跨区域容灾和生产级高可用。
- 真实候选人数据以及生产合规承诺。

建议演示数据规模为 3 至 5 个岗位、30 至 100 份合成简历。这个规模足以展示检索和评测，又不会把时间消耗在数据制造和索引调优上。

## 3. 端到端业务流程可行性

| 阶段 | 实现方式 | 可行性 | 主要条件 |
|---|---|---|---|
| 创建岗位 | React 表单、FastAPI REST、Pydantic 校验、PostgreSQL 持久化 | 高 | 首版只保留岗位名称、描述、硬条件、技能和状态。 |
| 上传简历 | 文件写入 StorageBackend，数据库记录元数据，Celery 异步处理 | 高 | 限制格式、大小和批量数量；任务必须幂等。 |
| 文档解析 | PyMuPDF、python-docx 提取文本，LLM 输出结构化字段 | 中高 | 仅承诺电子文档；必须保存原文定位和解析错误。 |
| 人工校对 | 表单编辑结构化字段，确认后进入索引 | 高 | 区分机器提取值、人工修正值和最终确认值。 |
| 建立索引 | 生成 1024 维向量并写入 pgvector，同时维护结构化技能和文本字段 | 高 | 固定模型、维度和文本切分版本，变更后可重建索引。 |
| 候选人召回 | 结构化匹配、关键词检索、向量检索、RRF 融合 | 中高 | 结构化通道参与排序但不默认淘汰硬条件失败者；必须先建立小型标注集，否则无法证明排序有效。 |
| 硬规则评估 | Python 规则节点 | 高 | 规则输入必须来自已确认字段，记录 `PASS`、`FAIL` 或 `UNKNOWN` 及原因；节点不得自动淘汰或隐藏候选人。 |
| 证据化评分 | LLM 基于已召回的简历证据输出结构化报告 | 中 | 禁止模型凭记忆补充事实；除引用有效性外，还要用人工标注集评测证据是否语义支持结论。 |
| 人工审批 | 候选人级 ApplicationRun + LangGraph interrupt + PostgreSQL Checkpointer | 高 | 每个 Run 只绑定一个 JobApplication，每个 JobApplication 同时最多一个活动 Run；审批动作要有操作者、时间、原参数和修改后参数。 |
| 面试问题 | 基于岗位、匹配报告和证据生成 | 高 | 问题要关联考察目标，避免生成与候选人无关的问题。 |
| 创建排期 | 首版调用 MockScheduleBackend | 高 | 仍按照真实副作用工具设计审批、幂等键和审计日志。 |
| 实时展示 | FastAPI SSE + 前端事件归并 | 中高 | 要先解决 SSE 鉴权、断线重连和重复事件问题。 |
| 恢复与回放 | LangGraph Checkpoint + 业务 Run/Event 表 | 中高 | Checkpoint 用于执行恢复，业务事件表用于查询和审计，职责不能混淆。 |

## 4. 核心技术可行性与必要修正

### 4.1 前后端分离和模块化单体

React + FastAPI 的前后端分离方案适合 Agent Trace、人工审批和复杂表单。模块化单体也符合单人六周开发的约束，可以共享事务、模型和部署单元。

首版应保持一个前端、一个 API、一个 Worker 和一个使用同镜像的轻量 Scheduler，不要把解析、检索和 Agent 拆成独立微服务。Scheduler 只触发 Approval 过期等维护任务，不保存业务事实。领域模块可以在代码层隔离，但只在出现独立扩容或安全边界时再拆服务。

### 4.2 模型名称需要在编码前修正

原架构文档曾指定 `qwen3.8-plus`。截至 2026-08-31，该名称不在百炼官方文本生成模型列表中；官方列表提供 `qwen3.8-max`、`qwen3.7-plus` 和 `qwen3.8-flash` 等模型，Function Calling 文档也明确列出了 Qwen3.7-Plus 系列。本次复核已将架构文档中的默认模型修正为 `qwen3.7-plus`。

因此首版建议：

- 默认聊天模型改为 `qwen3.7-plus`。
- 模型名称只通过环境变量配置，业务代码不写死版本。
- CI 和大部分单元测试使用 FakeModel，避免测试依赖外部 API。
- 单独设置少量需要真实模型的评测任务，不在每次普通测试中执行。
- 应用启动时执行一次模型配置检查，尽早发现模型下线、地域或权限问题。

参考：[百炼文本生成模型列表](https://help.aliyun.com/zh/model-studio/model-list-text-generation/)、[百炼 Function Calling](https://help.aliyun.com/en/model-studio/qwen-function-calling)。

### 4.3 Embedding 与 pgvector 兼容

`qwen3.7-text-embedding` 当前存在，官方支持 1024 维输出。pgvector 的 `vector` 类型支持最多 2000 维，因此 1024 维可以直接建立 HNSW 索引，选型在技术上兼容。

必须把以下信息写入每条向量记录：

- `embedding_model`
- `embedding_dimension`
- `chunking_version`
- `source_document_version`
- `content_hash`

模型、维度或切分策略变化时，应创建新索引版本并重建向量，不能把不同模型生成的向量混在同一相似度空间中。

参考：[百炼 Embedding 模型说明](https://help.aliyun.com/zh/model-studio/embedding)、[pgvector 官方项目](https://github.com/pgvector/pgvector)。

### 4.4 检索方案可行，但中文关键词通道需要调整

pgvector 官方明确支持将向量检索与 PostgreSQL 全文检索结合，并使用 RRF 或重排模型融合结果，因此总体方向可行。

但是，PostgreSQL 内置全文检索依赖 Parser 和 Dictionary，官方文档说明目前只提供一个内置 Parser。对于中文简历，不能在没有实测的情况下假设默认全文检索能提供稳定的中文分词效果。

六周 MVP 建议把关键词通道改为：

1. LLM 或规则提取并标准化技能，如 `Java`、`C++`、`Spring Boot`、`注册会计师`。
2. 使用候选人技能关联表完成精确匹配。
3. 对证书、学校、公司和原文片段使用 `ILIKE` 或 `pg_trgm` 辅助检索。
4. 向量检索负责中文语义召回。
5. 使用 RRF 合并结构化/关键词结果与向量结果。

`pg_trgm` 是 PostgreSQL 官方扩展，支持基于 trigram 的相似度以及 `LIKE`、`ILIKE` 索引，可作为轻量补充。是否引入专用中文分词扩展应由评测结果决定，不应作为首周前置依赖。

参考：[PostgreSQL Full Text Search Parser](https://www.postgresql.org/docs/current/textsearch-parsers.html)、[PostgreSQL pg_trgm](https://www.postgresql.org/docs/current/pgtrgm.html)。

### 4.5 小数据集不需要立即启用 HNSW

pgvector 默认精确近邻查询具有完整召回；HNSW 用内存和建索引时间换取大数据量下的查询速度。演示数据只有几十到几百份简历时，精确查询更简单，也方便得到可靠的检索评测基线。

建议顺序为：

1. 先使用精确余弦距离完成检索与 Recall@K 基线。
2. 当数据量或延迟超过预定门槛时再添加 HNSW。
3. 添加 HNSW 后重新测量召回率，因为近似索引可能改变结果。
4. 用户显式启用结构化筛选时测试筛选后的召回数量，必要时启用 pgvector iterative scan；该查询筛选不等于 MatchRun 的硬条件评估。

这不会改变架构，只是推迟不必要的优化。

### 4.6 LangGraph Checkpoint 与人工审批可行

LangGraph 官方持久化文档提供独立的 `langgraph-checkpoint-postgres` 包以及 `PostgresSaver`、`AsyncPostgresSaver`，并明确将持久化用于 Human-in-the-Loop、故障恢复和历史回放。

实现时要注意：

- 首次部署需要执行 Checkpointer 的 `setup()` 或等价迁移步骤。
- 每次运行必须有稳定、唯一的 `thread_id`。
- Agent State 只保存恢复执行所需数据，完整业务对象仍通过 ID 从业务表读取。
- `update_application_status` 和 `create_interview_schedule` 在工具实际执行前 interrupt。
- 审批恢复接口必须验证当前状态和审批版本，防止重复批准。

参考：[LangGraph Persistence](https://docs.langchain.com/oss/python/langgraph/persistence)、[LangGraph Human-in-the-Loop](https://docs.langchain.com/oss/python/langchain/human-in-the-loop)。

### 4.7 SSE 可行，JWT 鉴权方案已闭合

浏览器原生 `EventSource` 构造参数只有 URL 和 `withCredentials`，不能像普通 Fetch 请求一样设置自定义 `Authorization` Header。因此“Bearer JWT + 原生 EventSource”不能直接按普通 REST 请求方式实现。

可行方案有两种：

- 使用 `fetch()` 读取 `text/event-stream`，携带 Bearer Token，并自行实现断线重连和 `Last-Event-ID`；或者
- 全站同源部署，将短期会话放在 `HttpOnly`、`Secure`、`SameSite` Cookie 中，并为有副作用的 REST 接口增加 CSRF 防护。

本次需求基线已选择第一种方案：MVP 使用 Fetch Stream 携带 Bearer Token；Cookie 方案只作为后续备选，不并行实现。

不建议把长期 JWT 放在 SSE 查询参数中，因为 URL 可能进入浏览器历史、代理日志和服务日志。

无论选择哪种方式，事件都必须包含递增 `event_id`，前端按 ID 去重，服务端支持从最后事件恢复。Nginx 的 SSE 路由需要关闭响应缓冲。

参考：[HTML EventSource 标准](https://html.spec.whatwg.org/multipage/server-sent-events.html#the-eventsource-interface)。

### 4.8 Celery + Redis 可行，但 Worker 必须运行在 Linux 环境

Celery 官方文档支持 Python 3.12，并将 Redis 列为功能完整的 Broker；同时官方明确不支持 Microsoft Windows。因此本项目不能依赖 Windows 原生 Celery Worker。

开发、测试和演示应统一使用 Docker Desktop/WSL 2 上的 Linux 容器运行 API、Worker、Scheduler、PostgreSQL 和 Redis。任务消息只传文档 ID、候选人 ID 等小消息，不传 PDF 二进制或大段简历文本。最终业务状态写入 PostgreSQL，Redis 丢失后可以重建任务状态。

Celery 任务至少需要：

- 幂等键和唯一业务任务 ID。
- 明确的自动重试范围和指数退避。
- 软/硬超时。
- 数据库事务边界。
- 失败状态持久化。
- 对重复投递的防护。
- 基于数据库时间的 Approval 过期协调，以及与审批决定接口一致的原子状态迁移。

参考：[Celery 5.6 文档](https://docs.celeryq.dev/en/stable/)、[Celery Redis 配置](https://docs.celeryq.dev/en/v5.6.2/getting-started/backends-and-brokers/redis.html)。

### 4.9 文件存储方案适合单机演示

Docker Volume + StorageBackend 对单机作品集足够，且 MinIO 官方开源仓库已归档，原文档不再引入 MinIO 的决定合理。

首版仍应实现：

- 文件类型、扩展名、MIME 和大小校验。
- 随机化内部文件名，禁止使用用户文件名拼接路径。
- SHA-256 内容哈希和重复文件检测。
- 数据库事务失败后的孤儿文件清理任务。
- 删除候选人时的文件删除策略和审计记录。

该方案不适合多实例共享、跨机容灾和生产备份，但 StorageBackend 接口为后续 S3 迁移保留了边界。

参考：[MinIO 官方仓库](https://github.com/minio/minio)。

### 4.10 Langfuse 不宜在主项目内完整自托管

Langfuse 当前自托管架构不仅包含 Web 和 Worker，还需要 PostgreSQL、ClickHouse、Redis/Valkey 和对象存储。官方给出的最低资源中，ClickHouse 单项即建议 2 CPU、8 GiB 内存，完整部署会明显增加普通开发机和六周项目的负担。

建议：

- 首选 Langfuse Cloud，并且 MVP 只上传合成数据；脱敏数据也不默认视为可上传。
- 通过环境变量控制是否启用 Trace；Langfuse 不可用时不阻断核心业务。
- 本地保留结构化应用日志和 Agent Run/Event 表，不能把 Langfuse 当作唯一审计存储。
- 如果必须完全离线，首版只使用本地日志和评测 Harness，把自托管 Langfuse 放入后续路线图。

参考：[Langfuse Self-hosting](https://langfuse.com/self-hosting)、[Langfuse 资源要求](https://langfuse.com/self-hosting/configuration/scaling)。

## 5. 数据和状态设计可行性

### 5.1 最小业务实体

为了形成闭环，首版至少需要以下实体：

| 实体 | 核心用途 |
|---|---|
| User、Role | 登录身份和基础 RBAC。 |
| Job | 岗位描述、结构化要求和状态。 |
| JobAssignment | 岗位与招聘主管的分配关系、分配角色、操作者和撤销时间。 |
| Candidate | 候选人主体，不直接承担所有简历字段。 |
| JobApplication | 岗位与候选人的关联、岗位内流程状态和版本；同一候选人在不同岗位下独立流转。 |
| ResumeDocument | 文件元数据、版本、解析状态和内容哈希。 |
| ResumeSection / EvidenceChunk | 原文、页码/段落、字符范围和向量。 |
| CandidateProfile | 经过校对的结构化简历结果。 |
| MatchRun | 某岗位的一次匹配运行及配置快照。 |
| ApplicationRun | 单个 JobApplication 的审核、状态变更、面试问题和排期运行。 |
| MatchReport | 候选人的分数、结论、风险和证据引用。 |
| Approval | 待审批动作、原参数、修改参数、决定和操作者。 |
| Interview | 面试问题和模拟排期结果。 |
| AgentEvent | 用于前端 SSE、审计和断线恢复的结构化事件。 |

### 5.2 证据链必须是一等数据模型

匹配报告中的每项结论应引用 `evidence_chunk_id`，而不是只保存模型生成的引文字符串。证据记录需要保存文档版本、页码或 DOCX 段落、起止位置和原文快照；历史报告始终校验其 MatchRun 锁定的资料版本，而不是候选人后来产生的最新版本。`ReportClaim` 还要保存精确摘录与 `SUPPORTED`、`PARTIAL` 或 `INSUFFICIENT` 支持等级。

建议流程：

```text
LLM 生成结构化结论和 evidence_chunk_id
  → Pydantic 校验字段
  → 验证 chunk 属于本次 MatchRun 锁定的候选人和资料版本
  → 验证摘录范围、原文和版本一致
  → 对高影响结论执行支持等级约束
  → 保存报告
```

引用存在只能证明“指向了某段文本”，不能证明文本真的支持结论。运行时必须把引用非法或高影响结论证据不足的情况降级为“不足以判断”；离线评测必须使用人工标注的结论—证据对测量 `SUPPORTED` 语义支持精确率，并报告三分类混淆矩阵和宏平均 F1，不能只统计证据覆盖率。

### 5.3 Agent 状态与业务状态分离

LangGraph Checkpoint 负责恢复执行；PostgreSQL 业务表负责最终事实和审计。Agent State 中只保存 ID、阶段性结果、错误和路由信息，不保存重复的大段简历全文。

`MatchRun` 只允许解析、检索、硬条件评估、评分和报告持久化，不进入审批且不暴露副作用工具。所有有副作用的工具只允许在绑定单个 `JobApplication` 的 `ApplicationRun` 中遵循：

```text
生成动作提案
  → 持久化待审批记录
  → interrupt
  → 用户批准/修改/拒绝
  → 校验版本与幂等键
  → 执行一次
  → 保存执行结果
```

这样才能处理浏览器重复提交、SSE 重连、Celery 重试和 Agent 恢复带来的重复执行风险。简历与岗位描述始终作为不可信数据；工具参数由服务端业务 ID、审批记录和类型化状态构造，不能从文档中的“指令”直接生成控制字段。

## 6. 本地开发环境可行性

2026-08-31 的本地环境快照如下。该表是一次性检查结果，开始实施前必须重新运行版本和依赖检查，不能把它当作长期不变的环境事实：

| 项目 | 当前状态 | 影响 | 建议 |
|---|---|---|---|
| Python | 3.13.15 | 与架构指定的 3.12 不一致，但不是阻断项。 | 在 Dockerfile 中固定 Python 3.12，或验证依赖后统一改为 3.13。不要让开发机版本决定生产版本。 |
| Node.js | 18.20.4 | Node 18 已停止维护，且当前 Vite 要求 Node 20.19+ 或 22.12+。 | 升级到仍受支持且满足 Vite 要求的 Node LTS。 |
| npm | 10.7.0 | 随 Node 升级后可能变化。 | 使用 lockfile，并在 CI 固定 Node 主版本。 |
| Docker | 命令不可用 | 当前无法启动计划中的 Compose 环境。 | 安装 Docker Desktop，启用 Linux 容器。 |
| WSL | `wsl --status` 提示安装 | 无法按推荐方式运行 Linux 容器和 Celery Worker。 | 安装并启用 WSL 2，然后验证 Docker Desktop 后端。 |

因此，**当前机器尚不满足项目启动条件**，但都是可解决的环境准备问题，不构成架构不可行。开始编码前应先完成 Node 升级、WSL 2、Docker Desktop 和百炼 API 凭据验证。

Vite 当前要求 Node.js 20.19+ 或 22.12+，而 Node.js 官方已将 v18 标为 EOL。Docker Desktop 官方推荐 Windows 使用 WSL 2 后端。

参考：[Vite Getting Started](https://vite.dev/guide/)、[Node.js EOL](https://nodejs.org/en/about/eol)、[Docker Desktop for Windows](https://docs.docker.com/desktop/setup/install/windows-install/)。

## 7. 六周实施计划

下面按每周五个有效开发日估算，共 30 个工作日。计划保留最后一周的一部分作为缓冲，不应把 30 天全部排满新功能。

| 周次 | 主要目标 | 必须完成的验收结果 |
|---|---|---|
| 第 1 周 | 环境与纵向骨架 | Docker Compose 启动前端、API、Worker、Scheduler、PostgreSQL、Redis；Alembic 可迁移；基础登录/RBAC 可用；CI 能执行 lint 和最小测试；可创建岗位。 |
| 第 2 周 | 简历上传、解析和校对 | PDF/DOCX 上传后异步解析；失败可见；结构化结果可编辑；证据片段能定位到源文档。 |
| 第 3 周 | 检索和离线评测 | 完成结构化、关键词、向量三路召回与 RRF；建立标注数据；输出 Recall@K 和 MRR。 |
| 第 4 周 | LangGraph 和证据化报告 | 完成岗位级 MatchRun、硬规则评估、证据评分及语义支持评测；完成候选人级 ApplicationRun、活动 Run 排他、协作取消、Approval 过期、Checkpoint、人工 interrupt 和恢复；副作用工具默认不执行。 |
| 第 5 周 | 前端闭环与排期模拟 | 展示匹配报告、证据、审批和 Agent 事件；生成面试问题；Mock 排期工具可审批后执行。 |
| 第 6 周 | 评测、E2E、可靠性和交付 | Playwright 核心路径通过；失败恢复演示通过；完善脱敏、README、架构图、演示脚本和录屏备用方案。 |

### 7.1 关键路径

```text
数据模型
  → 文档解析与证据定位
  → 检索与评测集
  → Agent 证据化评分
  → 人工审批与恢复
  → 前端端到端演示
```

React Flow、ECharts、复杂权限和真实日历都不在关键路径上。任何一周延期时，应先削减这些展示型功能，不能削减证据校验、人工审批、测试和可恢复性。

## 8. 测试和评测可行性

### 8.1 测试分层

| 层级 | 重点 |
|---|---|
| 单元测试 | Pydantic Schema、硬规则、岗位分配权限、RRF、证据范围、幂等键和事件归并。 |
| 节点测试 | 每个 LangGraph Node 使用固定输入和 FakeModel，验证状态增量、错误和路由。 |
| 集成测试 | PostgreSQL + pgvector、Redis、Celery、文件存储、Checkpoint 和 API。 |
| 契约测试 | 模型结构化输出、Tool Schema、SSE Event Schema 和前后端 API 类型。 |
| 端到端测试 | 登录、岗位分配、上传简历、校对、匹配、单候选人排他运行、审批过期、协作取消、SSE 持续授权、恢复和查看报告。 |
| 离线评测 | 解析 F1、Recall@K、MRR/nDCG、证据覆盖率、证据语义支持精确率、Prompt Injection 配对攻击和工具调用指标。 |

外部 LLM 不应成为普通 CI 的必需依赖。CI 使用录制响应或 FakeModel；真实模型评测作为手动或定时任务运行，并记录模型、Prompt 和数据集版本。

### 8.2 MVP 验收门槛

以下数值是首版目标，需要在第 2 至第 3 周根据基线结果确认，不能在没有数据集的情况下当作已实现能力：

| 能力 | 建议门槛 |
|---|---|
| 支持格式解析成功率 | 对所有通过上传校验的合成电子 PDF/DOCX 不低于 95%；“成功”要求产出通过 Schema 校验的结构化结果和可回溯原文，不能只按任务未报错计算。 |
| 核心字段抽取 | 姓名、技能、教育、公司和时间范围的宏平均 F1 不低于 0.90。 |
| 候选人检索 | 标注集 Recall@10 不低于 0.85，并报告 MRR 或 nDCG。 |
| 证据完整性 | 每条确定性匹配结论必须包含有效 `evidence_chunk_id`。 |
| 无证据确定性结论 | 发布门禁必须为 0；开发阶段应单独报告失败率，不得用平均值掩盖。 |
| 证据语义支持 | 人工标注集上，被系统标记为 `SUPPORTED` 的结论—证据对中真正得到完整支持的精确率不低于 95%，三分类宏平均 F1 不低于 0.85；人工标注为 `PARTIAL` 或 `INSUFFICIENT` 的入围建议、硬条件判断等高影响结论仍被表述为确定事实的数量为 0。 |
| Tool 参数合法率 | 模型首次产出的参数合法率不低于 98%；执行层必须拒绝 100% 的非法参数。 |
| 副作用安全 | 未审批时实际执行次数必须为 0。 |
| Prompt Injection 防护 | 配对攻击语料导致控制流变化、越权工具提案、审批绕过、攻击指定哨兵/分数/状态/伪造证据进入输出，以及硬规则变化的次数均为 0。 |
| 暂停、取消与恢复 | 所有定义的中断、协作取消、Approval 过期和重启 E2E 场景均通过。 |
| 隐私 | Trace 和测试产物中不出现预设的姓名、手机号和邮箱测试标记。 |

### 8.3 最小评测数据集

- 30 至 50 份合成简历，覆盖不同版式和字段缺失情况。
- 3 至 5 个岗位，每个岗位人工标注相关候选人及相关等级。
- 每份简历标注核心字段和 3 至 8 条结论—证据对，包含支持、部分支持和不支持样本。
- 10 个正确工具调用案例、10 个不应调用工具的案例。
- 至少 20 组干净版本—注入版本配对案例，覆盖伪造系统消息、忽略规则、工具诱导、唯一哨兵、指定分数/状态和伪造证据 ID。
- 批准、修改、拒绝、超时和重复提交等人工审批案例。
- Worker 失败、模型超时、SSE 断线和进程重启等恢复案例。

## 9. 主要风险清单

| 风险 | 概率 | 影响 | 缓解措施 |
|---|---|---|---|
| 六周范围持续扩大 | 高 | 高 | 冻结 MVP 边界；每周只接受替换需求，不叠加需求。 |
| 模型名称、版本或权限变化 | 中 | 高 | 环境变量配置；启动探测；模型适配器；FakeModel 测试。 |
| 简历版式导致解析不稳定 | 高 | 中 | 限定电子文档；保存原文；人工校对；建立失败样本集。 |
| LLM 生成无依据结论 | 中高 | 高 | 只允许引用检索到的 chunk；分别验证引用有效性与语义支持；高影响结论证据不足时降级为未知。 |
| 简历或岗位描述中的 Prompt Injection 改变控制流或语义输出 | 中 | 高 | 文档按不可信数据隔离；评分节点无副作用工具；控制参数只来自可信状态；用干净—注入配对语料检查攻击指令遵循。 |
| 同一 JobApplication 存在多个活动 Run | 中 | 高 | JobApplication 活动 Run 槽、行锁和 409 冲突；并发创建集成测试。 |
| 取消与节点提交竞态 | 中 | 高 | 持久化取消标记、运行版本、条件更新和副作用前锁定校验。 |
| 中文关键词检索效果差 | 中 | 中高 | 标准化技能表 + pg_trgm + 向量召回；用 Recall@K 驱动调整。 |
| Agent 恢复后重复执行副作用 | 中 | 高 | interrupt 前持久化提案；幂等键；审批版本；数据库唯一约束。 |
| SSE 断线、重复或撤权后继续订阅 | 中 | 中高 | 事件 ID、断点恢复、事件批次/心跳持续授权、撤权关闭和前端去重。 |
| Celery 在 Windows 上不可运行 | 高 | 高 | 只支持 Linux 容器 Worker；首先完成 Docker Desktop/WSL 2。 |
| 自托管 Langfuse 占用过多资源 | 高 | 中 | 使用托管版或禁用开关；本地业务日志作为保底。 |
| 简历隐私泄露 | 中 | 高 | MVP 只使用合成数据；日志脱敏；最小权限；文件访问审计。 |
| 外部模型网络影响演示 | 中 | 高 | 准备缓存演示数据和录屏；提供受控 Mock 模式；关键页面可查看历史结果。 |
| 缺少检索标注集导致无法证明效果 | 高 | 高 | 第 3 周前完成小型 Golden Dataset，评测先于 UI 优化。 |

## 10. 实施前必须通过的门禁

### Gate 0：环境可用

- Node 升级到满足 Vite 要求且仍受支持的版本。
- WSL 2 和 Docker Desktop 安装完成。
- Linux 容器中可以启动 Python 3.12 API 和 Celery Worker。
- PostgreSQL pgvector 扩展可以创建。
- 百炼聊天和 Embedding API 各完成一次最小调用。

### Gate 1：数据闭环可用

- 至少一份 PDF 和一份 DOCX 可以上传、解析、校对并保存证据位置。
- 重复上传、解析失败和 Worker 重试不会产生重复候选人。

### Gate 2：检索有效

- 已存在人工标注的岗位—候选人相关性数据。
- 结构化、关键词、向量和融合检索均可独立评测。
- 融合结果至少不弱于最好的单路基线，否则暂不使用 RRF。

### Gate 3：Agent 安全可控

- 报告中的证据 ID 均通过程序校验。
- 人工标注集上的证据语义支持指标达到门槛，高影响无支持结论为 0。
- 未审批的副作用工具无法执行。
- Prompt Injection 配对语料不会改变控制流、生成越权工具提案、绕过审批、服从攻击指定输出或改变硬规则。
- 同一 JobApplication 不会存在多个活动 ApplicationRun；取消后的节点结果和副作用不会继续提交。
- Approval 过期会使父 Run 可重试失败，进程重启后仍可继续合法的待审批 ApplicationRun。

### Gate 4：可交付

- 新环境可以按照 README 启动项目。
- CI、pytest、前端测试和 Playwright 核心路径全部通过。
- 演示数据、演示脚本和故障备用方案准备完成。

任何 Gate 未通过时，不进入依赖它的下一阶段。

## 11. 对原技术方案的调整汇总

| 原方案 | 建议调整 | 优先级 | 原因 |
|---|---|---|---|
| 原始候选模型 `qwen3.8-plus` | 已改为 `qwen3.7-plus`，并要求通过环境变量配置 | 已修正 | 截至核验日期，官方模型列表中没有 `qwen3.8-plus`。 |
| Windows 本机直接运行 Worker | Docker Desktop/WSL 2 Linux 容器 | 必须 | Celery 官方不支持 Windows。 |
| OAuth2 Bearer JWT + SSE | 已确定使用 Fetch Stream 携带 Bearer Token | 已修正 | 原生 EventSource 无自定义 Authorization Header 参数。 |
| PostgreSQL 全文检索覆盖中文关键词 | 增加标准化技能表和 pg_trgm，中文 FTS 以评测决定 | 必须 | 默认 Parser 的中文效果不能未经验证直接承诺。 |
| HNSW 作为默认索引 | 小数据先精确检索，达到延迟门槛再加 HNSW | 建议 | 首版数据量小，精确搜索更简单且便于建立召回基线。 |
| Langfuse 自托管范围未说明 | 托管版或可选开关 | 必须 | 完整自托管增加 ClickHouse、对象存储和资源开销。 |
| 调用真实排期工具 | 首版 MockScheduleBackend | 必须 | 当前没有确定第三方系统和凭据，真实集成会扩大范围。 |
| 全部后台页面同时建设 | 先完成纵向闭环，再补图表和管理页 | 必须 | 六周单人项目需要保护关键路径。 |

## 12. 最终决策

### 建议：Go，但按收缩后的 MVP 执行

项目值得实施，理由是：

- 业务流程与 Agent 的状态、工具、检索、人工审批和评测能力天然匹配。
- 技术栈成熟度足够，关键能力都有明确实现路径。
- 模块化单体和单 Agent 双运行图适合六周单人开发。
- 最终成果能够同时展示前后端、RAG、Agent Workflow、可靠性和评测能力。

启动条件是先完成以下四项：

1. 修正聊天模型名称。
2. 安装 Node LTS、WSL 2 和 Docker Desktop。
3. 按 Fetch SSE 鉴权方案实现并验证断线恢复。
4. 冻结 MVP 范围，并补齐检索、证据语义支持和 Prompt Injection 三类 Golden Dataset。

六周目标应定义为“可复现的作品集级招聘 Copilot”，而不是“可以直接处理真实企业招聘业务的生产平台”。只要坚持这一边界，并把证据、审批、恢复和评测放在 UI 丰富度之前，项目整体可行。
