# 企业招聘 Copilot 环境配置清单

> 清单版本：1.9
>
> 检查日期：2026-09-02
>
> 适用环境：Windows 开发主机 + WSL 2 + Docker Desktop Linux 容器
>
> 编码计划：[编码实现计划 1.0](./编码实现计划.md)
>
> 详细设计：[详细设计说明书 1.0](./详细设计说明书.md)
>
> 技术栈依据：[技术栈选型与架构决策](./技术栈选型与架构决策.md)
>
> 可行性依据：[项目可行性分析 1.3](./项目可行性分析.md)

## 1. 使用说明

本清单用于完成编码计划 Gate 0 和第 1 周环境准备。每项应标记为：

- `[x]`：已经验证满足。
- `[ ]`：尚未验证或尚未配置。
- `[!]`：已检测到不满足，需要处理。
- `[N/A]`：当前环境明确不使用。

本文只记录配置名称、安全示例、检查命令和通过条件，不保存任何真实密码、Token、API Key 或个人数据。实际环境值写入 Git 忽略的 `.env`、部署 Secret 或 GitHub Actions Secret。

## 2. 当前主机快照

以下结果来自 2026-09-01 的实际安装与复核；它是一次性快照，环境变化后必须重新执行验证，不能作为长期事实。

| 项目 | 当前结果 | 状态 | 处理建议 |
|---|---|---|---|
| Git | 2.55.0.windows.3 | [x] | 满足 Git 仓库操作。 |
| Python | 3.13.15 | [x] | 仅作为可选宿主机工具；后端基线仍固定为 Python 3.12 容器。 |
| Node.js | 22.23.2 | [x] | 已写入系统 PATH 首位；新终端直接生效。 |
| npm | 10.7.0 | [x] | 随 Node 22 验证可用，项目仍需提交 lockfile。 |
| Docker Desktop | 4.88.1 | [x] | Linux daemon 正常运行；已关闭当前项目不需要的 Docker AI/Model Runner。 |
| Docker CLI | 29.7.2 | [x] | 已加入系统 PATH。 |
| Docker Compose | v5.4.0 | [x] | Docker Desktop 自带的 Compose 插件可执行。 |
| WSL | 2.7.12.0 | [x] | WSL 2 已运行，Ubuntu 24.04.4 LTS 已注册到 D 盘。 |
| Git 恢复副本 | `D:\develop\git-remotes\resume.git` | [x] | `main` 与 `imp-008-recovered` 已推送到本机裸仓库。 |
| 百炼模型凭据 | 未自动检查 | [ ] | 手动准备开发凭据；普通测试仍使用 FakeModel。 |

当前阻塞结论：基础环境不存在系统级阻塞。Node、Docker Desktop、WSL 2 和 Ubuntu 24.04.4 LTS 已安装；Docker Linux daemon、Ubuntu Integration、Python 3.12、Redis 和 pgvector 容器探针均已通过。宿主机 Python 3.13.15 只作为可选工具，API、Worker 和 Scheduler 统一在 Python 3.12 Linux 容器中运行。

### 2.1 实际落地路径

| 组件 | 路径 | 当前状态 |
|---|---|---|
| Node.js 22 | `D:\develop\node-v22.23.2-win-x64` | 已安装并加入系统 PATH。 |
| WSL 安装包 | `D:\develop\installers\wsl.2.7.12.0.x64.msi` | Microsoft 数字签名有效，平台包已安装。 |
| Ubuntu 24.04 LTS | `D:\develop\wsl\Ubuntu-24.04` | 已注册为 WSL 2；`ext4.vhdx` 位于该目录。 |
| Docker Desktop | `D:\develop\Docker` | 已安装。 |
| Docker WSL 数据 | `D:\develop\DockerData\wsl` | 已生成 `main/ext4.vhdx` 和 `disk/docker_data.vhdx`。 |
| Git 裸仓库备份 | `D:\develop\git-remotes\resume.git` | 已配置为当前仓库的 `origin`；后续仍建议增加异机托管。 |
| 安装包缓存 | `D:\develop\installers` | Node、WSL 和 Docker 安装介质。 |

WSL 系统二进制、Windows 可选功能、Docker 服务/驱动、注册表和少量公共配置由 Windows 放在系统默认位置；这些部分不支持或不适合强制迁移。发行版、Docker 程序主体和主要运行数据均优先放在 D 盘。

## 3. 宿主机与硬件

### 3.1 Windows

- [ ] Windows 已安装当前安全更新。
- [x] BIOS/UEFI 硬件虚拟化已启用；Windows 检测到 Hypervisor。
- [x] Windows 的“虚拟机平台”和 WSL 功能已启用并已生效。
- [x] PowerShell 7 可运行项目辅助脚本，且系统级脚本兼容 Windows PowerShell 5.1。
- [x] Git 可用，当前仓库能正常提交。
- [x] 系统时间和时区可识别；容器仍统一使用 UTC。
- [x] 工作目录 `D:\code\resume` 已通过 Docker BuildKit 构建上下文验证可访问。

### 3.2 资源建议

| 资源 | 最低建议 | 推荐 | 说明 |
|---|---:|---:|---|
| CPU | 4 核 | 8 核 | Worker、PostgreSQL 和前端构建会并行占用。 |
| 内存 | 8 GiB | 16 GiB | Docker Desktop 建议至少分配 6–8 GiB。 |
| 可用磁盘 | 20 GiB | 40 GiB | 镜像、Node 依赖、Volume 和测试产物。 |
| 网络 | 可访问依赖仓库 | 稳定宽带 | 首次拉取镜像/依赖和手动真实模型评测。 |

- [x] Docker Desktop 已分配 32 CPU、约 8 GiB 内存，达到最低建议。
- [x] D 盘剩余空间满足要求，安装前检查约有 580 GiB 可用。
- [ ] 公司代理/VPN 不会阻断容器 DNS、包管理器和模型端点。

## 4. WSL 2 配置

### 4.1 安装检查

在管理员 PowerShell 中完成安装，重启后检查：

~~~powershell
wsl --status
wsl --version
wsl --list --verbose
~~~

通过条件：

- [x] `wsl --status` 已识别 WSL 2.7.12，默认版本为 2。
- [x] 默认 WSL 版本已设为 2。
- [x] Ubuntu 24.04.4 LTS 可以启动并执行 Linux 命令。
- [x] `wsl --list --verbose` 中 `Ubuntu-24.04` 的 VERSION 为 2。
- [x] WSL 内可以解析 `archive.ubuntu.com`，容器可以拉取项目探针镜像。

### 4.2 使用约束

- Celery Worker 不在 Windows 原生进程中运行。
- API、Worker、Scheduler、PostgreSQL 和 Redis 统一使用 Linux 容器。
- 不在 Windows 与 WSL 中分别维护两套数据库或 Redis。
- 文件权限问题优先通过容器用户和 Volume 配置解决，不使用全目录宽松权限。

## 5. Docker Desktop 与 Compose

### 5.1 Docker Desktop

- [x] Docker Desktop 4.88.1 已安装到 D 盘。
- [x] 使用 WSL 2 backend，daemon 报告 Linux/WSL2 内核。
- [x] 安装参数已禁用 Windows containers，固定 Linux containers。
- [x] 已为默认发行版 `Ubuntu-24.04` 启用 Integration。
- [x] Windows 与 Ubuntu 内的 CLI 均可访问同一个 Linux daemon。
- [x] Ubuntu 的 `/usr/local/bin/docker` 已链接到 Docker Desktop WSL CLI，Compose 插件链接完整。
- [x] Compose 插件可用，命令为 `docker compose`，不是旧的 `docker-compose`。
- [x] `EnableDockerAI=false`；规避 Windows build 26200 非正常退出后 AF_UNIX socket 无法清理的已知问题。

若 Docker Desktop 启动时报告 `sailor-ingest.sock` 或 `engine.sock` 无法访问，执行 `./scripts/project.ps1 docker-recover`。该命令只会把冲突目录和设置文件改名/复制为带时间戳的备份，不会删除镜像、容器数据或 Docker 数据 VHDX。

验证命令：

~~~powershell
docker --version
docker compose version
docker info --format '{{.OSType}}'
docker run --rm hello-world
~~~

通过条件：OSType 为 `linux`，hello-world 容器正常退出。2026-09-02 实测结果：Docker Engine 29.7.2、Python 3.12.14、Redis 7.4（PING=PONG）、PostgreSQL 17.11 + pgvector 0.8.6 均通过；环境探针为 `tests/validate_container_runtime.ps1`，项目 Compose 持久性探针为 `tests/validate_compose_stack.ps1`。

### 5.2 项目容器

计划中的 Compose 服务：

| 服务 | 用途 | 是否对宿主机开放 |
|---|---|---:|
| nginx | 浏览器统一入口、静态文件和反向代理 | 是，8080。 |
| web | React 构建/开发服务 | 开发时可选 5173。 |
| api | FastAPI REST/SSE | 调试时可选 8000。 |
| worker | Celery 任务执行 | 否。 |
| scheduler | Celery Beat 维护任务发布 | 否。 |
| postgres | PostgreSQL + pgvector | 调试时可选 5433→5432。 |
| redis | Celery Broker/短期通知 | 调试时可选 6380→6379。 |

- [ ] API、Worker、Scheduler 使用同一 Python 应用镜像。
- [x] Alembic 使用独立一次性 migrate 命令，不由多个服务并发执行。
- [ ] SSE 路由在 Nginx 中关闭代理缓冲。
- [x] PostgreSQL、Redis、Storage 和测试产物使用独立命名 Volume。
- [x] PostgreSQL/Redis healthcheck 只检查本地服务，不调用外部模型。

## 6. 运行时与工具链

### 6.1 必选版本策略

| 工具 | 项目基线 | 主机要求 | 固定方式 |
|---|---|---|---|
| Python | 3.12.x | 可不安装同版本 | Docker 基础镜像固定 3.12 小版本。 |
| Node.js | 22 LTS，>=22.12 | 必须升级 | `.nvmrc`/`.node-version` + CI 主版本。 |
| npm | 随 Node 22 的受支持版本 | 安装后复核 | `package-lock.json`。 |
| PostgreSQL | 支持 pgvector 的固定镜像 | 不需本机安装 | Compose 镜像 digest/tag。 |
| Redis | 固定稳定镜像 | 不需本机安装 | Compose 镜像 digest/tag。 |
| Docker Compose | v2 | 必须可用 | Docker Desktop。 |

不使用“latest”镜像标签。第一个工程骨架提交中必须记录准确镜像 tag、Python 依赖锁和 Node lockfile。

### 6.2 Node 升级后检查

~~~powershell
node --version
npm --version
npm config get registry
~~~

- [x] Node 为 22.23.2，满足不低于 22.12 的要求。
- [x] npm 能访问 registry；项目级 `.npmrc` 固定官方 registry，避免镜像审计 API/CDN 差异。
- [x] 本次配置没有全局安装项目业务依赖。
- [x] 前端安装已使用 `npm ci` 验证 `package-lock.json`。

### 6.3 可选本机工具

- [ ] VS Code/Cursor 或其他支持 Python、TypeScript、Docker 的编辑器。
- [ ] PostgreSQL 客户端，仅用于调试，不替代迁移脚本。
- [ ] Redis CLI，可通过容器执行，不要求本机安装。
- [ ] HTTP/SSE 调试工具；不得把 Bearer Token 保存到仓库文件。

## 7. 仓库环境文件

编码阶段应创建并维护：

| 文件 | 是否提交 | 用途 |
|---|---:|---|
| `.env.example` | 是 | 所有变量名和安全占位符。 |
| `.env` | 否 | 本机开发 Secret 和覆盖值。 |
| `.env.test` | 否或只提交无 Secret 模板 | 本地测试隔离配置。 |
| `.env.demo` | 否 | 演示环境 Secret/覆盖。 |
| `.gitignore` | 是 | 忽略真实 `.env` 及本机 Secret 变体，并明确保留 `.env.example`。 |
| `compose.yaml` | 是 | 统一服务、网络、健康和 Volume。 |
| `compose.override.yaml` | 是或按约定 | 仅开发端口和热重载。 |
| `pyproject.toml` | 是 | Python 依赖和 Ruff/mypy/pytest 配置。 |
| Python lockfile | 是 | 固定后端依赖。 |
| `package.json`/`package-lock.json` | 是 | 前端依赖与命令。 |
| `.nvmrc` 或 `.node-version` | 是 | Node 主版本。 |

- [x] `.gitignore` 明确忽略 `.env`、`.env.local`、`.env.test.local` 和 `.env.demo`。
- [x] `.env.example` 中只有不可用占位符，没有可用 Secret。
- [x] 本机 `.env` 已通过 `./scripts/project.ps1 env-init` 安全生成，且生成器不会覆盖已有文件或输出 Secret。
- [x] 当前测试 Fixture 和 smoke 测试只包含合成数据，不含真实个人数据。
- [x] IMP-001 提交前运行结构契约中的 Secret 模式扫描并检查 staged diff。

## 8. 应用环境变量

### 8.1 应用与日志

| 变量 | local 建议值 | 必需 | Secret | 说明 |
|---|---|---:|---:|---|
| APP_ENV | `local` | 是 | 否 | 允许 local、test、demo。 |
| APP_NAME | `resume-copilot` | 是 | 否 | 日志和 Trace 服务名。 |
| LOG_LEVEL | `INFO` | 是 | 否 | 开发调试临时使用 DEBUG。 |
| LOG_FORMAT | `json` | 是 | 否 | 结构化日志。 |
| PUBLIC_BASE_URL | `http://localhost:8080` | 是 | 否 | 浏览器统一入口。 |
| API_BASE_PATH | `/api/v1` | 是 | 否 | REST 前缀。 |
| CORS_ORIGINS | `http://localhost:5173` | 开发必需 | 否 | Nginx 同源运行时可收紧。 |
| REQUEST_ID_HEADER | `X-Request-ID` | 是 | 否 | 请求关联。 |

### 8.2 认证

| 变量 | local 建议值 | 必需 | Secret | 说明 |
|---|---|---:|---:|---|
| JWT_SECRET | `<random-48-bytes-or-more>` | 是 | 是 | 不复用数据库密码。 |
| JWT_ALGORITHM | `HS256` | 是 | 否 | MVP 固定。 |
| ACCESS_TOKEN_TTL_MINUTES | `30` | 是 | 否 | 短期 Access Token。 |
| PASSWORD_HASH_SCHEME | `argon2` | 是 | 否 | 不允许明文或快速哈希。 |
| LOGIN_RATE_LIMIT | `10/minute` | 是 | 否 | 具体实现阶段转为类型化配置。 |

生成本机随机 Secret 的示例命令；输出只写入 `.env`，不要复制到文档、日志或聊天：

~~~powershell
[Convert]::ToBase64String([Security.Cryptography.RandomNumberGenerator]::GetBytes(48))
~~~

### 8.3 PostgreSQL

| 变量 | local 建议值 | 必需 | Secret | 说明 |
|---|---|---:|---:|---|
| POSTGRES_DB | `resume_copilot` | 是 | 否 | 开发数据库。 |
| POSTGRES_USER | `resume_app` | 是 | 否 | 应用账号。 |
| POSTGRES_PASSWORD | `<random-password>` | 是 | 是 | 不提交 Git。 |
| DATABASE_URL | `postgresql+asyncpg://resume_app:<password>@postgres:5432/resume_copilot` | 是 | 是 | 容器网络 URL。 |
| DB_POOL_SIZE | `5` | 是 | 否 | MVP 单机默认。 |
| DB_POOL_TIMEOUT | `30` | 是 | 否 | 秒。 |
| DB_ECHO | `false` | 是 | 否 | 避免 SQL 日志泄露内容。 |

测试使用独立 `resume_copilot_test` 数据库/Schema，不与开发或演示数据库复用。

### 8.4 Redis 与 Celery

| 变量 | local 建议值 | 必需 | Secret | 说明 |
|---|---|---:|---:|---|
| REDIS_URL | `redis://redis:6379/0` | 是 | 视部署 | 短期通知/协调。 |
| CELERY_BROKER_URL | `redis://redis:6379/1` | 是 | 视部署 | Broker 独立 DB。 |
| CELERY_RESULT_BACKEND | `redis://redis:6379/2` | 可选 | 视部署 | 不作为业务事实。 |
| CELERY_TASK_ALWAYS_EAGER | `false` | 是 | 否 | 仅单元测试可显式 true。 |
| CELERY_WORKER_CONCURRENCY | `2` | 是 | 否 | 根据资源调整。 |
| CELERY_TASK_TIME_LIMIT | `300` | 是 | 否 | 秒；节点仍有更细超时。 |
| SCHEDULER_TIMEZONE | `UTC` | 是 | 否 | Approval 到期仍以数据库时间为准。 |

Redis 清空不能改变 PostgreSQL 中的 Document、Run、Approval、Interview 或 AgentEvent 事实。

### 8.5 文件与解析

| 变量 | local 建议值 | 必需 | Secret | 说明 |
|---|---|---:|---:|---|
| STORAGE_BACKEND | `local-volume` | 是 | 否 | MVP 固定。 |
| STORAGE_ROOT | `/data/resumes` | 是 | 否 | 必须位于挂载 Volume。 |
| MAX_FILE_SIZE_MB | `10` | 是 | 否 | 需求默认。 |
| MAX_BATCH_FILES | `20` | 是 | 否 | 需求默认。 |
| MAX_PDF_PAGES | `50` | 是 | 否 | 建议初值，编码时用样本复核。 |
| MAX_EXTRACTED_CHARS | `200000` | 是 | 否 | 建议初值。 |
| PARSER_TIMEOUT_SECONDS | `60` | 是 | 否 | 超时转稳定错误码。 |
| PARSER_VERSION | `v1` | 是 | 否 | 写入文档快照。 |

### 8.6 模型与 Embedding

| 变量 | local 建议值 | 必需 | Secret | 说明 |
|---|---|---:|---:|---|
| MODEL_BASE_URL | `<Bailian-OpenAI-compatible-base-url>` | 真模型必需 | 否 | 从账号官方配置复制并验证。 |
| QWEN_API_KEY | `<secret>` | 真模型必需 | 是 | 普通 CI 不配置。 |
| CHAT_MODEL | `qwen3.7-plus` | 是 | 否 | 只通过环境变量选择。 |
| MODEL_TIMEOUT_SECONDS | `60` | 是 | 否 | 单次请求超时。 |
| EMBEDDING_MODEL | `qwen3.7-text-embedding` | 是 | 否 | 与快照一致。 |
| EMBEDDING_DIMENSION | `1024` | 是 | 否 | 启动时校验。 |
| EMBEDDING_BATCH_SIZE | `16` | 是 | 否 | 建议初值，按限流调整。 |
| MOCK_MODEL_MODE | `true` | 开发建议 | 否 | 环境未就绪时显式使用。 |

`MOCK_MODEL_MODE=true` 的结果必须在 API、UI 和 Trace 中显著标识。真模型连通性通过独立诊断命令验证，不放入 readiness。

### 8.7 检索、运行和 SSE

| 变量 | local 建议值 | 必需 | Secret | 说明 |
|---|---|---:|---:|---|
| TOP_K | `10` | 是 | 否 | 与 Recall@10 门槛对应。 |
| RRF_K | `60` | 是 | 否 | 建议初值，变更进入配置快照。 |
| STRUCTURED_WEIGHT | `1.0` | 是 | 否 | 非负。 |
| KEYWORD_WEIGHT | `1.0` | 是 | 否 | 非负。 |
| VECTOR_WEIGHT | `1.0` | 是 | 否 | 非负。 |
| HNSW_ENABLED | `false` | 是 | 否 | MVP 默认精确搜索。 |
| NODE_TIMEOUT_SECONDS | `120` | 是 | 否 | 节点总超时。 |
| MAX_TRANSIENT_RETRIES | `2` | 是 | 否 | 只覆盖临时错误。 |
| APPROVAL_TTL_MINUTES | `60` | 是 | 否 | 演示默认，数据库时间判断。 |
| SSE_HEARTBEAT_SECONDS | `15` | 是 | 否 | 小于代理空闲超时。 |
| SSE_BATCH_SIZE | `100` | 是 | 否 | 每批发送前重新授权。 |
| SSE_RETRY_MILLISECONDS | `1000` | 是 | 否 | 前端初始重连提示。 |

### 8.8 Langfuse 和功能开关

| 变量 | local 建议值 | 必需 | Secret | 说明 |
|---|---|---:|---:|---|
| LANGFUSE_ENABLED | `false` | 是 | 否 | 无配置时关闭。 |
| LANGFUSE_HOST | `<optional-host>` | 启用时 | 否 | 可选 Cloud。 |
| LANGFUSE_PUBLIC_KEY | `<optional-key>` | 启用时 | 是 | 不提交。 |
| LANGFUSE_SECRET | `<optional-secret>` | 启用时 | 是 | 与应用配置 Schema 一致，不提交。 |
| PROMPT_VERSION | `v1` | 是 | 否 | 写入 Run 快照。 |
| RULE_VERSION | `v1` | 是 | 否 | 写入硬规则结果。 |

## 9. 端口与网络

### 9.1 建议端口

| 入口 | 宿主机端口 | 容器端口 | 默认访问者 |
|---|---:|---:|---|
| Nginx | 8080 | 80 | 浏览器。 |
| Web dev server | 5173 | 5173 | 开发浏览器。 |
| FastAPI debug | 8000 | 8000 | 开发调试。 |
| PostgreSQL debug | 5433 | 5432 | 本机 DB 工具。 |
| Redis debug | 6380 | 6379 | 本机调试工具。 |

- [ ] 启动前确认端口未被占用。
- [ ] demo 模式只公开 Nginx；数据库和 Redis 不公开到外部网络。
- [ ] API、Worker、Scheduler 使用 Compose 服务名访问 postgres/redis。
- [x] 当前认证 API 只通过 `Authorization: Bearer` 接收 JWT，不放入 URL。
- [ ] Nginx 对 `/api/v1` 和事件路径使用同源代理。

PowerShell 端口检查示例：

~~~powershell
Get-NetTCPConnection -State Listen | Where-Object LocalPort -In 8080,5173,8000,5433,6380
~~~

## 10. Volume、目录与权限

计划中的命名 Volume：

| Volume | 挂载目标 | 数据性质 | 可否直接删除 |
|---|---|---|---:|
| postgres_data | PostgreSQL 数据目录 | 核心业务事实 | 否，除非明确重置合成环境。 |
| redis_data | Redis 数据目录 | 可重建任务/通知 | 可重建，但先停止服务。 |
| resume_storage | `/data/resumes` | 原始 PDF/DOCX | 需与数据库一致清理。 |
| test_artifacts | 测试/评测输出 | 可重建 | 可以。 |

- [ ] Storage 目录只允许 API/Worker 所需用户访问。
- [ ] 原文件名不参与实际存储路径。
- [ ] 数据库、Storage 和迁移版本作为一个演示快照管理。
- [ ] 重置脚本明确列出将删除的命名 Volume，不使用宽泛路径或未解析变量。
- [ ] 备份/重置演练只使用合成数据。

## 11. 开发、测试与演示环境隔离

| 项目 | local | test | demo |
|---|---|---|---|
| APP_ENV | local | test | demo |
| 数据库 | resume_copilot | 独立 test DB/Schema | 独立 demo Volume |
| Redis DB | 0/1/2 | 3/4/5 或独立容器 | 独立实例/DB |
| Storage | resume_storage | 临时测试目录/Volume | demo 专用 Volume |
| 模型 | Fake 或真模型 | FakeModel/录制响应 | 固定真模型或显式 Mock |
| Langfuse | 默认关闭 | 关闭 | 可选开启 |
| 日志 | INFO/DEBUG | WARNING/捕获 | INFO JSON |

- [ ] 测试不会读取开发 `.env` 中的真实模型 Key。
- [ ] test 数据库名称含明确 test 标记。
- [ ] 测试结束只清理其独立 Schema/Volume。
- [ ] demo 只使用合成数据，页面标识 Mock/真实模型模式。
- [ ] 真实模型评测独立于普通 CI，并记录模型/Prompt/数据集版本。

## 12. 外部服务与凭据

### 12.1 百炼

- [ ] 已获得 OpenAI 兼容接口所需 API Key。
- [ ] 已确认账号可访问 CHAT_MODEL。
- [ ] 已确认账号可访问 EMBEDDING_MODEL 和 1024 维输出。
- [ ] 已记录限流、区域和费用上限，但不写入 Git。
- [ ] 使用最小诊断脚本分别测试 Chat 和 Embedding。
- [ ] API Key 只存在 `.env`/Secret，日志只记录 provider request ID。

### 12.2 Langfuse

- [ ] 默认 `LANGFUSE_ENABLED=false` 时全流程可运行。
- [ ] 如启用，Host/Public Key/Secret Key 均来自环境变量。
- [ ] 只发送合成内容。
- [ ] Langfuse 不可用时只降级 Trace，不阻断业务流程。

### 12.3 GitHub Actions

- [x] 普通 CI 不配置 QWEN_API_KEY。
- [x] 普通 CI 使用 `MOCK_MODEL_MODE=true`，不调用真实模型。
- [ ] 手动真实模型工作流单独配置受保护 Secret。
- [x] Workflow 不通过 echo、命令参数或测试报告输出 Secret。
- [ ] 测试产物上传前执行敏感标记扫描。

## 13. 初始化顺序

严格按以下顺序准备，避免同时排查多层问题：

1. 启用硬件虚拟化并安装 WSL 2。
2. 安装 Docker Desktop，启用 WSL 2 backend 和 Linux containers。
3. 升级 Node 至 22 LTS，复核 npm。
4. 验证 Git、端口、磁盘和容器网络。
5. 在 IMP-001/IMP-003 中创建锁文件、Dockerfile 和 Compose。
6. 执行 `./scripts/project.ps1 env-init`，从 `.env.example` 生成包含随机 JWT/DB Secret 的本机 `.env`。
7. 先以 `MOCK_MODEL_MODE=true` 启动本地服务。
8. 执行 Alembic 空库迁移和 pgvector 扩展探针。
9. 验证 API、Worker、Scheduler、PostgreSQL、Redis 和 Storage 健康。
10. 单独配置并验证百炼模型/Embedding。
11. 运行完整环境验收命令并保存非敏感结果。

## 14. 环境验收命令

以下是工程骨架建立后的目标命令；当前尚未创建的脚本在 IMP-001 至 IMP-004 中实现。

### 14.1 主机层

~~~powershell
git --version
wsl --status
wsl --list --verbose
docker --version
docker compose version
node --version
npm --version
~~~

### 14.2 Compose 层

~~~powershell
docker compose config --quiet
docker compose build
docker compose up -d postgres redis
docker compose run --rm migrate alembic upgrade head
docker compose up -d api worker scheduler web nginx
docker compose ps
~~~

### 14.3 服务层

~~~powershell
Invoke-RestMethod http://localhost:8080/api/v1/health/live
Invoke-RestMethod http://localhost:8080/api/v1/health/ready
docker compose exec postgres psql -U resume_app -d resume_copilot -c "SELECT extname FROM pg_extension WHERE extname = 'vector';"
docker compose exec redis redis-cli ping
~~~

### 14.4 工程层

~~~powershell
ruff check backend tests
mypy backend
pytest -q
npm --prefix apps/web run typecheck
npm --prefix apps/web run test
npm --prefix apps/web run build
& '.\tests\validate_documents.ps1'
~~~

所有命令通过后再开始依赖这些能力的工作包。真实模型诊断失败不阻塞 FakeModel 编码，但必须在进入真实评测和演示前解决。

## 15. 常见问题检查

| 现象 | 优先检查 | 禁止做法 |
|---|---|---|
| docker 命令不存在 | Docker Desktop 安装、PATH、重启终端 | 改用 Windows 原生 Celery。 |
| Docker daemon 不可达 | Desktop 是否启动、WSL integration | 重复安装随机 Docker 组件。 |
| Docker 提示 `sailor-ingest.sock`/`engine.sock` 无法访问 | 执行 `./scripts/project.ps1 docker-recover`；脚本会停止异常进程、关闭 WSL、同时备份冲突目录并保持 Docker AI 关闭 | 点击恢复出厂设置或删除 Docker 数据 VHDX。 |
| Ubuntu 中仅缺少 `docker` 命令，但 `/mnt/wsl/docker-desktop` 已挂载 | 检查 Integration 开关与 `/usr/local/bin/docker` 链接；链接目标应为 Docker Desktop 提供的 WSL CLI | 在 Ubuntu 中另装一套 Docker daemon。 |
| OSType 不是 linux | 切换 Linux containers | 在 Windows 容器运行 Worker。 |
| WSL 仍提示安装 | Windows 功能、重启、虚拟化 | 绕过 WSL 直接假设兼容。 |
| Vite 拒绝启动 | Node 是否 >=22.12、lockfile | 继续使用 Node 18。 |
| pgvector 扩展不存在 | PostgreSQL 镜像、迁移权限 | 改用 SQLite 代替集成测试。 |
| Worker 收不到任务 | Broker URL、队列路由、容器 DNS | 把 Redis 状态当业务事实。 |
| SSE 延迟聚合 | Nginx proxy_buffering、心跳 | 把 JWT 放查询参数。 |
| Volume 权限失败 | 容器 UID/GID、挂载目录 | 给整个工作盘开放宽松权限。 |
| 模型 401/403 | Key、Base URL、模型权限/区域 | 将 Key 打印到日志。 |
| 环境变量未生效 | Compose 展开结果、Pydantic 启动错误 | 在业务代码写死 Secret。 |

## 16. 安全检查

- [ ] `.env` 和所有 Secret 文件未被 Git 跟踪。
- [ ] `git diff --cached` 不包含 JWT_SECRET、数据库密码或 API Key。
- [ ] Docker image history 不包含通过 ARG 写入的 Secret。
- [ ] Compose 日志不打印完整环境变量。
- [x] API 错误契约测试确认不返回 DATABASE_URL、文件路径、模型响应或栈。
- [x] 结构化日志测试确认 Bearer Token、Secret、邮箱和手机号被脱敏。
- [ ] Storage、日志和测试产物只包含合成数据。
- [ ] demo 环境数据库/Redis 不绑定公共网卡。
- [x] 默认账号密码不写入 README；通过受控 bootstrap 命令从临时环境变量创建。
- [ ] 删除/重置脚本只能操作显式命名的项目 Volume。

## 17. Gate 0 通过标准

编码计划 IMP-001 至 IMP-007 开始前，以下项目必须全部满足：

- [x] WSL 2 已安装，默认版本为 2。
- [x] Docker Desktop 使用 WSL 2 backend 和 Linux containers。
- [x] `docker compose` 可用，hello-world 通过。
- [x] Node 22 LTS（22.23.2）、npm 10.7.0 可用，项目 lockfile 已通过 `npm ci`。
- [x] Python 3.12.14 基础容器可以运行；项目 API/Worker 镜像在 IMP-001 创建。
- [x] PostgreSQL 17 中可以创建 pgvector 0.8.6 扩展。
- [x] Redis 可连接，应用 readiness 的故障降级与恢复探针通过。
- [ ] Linux 容器中的 Celery Worker 可启动。
- [ ] LangGraph Checkpointer 最小保存/恢复探针通过。
- [ ] PDF/DOCX 固定样本解析探针通过。
- [ ] FastAPI/Nginx Bearer SSE 探针通过。
- [x] `.env.example` 已覆盖设计变量，`.env` 被 Git 忽略且结构测试未发现真实 Secret 模式。
- [x] 本机 `.env` 已生成，JWT/数据库密码满足随机长度契约，且 Compose 配置验证通过。
- [x] FakeModel 模式不依赖模型外网，普通 CI 可以运行统一 `verify` 门禁。
- [x] 文档契约测试通过，IMP-001 变更范围和工作区状态明确。

## 18. 当前下一步

基础环境、数据库迁移、认证和岗位/分配授权纵向切片已经通过，下一步进入首个前端业务页面：

1. IMP-001 已完成 Python/Node 工程骨架、依赖锁、统一命令和后端/前端 smoke 测试。
2. IMP-002 已完成 Settings 启动校验、AppError、request_id、结构化脱敏日志及 18 项后端单元/契约测试。
3. IMP-003 已完成固定 digest 的 PostgreSQL/pgvector、Redis、四个命名 Volume、健康检查和持久性探针。
4. IMP-004 已完成 SQLAlchemy 异步基础、Alembic 初始迁移、一次性 migrate 服务、CI 与 PostgreSQL/Redis/Storage readiness。
5. IMP-005 已完成 User、受控账号初始化、OAuth2 Password Flow、Argon2、JWT、当前用户和角色校验。
6. IMP-006 已完成 Job、不可变 JobVersion、JobAssignment、乐观锁、状态迁移和撤权后立即失效的资源级授权。
7. 下一步执行 IMP-007，实现 Web 登录、岗位页面和浏览器纵向 smoke 测试。

安装系统组件会改变主机状态，本次系统组件安装均已在用户授权下完成；当前不自动重启电脑，也不自动接受 Docker Desktop 的许可条款。

## 19. 清单结论

当前 Git、Node 22、WSL 2、Ubuntu 24.04.4 LTS、Docker Desktop、Compose、Python 3.12、Redis 和 pgvector 基础环境均已验证；本机 Git 裸仓库恢复副本和未跟踪 `.env` 也已就绪。IMP-003 已证明基础服务健康且 PostgreSQL/Redis 数据在容器重建后保持；IMP-004 已证明空库迁移、`vector(1024)`、应用 readiness 降级恢复和统一 CI 门禁；IMP-005 已证明认证安全契约；IMP-006 已证明岗位版本不可变、状态迁移受控、乐观锁冲突和 JobAssignment 在线撤权。Gate 0 剩余项主要是 Celery、LangGraph、解析器和 SSE 技术探针。项目继续以 Linux 容器中的 Python 3.12、PostgreSQL + pgvector、Redis/Celery 和 Node 22 前端作为唯一可复现基线。

环境变量必须由 Pydantic Settings 在 API、Worker 和 Scheduler 启动时统一校验；Secret 只通过未跟踪 `.env` 或部署 Secret 注入。普通开发和 CI 默认使用 FakeModel，真实模型与 Langfuse 作为显式、可关闭的外部配置。
