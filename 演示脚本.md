# resume-copilot 演示脚本（v1.1 · 双版本）

> 面向场景：面试现场 / 视频演示 / 屏幕共享讲解。目标岗位 AI Agent 开发。
> 核心策略：**不讲 CRUD，只讲 Agent 运行时的可信性**——证据链、双审批、幂等副作用、可恢复。
>
> 本文件提供**两个独立版本**，不要混用：
>
> | 版本 | 用途 | 时长 | 特点 |
> |---|---|---|---|
> | [版本一：5 分钟录屏版](#版本一5-分钟录屏版可剪辑) | 提交给招聘方的视频、异步观看 | 5 min | 逐镜脚本、可剪辑、**不依赖实时等待**、不演示交互式故障恢复 |
> | [版本二：10 分钟现场版](#版本二10-分钟现场版交互) | 面试现场、屏幕共享 | 10 min | 交互式，含**故障恢复**与撤权演示，允许临场取舍 |
>
> 两个版本讲的是**同一条链路、同一个候选人**：推荐理由 → 证据定位 → 人工修改或否决 → 故障恢复。
> 区别只在取舍，不在内容——所以先定版本，再照对应章节走，不要中途切换。

---

## 0. 演示前准备（两个版本共用，至少提前 15 分钟）

```powershell
# 0) 首次使用时生成本地 .env；已有 .env 时不要重复执行
# pwsh scripts/project.ps1 env-init

# 1) 推荐走“干净演示”路径：先移除本项目旧容器与卷，再一键起栈
cd D:\code\resume
docker compose --profile tools down --volumes --remove-orphans
pwsh scripts/start_stack.ps1 -KeepRunning
# 预期结尾输出：浏览器登录 smoke 通过；入口 http://localhost:8080
# 注意：start_stack.ps1 会拒绝已有同名 Compose 资源，这是“全新环境”门禁，不是故障。

# 如果明确要保留已有数据，不要运行 start_stack.ps1，改用：
# docker compose up -d --wait
# pwsh scripts/project.ps1 seed

# 2) 三项健康检查（各 1 秒，可作为开场前的仪式感）
curl.exe -i http://localhost:8080/healthz              # 期望 200 ok
curl.exe http://localhost:8080/api/v1/health/ready     # 期望 {"status":"ready"}（覆盖 PG/Redis/Storage）
curl.exe http://localhost:8080/api/v1/metrics          # 记下 401/403 计数，安全幕要用

# 3) 确认演示上传材料存在（使用仓库自带、可复现的合成简历）
Get-Item apps/web/e2e/fixtures/e2e-candidate-resume.pdf

# 4) 浏览器打开 http://localhost:8080，先用 hr.demo 登录并检查岗位页
# 5) 浏览器缩放调到 90%，关闭通知和无关标签页；现场版另备一个无痕窗口给 hm.demo
# 6) 现场版保留一个终端窗口用于故障恢复幕。不要在投屏中展示 .env、PAT 或 Authorization 头。
```

**风险预案**：

| 症状 | 处理方式 |
|---|---|
| 提示已有同项目资源 | 若允许清数据，运行 `docker compose --profile tools down --volumes --remove-orphans` 后重试；若要保留数据，直接 `docker compose up -d --wait`。 |
| 8080/8000/5433/6380 端口占用 | 先用 `docker ps` 找到占用者；不要直接运行 Docker 自愈脚本。必要时在 `.env` 调整 `*_HOST_PORT`。 |
| Docker Desktop 本身不可用 | 才使用 `pwsh scripts/project.ps1 docker-recover`，它用于 Docker/WSL 故障，不是普通端口冲突处理器。 |
| 演示中 API/Worker 被重启 | `docker compose up -d --wait` 恢复；运行与审批状态应从 PostgreSQL checkpoint 恢复。 |
| 上传显示“重复” | 当前卷以前上传过相同内容；直接使用已有记录，或在演示前按“干净演示”路径重建卷。文件名不同也不会绕过内容 SHA256 去重。 |
| 网络/机器卡顿导致某幕超时 | 照「现场状态速查」表处理，**保留错误码和请求编号**再继续；不要现场重试到超时耗尽讲解时间。 |

**演示账号**：

| 角色 | 用户名 | 密码 | 用途 |
|---|---|---|---|
| HR（主视角） | `hr.demo` | `demo-password-123` | 全程用这个 |
| 招聘经理（现场版安全幕用） | `hm.demo` | `demo-password-123` | 撤权场景演示 |

**预置数据**：岗位「[DEMO] 高级后端工程师（Go / Python）」+ 5 名合成候选人（Demo 林知远 / 沈曜 / 苏微 / 周屿 / 何笙），每人 3 条证据块，全部就绪。模型默认 **Mock 模式**——每个页面顶部都有一条横幅直接写明，不用口头补充。`hm.demo` 已分配到该岗位，便于演示资源级授权与撤权。

**两个版本都只用一位主角**：`Demo 林知远`。整条链路——推荐理由、证据定位、人工修改或否决、故障恢复——都落在他一个人身上。切候选人会让观众失去「同一个人被一步步处理」的连续性，而这条连续性正是要展示的东西。

**开场前最后检查**：保持数据处于未运行状态，不要提前点击“启动批量分析”；确认候选人页能看到 5 名预置候选人、负责人分配里有一条有效记录、文档页没有 `e2e-candidate-resume.pdf` 的既有上传记录。

---

# 版本一：5 分钟录屏版（可剪辑）

**录屏与现场是两种媒介，脚本必须不同。** 录屏没有观众在场，所以：

- **不展示任何实时等待**。所有 Celery 多跳（上传 → 解析 → 校对 → embedding）都在录制前跑完，只录「结果已经在那里」的画面。等 40 秒的进度条在视频里是纯粹的损耗。
- **不做交互式故障恢复**。重启容器、等健康检查、刷新页面这套动作在现场有说服力，在视频里只是几秒黑屏。改用「展示恢复后的事实 + 指出哪条测试守着它」。
- **不演示撤权**。撤权需要第二个窗口、手工撤销再手工恢复授权，剪辑成本高且容易留下错误状态。
- **允许逐镜重录**。每镜独立成立，旁白按下面的文字念，语速放慢一点。

**录制规格**：1920×1080，浏览器缩放 90%，隐藏书签栏。鼠标移动放慢，点击前停顿半秒。

## 镜 1 ｜ 0:00–0:40 ｜ 开场与边界（40 s）

**画面**：登录后的岗位工作台，顶部**模型模式横幅**清晰可见。

**操作**：`hr.demo` 登录 → 停留 2 秒让横幅可读 → 展开「模型详情」→ 收起。

**旁白**：
> 「这是我用六周做的企业招聘 Copilot，定位是 HR 的**辅助决策**系统，不是自动决策系统。它要解决的是：当 LLM 参与招聘判断时，怎么让它的每个结论**可验证、可审计、可否决**。
> 先把边界说清楚——顶部这条横幅写明当前跑的是 Mock 模型，确定性假模型，不调用外部服务。它能证明流程、契约和可复现性，**不能**证明真实模型效果。真实模型入口已经就绪，缺的只是凭据和预算。我把它放在每个页面顶部，是因为读者应该在读任何结论之前就知道这一点，而不是读完才被告知。」

**必须出现的权威信号**：横幅文字「Mock 模型（确定性假模型，不调用外部服务）」。

## 镜 2 ｜ 0:40–1:30 ｜ 推荐理由：为什么是林知远（50 s）

**画面**：`启动批量分析` → MatchRun 详情页 → 时间线 → 候选人排名表 → 林知远那一行的报告。

**操作**：岗位详情页点「启动批量分析」→ 页面自动进入批量匹配详情 → **剪辑掉等待**（直接切到已完成）→ 排名表停在林知远那行。

**旁白**：
> 「批量匹配跑完了。排名表以**姓名**为主标识，下面一行是经验、学历、技能摘要；Profile 短 ID 在最下面一行，那是接口和排障用的辅助追踪信息。
> 为什么林知远排第一？三个可核对的东西：检索排序换算分——注意这是**排序换算分，不是模型置信度**；硬性条件的判定结果；以及下方报告里的逐条结论。分数、判定、结论三者都能点开看依据，没有一处是「模型觉得他不错」。」

**必须出现的权威信号**：排名表出现「林知远」及其分数与硬规则结果。

## 镜 3 ｜ 1:30–2:30 ｜ 证据定位：从结论跳到原文（60 s）

**画面**：报告区。鼠标依次划过一条 `规则判定` 结论、一条 `模型解释` 结论、一条 `部分支持` 结论，然后点「查看原文 →」。

**操作**：停在报告开头（结论来源统计行）→ 展开一条规则结论的证据 → 点「查看原文 →」→ 页面跳到简历原文页并**高亮定位**到该段 → 返回报告。

**旁白**：
> 「报告开头先说明结论来源：几条是规则判定，几条是模型解释。这个区分是硬性的——规则判定是确定性结论，模型解释的支持等级**上限只能是部分支持**，因为它引用合法只证明「这句话确实在原文里」，不证明「原文支持这个结论」。
> 每条结论后面跟着它引用的原文位置。点『查看原文』，页面直接跳到那一段并定位高亮，不用自己去翻文档。如果证据不足，系统**强制降级**为『不足以判断』，而不是让模型编一个理由。这是我在 Agent 评测里最看重的设计：**引用非法或证据不足时，宁可不回答**。」

**必须出现的权威信号**：原文页出现「已定位到报告引用的原文片段」提示。

## 镜 4 ｜ 2:30–4:00 ｜ 审批：改参数、批准、执行（90 s）★核心

**画面**：`启动单人流程` → 流程停在「等待审批」→ 审批页四个区块 → 修改参数 → 通过 → 已批准 → 已执行。

**操作**：
1. 排名表林知远那行点「启动单人流程」→ 流程停在「等待审批」（剪辑掉等待）。
2. 点「查看并决策 →」。**停留 3 秒**，让四个区块都可读：拟执行动作 / 原提案与参数 / 当前版本 / 执行结果。
3. 点「修改参数」→ 把目标状态改成另一个值 → 应用 → 差异表把该行标为「已修改」→ 点「通过」。
4. 状态变「已批准」→ **停 2 秒** → 状态变「已执行」→ 点「返回申请流程查看结果 →」。

**旁白**：
> 「ApplicationRun 是固定 8 节点图，它在 `human_review` 节点**必须停下等人**——这是引擎级中断，不是前端拦截，绕不过去。
> 审批人看到的不是黑盒结论，是**结构化的变更提案**：上面这句把动作读成一句话，下面这张表是模型提案与我改后参数的逐项差异，标出哪一项被改了。我可以批准、修改后批准、或者驳回。
> 注意这里有个容易被忽略的区别，我特意在界面上分开显示：**已批准**只表示决策记录下来了，副作用还没发生；**已执行**才表示真的落库、而且恰好一次。决策和执行是两步，中间由 SideEffect 服务带着乐观锁和幂等键去写库，所以页面会轮询等它，而不是批完就显示成功。
> 整条链路的副作用——改申请状态、建面试——**节点本身不写库**，它们只产出类型化的状态。Celery 允许至少一次投递，但审批状态机加乐观锁加幂等键让业务效果等效为**只执行一次**。」

**必须出现的权威信号**：先出现「已批准」，再出现「已执行」——两者必须都能拍到。

## 镜 5 ｜ 4:00–4:40 ｜ 故障恢复与不重复写入（40 s）

**画面**：回到 ApplicationRun 页面，展示恢复后的状态与时间线；再切到终端，跑一条测试并停住。

**操作**：回到流程页（展示 Run 状态与审批记录完好）→ 切终端，运行：
```bash
python -m pytest -q tests/unit/test_concurrency_recovery.py -k "replay or cancel" -v
```

**旁白**：
> 「每个节点转移都落了 checkpoint 到 Postgres，容器随便杀，恢复后引擎从断点重放，事件序列号保证前端不重复渲染。
> 但恢复本身不是重点，重点是**恢复不会多写一次**。这一条是有自动化测试守着的：往已经提交过的写操作上重放，结果仍然是恰好一次；取消生效之后，后续的副作用节点不会再执行——取消后的写入次数是零。Celery 仍然是至少一次投递，业务可观察的效果是等效的恰好一次，这两件事我不混着说。」

**必须出现的权威信号**：pytest 输出里的 `passed`，且测试名可读。

## 镜 6 ｜ 4:40–5:00 ｜ 收尾（20 s）

**画面**：停在已完成的 ApplicationRun 或面试详情页（**不要停在空页面**）。

**旁白**：
> 「总结三件事：第一，**Agent 运行时可以自研且可控**——引擎、checkpoint、中断、恢复都是自己的代码，为什么不用 LangGraph 在 ADR-0001 里有完整论证；第二，**LLM 的结论可以工程化地变得可信**——证据引用、支持度分级、不足即降级；第三，**人机协作的副作用可以做到业务等效的恰好一次**。仓库里的评测报告写明了每个数字的适用边界，欢迎按 README 的复现步骤自己跑一遍。」

---

# 版本二：10 分钟现场版（交互）

现场版的价值在于**可以出事**。观众看到的是真实系统在真实等待、真实失败、真实恢复——所以这里保留交互，也保留取舍方案。

**统一取舍原则**（时间不够时按此顺序砍）：

1. 先砍**安全幕的撤权演示**（口述「SSE 每批持续授权」即可）
2. 再砍**上传解析幕**（口述「解析结果需人工校对」即可）
3. 最后才砍**故障恢复幕**——它是这个项目最不可替代的一段，尽量保住
4. **永远不砍**：证据定位、审批的「已批准 ≠ 已执行」区别

## 时间表

| 时间 | 幕 | 内容 | 时长 |
|---|---|---|---|
| 0:00–1:00 | 1 | 开场 + 登录 + 模型模式横幅 | 1 min |
| 1:00–1:40 | 2 | 岗位与候选人（证据链一等公民） | 0.7 min |
| 1:40–3:30 | 3 | MatchRun：SSE 时间线 + 推荐理由 + 证据定位直达原文 | 1.8 min |
| 3:30–6:30 | 4 | ApplicationRun 双审批：**人工修改或否决** | 3 min |
| 6:30–8:00 | 5 | **故障恢复**与不重复写入 | 1.5 min |
| 8:00–9:00 | 6 | 安全：撤权即断（可砍） | 1 min |
| 9:00–10:00 | 7 | 收尾 + 评测页 | 1 min |

## 幕 1 ｜ 开场 + 登录 + 边界（1 min）

**操作**：打开 `http://localhost:8080` → 输入 `hr.demo` / `demo-password-123` → 点击“进入工作台” → 停 2 秒让顶部横幅可读 → 展开「模型详情」。

**讲解词**：
> 「这是我用六周做的企业招聘 Copilot，定位是 HR 的**辅助决策**系统，不是自动决策系统。它解决的核心问题是：当 LLM 参与招聘判断时，怎么让它的每个结论**可验证、可审计、可否决**。三条硬约束贯穿始终：确定性结论必须引用简历原文证据；所有副作用必须人工审批，并通过幂等与并发控制实现业务等效的只执行一次；任何故障可恢复且不产生重复写入。
> 顶部这条横幅写明当前跑的是 Mock 模型——**能证明流程、契约与可复现性，不能证明真实模型效果**。真实模型入口已经就绪，缺的只是凭据和预算。我把它放在每个页面顶部，是为了让你在读任何结论之前就知道这件事。」

**亮点强调**：登录这一步背后是 RBAC——服务端**每次请求**都重校验权限，不信任前端状态。

## 幕 2 ｜ 岗位与候选人（40 s）

**操作**：岗位列表页 → 点开「[DEMO] 高级后端工程师（Go / Python）」→ 看岗位描述与要求（必备技能 Go/Python/PostgreSQL，最低 3 年，本科）→ 顶部导航进入“候选人”→ 选择该岗位 → 点开「Demo 林知远」。

**讲解词**：
> 「岗位有版本化描述，候选人档案来自简历解析。注意候选人详情页：结构化资料下方保留了可定位的简历**原文摘录**，还能查看页码或段落——这就是证据链的一等公民设计，后面所有 AI 结论都建立在这些证据上。今天整场只讲林知远一个人，你就能看到同一个人被一步步处理。」

**亮点强调**：`EvidenceChunk` 带精确摘录 + `text_sha256` + 页码/段落定位。

## 幕 3 ｜ MatchRun：批量匹配与证据定位（1.8 min）★核心

**操作**：回到 DEMO 岗位详情页 → 点击“启动批量分析” → 页面自动进入批量匹配详情 → 指着 **RunTimeline** 讲 SSE 实时事件 → 等状态“已完成” → 看候选人排名 → 点开林知远的报告 → 点「查看原文 →」跳到原文定位。

**讲解词（时间线还在滚动时说）**：
> 「现在跑的是 MatchRun——岗位级批量分析，5 个候选人并行。注意两点：第一，右边时间线是 **SSE 实时事件流**，每个节点的开始/结束都带单调递增序列号，断线自动用 Last-Event-ID 续传，不丢事件、不重复渲染；第二，MatchRun 是**只读分析**，不产生任何副作用、不需要审批，因为它不改任何业务数据。」

**排名出来后**：
> 「排名表以**姓名**为主标识，下面一行是经验 / 学历 / 技能摘要，再下面才是 Profile 短 ID——那是接口和排障用的辅助追踪信息，讲解时不用念。右列是检索排序换算分与硬规则结果，注意这是**排序换算分，不是模型置信度**。
> 报告同样按候选人组织，每条结论后面跟着它引用的原文位置，点『查看原文』直接跳到那段原文并高亮定位。报告开头会写清结论来源——哪些是规则判定、哪些是模型解释。模型解释的支持等级**上限只能是部分支持**，因为它引用合法只证明位置真实，不证明语义支持。如果证据不足，系统**强制降级**为『不足以判断』，而不是让模型编一个理由。这是我在 Agent 评测里最看重的设计：**引用非法或证据不足时，宁可不回答**。」

**现场提示**：排名表、报告标题和证据摘录现在都显示姓名，可以直接讲具体人物。Profile 短 ID 仍在每行的「辅助追踪」行里，用于和接口 / 日志对照，不需要提前记下来。

**时间线提示**：时间线正文只显示可读的节点名、状态和失败原因，内部事件类型、节点 id 与消息键收在每行的「辅助详情」折叠里。要讲技术细节时展开它，讲业务时保持折叠。

**亮点强调**：自研 `RunEngine` + `SqlCheckpointer`（不是 LangGraph，ADR-0001 有正式决策记录）；每次节点转移都有 checkpoint，崩溃可从断点恢复。

## 幕 4 ｜ ApplicationRun 双审批：人工修改或否决（3 min）★★最核心

**操作**：从排名表林知远那行点击“启动单人流程” → 进入“单人招聘流程”页面。流程按下面的剧本走：

1. **发起后**：Run 跑到 `human_review` 节点**停住**，状态 WAITING_APPROVAL。
   > 「ApplicationRun 是固定 8 节点图：load_application → analyze_candidate → human_review【中断①】→ update_application_status → generate_interview_questions → wait_schedule_approval【条件中断②】→ create_interview_schedule → finalize_run。注意它在 human_review **必须停下等人**——这是引擎级中断，不是前端拦截，绕不过去。」

2. **看审批卡片**：在“当前审批”点击“查看并决策 →”，展示四个区块——**拟执行动作**（一句可读句子，如「将申请状态更新为『已入围』。」）、**原提案与参数**（对齐表格逐项标出相同 / 已修改 / 新增 / 已移除）、**当前版本**（审批版本、决策依据的申请版本、幂等键）、**执行结果**，以及“通过 / 修改参数 / 驳回”。
   > 「审批人看到的不是黑盒结论，是**结构化的变更提案**：建议把该候选人推进到什么状态、依据哪些证据。上面这句是把动作读成一句话，下面这张表是模型提案与人改后参数的逐项差异。」

3. **人工修改（EDITED）**：点“修改参数” → 把目标状态改成另一个值 → 应用 → 差异表把该行标为「已修改」→ 点“通过”。
   > 「这就是『人在环里』的实质：不是让人点一下确认按钮，而是让人能**改**。系统记录的是我改后的参数，不是模型原来的提案，两者在界面上和数据库里都分得开。」

   **备选：人工否决（REJECTED）**。如果时间够、或者观众更关心「AI 被拒绝」这一面，第 3 步改为点“驳回”：
   > 「我也可以直接否决。驳回是终态，不会产生任何写入——**被否决的审批执行次数是零**。流程页会显示『已驳回』并给出下一步，而不是停在一个看起来还在转的状态上。」

4. **批准与执行**：点“通过” → 页面顶部状态变成「已批准」，此时**不要立刻说“已经改好了”**：页面会明确写着写入尚未发生、并自动轮询等执行结果 → 等状态转为「已执行」再继续 → 点击“返回申请流程查看结果 →” → Run 恢复执行 → 林知远进入 SHORTLISTED，生成面试问题并在 `wait_schedule_approval` **再次停住**。
   > 「这里有个容易被忽略的区别，我特意在界面上分开显示：**已批准**只表示决策记录下来了，副作用还没发生；**已执行**才表示真的落库、而且恰好一次。决策和执行是两步，中间由 SideEffect 服务带着乐观锁 CAS 和幂等键去落库——所以页面会轮询等它，而不是批完就显示成功。」

5. **第二次批准**：等待“当前审批”显示“创建面试安排” → 再次点击“查看并决策 →”与“通过” → 返回申请流程 → 等待“已完成 / 已排期” → 点击“查看面试 →”。
   > 「第二次中断是**条件中断**：只有进入 SHORTLISTED 需要排面试时才触发。同一次运行，两次人工把关——推荐入和排面试是两个不同级别的副作用。」

**讲解词（收尾）**：
> 「整条链路的副作用——改申请状态、建面试——**节点本身不写库**，它们只产出类型化的状态；真正写表的是独立的 SideEffect 服务。Celery 可以至少一次投递，但 Approval 状态机 + 乐观锁 CAS + 幂等键让业务效果等效为**只执行一次**。我可以现场展示：同一审批重复调用 API 会返回 409，`/api/v1/metrics` 里有计数。」

**亮点强调**：审批过期可重试、JobApplication 活动槽排他（同一申请同时只有一个活动 Run）。

## 幕 5 ｜ 故障恢复与不重复写入（1.5 min）★★最不可替代

**操作**：挑一个停在审批中断的 Run（就用刚跑完的林知远那条），先记下 URL 和 Run 短 ID → 在另一个终端运行 `docker compose restart api worker` → 等待 `docker compose ps` 显示 API 恢复健康 → 回到页面刷新，Run 状态和审批请求应**完好如初**。若页面短暂报 503，等待数秒后再刷新，这是恢复窗口而不是数据丢失。

**讲解词**：
> 「容器随便杀。每个节点转移都落了 checkpoint 到 Postgres，恢复后引擎从断点重放，事件序列号保证前端不重复渲染。
> 但恢复本身不是重点，重点是**恢复不会多写一次**。协作式取消同样：取消请求生效后，后续副作用节点**不会**再执行——取消后的写入次数是零。这两条都有自动化测试断言，我可以现场指给你看。」

**现场可选**：切到终端跑 `python -m pytest -q tests/unit/test_concurrency_recovery.py -k "replay or cancel" -v`，指出测试名。

**亮点强调**：这是「Agent 可靠性工程」的最直接证据，面试 Agent 岗时重点讲。

**顺手提一句界面上的恢复指引**：失败、取消、等待审批、已完成四种终态在流程页各有不同的提示与下一步按钮——失败与取消给「重试」，等待审批给「等待」，已完成给「查看结果」。把「等」和「重试」显示成同一种提示会让人做错操作，所以它们必须分开。

## 幕 6 ｜ 安全与授权（1 min，可砍）

**操作**：复制当前运行 URL → 另开无痕窗口登录 `hm.demo`，再打开该 URL → 回到 HR 主窗口的 DEMO 岗位详情，在“负责人分配”表找到唯一有效的招聘主管记录，先记下其用户 ID，再点击“撤销” → 无痕窗口中的 SSE 流应在一次心跳内收到撤权并停止。刷新后，资源以 **404** 表达（不泄露存在性），时间线显示撤权原因。

> 「授权不是登录时查一次就完——SSE 每一批事件、每个心跳都持续重校验。权限被撤，正在观看的流当场断掉。」

**演示后恢复**：回到负责人分配表，把刚才记下的招聘主管用户 ID 填入输入框并点击“分配”，避免后续演示失去 `hm.demo` 授权。

**口述替代**（时间不够时）：「简历和岗位描述在架构里被定义为**不可信数据**：它们进模型上下文，但永远不能改变控制流、权限或工具参数。有配对的注入回归测试守着这条线——系统侧放行越权、副作用、审批绕过是 **0/17**。」

## 幕 7 ｜ 收尾（1 min）

**操作**：回到岗位页总览。如果“评测”页已有运行记录，打开它展示持久化指标；若页面为空，不要把空页面作为结尾，改为停在已完成的 ApplicationRun 或面试详情页。

**讲解词**：
> 「总结一下这个项目证明的三件事：第一，**Agent 运行时可以自研且可控**——引擎、checkpoint、中断、恢复都是自己的代码，为什么不用 LangGraph 在 ADR-0001 里有完整论证；第二，**LLM 的结论可以工程化地变得可信**——证据引用、支持度分级、不足即降级；第三，**人机协作的副作用可以做到业务等效的 exactly-once**——底层任务允许至少一次投递，业务层用双审批、幂等键、乐观锁与活动槽消除重复副作用。CI 覆盖 882 项后端测试、231 项前端测试、浏览器 E2E 和完整活栈门禁。评测报告里每个数字都写明了自己的适用边界，README 里有复现步骤。」

---

## 附 A：Mock 模式的验证范围

现场可能没有外部模型凭据，这是**事先声明**的边界，不是缺陷。这一段两个版本都要能讲。

**怎么识别**：不需要靠记忆或文档——每个页面顶部都有横幅。Mock 时显示「Mock 模型（确定性假模型，不调用外部服务）」，展开「模型详情」可以看到对话模型、向量模型、提示词版本与规则版本。读取失败时横幅会显示「模型模式未知」，并提示不要据本页结论推断模型效果——**「无法确认」和「不是 Mock」是两句不同的话**，所以不会静默不显示。

**Mock 能证明什么**（可以指着界面讲）：

| 能证明 | 证据 |
|---|---|
| 链路真的通了 | 抽取 → 召回 → 硬性规则 → 报告 → 图执行，全程走实际代码，没有环节被替换成预置结果 |
| 契约真的被守住 | 模型引用在持久化前经服务端反查切片并校验；非法引用被拒而不是被夹取；要求 `HIGH` 影响判为契约违规 |
| 审批真的绕不过去 | 注入文本 17/17 例可测量，系统放行越权 / 副作用 / 审批绕过 **0/17** |
| 失败语义真的成立 | 空语料、缺录制、门禁不可达、超出调用预算一律非零退出，不产生通过结论 |
| 结果真的可复现 | 同一输入每次得到同一结果；报告写明数据集 content_hash 与各环节版本 |

**Mock 不能证明什么**（必须主动说，不能等人问）：

- **抽取质量**：替身的词表有限，字段 F1 只反映替身能读到哪些字面词。报告里 skills 召回 0.882 的缺口正是刻意设计的词表外技能。
- **检索效果**：向量由 `fake-embed-v1` 产生，Recall@5 / MRR / nDCG 不能外推到真实 embedding。
- **真实模型的抗注入能力**：报告里「模型遵循 5/17」只是替身能读到哪些字面词，**不可外推**；可直接引用的是系统侧的 0/17。
- **延迟与成本**：替身不调用网络，耗时不代表真实链路；费用在未声明计价版本时不估算。

**真实模型入口已就绪**：`python scripts/run_evaluation.py --live --max-calls N`。`--live` 未声明预算会被拒绝启动（`EVALUATION_BUDGET_NOT_STATED`），因为真实评测是唯一按次花钱的环节，预算必须是运行参数而不是从账单倒推。换真实凭据后，报告会自动改标为「真实模型评测」一节，与替身结果**分节呈现、拒绝合并**。

---

## 附 B：高频追问 Q&A 预案

| 追问 | 回答要点 |
|---|---|
| 为什么不用 LangGraph？ | 需要的是「确定性重放 + 引擎级条件中断 + checkpoint 落 PG + 事件序列可续传」的组合；当前自研小型引擎能把中断/恢复语义完全显式化。ADR-0001 有对比论证，且被 `test_fin009_adr_contract.py` 契约测试守住。 |
| 幂等怎么实现「恰好一次」？ | Celery 仍是至少一次投递；业务层通过 Approval 状态机（PENDING→终态的一次性流转）、乐观锁 CAS、`idempotency_key` 唯一约束与副作用结果复用，实现业务可观察效果的等效 exactly-once。重复决定返回 409，已经成功的副作用不会再执行。 |
| 「已批准」和「已执行」为什么要分开？ | 决策和执行是两步，中间隔着 SideEffect 服务。批完就显示成功，会在执行失败时让审批人以为已经生效——所以界面先显示「已批准 · 写入尚未发生」，轮询到「已执行」才代表恰好落库一次。 |
| 证据「SUPPORTED/PARTIAL/INSUFFICIENT」怎么定的？ | 模型输出带 claim→evidence 引用，服务端校验引用合法性（chunk 存在、哈希一致、归属正确），再按覆盖率分级；非法引用直接判 INSUFFICIENT 并降级结论。模型解释的支持等级上限是 PARTIAL。 |
| 综合分是模型置信度吗？ | 不是。它是检索排序的换算分（满分 100），由快照排名与硬性规则计算，报告与界面都显式标注。把排序当成「模型有多确信」是这个项目最容易出现的误读。 |
| SSE 为什么自己写不用轮询？ | 时间线是长时运行的节点事件流，需要低延迟 + 断线续传；`Last-Event-ID` + 服务端 1 起单调序列号 + 每批持续授权。可以主动讲踩过的坑：客户端无游标首帧误判跳号导致重连死循环，回归测试已锁死。 |
| 模型是真调用吗？ | 默认 Mock 模式（FakeModel，CI 稳定、离线可跑）——**页面顶部横幅已经把这一条直接写出来了**，含对话 / 向量模型名与提示词、规则版本；真实 Qwen 网关机制已就绪，有 `model_diagnose` 诊断命令与显式评测入口（`--live`），换真实 key 即可启用。详见附 A。 |
| 测试规模？ | 后端 882 项 pytest、前端 231 项 vitest、5 份 Playwright E2E spec；CI 与本地共用 `pwsh scripts/project.ps1 verify`。精确数量会随提交变化，现场以最新 CI 输出为准。 |
| 最难的 bug？ | 推荐讲 SSE 序列号那个：三层问题（连接池被流耗尽、基础设施异常被误报成撤权、无游标首帧误判跳号死循环），定位过程体现调试功力。 |
| 这个项目有什么没做完？ | 真实模型凭据下的端到端链路与真实效果评测未执行，属凭据与预算问题，不是机制问题；本机的浏览器 E2E 与投屏分辨率检查也未执行，已显式记录而不是按组件测试推断。 |

## 附 C：现场状态速查

| 阶段 | 页面应出现的权威信号 | 超时后的动作 |
|---|---|---|
| 起栈 | `STACK_READY entry=http://localhost:8080` | 查看 `docker compose ps` 与 `docker compose logs api worker`。 |
| 文档解析 | 队列状态“待校对” | 点击“刷新”；60 秒无进展则展示错误码后跳过本幕。 |
| 资料确认 | “资料 已就绪” | 检查是否先选择了“写操作岗位”。 |
| MatchRun | 状态“已完成”，排名表出现记录 | 查看时间线最后事件；失败时可点“重试”。 |
| 第一次中断 | “等待审批” + “更新申请状态” | 刷新 ApplicationRun；不要重复启动同一申请。 |
| 审批决策 | 先「已批准」后「已执行」 | 停在「已批准」说明执行尚未完成，**不要**当作成功；刷新等待轮询结果。 |
| 第二次中断 | “创建面试安排” | 等待 Worker 恢复 Run 后再刷新。 |
| 流程完成 | “已完成” + “已排期” + “查看面试 →” | 若审批已执行但页面未更新，返回流程页刷新。 |

**演示纪律**：只把页面上的状态和持久化事实当作成功信号，不以“按钮点过了”代替完成；任何失败都保留错误码和请求编号，这本身也是可观测性设计的一部分。

## 附 D：演示后清理

```powershell
# 只重建 DEMO 标记的数据；不会删除你手工上传的非 DEMO 文档
pwsh scripts/project.ps1 reset-demo

# 若下次还要演示“上传同一份 fixture”，必须清卷，避免内容 SHA256 被判重复
# 注意必须带 --profile tools，否则 storage-init 持有的 storage 卷删不掉
docker compose --profile tools down --volumes --remove-orphans
```
